---
uid: web-pages/overview/data/7-displaying-data-in-a-chart
title: "Exibindo dados em um gráfico com páginas da Web do ASP.NET (Razor) | Microsoft Docs"
author: microsoft
description: "Este capítulo explica como exibir dados em um gráfico. Nos capítulos anteriores, você aprendeu como exibir os dados manualmente e em uma grade. Este capítulo explica..."
ms.author: aspnetcontent
manager: wpickett
ms.date: 05/22/2012
ms.topic: article
ms.assetid: f889fd46-4dac-4ecb-83d8-60e64c22036e
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/data/7-displaying-data-in-a-chart
msc.type: authoredcontent
ms.openlocfilehash: f252b74bc42d0ea65b8b1150973c4f3c50cc9cf4
ms.sourcegitcommit: 060879fcf3f73d2366b5c811986f8695fff65db8
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 01/24/2018
---
<a name="displaying-data-in-a-chart-with-aspnet-web-pages-razor"></a><span data-ttu-id="bf720-105">Exibindo dados em um gráfico com páginas da Web do ASP.NET (Razor)</span><span class="sxs-lookup"><span data-stu-id="bf720-105">Displaying Data in a Chart with ASP.NET Web Pages (Razor)</span></span>
====================
<span data-ttu-id="bf720-106">por [Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="bf720-106">by [Microsoft](https://github.com/microsoft)</span></span>

> <span data-ttu-id="bf720-107">Este artigo explica como usar um gráfico para exibir dados em um site de páginas da Web do ASP.NET (Razor) usando o `Chart` auxiliar.</span><span class="sxs-lookup"><span data-stu-id="bf720-107">This article explains how to use a chart to display data in an ASP.NET Web Pages (Razor) website by using the `Chart` helper.</span></span>
> 
> <span data-ttu-id="bf720-108">**Você aprenderá**:</span><span class="sxs-lookup"><span data-stu-id="bf720-108">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="bf720-109">Como exibir dados em um gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-109">How to display data in a chart.</span></span>
> - <span data-ttu-id="bf720-110">Como os gráficos de estilo usando temas internos.</span><span class="sxs-lookup"><span data-stu-id="bf720-110">How to style charts using built-in themes.</span></span>
> - <span data-ttu-id="bf720-111">Como salvar gráficos e como armazenar em cache-los para melhorar o desempenho.</span><span class="sxs-lookup"><span data-stu-id="bf720-111">How to save charts and how to cache them for better performance.</span></span>
> 
> <span data-ttu-id="bf720-112">Esses são o ASP.NET recursos introduzidos no artigo de programação:</span><span class="sxs-lookup"><span data-stu-id="bf720-112">These are the ASP.NET programming features introduced in the article:</span></span>
> 
> - <span data-ttu-id="bf720-113">O `Chart` auxiliar.</span><span class="sxs-lookup"><span data-stu-id="bf720-113">The `Chart` helper.</span></span>
> 
> > [!NOTE]
> > <span data-ttu-id="bf720-114">As informações neste artigo se aplica a páginas da Web de ASP.NET 1.0 e 2 páginas da Web.</span><span class="sxs-lookup"><span data-stu-id="bf720-114">The information in this article applies to ASP.NET Web Pages 1.0 and Web Pages 2.</span></span>


<a id="The_Chart_Helper"></a>
## <a name="the-chart-helper"></a><span data-ttu-id="bf720-115">O auxiliar de gráfico</span><span class="sxs-lookup"><span data-stu-id="bf720-115">The Chart Helper</span></span>

<span data-ttu-id="bf720-116">Quando você deseja exibir seus dados de formulário gráfico, você pode usar `Chart` auxiliar.</span><span class="sxs-lookup"><span data-stu-id="bf720-116">When you want to display your data in graphical form, you can use `Chart` helper.</span></span> <span data-ttu-id="bf720-117">O `Chart` auxiliar pode processar uma imagem que exibe dados em uma variedade de tipos de gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-117">The `Chart` helper can render an image that displays data in a variety of chart types.</span></span> <span data-ttu-id="bf720-118">Ele dá suporte a várias opções para formatação e rotulagem.</span><span class="sxs-lookup"><span data-stu-id="bf720-118">It supports many options for formatting and labeling.</span></span> <span data-ttu-id="bf720-119">O `Chart` auxiliar pode processar mais de 30 tipos de gráficos, incluindo todos os tipos de gráficos que você pode estar familiarizado com o Microsoft Excel ou outras ferramentas &#8212; gráficos de área, barra gráficos, gráficos de coluna, linha gráficos e gráficos de pizza, juntamente com mais gráficos especializados, como gráficos de ações.</span><span class="sxs-lookup"><span data-stu-id="bf720-119">The `Chart` helper can render more than 30 types of charts, including all the types of charts that you might be familiar with from Microsoft Excel or other tools &#8212; area charts, bar charts, column charts, line charts, and pie charts, along with more specialized charts like stock charts.</span></span>

| <span data-ttu-id="bf720-120">**Gráfico de área** ![Descrição: a imagem do tipo de gráfico de área](7-displaying-data-in-a-chart/_static/image1.jpg)</span><span class="sxs-lookup"><span data-stu-id="bf720-120">**Area chart** ![Description: Picture of the Area chart type](7-displaying-data-in-a-chart/_static/image1.jpg)</span></span> | <span data-ttu-id="bf720-121">**Gráfico de barras** ![Descrição: a imagem do tipo de gráfico de barras](7-displaying-data-in-a-chart/_static/image2.jpg)</span><span class="sxs-lookup"><span data-stu-id="bf720-121">**Bar chart** ![Description: Picture of the Bar chart type](7-displaying-data-in-a-chart/_static/image2.jpg)</span></span> |
| --- | --- |
| <span data-ttu-id="bf720-122">**Gráfico de colunas** ![Descrição: a imagem do tipo de gráfico de coluna](7-displaying-data-in-a-chart/_static/image3.jpg)</span><span class="sxs-lookup"><span data-stu-id="bf720-122">**Column chart** ![Description: Picture of the Column chart type](7-displaying-data-in-a-chart/_static/image3.jpg)</span></span> | <span data-ttu-id="bf720-123">**Gráfico de linhas** ![Descrição: a imagem do tipo de gráfico de linha](7-displaying-data-in-a-chart/_static/image4.jpg)</span><span class="sxs-lookup"><span data-stu-id="bf720-123">**Line chart** ![Description: Picture of the Line chart type](7-displaying-data-in-a-chart/_static/image4.jpg)</span></span> |
| <span data-ttu-id="bf720-124">**Gráfico de pizza** ![Descrição: a imagem do tipo de gráfico de pizza](7-displaying-data-in-a-chart/_static/image5.jpg)</span><span class="sxs-lookup"><span data-stu-id="bf720-124">**Pie chart** ![Description: Picture of the Pie chart type](7-displaying-data-in-a-chart/_static/image5.jpg)</span></span> | <span data-ttu-id="bf720-125">**Gráfico de estoque** ![Descrição: a imagem do tipo de gráfico de estoque](7-displaying-data-in-a-chart/_static/image6.jpg)</span><span class="sxs-lookup"><span data-stu-id="bf720-125">**Stock chart** ![Description: Picture of the Stock chart type](7-displaying-data-in-a-chart/_static/image6.jpg)</span></span> |

### <a name="chart-elements"></a><span data-ttu-id="bf720-126">Elementos do gráfico</span><span class="sxs-lookup"><span data-stu-id="bf720-126">Chart Elements</span></span>

<span data-ttu-id="bf720-127">Os gráficos mostram dados e elementos adicionais, como legendas, eixos, série e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="bf720-127">Charts show data and additional elements like legends, axes, series, and so on.</span></span> <span data-ttu-id="bf720-128">A figura a seguir mostra muitos dos elementos de gráfico que você pode personalizar quando você usa o `Chart` auxiliar.</span><span class="sxs-lookup"><span data-stu-id="bf720-128">The following picture shows many of the chart elements that you can customize when you use the `Chart` helper.</span></span> <span data-ttu-id="bf720-129">Este artigo mostra como definir algumas (não todos) desses elementos.</span><span class="sxs-lookup"><span data-stu-id="bf720-129">This article shows you how to set some (not all) of these elements.</span></span>

![Descrição: Imagem mostrando elementos do gráfico](7-displaying-data-in-a-chart/_static/image7.jpg)

<a id="Creating_a_Chart"></a>
## <a name="creating-a-chart-from-data"></a><span data-ttu-id="bf720-131">Criar um gráfico de dados</span><span class="sxs-lookup"><span data-stu-id="bf720-131">Creating a Chart from Data</span></span>

<span data-ttu-id="bf720-132">Os dados exibidos em um gráfico podem ser de uma matriz, a partir dos resultados de um banco de dados ou de dados que estão em um arquivo XML.</span><span class="sxs-lookup"><span data-stu-id="bf720-132">The data you display in a chart can be from an array, from the results returned from a database, or from data that's in an XML file.</span></span>

### <a name="using-an-array"></a><span data-ttu-id="bf720-133">Usando uma matriz</span><span class="sxs-lookup"><span data-stu-id="bf720-133">Using an Array</span></span>

<span data-ttu-id="bf720-134">Conforme explicado em [Introdução ao ASP.NET páginas da Web de programação usando a sintaxe do Razor](https://go.microsoft.com/fwlink/?LinkId=202890), uma matriz lhe permite armazenar uma coleção de itens semelhantes em uma única variável.</span><span class="sxs-lookup"><span data-stu-id="bf720-134">As explained in [Introduction to ASP.NET Web Pages Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890), an array lets you store a collection of similar items in a single variable.</span></span> <span data-ttu-id="bf720-135">Você pode usar matrizes para conter os dados que você deseja incluir em seu gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-135">You can use arrays to contain the data that you want to include in your chart.</span></span>

<span data-ttu-id="bf720-136">Este procedimento mostra como você pode criar um gráfico de dados em matrizes, usando o tipo de gráfico padrão.</span><span class="sxs-lookup"><span data-stu-id="bf720-136">This procedure shows how you can create a chart from data in arrays, using the default chart type.</span></span> <span data-ttu-id="bf720-137">Ele também mostra como exibir o gráfico dentro da página.</span><span class="sxs-lookup"><span data-stu-id="bf720-137">It also shows how to display the chart within the page.</span></span>

1. <span data-ttu-id="bf720-138">Criar um novo arquivo denominado *ChartArrayBasic.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="bf720-138">Create a new file named *ChartArrayBasic.cshtml*.</span></span>
2. <span data-ttu-id="bf720-139">Substitua o conteúdo existente com o seguinte:</span><span class="sxs-lookup"><span data-stu-id="bf720-139">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample1.cshtml)]

    <span data-ttu-id="bf720-140">Primeiro, o código cria um novo gráfico e define a largura e altura.</span><span class="sxs-lookup"><span data-stu-id="bf720-140">The code first creates a new chart and sets its width and height.</span></span> <span data-ttu-id="bf720-141">Especifique o título do gráfico usando o `AddTitle` método.</span><span class="sxs-lookup"><span data-stu-id="bf720-141">You specify the chart title by using the `AddTitle` method.</span></span> <span data-ttu-id="bf720-142">Para adicionar dados, use o `AddSeries` método.</span><span class="sxs-lookup"><span data-stu-id="bf720-142">To add data, you use the `AddSeries` method.</span></span> <span data-ttu-id="bf720-143">Neste exemplo, você deve usar o `name`, `xValue`, e `yValues` parâmetros o `AddSeries` método.</span><span class="sxs-lookup"><span data-stu-id="bf720-143">In this example, you use the `name`, `xValue`, and `yValues` parameters of the `AddSeries` method.</span></span> <span data-ttu-id="bf720-144">O `name` parâmetro é exibido na legenda do gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-144">The `name` parameter is displayed in the chart legend.</span></span> <span data-ttu-id="bf720-145">O `xValue` parâmetro contém uma matriz de dados que são exibidos ao longo do eixo horizontal do gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-145">The `xValue` parameter contains an array of data that's displayed along the horizontal axis of the chart.</span></span> <span data-ttu-id="bf720-146">O `yValues` parâmetro contém uma matriz de dados que são usados para plotar os pontos verticais do gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-146">The `yValues` parameter contains an array of data that's used to plot the vertical points of the chart.</span></span>

    <span data-ttu-id="bf720-147">O `Write` método renderiza, na verdade, o gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-147">The `Write` method actually renders the chart.</span></span> <span data-ttu-id="bf720-148">Nesse caso, porque você não especificar um tipo de gráfico, o `Chart` auxiliar processa seu gráfico padrão, que é um gráfico de colunas.</span><span class="sxs-lookup"><span data-stu-id="bf720-148">In this case, because you didn't specify a chart type, the `Chart` helper renders its default chart, which is a column chart.</span></span>
3. <span data-ttu-id="bf720-149">Execute a página no navegador.</span><span class="sxs-lookup"><span data-stu-id="bf720-149">Run the page in the browser.</span></span> <span data-ttu-id="bf720-150">O navegador exibe o gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-150">The browser displays the chart.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image8.jpg)

### <a name="using-a-database-query-for-chart-data"></a><span data-ttu-id="bf720-151">Usando uma consulta de banco de dados para dados do gráfico</span><span class="sxs-lookup"><span data-stu-id="bf720-151">Using a Database Query for Chart Data</span></span>

<span data-ttu-id="bf720-152">Se as informações que você deseja que o gráfico estão em um banco de dados, você pode executar uma consulta de banco de dados e, em seguida, usar dados de resultados para criar o gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-152">If the information you want to chart is in a database, you can run a database query and then use data from the results to create the chart.</span></span> <span data-ttu-id="bf720-153">Este procedimento mostra como ler e exibir dados do banco de dados criado no artigo [Introdução ao trabalho com um banco de dados em Sites de páginas da Web do ASP.NET](https://go.microsoft.com/fwlink/?LinkId=202893).</span><span class="sxs-lookup"><span data-stu-id="bf720-153">This procedure shows you how to read and display the data from the database created in the article [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>

1. <span data-ttu-id="bf720-154">Adicionar uma *aplicativo\_dados* pasta para a raiz do site da Web se a pasta ainda não existir.</span><span class="sxs-lookup"><span data-stu-id="bf720-154">Add an *App\_Data* folder to the root of the website if the folder does not already exist.</span></span>
2. <span data-ttu-id="bf720-155">No *aplicativo\_dados* pasta, adicione o arquivo de banco de dados denominado *SmallBakery.sdf* que é descrita em [Introdução ao trabalho com um banco de dados em Sites de páginas da Web do ASP.NET](https://go.microsoft.com/fwlink/?LinkId=202893).</span><span class="sxs-lookup"><span data-stu-id="bf720-155">In the *App\_Data* folder, add the database file named *SmallBakery.sdf* that's described in [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>
3. <span data-ttu-id="bf720-156">Criar um novo arquivo denominado *ChartDataQuery.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="bf720-156">Create a new file named *ChartDataQuery.cshtml*.</span></span>
4. <span data-ttu-id="bf720-157">Substitua o conteúdo existente com o seguinte:</span><span class="sxs-lookup"><span data-stu-id="bf720-157">Replace the existing content with the following:</span></span>   

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample2.cshtml)]

    <span data-ttu-id="bf720-158">O código primeiro abre o banco de dados SmallBakery e atribui a uma variável chamada `db`.</span><span class="sxs-lookup"><span data-stu-id="bf720-158">The code first opens the SmallBakery database and assigns it to a variable named `db`.</span></span> <span data-ttu-id="bf720-159">Essa variável representa um `Database` objeto que pode ser usado para ler e gravar no banco de dados.</span><span class="sxs-lookup"><span data-stu-id="bf720-159">This variable represents a `Database` object that can be used to read from and write to the database.</span></span> <span data-ttu-id="bf720-160">Em seguida, o código executa uma consulta SQL para obter o nome e o preço de cada produto.</span><span class="sxs-lookup"><span data-stu-id="bf720-160">Next, the code runs a SQL query to get the name and price of each product.</span></span> <span data-ttu-id="bf720-161">O código cria um novo gráfico e passa a consulta de banco de dados a ele chamando o gráfico `DataBindTable` método.</span><span class="sxs-lookup"><span data-stu-id="bf720-161">The code creates a new chart and passes the database query to it by calling the chart's `DataBindTable` method.</span></span> <span data-ttu-id="bf720-162">Esse método usa dois parâmetros: o `dataSource` parâmetro é para os dados da consulta e o `xField` parâmetro permite que você defina a coluna de dados é usada para o eixo de x do gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-162">This method takes two parameters: the `dataSource` parameter is for the data from the query, and the `xField` parameter lets you set which data column is used for the chart's x-axis.</span></span>

    <span data-ttu-id="bf720-163">Como uma alternativa ao uso de `DataBindTable` método, você pode usar o `AddSeries` método o `Chart` auxiliar.</span><span class="sxs-lookup"><span data-stu-id="bf720-163">As an alternative to using the `DataBindTable` method, you can use the `AddSeries` method of the `Chart` helper.</span></span> <span data-ttu-id="bf720-164">O `AddSeries` método permite que você defina o `xValue` e `yValues` parâmetros.</span><span class="sxs-lookup"><span data-stu-id="bf720-164">The `AddSeries` method lets you set the `xValue` and `yValues` parameters.</span></span> <span data-ttu-id="bf720-165">Por exemplo, em vez de usar o `DataBindTable` método assim:</span><span class="sxs-lookup"><span data-stu-id="bf720-165">For example, instead of using the `DataBindTable` method like this:</span></span>

    [!code-css[Main](7-displaying-data-in-a-chart/samples/sample3.css)]

    <span data-ttu-id="bf720-166">Você pode usar o `AddSeries` método assim:</span><span class="sxs-lookup"><span data-stu-id="bf720-166">You can use the `AddSeries` method like this:</span></span>

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample4.html)]

    <span data-ttu-id="bf720-167">Ambas renderizam os mesmos resultados.</span><span class="sxs-lookup"><span data-stu-id="bf720-167">Both render the same results.</span></span> <span data-ttu-id="bf720-168">O `AddSeries` método é mais flexível porque você pode especificar o tipo de gráfico e os dados mais explicitamente, mas o `DataBindTable` método é mais fácil de usar se não precisar de mais flexibilidade.</span><span class="sxs-lookup"><span data-stu-id="bf720-168">The `AddSeries` method is more flexible because you can specify the chart type and data more explicitly, but the `DataBindTable` method is easier to use if you don't need the extra flexibility.</span></span>
5. <span data-ttu-id="bf720-169">Execute a página em um navegador.</span><span class="sxs-lookup"><span data-stu-id="bf720-169">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image9.jpg)

### <a name="using-xml-data"></a><span data-ttu-id="bf720-170">Usando dados XML</span><span class="sxs-lookup"><span data-stu-id="bf720-170">Using XML Data</span></span>

<span data-ttu-id="bf720-171">A terceira opção para criar gráficos é usar um arquivo XML como os dados do gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-171">The third option for charting is to use an XML file as the data for the chart.</span></span> <span data-ttu-id="bf720-172">Isso requer que o arquivo XML também tem um arquivo de esquema (*. xsd* arquivo) que descreve a estrutura XML.</span><span class="sxs-lookup"><span data-stu-id="bf720-172">This requires that the XML file also have a schema file (*.xsd* file) that describes the XML structure.</span></span> <span data-ttu-id="bf720-173">Este procedimento mostra como ler dados de um arquivo XML.</span><span class="sxs-lookup"><span data-stu-id="bf720-173">This procedure shows you how to read data from an XML file.</span></span>

1. <span data-ttu-id="bf720-174">No *aplicativo\_dados* pasta, crie um novo arquivo XML denominado *data.xml*.</span><span class="sxs-lookup"><span data-stu-id="bf720-174">In the *App\_Data* folder, create a new XML file named *data.xml*.</span></span>
2. <span data-ttu-id="bf720-175">Substitua o XML existente com o seguinte, que alguns dados XML sobre funcionários de uma empresa fictícia.</span><span class="sxs-lookup"><span data-stu-id="bf720-175">Replace the existing XML with the following, which is some XML data about employees in a fictional company.</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample5.xml)]
3. <span data-ttu-id="bf720-176">No *aplicativo\_dados* pasta, crie um novo arquivo XML denominado *data.xsd*.</span><span class="sxs-lookup"><span data-stu-id="bf720-176">In the *App\_Data* folder, create a new XML file named *data.xsd*.</span></span> <span data-ttu-id="bf720-177">(Observe que a extensão esse tempo é *. xsd*.)</span><span class="sxs-lookup"><span data-stu-id="bf720-177">(Note that the extension this time is *.xsd*.)</span></span>
4. <span data-ttu-id="bf720-178">Substitua o XML existente com o seguinte:</span><span class="sxs-lookup"><span data-stu-id="bf720-178">Replace the existing XML with the following:</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample6.xml)]
5. <span data-ttu-id="bf720-179">Na raiz do site, crie um novo arquivo denominado *ChartDataXML.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="bf720-179">In the root of the website, create a new file named *ChartDataXML.cshtml*.</span></span>
6. <span data-ttu-id="bf720-180">Substitua o conteúdo existente com o seguinte:</span><span class="sxs-lookup"><span data-stu-id="bf720-180">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample7.cshtml)]

    <span data-ttu-id="bf720-181">O código cria primeiro um `DataSet` objeto.</span><span class="sxs-lookup"><span data-stu-id="bf720-181">The code first creates a `DataSet` object.</span></span> <span data-ttu-id="bf720-182">Esse objeto é usado para gerenciar os dados que são lidos a partir do arquivo XML e organizá-los de acordo com as informações no arquivo de esquema.</span><span class="sxs-lookup"><span data-stu-id="bf720-182">This object is used to manage the data that's read from the XML file and organize it according to the information in the schema file.</span></span> <span data-ttu-id="bf720-183">(Observe que a parte superior do código inclui a instrução `using SystemData`.</span><span class="sxs-lookup"><span data-stu-id="bf720-183">(Notice that the top of the code includes the statement `using SystemData`.</span></span> <span data-ttu-id="bf720-184">Isso é necessário para trabalhar com o `DataSet` objeto.</span><span class="sxs-lookup"><span data-stu-id="bf720-184">This is required in order to be able to work with the `DataSet` object.</span></span> <span data-ttu-id="bf720-185">Para obter mais informações, consulte [ &quot;usando&quot; instruções e nomes totalmente qualificados](#SB_UsingStatements) posteriormente neste artigo.)</span><span class="sxs-lookup"><span data-stu-id="bf720-185">For more information, see [&quot;Using&quot; Statements and Fully Qualified Names](#SB_UsingStatements) later in this article.)</span></span>

    <span data-ttu-id="bf720-186">Em seguida, o código cria um `DataView` objeto com base no conjunto de dados.</span><span class="sxs-lookup"><span data-stu-id="bf720-186">Next, the code creates a `DataView` object based on the dataset.</span></span> <span data-ttu-id="bf720-187">A exibição de dados fornece um objeto que o gráfico pode vincular a &#8212; ou seja, leitura e plotar.</span><span class="sxs-lookup"><span data-stu-id="bf720-187">The data view provides an object that the chart can bind to &#8212; that is, read and plot.</span></span> <span data-ttu-id="bf720-188">Associa o gráfico para os dados usando o `AddSeries` método, como você viu anteriormente quando representando os dados de matriz, exceto que desta vez o `xValue` e `yValues` parâmetros são definidos para o `DataView` objeto.</span><span class="sxs-lookup"><span data-stu-id="bf720-188">The chart binds to the data using the `AddSeries` method, as you saw earlier when charting the array data, except that this time the `xValue` and `yValues` parameters are set to the `DataView` object.</span></span>

    <span data-ttu-id="bf720-189">Este exemplo mostra como especificar um determinado tipo de gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-189">This example also shows you how to specify a particular chart type.</span></span> <span data-ttu-id="bf720-190">Quando os dados são adicionados no `AddSeries` método, o `chartType` parâmetro também é configurado para exibir um gráfico de pizza.</span><span class="sxs-lookup"><span data-stu-id="bf720-190">When the data is added in the `AddSeries` method, the `chartType` parameter is also set to display a pie chart.</span></span>
7. <span data-ttu-id="bf720-191">Execute a página em um navegador.</span><span class="sxs-lookup"><span data-stu-id="bf720-191">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image10.jpg)

> [!TIP] 
> 
> <a id="SB_UsingStatements"></a>
> ### <a name="using-statements-and-fully-qualified-names"></a><span data-ttu-id="bf720-192">Instruções "Using" e nomes totalmente qualificados</span><span class="sxs-lookup"><span data-stu-id="bf720-192">"Using" Statements and Fully Qualified Names</span></span>
> 
> <span data-ttu-id="bf720-193">O ASP.NET Web Pages com sintaxe Razor com base no .NET Framework consiste em vários milhares de componentes (classes).</span><span class="sxs-lookup"><span data-stu-id="bf720-193">The .NET Framework that ASP.NET Web Pages with Razor syntax is based on consists of many thousands of components (classes).</span></span> <span data-ttu-id="bf720-194">Para torná-lo gerenciável para trabalhar com todas essas classes, são organizados em *namespaces*, que são um pouco como bibliotecas.</span><span class="sxs-lookup"><span data-stu-id="bf720-194">To make it manageable to work with all these classes, they're organized into *namespaces*, which are somewhat like libraries.</span></span> <span data-ttu-id="bf720-195">Por exemplo, o `System.Web` namespace contém classes que oferecem suporte à comunicação navegador para o servidor, o `System.Xml` namespace contém classes que são usadas para criar e ler arquivos XML, e o `System.Data` namespace contém classes que permitem que você trabalhe com dados.</span><span class="sxs-lookup"><span data-stu-id="bf720-195">For example, the `System.Web` namespace contains classes that support browser/server communication, the `System.Xml` namespace contains classes that are used to create and read XML files, and the `System.Data` namespace contains classes that let you work with data.</span></span>
> 
> <span data-ttu-id="bf720-196">Para acessar uma dada classe do .NET Framework, o código precisa saber não apenas o nome da classe, mas também o namespace que a classe está em.</span><span class="sxs-lookup"><span data-stu-id="bf720-196">In order to access any given class in the .NET Framework, code needs to know not just the class name, but also the namespace that the class is in.</span></span> <span data-ttu-id="bf720-197">Por exemplo, para usar o `Chart` auxiliar, o código precisa localizar o `System.Web.Helpers.Chart` classe, que combina o namespace (`System.Web.Helpers`) com o nome de classe (`Chart`).</span><span class="sxs-lookup"><span data-stu-id="bf720-197">For example, in order to use the `Chart` helper, code needs to find the `System.Web.Helpers.Chart` class, which combines the namespace (`System.Web.Helpers`) with the class name (`Chart`).</span></span> <span data-ttu-id="bf720-198">Isso é conhecido como a classe *totalmente qualificado* nome &#8212; seu local ambíguo concluída dentro do vastness do .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="bf720-198">This is known as the class's *fully-qualified* name &#8212; its complete, unambiguous location within the vastness of the .NET Framework.</span></span> <span data-ttu-id="bf720-199">No código, isso seria semelhante ao seguinte:</span><span class="sxs-lookup"><span data-stu-id="bf720-199">In code, this would look like the following:</span></span>
> 
> `var myChart = new System.Web.Helpers.Chart(width: 600, height: 400) // etc.`
> 
> <span data-ttu-id="bf720-200">No entanto, é difícil (e propenso a erros) para usar esses nomes longos, totalmente qualificado toda vez que você quer se referir a uma classe ou o auxiliar.</span><span class="sxs-lookup"><span data-stu-id="bf720-200">However, it's cumbersome (and error prone) to have to use these long, fully-qualified names every time you want to refer to a class or helper.</span></span> <span data-ttu-id="bf720-201">Portanto, para tornar mais fácil de usar nomes de classe, você pode *importar* os namespaces que você está interessado, que é geralmente é apenas uma série dentre vários namespaces no .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="bf720-201">Therefore, to make it easier to use class names, you can *import* the namespaces you're interested in, which is usually is just a handful from among the many namespaces in the .NET Framework.</span></span> <span data-ttu-id="bf720-202">Se você importar um namespace, você pode usar um nome de classe (`Chart`) em vez do nome totalmente qualificado (`System.Web.Helpers.Chart`).</span><span class="sxs-lookup"><span data-stu-id="bf720-202">If you've imported a namespace, you can use just a class name (`Chart`) instead of the fully qualified name (`System.Web.Helpers.Chart`).</span></span> <span data-ttu-id="bf720-203">Quando seu código é executado e encontra um nome de classe, ele pode examinar apenas os namespaces que você importou para localizar a classe.</span><span class="sxs-lookup"><span data-stu-id="bf720-203">When your code runs and encounters a class name, it can look in just the namespaces you've imported to find that class.</span></span>
> 
> <span data-ttu-id="bf720-204">Quando você usa páginas da Web ASP.NET com sintaxe do Razor para criar páginas da web, você normalmente usa o mesmo conjunto de classes de cada vez, incluindo o `WebPage` classe, os vários auxiliares e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="bf720-204">When you use ASP.NET Web Pages with Razor syntax to create web pages, you typically use the same set of classes each time, including the `WebPage` class, the various helpers, and so on.</span></span> <span data-ttu-id="bf720-205">Para salvar o trabalho de importação os namespaces relevantes toda vez que você cria um site, o ASP.NET está configurado para que ele importa automaticamente um conjunto de namespaces de núcleo para todos os sites.</span><span class="sxs-lookup"><span data-stu-id="bf720-205">To save you the work of importing the relevant namespaces every time you create a website, ASP.NET is configured so it automatically imports a set of core namespaces for every website.</span></span> <span data-ttu-id="bf720-206">É por isso que você ainda não teve que lidar com namespaces ou importando até agora; todas as classes que você já trabalhou com estão nos namespaces que já foram importados para você.</span><span class="sxs-lookup"><span data-stu-id="bf720-206">That's why you haven't had to deal with namespaces or importing up to now; all the classes you've worked with are in namespaces that are already imported for you.</span></span>
> 
> <span data-ttu-id="bf720-207">No entanto, às vezes você precisa trabalhar com uma classe que não está em um namespace que é importado automaticamente para você.</span><span class="sxs-lookup"><span data-stu-id="bf720-207">However, sometimes you need to work with a class that isn't in a namespace that's automatically imported for you.</span></span> <span data-ttu-id="bf720-208">Nesse caso, você pode usar do nome da classe totalmente qualificado, ou você pode importar manualmente o namespace que contém a classe.</span><span class="sxs-lookup"><span data-stu-id="bf720-208">In that case, you can either use that class's fully-qualified name, or you can manually import the namespace that contains the class.</span></span> <span data-ttu-id="bf720-209">Para importar um namespace, você deve usar o `using` instrução (`import` no Visual Basic), como você viu no exemplo anterior o artigo.</span><span class="sxs-lookup"><span data-stu-id="bf720-209">To import a namespace, you use the `using` statement (`import` in Visual Basic), as you saw in an example earlier the article.</span></span>
> 
> <span data-ttu-id="bf720-210">Por exemplo, o `DataSet` classe é o `System.Data` namespace.</span><span class="sxs-lookup"><span data-stu-id="bf720-210">For example, the `DataSet` class is in the `System.Data` namespace.</span></span> <span data-ttu-id="bf720-211">O `System.Data` namespace não está automaticamente disponível para páginas ASP.NET Razor.</span><span class="sxs-lookup"><span data-stu-id="bf720-211">The `System.Data` namespace is not automatically available to ASP.NET Razor pages.</span></span> <span data-ttu-id="bf720-212">Portanto, para trabalhar com o `DataSet` usando seu nome totalmente qualificado da classe, você pode usar código como este:</span><span class="sxs-lookup"><span data-stu-id="bf720-212">Therefore, to work with the `DataSet` class using its fully qualified name, you can use code like this:</span></span>
> 
> `var dataSet = new System.Data.DataSet();`
> 
> <span data-ttu-id="bf720-213">Se você precisar usar o `DataSet` classe várias vezes você pode importar um namespace como isso e, em seguida, use apenas o nome de classe no código:</span><span class="sxs-lookup"><span data-stu-id="bf720-213">If you have to use the `DataSet` class repeatedly you can import a namespace like this and then use just the class name in code:</span></span>
> 
> [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample8.cshtml)]
> 
> <span data-ttu-id="bf720-214">Você pode adicionar `using` instruções para todos os outros namespaces do .NET Framework que você deseja referenciar.</span><span class="sxs-lookup"><span data-stu-id="bf720-214">You can add `using` statements for any other .NET Framework namespaces that you want to reference.</span></span> <span data-ttu-id="bf720-215">No entanto, conforme observado, você não precisará fazer isso com frequência, porque a maioria das classes que você vai trabalhar com namespaces, que são importados automaticamente pelo ASP.NET para uso em *. cshtml* e *. vbhtml* páginas.</span><span class="sxs-lookup"><span data-stu-id="bf720-215">However, as noted, you won't need to do this often, because most of the classes that you'll work with are in namespaces that are imported automatically by ASP.NET for use in *.cshtml* and *.vbhtml* pages.</span></span>


<a id="Displaying_Charts"></a>
## <a name="displaying-charts-inside-a-web-page"></a><span data-ttu-id="bf720-216">Exibir gráficos dentro de uma página da Web</span><span class="sxs-lookup"><span data-stu-id="bf720-216">Displaying Charts Inside a Web Page</span></span>

<span data-ttu-id="bf720-217">Nos exemplos a você viu até agora, você cria um gráfico e, em seguida, o gráfico é renderizado diretamente para o navegador como um elemento gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-217">In the examples you've seen so far, you create a chart and then the chart is rendered directly to the browser as a graphic.</span></span> <span data-ttu-id="bf720-218">No entanto, em muitos casos, você deseja exibir um gráfico como parte de uma página, não apenas por si só no navegador.</span><span class="sxs-lookup"><span data-stu-id="bf720-218">In many cases, though, you want to display a chart as part of a page, not just by itself in the browser.</span></span> <span data-ttu-id="bf720-219">Para fazer isso requer um processo de duas etapas.</span><span class="sxs-lookup"><span data-stu-id="bf720-219">To do that requires a two-step process.</span></span> <span data-ttu-id="bf720-220">A primeira etapa é criar uma página que gera o gráfico, como você já viu.</span><span class="sxs-lookup"><span data-stu-id="bf720-220">The first step is to create a page that generates the chart, as you've already seen.</span></span>

<span data-ttu-id="bf720-221">A segunda etapa é exibir a imagem resultante em outra página.</span><span class="sxs-lookup"><span data-stu-id="bf720-221">The second step is to display the resulting image in another page.</span></span> <span data-ttu-id="bf720-222">Para exibir a imagem, você pode usar um HTML `<img>` elemento, da mesma maneira que faria para exibir qualquer imagem.</span><span class="sxs-lookup"><span data-stu-id="bf720-222">To display the image, you use an HTML `<img>` element, in the same way you would to display any image.</span></span> <span data-ttu-id="bf720-223">No entanto, em vez de fazer referência um *. jpg* ou *. PNG* arquivo, o `<img>` referências de elementos de *. cshtml* arquivo que contém o `Chart` auxiliar que cria o gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-223">However, instead of referencing a *.jpg* or *.png* file, the `<img>` element references the *.cshtml* file that contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="bf720-224">Quando a página de exibição é executada, o `<img>` elemento obtém a saída do `Chart` auxiliar e renderiza o gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-224">When the display page runs, the `<img>` element gets the output of the `Chart` helper and renders the chart.</span></span>

![](7-displaying-data-in-a-chart/_static/image11.jpg)

1. <span data-ttu-id="bf720-225">Crie um arquivo chamado *ShowChart.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="bf720-225">Create a file named *ShowChart.cshtml*.</span></span>
2. <span data-ttu-id="bf720-226">Substitua o conteúdo existente com o seguinte:</span><span class="sxs-lookup"><span data-stu-id="bf720-226">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample9.html)]

    <span data-ttu-id="bf720-227">O código usa o `<img>` elemento para exibir o gráfico que você criou anteriormente no *ChartArrayBasic.cshtml* arquivo.</span><span class="sxs-lookup"><span data-stu-id="bf720-227">The code uses the `<img>` element to display the chart that you created earlier in the *ChartArrayBasic.cshtml* file.</span></span>
3. <span data-ttu-id="bf720-228">Execute a página da web em um navegador.</span><span class="sxs-lookup"><span data-stu-id="bf720-228">Run the web page in a browser.</span></span> <span data-ttu-id="bf720-229">O *ShowChart.cshtml* arquivo exibe a imagem de gráfico com base no código dentro de *ChartArrayBasic.cshtml* arquivo.</span><span class="sxs-lookup"><span data-stu-id="bf720-229">The *ShowChart.cshtml* file displays the chart image based on the code contained in the *ChartArrayBasic.cshtml* file.</span></span>

<a id="Styling_a_Chart"></a>
## <a name="styling-a-chart"></a><span data-ttu-id="bf720-230">Um gráfico de estilo</span><span class="sxs-lookup"><span data-stu-id="bf720-230">Styling a Chart</span></span>

<span data-ttu-id="bf720-231">O `Chart` auxiliar dá suporte a um grande número de opções que permitem personalizar a aparência do gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-231">The `Chart` helper supports a large number of options that let you customize the appearance of the chart.</span></span> <span data-ttu-id="bf720-232">Você pode definir cores, fontes, bordas e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="bf720-232">You can set colors, fonts, borders, and so on.</span></span> <span data-ttu-id="bf720-233">Uma maneira fácil de personalizar a aparência de um gráfico é usar um *tema*.</span><span class="sxs-lookup"><span data-stu-id="bf720-233">An easy way to customize the appearance of a chart is to use a *theme*.</span></span> <span data-ttu-id="bf720-234">Os temas são conjuntos de informações que especificam como renderizar um gráfico usando fontes, cores, rótulos, paletas, bordas e efeitos.</span><span class="sxs-lookup"><span data-stu-id="bf720-234">Themes are collections of information that specify how to render a chart using fonts, colors, labels, palettes, borders, and effects.</span></span> <span data-ttu-id="bf720-235">(Observe que o estilo de um gráfico não indica o tipo de gráfico).</span><span class="sxs-lookup"><span data-stu-id="bf720-235">(Note that the style of a chart does not indicate the type of chart.)</span></span>

<span data-ttu-id="bf720-236">A tabela a seguir lista os temas internos.</span><span class="sxs-lookup"><span data-stu-id="bf720-236">The following table lists built-in themes.</span></span>

| <span data-ttu-id="bf720-237">{1&gt;Tema&lt;1}</span><span class="sxs-lookup"><span data-stu-id="bf720-237">Theme</span></span> | <span data-ttu-id="bf720-238">Descrição</span><span class="sxs-lookup"><span data-stu-id="bf720-238">Description</span></span> |
| --- | --- |
| `Vanilla` | <span data-ttu-id="bf720-239">Exibe colunas vermelhas em um plano de fundo branco.</span><span class="sxs-lookup"><span data-stu-id="bf720-239">Displays red columns on a white background.</span></span> |
| `Blue` | <span data-ttu-id="bf720-240">Exibe azul colunas em uma cor de fundo azul.</span><span class="sxs-lookup"><span data-stu-id="bf720-240">Displays blue columns on a blue gradient background.</span></span> |
| `Green` | <span data-ttu-id="bf720-241">Exibe azul colunas em um plano de fundo do gradiente verde.</span><span class="sxs-lookup"><span data-stu-id="bf720-241">Displays blue columns on a green gradient background.</span></span> |
| `Yellow` | <span data-ttu-id="bf720-242">Exibe colunas laranjas em um plano de fundo do gradiente amarelo.</span><span class="sxs-lookup"><span data-stu-id="bf720-242">Displays orange columns on a yellow gradient background.</span></span> |
| `Vanilla3D` | <span data-ttu-id="bf720-243">Exibe colunas vermelhas 3D em um plano de fundo branco.</span><span class="sxs-lookup"><span data-stu-id="bf720-243">Displays 3-D red columns on a white background.</span></span> |

<span data-ttu-id="bf720-244">Você pode especificar o tema a ser usado ao criar um novo gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-244">You can specify the theme to use when you create a new chart.</span></span>

1. <span data-ttu-id="bf720-245">Criar um novo arquivo denominado *ChartStyleGreen.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="bf720-245">Create a new file named *ChartStyleGreen.cshtml*.</span></span>
2. <span data-ttu-id="bf720-246">Substitua o conteúdo existente na página com o seguinte:</span><span class="sxs-lookup"><span data-stu-id="bf720-246">Replace the existing content in the page with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample10.cshtml)]

    <span data-ttu-id="bf720-247">Esse código é igual ao exemplo anterior que usa o banco de dados para os dados, mas adiciona o `theme` parâmetro quando ele cria o `Chart` objeto.</span><span class="sxs-lookup"><span data-stu-id="bf720-247">This code is the same as the earlier example that uses the database for data, but adds the `theme` parameter when it creates the `Chart` object.</span></span> <span data-ttu-id="bf720-248">O exemplo a seguir mostra o código alterado:</span><span class="sxs-lookup"><span data-stu-id="bf720-248">The following shows the changed code:</span></span>

    [!code-csharp[Main](7-displaying-data-in-a-chart/samples/sample11.cs)]
3. <span data-ttu-id="bf720-249">Execute a página em um navegador.</span><span class="sxs-lookup"><span data-stu-id="bf720-249">Run the page in a browser.</span></span> <span data-ttu-id="bf720-250">Você verá os mesmos dados que antes, mas o gráfico se parece mais refinado:</span><span class="sxs-lookup"><span data-stu-id="bf720-250">You see the same data as before, but the chart looks more polished:</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image12.jpg)

<a id="Saving_a_Chart"></a>
## <a name="saving-a-chart"></a><span data-ttu-id="bf720-251">Salvar um gráfico</span><span class="sxs-lookup"><span data-stu-id="bf720-251">Saving a Chart</span></span>

<span data-ttu-id="bf720-252">Quando você usa o `Chart` auxiliar como você viu até o momento neste artigo, o auxiliar recria o gráfico do zero cada vez que ele é invocado.</span><span class="sxs-lookup"><span data-stu-id="bf720-252">When you use the `Chart` helper as you've seen so far in this article, the helper re-creates the chart from scratch each time it's invoked.</span></span> <span data-ttu-id="bf720-253">Se necessário, o código para o gráfico também consulta novamente o banco de dados ou novamente lê o arquivo XML para obter os dados.</span><span class="sxs-lookup"><span data-stu-id="bf720-253">If necessary, the code for the chart also re-queries the database or re-reads the XML file to get the data.</span></span> <span data-ttu-id="bf720-254">Em alguns casos, isso pode ser uma operação complexa, como se o banco de dados que você está consultando for grande, ou se o arquivo XML contém muitos dados.</span><span class="sxs-lookup"><span data-stu-id="bf720-254">In some cases, doing this can be a complex operation, such as if the database that you're querying is large, or if the XML file contains a lot of data.</span></span> <span data-ttu-id="bf720-255">Mesmo se o gráfico não envolve muitos dados, o processo de criar dinamicamente uma imagem usa os recursos do servidor e, se muitas pessoas solicitar a página ou páginas que exibem o gráfico, pode haver um impacto no desempenho do seu site.</span><span class="sxs-lookup"><span data-stu-id="bf720-255">Even if the chart doesn't involve a lot of data, the process of dynamically creating an image takes up server resources, and if many people request the page or pages that display the chart, there can be an impact on the performance of your website.</span></span>

<span data-ttu-id="bf720-256">Para ajudar a reduzir o impacto potencial no desempenho de criação de um gráfico, você pode criar um tempo de gráfico primeiro precisar dele e, em seguida, salvá-lo.</span><span class="sxs-lookup"><span data-stu-id="bf720-256">To help you reduce the potential performance impact of creating a chart, you can create a chart the first time you need it and then save it.</span></span> <span data-ttu-id="bf720-257">Quando o gráfico for necessário novamente, em vez de regeneração-lo, é possível buscar a versão salva e renderizá.</span><span class="sxs-lookup"><span data-stu-id="bf720-257">When the chart is needed again, rather than regenerating it, you can just fetch the saved version and render that.</span></span>

<span data-ttu-id="bf720-258">Você pode salvar um gráfico das seguintes maneiras:</span><span class="sxs-lookup"><span data-stu-id="bf720-258">You can save a chart in these ways:</span></span>

- <span data-ttu-id="bf720-259">Armazenar em cache o gráfico na memória do computador (no servidor).</span><span class="sxs-lookup"><span data-stu-id="bf720-259">Cache the chart in computer memory (on the server).</span></span>
- <span data-ttu-id="bf720-260">Salve o gráfico como um arquivo de imagem.</span><span class="sxs-lookup"><span data-stu-id="bf720-260">Save the chart as an image file.</span></span>
- <span data-ttu-id="bf720-261">Salve o gráfico como um arquivo XML.</span><span class="sxs-lookup"><span data-stu-id="bf720-261">Save the chart as an XML file.</span></span> <span data-ttu-id="bf720-262">Essa opção permite modificar o gráfico antes de salvá-la.</span><span class="sxs-lookup"><span data-stu-id="bf720-262">This option lets you modify the chart before you save it.</span></span>

### <a name="caching-a-chart"></a><span data-ttu-id="bf720-263">Um gráfico de cache</span><span class="sxs-lookup"><span data-stu-id="bf720-263">Caching a Chart</span></span>

<span data-ttu-id="bf720-264">Depois de criar um gráfico, você pode armazenar em cache-lo.</span><span class="sxs-lookup"><span data-stu-id="bf720-264">After you've created a chart, you can cache it.</span></span> <span data-ttu-id="bf720-265">Um gráfico de cache significa que ele não precisa ser criado novamente se ele precisar ser exibida novamente.</span><span class="sxs-lookup"><span data-stu-id="bf720-265">Caching a chart means that it doesn't have to be re-created if it needs to be displayed again.</span></span> <span data-ttu-id="bf720-266">Quando você salva um gráfico no cache, você atribuir uma chave que deve ser exclusiva para o gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-266">When you save a chart in the cache, you give it a key that must be unique to that chart.</span></span>

<span data-ttu-id="bf720-267">Gráficos salvos em cache podem ser removidos se o servidor ficar com pouco memória.</span><span class="sxs-lookup"><span data-stu-id="bf720-267">Charts saved to the cache might be removed if the server runs low on memory.</span></span> <span data-ttu-id="bf720-268">Além disso, o cache é limpo se seu aplicativo for reiniciado por qualquer motivo.</span><span class="sxs-lookup"><span data-stu-id="bf720-268">In addition, the cache is cleared if your application restarts for any reason.</span></span> <span data-ttu-id="bf720-269">Portanto, o modo padrão para trabalhar com um gráfico em cache é sempre verificar primeiro se ele está disponível no cache e se não, em seguida criar ou recriá-la.</span><span class="sxs-lookup"><span data-stu-id="bf720-269">Therefore, the standard way to work with a cached chart is to always check first whether it's available in the cache, and if not, then to create or re-create it.</span></span>

1. <span data-ttu-id="bf720-270">Na raiz do seu site, crie um arquivo chamado *ShowCachedChart.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="bf720-270">At the root of your website, create a file named *ShowCachedChart.cshtml*.</span></span>
2. <span data-ttu-id="bf720-271">Substitua o conteúdo existente com o seguinte:</span><span class="sxs-lookup"><span data-stu-id="bf720-271">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample12.html)]

    <span data-ttu-id="bf720-272">O `<img>` marca inclui um `src` atributo que aponta para o *ChartSaveToCache.cshtml* de arquivos e passa uma chave para a página como uma cadeia de caracteres de consulta.</span><span class="sxs-lookup"><span data-stu-id="bf720-272">The `<img>` tag includes a `src` attribute that points to the *ChartSaveToCache.cshtml* file and passes a key to the page as a query string.</span></span> <span data-ttu-id="bf720-273">A chave contém o valor &quot;myChartKey&quot;.</span><span class="sxs-lookup"><span data-stu-id="bf720-273">The key contains the value &quot;myChartKey&quot;.</span></span> <span data-ttu-id="bf720-274">O *ChartSaveToCache.cshtml* arquivo contém o `Chart` auxiliar que cria o gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-274">The *ChartSaveToCache.cshtml* file contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="bf720-275">Você criará nesta página em breve.</span><span class="sxs-lookup"><span data-stu-id="bf720-275">You'll create this page in a moment.</span></span>

    <span data-ttu-id="bf720-276">No final da página, há um link para uma página chamada *ClearCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="bf720-276">At the end of the page there's a link to a page named *ClearCache.cshtml*.</span></span> <span data-ttu-id="bf720-277">Que é uma página que você também criará em breve.</span><span class="sxs-lookup"><span data-stu-id="bf720-277">That's a page that you'll also create shortly.</span></span> <span data-ttu-id="bf720-278">É necessário o *ClearCache.cshtml* somente para testar o cache para este exemplo, não é um link ou uma página que você normalmente incluiria ao trabalhar com gráficos armazenados em cache.</span><span class="sxs-lookup"><span data-stu-id="bf720-278">You need the *ClearCache.cshtml* only to test caching for this example — it's not a link or page that you'd normally include when working with cached charts.</span></span>
3. <span data-ttu-id="bf720-279">Na raiz do seu site, crie um novo arquivo denominado *ChartSaveToCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="bf720-279">At the root of your website, create a new file named *ChartSaveToCache.cshtml*.</span></span>
4. <span data-ttu-id="bf720-280">Substitua o conteúdo existente com o seguinte:</span><span class="sxs-lookup"><span data-stu-id="bf720-280">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample13.cshtml)]

    <span data-ttu-id="bf720-281">O código primeiro verifica se nada foi passado como o valor da chave na cadeia de caracteres de consulta.</span><span class="sxs-lookup"><span data-stu-id="bf720-281">The code first checks whether anything was passed as the key value in the query string.</span></span> <span data-ttu-id="bf720-282">Se assim, o código tenta ler um gráfico no cache chamando o `GetFromCache` método e passá-lo a chave.</span><span class="sxs-lookup"><span data-stu-id="bf720-282">If so, the code tries to read a chart out of the cache by calling the `GetFromCache` method and passing it the key.</span></span> <span data-ttu-id="bf720-283">Se for descoberto que não há nada no cache sob essa chave (o que acontece na primeira vez que o gráfico é solicitado), o código cria o gráfico como de costume.</span><span class="sxs-lookup"><span data-stu-id="bf720-283">If it turns out that there's nothing in the cache under that key (which would happen the first time that the chart is requested), the code creates the chart as usual.</span></span> <span data-ttu-id="bf720-284">Quando o gráfico for concluído, o código salva em cache chamando `SaveToCache`.</span><span class="sxs-lookup"><span data-stu-id="bf720-284">When the chart is finished, the code saves it to the cache by calling `SaveToCache`.</span></span> <span data-ttu-id="bf720-285">Esse método requer uma chave (para que o gráfico pode ser solicitado posteriormente) e a quantidade de tempo que o gráfico deve ser salvo no cache.</span><span class="sxs-lookup"><span data-stu-id="bf720-285">That method requires a key (so the chart can be requested later), and the amount of time that the chart should be saved in the cache.</span></span> <span data-ttu-id="bf720-286">(A hora exata que você deve armazenar em cache um gráfico dependem quantas vezes você considerar que podem alterar os dados que ele representa.) O `SaveToCache` método também requer um `slidingExpiration` parâmetro &#8212; se isso for definido como true, o tempo limite do contador é redefinido cada vez que o gráfico é acessado.</span><span class="sxs-lookup"><span data-stu-id="bf720-286">(The exact time you'd cache a chart would depend on how often you thought the data it represents might change.) The `SaveToCache` method also requires a `slidingExpiration` parameter &#8212; if this is set to true, the timeout counter is reset each time the chart is accessed.</span></span> <span data-ttu-id="bf720-287">Nesse caso, ele em vigor significa que a entrada do cache do gráfico irá expirar em 2 minutos após a última vez em que alguém acessado o gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-287">In this case, it in effect means that the chart's cache entry expires 2 minutes after the last time someone accessed the chart.</span></span> <span data-ttu-id="bf720-288">(A alternativa para a expiração deslizante é expiração absoluta, que significa que a entrada do cache seria expirar exatamente 2 minutos depois que ele foi colocado em cache, não importa quantas vezes ela havia sido acessada.)</span><span class="sxs-lookup"><span data-stu-id="bf720-288">(The alternative to sliding expiration is absolute expiration, meaning that the cache entry would expire exactly 2 minutes after it was put into the cache, no matter how often it had been accessed.)</span></span>

    <span data-ttu-id="bf720-289">Por fim, o código usa o `WriteFromCache` método para buscar e renderizar o gráfico do cache.</span><span class="sxs-lookup"><span data-stu-id="bf720-289">Finally, the code uses the `WriteFromCache` method to fetch and render the chart from the cache.</span></span> <span data-ttu-id="bf720-290">Observe que esse método está fora do `if` bloco que verifica o cache, pois ele receberá o gráfico do cache se o gráfico houve inicial ou teve de ser gerado e salvo no cache.</span><span class="sxs-lookup"><span data-stu-id="bf720-290">Note that this method is outside the `if` block that checks the cache, because it will get the chart from the cache whether the chart was there to begin with or had to be generated and saved in the cache.</span></span>

    <span data-ttu-id="bf720-291">Observe que no exemplo, o `AddTitle` método inclui um carimbo de hora.</span><span class="sxs-lookup"><span data-stu-id="bf720-291">Notice that in the example, the `AddTitle` method includes a timestamp.</span></span> <span data-ttu-id="bf720-292">(Ele adiciona a data atual e hora &#8212; `DateTime.Now` &#8212; para o título.)</span><span class="sxs-lookup"><span data-stu-id="bf720-292">(It adds the current date and time &#8212; `DateTime.Now` &#8212; to the title.)</span></span>
5. <span data-ttu-id="bf720-293">Criar uma nova página chamada *ClearCache.cshtml* e substituir seu conteúdo com o seguinte:</span><span class="sxs-lookup"><span data-stu-id="bf720-293">Create a new page named *ClearCache.cshtml* and replace its content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample14.cshtml)]

    <span data-ttu-id="bf720-294">Esta página usa o `WebCache` auxiliar para remover o plano que está armazenada em cache *ChartSaveToCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="bf720-294">This page uses the `WebCache` helper to remove the chart that's cached in *ChartSaveToCache.cshtml*.</span></span> <span data-ttu-id="bf720-295">Conforme observado anteriormente, normalmente você não precisa ter uma página como essa.</span><span class="sxs-lookup"><span data-stu-id="bf720-295">As noted earlier, you don't normally have to have a page like this.</span></span> <span data-ttu-id="bf720-296">Você está criando-o aqui apenas para tornar mais fácil de testar o armazenamento em cache.</span><span class="sxs-lookup"><span data-stu-id="bf720-296">You're creating it here only to make it easier to test caching.</span></span>
6. <span data-ttu-id="bf720-297">Execute o *ShowCachedChart.cshtml* página da web em um navegador.</span><span class="sxs-lookup"><span data-stu-id="bf720-297">Run the *ShowCachedChart.cshtml* web page in a browser.</span></span> <span data-ttu-id="bf720-298">A página exibe a imagem do gráfico com base no código dentro de *ChartSaveToCache.cshtml* arquivo.</span><span class="sxs-lookup"><span data-stu-id="bf720-298">The page displays the chart image based on the code contained in the *ChartSaveToCache.cshtml* file.</span></span> <span data-ttu-id="bf720-299">Observe que o carimbo de hora diz o título do gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-299">Take note of what the timestamp says in the chart title.</span></span> 

    ![Descrição: Imagem de gráfico básico com carimbo de hora em que o título do gráfico](7-displaying-data-in-a-chart/_static/image13.jpg)
7. <span data-ttu-id="bf720-301">Feche o navegador.</span><span class="sxs-lookup"><span data-stu-id="bf720-301">Close the browser.</span></span>
8. <span data-ttu-id="bf720-302">Execute o *ShowCachedChart.cshtml* novamente.</span><span class="sxs-lookup"><span data-stu-id="bf720-302">Run the *ShowCachedChart.cshtml* again.</span></span> <span data-ttu-id="bf720-303">Observe que o carimbo de hora é o mesmo de antes, que indica que o gráfico não foi gerada novamente, mas em vez disso, foi lido no cache.</span><span class="sxs-lookup"><span data-stu-id="bf720-303">Notice that the timestamp is the same as before, which indicates that the chart was not regenerated, but was instead read from the cache.</span></span>
9. <span data-ttu-id="bf720-304">Em *ShowCachedChart.cshtml*, clique no **Limpar cache** link.</span><span class="sxs-lookup"><span data-stu-id="bf720-304">In *ShowCachedChart.cshtml*, click the **Clear cache** link.</span></span> <span data-ttu-id="bf720-305">Isso leva você para *ClearCache.cshtml*, que informa que o cache foi limpo.</span><span class="sxs-lookup"><span data-stu-id="bf720-305">This takes you to *ClearCache.cshtml*, which reports that the cache has been cleared.</span></span>
10. <span data-ttu-id="bf720-306">Clique o **retornar ao ShowCachedChart.cshtml** link ou execute novamente *ShowCachedChart.cshtml* do WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="bf720-306">Click the **Return to ShowCachedChart.cshtml** link, or re-run *ShowCachedChart.cshtml* from WebMatrix.</span></span> <span data-ttu-id="bf720-307">Observe que esse tempo o carimbo de hora foi alterado, porque o cache foi limpo.</span><span class="sxs-lookup"><span data-stu-id="bf720-307">Notice that this time the timestamp has changed, because the cache has been cleared.</span></span> <span data-ttu-id="bf720-308">Portanto, o código era necessário regenerar o gráfico e colocá-lo de volta para o cache.</span><span class="sxs-lookup"><span data-stu-id="bf720-308">Therefore, the code had to regenerate the chart and put it back into the cache.</span></span>

### <a name="saving-a-chart-as-an-image-file"></a><span data-ttu-id="bf720-309">Salvar um gráfico como um arquivo de imagem</span><span class="sxs-lookup"><span data-stu-id="bf720-309">Saving a Chart as an Image File</span></span>

<span data-ttu-id="bf720-310">Você também pode salvar um gráfico como um arquivo de imagem (por exemplo, como um *. jpg* arquivo) no servidor.</span><span class="sxs-lookup"><span data-stu-id="bf720-310">You can also save a chart as an image file (for example, as a *.jpg* file) on the server.</span></span> <span data-ttu-id="bf720-311">Você pode usar o arquivo de imagem de maneira que faria com qualquer imagem.</span><span class="sxs-lookup"><span data-stu-id="bf720-311">You can then use the image file the way you would any image.</span></span> <span data-ttu-id="bf720-312">A vantagem é que o arquivo é armazenado em vez de salvos em um cache temporário.</span><span class="sxs-lookup"><span data-stu-id="bf720-312">The advantage is the file is stored rather than saved to a temporary cache.</span></span> <span data-ttu-id="bf720-313">Você pode salvar uma nova imagem de gráfico em momentos diferentes (por exemplo, a cada hora) e, em seguida, mantenha um registro permanente das alterações que ocorrem ao longo do tempo.</span><span class="sxs-lookup"><span data-stu-id="bf720-313">You can save a new chart image at different times (for example, every hour) and then keep a permanent record of the changes that occur over time.</span></span> <span data-ttu-id="bf720-314">Observe que você deve garantir que seu aplicativo da web tem permissão para salvar um arquivo para a pasta no servidor onde você deseja colocar o arquivo de imagem.</span><span class="sxs-lookup"><span data-stu-id="bf720-314">Note that you must make sure that your web application has permission to save a file to the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="bf720-315">Na raiz do seu site, crie uma pasta chamada  *\_ChartFiles* se ele ainda não existir.</span><span class="sxs-lookup"><span data-stu-id="bf720-315">At the root of your website, create a folder named *\_ChartFiles* if it does not already exist.</span></span>
2. <span data-ttu-id="bf720-316">Na raiz do seu site, crie um novo arquivo denominado *ChartSave.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="bf720-316">At the root of your website, create a new file named *ChartSave.cshtml*.</span></span>
3. <span data-ttu-id="bf720-317">Substitua o conteúdo existente com o seguinte:</span><span class="sxs-lookup"><span data-stu-id="bf720-317">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample15.cshtml)]

    <span data-ttu-id="bf720-318">O código primeiro verifica se o *. jpg* arquivo existe chamando o `File.Exists` método.</span><span class="sxs-lookup"><span data-stu-id="bf720-318">The code first checks to see whether the *.jpg* file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="bf720-319">Se o arquivo não existir, o código cria um novo `Chart` de uma matriz.</span><span class="sxs-lookup"><span data-stu-id="bf720-319">If the file does not exist, the code creates a new `Chart` from an array.</span></span> <span data-ttu-id="bf720-320">Neste momento, o código chama o `Save` método e passa o `path` parâmetro para especificar o caminho do arquivo e o nome de arquivo onde salvar o gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-320">This time, the code calls the `Save` method and passes the `path` parameter to specify the file path and file name of where to save the chart.</span></span> <span data-ttu-id="bf720-321">No corpo da página, um `<img>` elemento usa o caminho para apontar para o *. jpg* arquivo a ser exibido.</span><span class="sxs-lookup"><span data-stu-id="bf720-321">In the body of the page, an `<img>` element uses the path to point to the *.jpg* file to display.</span></span>
4. <span data-ttu-id="bf720-322">Execute o *ChartSave.cshtml* arquivo.</span><span class="sxs-lookup"><span data-stu-id="bf720-322">Run the *ChartSave.cshtml* file.</span></span>
5. <span data-ttu-id="bf720-323">Retornar ao WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="bf720-323">Return to WebMatrix.</span></span> <span data-ttu-id="bf720-324">Observe que um arquivo de imagem denominado *chart01.jpg* foi salva no  *\_ChartFiles* pasta.</span><span class="sxs-lookup"><span data-stu-id="bf720-324">Notice that an image file named *chart01.jpg* has been saved in the *\_ChartFiles* folder.</span></span>

### <a name="saving-a-chart-as-an-xml-file"></a><span data-ttu-id="bf720-325">Salvar um gráfico como um arquivo XML</span><span class="sxs-lookup"><span data-stu-id="bf720-325">Saving a Chart as an XML File</span></span>

<span data-ttu-id="bf720-326">Por fim, você pode salvar um gráfico como um arquivo XML no servidor.</span><span class="sxs-lookup"><span data-stu-id="bf720-326">Finally, you can save a chart as an XML file on the server.</span></span> <span data-ttu-id="bf720-327">Uma vantagem de usar esse método sobre o gráfico de cache ou salvar o gráfico em um arquivo é que você pode modificar o XML antes de exibir o gráfico se você quiser.</span><span class="sxs-lookup"><span data-stu-id="bf720-327">An advantage of using this method over caching the chart or saving the chart to a file is that you could modify the XML before displaying the chart if you wanted to.</span></span> <span data-ttu-id="bf720-328">Seu aplicativo deve ter permissões de leitura/gravação para a pasta no servidor onde você deseja colocar o arquivo de imagem.</span><span class="sxs-lookup"><span data-stu-id="bf720-328">Your application has to have read/write permissions for the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="bf720-329">Na raiz do seu site, crie um novo arquivo denominado *ChartSaveXml.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="bf720-329">At the root of your website, create a new file named *ChartSaveXml.cshtml*.</span></span>
2. <span data-ttu-id="bf720-330">Substitua o conteúdo existente com o seguinte:</span><span class="sxs-lookup"><span data-stu-id="bf720-330">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample16.cshtml)]

    <span data-ttu-id="bf720-331">Esse código é semelhante ao código que você viu anteriormente para armazenar um gráfico no cache, exceto que ele usa um arquivo XML.</span><span class="sxs-lookup"><span data-stu-id="bf720-331">This code is similar to the code that you saw earlier for storing a chart in the cache, except that it uses an XML file.</span></span> <span data-ttu-id="bf720-332">O código primeiro verifica se o arquivo XML existe chamando o `File.Exists` método.</span><span class="sxs-lookup"><span data-stu-id="bf720-332">The code first checks to see whether the XML file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="bf720-333">Se o arquivo existir, o código cria um novo `Chart` de objeto e passa o nome de arquivo como o `themePath` parâmetro.</span><span class="sxs-lookup"><span data-stu-id="bf720-333">If the file does exist, the code creates a new `Chart` object and passes the file name as the `themePath` parameter.</span></span> <span data-ttu-id="bf720-334">Isso cria o gráfico com base no que se encontra no arquivo XML.</span><span class="sxs-lookup"><span data-stu-id="bf720-334">This creates the chart based on whatever's in the XML file.</span></span> <span data-ttu-id="bf720-335">Se o arquivo XML não existir, o código cria um gráfico como normal e, em seguida, chama `SaveXml` para salvá-lo.</span><span class="sxs-lookup"><span data-stu-id="bf720-335">If the XML file doesn't already exist, the code creates a chart like normal and then calls `SaveXml` to save it.</span></span> <span data-ttu-id="bf720-336">O gráfico é renderizado usando o `Write` método, como você viu antes.</span><span class="sxs-lookup"><span data-stu-id="bf720-336">The chart is rendered using the `Write` method, as you've seen before.</span></span>

    <span data-ttu-id="bf720-337">Assim como acontece com a página que mostrou cache, esse código inclui um carimbo de hora em que o título do gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-337">As with the page that showed caching, this code includes a timestamp in the chart title.</span></span>
3. <span data-ttu-id="bf720-338">Criar uma nova página chamada *ChartDisplayXMLChart.cshtml* e adicione a seguinte marcação a ela:</span><span class="sxs-lookup"><span data-stu-id="bf720-338">Create a new page named *ChartDisplayXMLChart.cshtml* and add the following markup to it:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample17.html)]
4. <span data-ttu-id="bf720-339">Execute o *ChartDisplayXMLChart.cshtml* página.</span><span class="sxs-lookup"><span data-stu-id="bf720-339">Run the *ChartDisplayXMLChart.cshtml* page.</span></span> <span data-ttu-id="bf720-340">O gráfico é exibido.</span><span class="sxs-lookup"><span data-stu-id="bf720-340">The chart is displayed.</span></span> <span data-ttu-id="bf720-341">Anote o carimbo de hora no título do gráfico.</span><span class="sxs-lookup"><span data-stu-id="bf720-341">Take note of the timestamp in the chart's title.</span></span>
5. <span data-ttu-id="bf720-342">Feche o navegador.</span><span class="sxs-lookup"><span data-stu-id="bf720-342">Close the browser.</span></span>
6. <span data-ttu-id="bf720-343">No WebMatrix, clique com botão direito do  *\_ChartFiles* pasta, clique em **atualização**e, em seguida, abra a pasta.</span><span class="sxs-lookup"><span data-stu-id="bf720-343">In WebMatrix, right-click the *\_ChartFiles* folder, click **Refresh**, and then open the folder.</span></span> <span data-ttu-id="bf720-344">O *XMLChart.xml* arquivo nessa pasta foi criado com o `Chart` auxiliar.</span><span class="sxs-lookup"><span data-stu-id="bf720-344">The *XMLChart.xml* file in this folder was created by the `Chart` helper.</span></span> 

    ![Descrição: A _ChartFiles pasta mostrando o arquivo XMLChart.xml criado pelo auxiliar de gráfico.](7-displaying-data-in-a-chart/_static/image14.jpg)
7. <span data-ttu-id="bf720-346">Execute o *ChartDisplayXMLChart.cshtml* página novamente.</span><span class="sxs-lookup"><span data-stu-id="bf720-346">Run the *ChartDisplayXMLChart.cshtml* page again.</span></span> <span data-ttu-id="bf720-347">O gráfico mostra o mesmo carimbo de hora na primeira vez que você executou a página.</span><span class="sxs-lookup"><span data-stu-id="bf720-347">The chart shows the same timestamp as the first time you ran the page.</span></span> <span data-ttu-id="bf720-348">Isso ocorre porque o gráfico está sendo gerado do XML que você salvou anteriormente.</span><span class="sxs-lookup"><span data-stu-id="bf720-348">That's because the chart is being generated from the XML you saved earlier.</span></span>
8. <span data-ttu-id="bf720-349">No WebMatrix, abra o  *\_ChartFiles* pasta e exclua o *XMLChart.xml* arquivo.</span><span class="sxs-lookup"><span data-stu-id="bf720-349">In WebMatrix, open the *\_ChartFiles* folder and delete the *XMLChart.xml* file.</span></span>
9. <span data-ttu-id="bf720-350">Execute o *ChartDisplayXMLChart.cshtml* página mais uma vez.</span><span class="sxs-lookup"><span data-stu-id="bf720-350">Run the *ChartDisplayXMLChart.cshtml* page once more.</span></span> <span data-ttu-id="bf720-351">Neste momento, o carimbo de hora é atualizado, pois o `Chart` auxiliar tinha que recriar o arquivo XML.</span><span class="sxs-lookup"><span data-stu-id="bf720-351">This time, the timestamp is updated, because the `Chart` helper had to recreate the XML file.</span></span> <span data-ttu-id="bf720-352">Se você quiser, marque o  *\_ChartFiles* pasta e observe que o arquivo XML está de volta.</span><span class="sxs-lookup"><span data-stu-id="bf720-352">If you want, check the *\_ChartFiles* folder and notice that the XML file is back.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="bf720-353">Recursos adicionais</span><span class="sxs-lookup"><span data-stu-id="bf720-353">Additional Resources</span></span>

- [<span data-ttu-id="bf720-354">Introdução ao trabalho com um banco de dados da Web do ASP.NET páginas de Sites</span><span class="sxs-lookup"><span data-stu-id="bf720-354">Introduction to Working with a Database in ASP.NET Web Pages Sites</span></span>](https://go.microsoft.com/fwlink/?LinkId=202893)
- [<span data-ttu-id="bf720-355">Usar o cache da Web ASP.NET de páginas de Sites para melhorar o desempenho</span><span class="sxs-lookup"><span data-stu-id="bf720-355">Using Caching in ASP.NET Web Pages Sites to Improve Performance</span></span>](https://go.microsoft.com/fwlink/?LinkId=202903)
- <span data-ttu-id="bf720-356">[Classe gráfico](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (referência de API de páginas da Web do ASP.NET no MSDN)</span><span class="sxs-lookup"><span data-stu-id="bf720-356">[Chart Class](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.NET Web Pages API reference on MSDN)</span></span>
