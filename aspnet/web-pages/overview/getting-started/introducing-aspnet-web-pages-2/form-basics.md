---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: Introdução ao ASP.NET Web Pages - Noções básicas de formulário HTML | Microsoft Docs
author: tfitzmac
description: Este tutorial mostra os fundamentos de como criar um formulário de entrada e como lidar com a entrada do usuário quando você usa o ASP.NET Web Pages (Razor). E agora que você...
ms.author: aspnetcontent
ms.date: 05/28/2015
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: 609c1c06ed8f29db82b5dd565a935440d4430819
ms.sourcegitcommit: b28cd0313af316c051c2ff8549865bff67f2fbb4
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 07/05/2018
ms.locfileid: "37815389"
---
<a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="c0b38-104">Introdução ao ASP.NET Web Pages - Noções básicas de formulário HTML</span><span class="sxs-lookup"><span data-stu-id="c0b38-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>
====================
<span data-ttu-id="c0b38-105">por [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="c0b38-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="c0b38-106">Este tutorial mostra os fundamentos de como criar um formulário de entrada e como lidar com a entrada do usuário quando você usa o ASP.NET Web Pages (Razor).</span><span class="sxs-lookup"><span data-stu-id="c0b38-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="c0b38-107">E agora que você tem um banco de dados, você usará o suas habilidades de formulário para permitir que os usuários a localizar filmes específicos no banco de dados.</span><span class="sxs-lookup"><span data-stu-id="c0b38-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="c0b38-108">Ele pressupõe que você tenha concluído a série por meio [Introduction para exibir dados usando o ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span><span class="sxs-lookup"><span data-stu-id="c0b38-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="c0b38-109">O que você aprenderá:</span><span class="sxs-lookup"><span data-stu-id="c0b38-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="c0b38-110">Como criar um formulário usando elementos HTML padrão.</span><span class="sxs-lookup"><span data-stu-id="c0b38-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="c0b38-111">Como ler o usuário do entrada em um formulário.</span><span class="sxs-lookup"><span data-stu-id="c0b38-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="c0b38-112">Como criar uma consulta SQL que seletivamente obtém os dados usando uma pesquisa de termos que o usuário fornece.</span><span class="sxs-lookup"><span data-stu-id="c0b38-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="c0b38-113">Como fazer com que os campos na página "lembrar" o que o usuário inseriu.</span><span class="sxs-lookup"><span data-stu-id="c0b38-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="c0b38-114">Recursos/tecnologias abordadas:</span><span class="sxs-lookup"><span data-stu-id="c0b38-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="c0b38-115">O objeto `Request`.</span><span class="sxs-lookup"><span data-stu-id="c0b38-115">The `Request` object.</span></span>
> - <span data-ttu-id="c0b38-116">O SQL `Where` cláusula.</span><span class="sxs-lookup"><span data-stu-id="c0b38-116">The SQL `Where` clause.</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="c0b38-117">O que você vai criar</span><span class="sxs-lookup"><span data-stu-id="c0b38-117">What You'll Build</span></span>

<span data-ttu-id="c0b38-118">No tutorial anterior, você criou um banco de dados, adicionar dados a ele e usado, em seguida, o `WebGrid` auxiliar para exibir os dados.</span><span class="sxs-lookup"><span data-stu-id="c0b38-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="c0b38-119">Neste tutorial, você adicionará uma caixa de pesquisa que permite que você encontre os filmes de um gênero específico ou cujo título contém qualquer palavra que você inserir.</span><span class="sxs-lookup"><span data-stu-id="c0b38-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="c0b38-120">(Por exemplo, você poderá encontrar todos os filmes cujo gênero é "Ação" ou cujo título contém "Harry" ou "Adventure.")</span><span class="sxs-lookup"><span data-stu-id="c0b38-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="c0b38-121">Quando você concluir este tutorial, você terá uma página como esta:</span><span class="sxs-lookup"><span data-stu-id="c0b38-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![Página de filmes com pesquisa de título e gênero](form-basics/_static/image1.png)

<span data-ttu-id="c0b38-123">A parte de listagem da página é o mesmo do último tutorial &mdash; uma grade.</span><span class="sxs-lookup"><span data-stu-id="c0b38-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="c0b38-124">A diferença será que a grade mostrará apenas os filmes que você pesquisou.</span><span class="sxs-lookup"><span data-stu-id="c0b38-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="c0b38-125">Sobre os formulários HTML</span><span class="sxs-lookup"><span data-stu-id="c0b38-125">About HTML Forms</span></span>

<span data-ttu-id="c0b38-126">(Se você tem experiência com a criação de formulários HTML e com a diferença entre `GET` e `POST`, você poderá ignorar esta seção.)</span><span class="sxs-lookup"><span data-stu-id="c0b38-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="c0b38-127">Um formulário tem elementos de entrada do usuário &mdash; caixas de texto, botões, botões de opção, caixas de seleção, listas suspensas e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="c0b38-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="c0b38-128">Os usuários preenchem esses controles ou faça seleções e, em seguida, enviarem o formulário clicando em um botão.</span><span class="sxs-lookup"><span data-stu-id="c0b38-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="c0b38-129">A sintaxe básica de HTML de um formulário é ilustrada por este exemplo:</span><span class="sxs-lookup"><span data-stu-id="c0b38-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="c0b38-130">Quando essa marcação é executado em uma página, ele cria um formulário simples que se parece com esta ilustração:</span><span class="sxs-lookup"><span data-stu-id="c0b38-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![Formulário básico de HTML como renderizado no navegador](form-basics/_static/image2.png)

<span data-ttu-id="c0b38-132">O `<form>` elemento abrange elementos HTML para serem enviadas.</span><span class="sxs-lookup"><span data-stu-id="c0b38-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="c0b38-133">(Um erro fácil fazer é adicionar elementos à página, mas esquecer de colocá-los dentro de um `<form>` elemento.</span><span class="sxs-lookup"><span data-stu-id="c0b38-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="c0b38-134">Nesse caso, nada será enviado.) O `method` atributo informa ao navegador como enviar a entrada do usuário.</span><span class="sxs-lookup"><span data-stu-id="c0b38-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="c0b38-135">Defina isso como `post` se você estiver executando uma atualização no servidor ou para `get` se você apenas estiver buscando dados do servidor.</span><span class="sxs-lookup"><span data-stu-id="c0b38-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> <span data-ttu-id="c0b38-136">**GET, POST e segurança do verbo HTTP**</span><span class="sxs-lookup"><span data-stu-id="c0b38-136">**GET, POST, and HTTP Verb Safety**</span></span>
> 
> <span data-ttu-id="c0b38-137">HTTP, o protocolo usado por navegadores e servidores para trocar informações, é surpreendentemente simple em suas operações básicas.</span><span class="sxs-lookup"><span data-stu-id="c0b38-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="c0b38-138">Os navegadores usam apenas alguns verbos para fazer solicitações para servidores.</span><span class="sxs-lookup"><span data-stu-id="c0b38-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="c0b38-139">Quando você escreve o código para a web, é útil entender esses verbos e usá-los como o navegador e o servidor.</span><span class="sxs-lookup"><span data-stu-id="c0b38-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="c0b38-140">Longe os verbos mais comumente usados são estes:</span><span class="sxs-lookup"><span data-stu-id="c0b38-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - <span data-ttu-id="c0b38-141">`GET`.</span><span class="sxs-lookup"><span data-stu-id="c0b38-141">`GET`.</span></span> <span data-ttu-id="c0b38-142">O navegador usará esse verbo para buscar algo no servidor.</span><span class="sxs-lookup"><span data-stu-id="c0b38-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="c0b38-143">Por exemplo, quando você digita uma URL no seu navegador, o navegador executa um `GET` operação para solicitar a página desejada.</span><span class="sxs-lookup"><span data-stu-id="c0b38-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="c0b38-144">Se a página inclui elementos gráficos, o navegador executa adicionais `GET` operações para obter as imagens.</span><span class="sxs-lookup"><span data-stu-id="c0b38-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="c0b38-145">Se o `GET` operação tem que passar informações para o servidor, as informações são passadas como parte da URL na cadeia de caracteres de consulta.</span><span class="sxs-lookup"><span data-stu-id="c0b38-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - <span data-ttu-id="c0b38-146">`POST`.</span><span class="sxs-lookup"><span data-stu-id="c0b38-146">`POST`.</span></span> <span data-ttu-id="c0b38-147">O navegador envia um `POST` solicitação a fim de enviar dados a serem adicionadas ou alteradas no servidor.</span><span class="sxs-lookup"><span data-stu-id="c0b38-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="c0b38-148">Por exemplo, o `POST` verbo é usado para criar registros em um banco de dados ou alterar os existentes.</span><span class="sxs-lookup"><span data-stu-id="c0b38-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="c0b38-149">Na maioria das vezes, quando você preenche um formulário e clique no botão Enviar, o navegador executa um `POST` operação.</span><span class="sxs-lookup"><span data-stu-id="c0b38-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="c0b38-150">Em um `POST` operação, os dados que está sendo passados para o servidor estão no corpo da página.</span><span class="sxs-lookup"><span data-stu-id="c0b38-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="c0b38-151">Uma importante distinção entre esses verbos é que um `GET` operação não deve alterar nada no servidor — ou colocá-lo de forma um pouco mais abstrata, um `GET` operação não resultará em uma alteração de estado no servidor.</span><span class="sxs-lookup"><span data-stu-id="c0b38-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="c0b38-152">Você pode executar um `GET` operação nos mesmos recursos de quantas vezes desejar, e esses recursos não são alterados.</span><span class="sxs-lookup"><span data-stu-id="c0b38-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="c0b38-153">(Um `GET` operação geralmente é dito "seguro", ou para usar um termo técnico, está *idempotente*.) Por outro lado, é claro, um `POST` solicitar alterações algo no servidor cada vez que você executa a operação.</span><span class="sxs-lookup"><span data-stu-id="c0b38-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="c0b38-154">Dois exemplos ajudarão a ilustrar essa distinção.</span><span class="sxs-lookup"><span data-stu-id="c0b38-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="c0b38-155">Quando você executa uma pesquisa usando um mecanismo como o Bing ou o Google, você preenche um formulário que consiste em uma caixa de texto e, em seguida, clique no botão Pesquisar.</span><span class="sxs-lookup"><span data-stu-id="c0b38-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="c0b38-156">O navegador executa um `GET` operação com o valor inserido na caixa de passada como parte da URL.</span><span class="sxs-lookup"><span data-stu-id="c0b38-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="c0b38-157">Usando um `GET` operação para esse tipo de formulário é bom, porque uma operação de pesquisa não altera todos os recursos no servidor, ele simplesmente busca informações.</span><span class="sxs-lookup"><span data-stu-id="c0b38-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="c0b38-158">Agora, considere o processo de ordenação algo on-line.</span><span class="sxs-lookup"><span data-stu-id="c0b38-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="c0b38-159">Preencha os detalhes do pedido e, em seguida, clique no botão Enviar.</span><span class="sxs-lookup"><span data-stu-id="c0b38-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="c0b38-160">Esta operação será um `POST` solicitar, porque a operação resultará em alterações no servidor, como um novo registro de ordem, uma alteração nas informações de conta e talvez muitas outras alterações.</span><span class="sxs-lookup"><span data-stu-id="c0b38-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="c0b38-161">Ao contrário de `GET` operação, você não pode se repetir seu `POST` solicitação — se você fez, cada vez que você reenviar a solicitação, geraria um novo pedido no servidor.</span><span class="sxs-lookup"><span data-stu-id="c0b38-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="c0b38-162">(Em casos como esse, sites geralmente avisará que você não clique em um botão enviar mais de uma vez ou desabilitará o botão Enviar para que você não envie o formulário novamente acidentalmente.)</span><span class="sxs-lookup"><span data-stu-id="c0b38-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="c0b38-163">No decorrer deste tutorial, você vai usar um `GET` operação e um `POST` operação para trabalhar com formulários HTML.</span><span class="sxs-lookup"><span data-stu-id="c0b38-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="c0b38-164">Vamos explicar cada motivo caso o verbo que você usa é aquele apropriado.</span><span class="sxs-lookup"><span data-stu-id="c0b38-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="c0b38-165">(Para saber mais sobre verbos HTTP, consulte o [definições de método](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) artigo no site do W3C.)</span><span class="sxs-lookup"><span data-stu-id="c0b38-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>


<span data-ttu-id="c0b38-166">A maioria dos elementos de entrada do usuário são HTML `<input>` elementos.</span><span class="sxs-lookup"><span data-stu-id="c0b38-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="c0b38-167">Elas se pareçam com `<input type="type" name="name">,` onde *tipo* indica o tipo de controle de entrada do usuário desejado.</span><span class="sxs-lookup"><span data-stu-id="c0b38-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="c0b38-168">Esses elementos são mais comuns:</span><span class="sxs-lookup"><span data-stu-id="c0b38-168">These elements are the common ones:</span></span>

- <span data-ttu-id="c0b38-169">Caixa de texto: `<input type="text">`</span><span class="sxs-lookup"><span data-stu-id="c0b38-169">Text box: `<input type="text">`</span></span>
- <span data-ttu-id="c0b38-170">Caixa de seleção: `<input type="check">`</span><span class="sxs-lookup"><span data-stu-id="c0b38-170">Check box: `<input type="check">`</span></span>
- <span data-ttu-id="c0b38-171">Botão de opção: `<input type="radio">`</span><span class="sxs-lookup"><span data-stu-id="c0b38-171">Radio button: `<input type="radio">`</span></span>
- <span data-ttu-id="c0b38-172">Botão: `<input type="button">`</span><span class="sxs-lookup"><span data-stu-id="c0b38-172">Button: `<input type="button">`</span></span>
- <span data-ttu-id="c0b38-173">Botão de envio: `<input type="submit">`</span><span class="sxs-lookup"><span data-stu-id="c0b38-173">Submit button: `<input type="submit">`</span></span>

<span data-ttu-id="c0b38-174">Você também pode usar o `<textarea>` elemento para criar uma caixa de texto de várias linhas e o `<select>` elemento para criar uma lista suspensa ou uma lista rolável.</span><span class="sxs-lookup"><span data-stu-id="c0b38-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="c0b38-175">(Para mais informações sobre HTML formarem elementos, consulte [formulários HTML e a entrada](http://www.w3schools.com/html/html_forms.asp) no site W3Schools.)</span><span class="sxs-lookup"><span data-stu-id="c0b38-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="c0b38-176">O `name` atributo é muito importante, porque o nome é como você obterá o valor do elemento mais tarde, como você verá em breve.</span><span class="sxs-lookup"><span data-stu-id="c0b38-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="c0b38-177">A parte interessante é que você, o desenvolvedor da página, fazer com a entrada do usuário.</span><span class="sxs-lookup"><span data-stu-id="c0b38-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="c0b38-178">Não há nenhum comportamento interno associado a esses elementos.</span><span class="sxs-lookup"><span data-stu-id="c0b38-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="c0b38-179">Em vez disso, você precisa obter os valores que o usuário tiver inserido ou selecionado e fazer algo com eles.</span><span class="sxs-lookup"><span data-stu-id="c0b38-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="c0b38-180">Esse é o que você aprenderá neste tutorial.</span><span class="sxs-lookup"><span data-stu-id="c0b38-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="c0b38-181">**HTML5 e formulários de entrada**</span><span class="sxs-lookup"><span data-stu-id="c0b38-181">**HTML5 and Input Forms**</span></span>
> 
> <span data-ttu-id="c0b38-182">Como você deve saber, HTML está em transição; a versão mais recente (HTML5) inclui suporte para as formas mais intuitivas para que os usuários insiram informações.</span><span class="sxs-lookup"><span data-stu-id="c0b38-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="c0b38-183">Por exemplo, em HTML5, você (o desenvolvedor da página) pode informar a página que você deseja que o usuário insira uma data.</span><span class="sxs-lookup"><span data-stu-id="c0b38-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="c0b38-184">O navegador pode exibir automaticamente um calendário, em vez de exigir que o usuário insira uma data manualmente.</span><span class="sxs-lookup"><span data-stu-id="c0b38-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="c0b38-185">No entanto, o HTML5 é novo e ainda não é suportado em todos os navegadores.</span><span class="sxs-lookup"><span data-stu-id="c0b38-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="c0b38-186">Páginas da Web do ASP.NET oferece suporte a HTML5 na medida em que faz o navegador do usuário de entrada.</span><span class="sxs-lookup"><span data-stu-id="c0b38-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="c0b38-187">Para ter uma ideia dos novos atributos para o `<input>` elemento em HTML5, consulte [HTML &lt;entrada&gt; atributo de tipo](http://www.w3schools.com/html/html_form_input_types.asp) no site W3Schools.</span><span class="sxs-lookup"><span data-stu-id="c0b38-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>


## <a name="creating-the-form"></a><span data-ttu-id="c0b38-188">Criando o formulário</span><span class="sxs-lookup"><span data-stu-id="c0b38-188">Creating the Form</span></span>

<span data-ttu-id="c0b38-189">No WebMatrix, nos **arquivos** espaço de trabalho, abra o *Movies.cshtml* página.</span><span class="sxs-lookup"><span data-stu-id="c0b38-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="c0b38-190">Após o fechamento `</h1>` marca e antes da abertura `<div>` marca do `grid.GetHtml` chamar, adicione a seguinte marcação:</span><span class="sxs-lookup"><span data-stu-id="c0b38-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="c0b38-191">Essa marcação cria um formulário que tenha uma caixa de texto denominada `searchGenre` e um botão Enviar.</span><span class="sxs-lookup"><span data-stu-id="c0b38-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="c0b38-192">O texto da caixa e enviar o botão são incluídos em uma `<form>` elemento cujo `method` atributo é definido como `get`.</span><span class="sxs-lookup"><span data-stu-id="c0b38-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="c0b38-193">(Lembre-se de que se você não colocar a caixa de texto e botão dentro de enviar um `<form>` elemento, nada será enviado quando você clica no botão.) Você usa o `GET` verbo aqui porque você está criando um formulário que não faz qualquer alteração no servidor — ele apenas resulta em uma pesquisa.</span><span class="sxs-lookup"><span data-stu-id="c0b38-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="c0b38-194">(No tutorial anterior, você usou um `post` método, que é como você envia alterações para o servidor.</span><span class="sxs-lookup"><span data-stu-id="c0b38-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="c0b38-195">Você verá que no próximo tutorial novamente.)</span><span class="sxs-lookup"><span data-stu-id="c0b38-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="c0b38-196">Execute a página.</span><span class="sxs-lookup"><span data-stu-id="c0b38-196">Run the page.</span></span> <span data-ttu-id="c0b38-197">Embora você ainda não definiu nenhum comportamento para o formulário, você pode ver sua aparência:</span><span class="sxs-lookup"><span data-stu-id="c0b38-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![Página de filmes com caixa de pesquisa para gênero](form-basics/_static/image3.png)

<span data-ttu-id="c0b38-199">Insira um valor na caixa de texto, como "Comédia."</span><span class="sxs-lookup"><span data-stu-id="c0b38-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="c0b38-200">Em seguida, clique em **gênero pesquisa**.</span><span class="sxs-lookup"><span data-stu-id="c0b38-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="c0b38-201">Anote a URL da página.</span><span class="sxs-lookup"><span data-stu-id="c0b38-201">Take note of the URL of the page.</span></span> <span data-ttu-id="c0b38-202">Porque você definiu a `<form>` do elemento `method` atributo `get`, o valor inserido é agora parte da cadeia de caracteres de consulta na URL, como este:</span><span class="sxs-lookup"><span data-stu-id="c0b38-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="c0b38-203">Valores de formulário de leitura</span><span class="sxs-lookup"><span data-stu-id="c0b38-203">Reading Form Values</span></span>

<span data-ttu-id="c0b38-204">A página já contém um código que obtém dados do banco de dados e exibe os resultados em uma grade.</span><span class="sxs-lookup"><span data-stu-id="c0b38-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="c0b38-205">Agora você precisa adicionar um código que lê o valor da caixa de texto para que você possa executar uma consulta SQL que inclui o termo de pesquisa.</span><span class="sxs-lookup"><span data-stu-id="c0b38-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="c0b38-206">Porque você definiu o método do formulário como `get`, você pode ler o valor digitado na caixa de texto usando código semelhante ao seguinte:</span><span class="sxs-lookup"><span data-stu-id="c0b38-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="c0b38-207">O `Request.QueryString` objeto (o `QueryString` propriedade da `Request` objeto) inclui os valores dos elementos que foram enviados como parte do `GET` operação.</span><span class="sxs-lookup"><span data-stu-id="c0b38-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="c0b38-208">O `Request.QueryString` propriedade contém um *coleção* (uma lista) dos valores que são enviados no formulário.</span><span class="sxs-lookup"><span data-stu-id="c0b38-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="c0b38-209">Para obter qualquer valor individual, você deve especificar o nome do elemento que você deseja.</span><span class="sxs-lookup"><span data-stu-id="c0b38-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="c0b38-210">É por isso que você precisa ter uma `name` atributo o `<input>` elemento (`searchTerm`) que cria a caixa de texto.</span><span class="sxs-lookup"><span data-stu-id="c0b38-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="c0b38-211">(Para obter mais informações sobre o `Request` do objeto, consulte a [barra lateral](#BKMK_TheRequestObject) posteriormente.)</span><span class="sxs-lookup"><span data-stu-id="c0b38-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="c0b38-212">É simple o suficiente para ler o valor da caixa de texto.</span><span class="sxs-lookup"><span data-stu-id="c0b38-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="c0b38-213">Porém, se o usuário não inserir nada em todos os na caixa de texto, mas clicou **pesquisa** de qualquer forma, você pode ignorar esse clique, já que não há nada para pesquisar.</span><span class="sxs-lookup"><span data-stu-id="c0b38-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="c0b38-214">O código a seguir é um exemplo que mostra como implementar essas condições.</span><span class="sxs-lookup"><span data-stu-id="c0b38-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="c0b38-215">(Você não precisa adicionar esse código ainda; você terá de fazer isso em instantes).</span><span class="sxs-lookup"><span data-stu-id="c0b38-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="c0b38-216">O teste é dividida desta forma:</span><span class="sxs-lookup"><span data-stu-id="c0b38-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="c0b38-217">Obter o valor de `Request.QueryString["searchGenre"]`, ou seja, o que foi inserido no valor de `<input>` elemento chamado `searchGenre`.</span><span class="sxs-lookup"><span data-stu-id="c0b38-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="c0b38-218">Descobrir se ele está vazio usando o `IsEmpty` método.</span><span class="sxs-lookup"><span data-stu-id="c0b38-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="c0b38-219">Esse método é o modo padrão para determinar se algo (por exemplo, um elemento de formulário) contém um valor.</span><span class="sxs-lookup"><span data-stu-id="c0b38-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="c0b38-220">Mas, você se importa realmente somente se ele tiver *não* vazio, portanto...</span><span class="sxs-lookup"><span data-stu-id="c0b38-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="c0b38-221">Adicione a `!` operador na frente do `IsEmpty` de teste.</span><span class="sxs-lookup"><span data-stu-id="c0b38-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="c0b38-222">(O `!` significa que operador NOT lógico).</span><span class="sxs-lookup"><span data-stu-id="c0b38-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="c0b38-223">Em inglês sem formatação, todo o `if` condição traduz da seguinte maneira: *se o elemento de searchGenre do formulário não estiver vazio, então...*</span><span class="sxs-lookup"><span data-stu-id="c0b38-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="c0b38-224">Este bloco prepara o terreno para a criação de uma consulta que usa o termo de pesquisa.</span><span class="sxs-lookup"><span data-stu-id="c0b38-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="c0b38-225">Você terá de fazer isso na próxima seção.</span><span class="sxs-lookup"><span data-stu-id="c0b38-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> <span data-ttu-id="c0b38-226">**O objeto de solicitação**</span><span class="sxs-lookup"><span data-stu-id="c0b38-226">**The Request Object**</span></span>
> 
> <span data-ttu-id="c0b38-227">O `Request` objeto contém todas as informações que o navegador envia para seu aplicativo quando uma página é solicitada ou enviada.</span><span class="sxs-lookup"><span data-stu-id="c0b38-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="c0b38-228">Este objeto inclui todas as informações fornecidas pelo usuário, como valores de caixa de texto ou um arquivo para carregar.</span><span class="sxs-lookup"><span data-stu-id="c0b38-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="c0b38-229">Ele também inclui todos os tipos de informações adicionais, assim como os cookies, valores de cadeia de consulta de URL (se houver), o caminho do arquivo da página que está em execução, o tipo de navegador que o usuário está usando, a lista de idiomas que são definidas no navegador e muito mais.</span><span class="sxs-lookup"><span data-stu-id="c0b38-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="c0b38-230">O `Request` objeto é um *coleção* (lista) de valores.</span><span class="sxs-lookup"><span data-stu-id="c0b38-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="c0b38-231">Você obtém um valor individual da coleção especificando seu nome:</span><span class="sxs-lookup"><span data-stu-id="c0b38-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="c0b38-232">O `Request` objeto realmente expõe várias subconjuntos.</span><span class="sxs-lookup"><span data-stu-id="c0b38-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="c0b38-233">Por exemplo:</span><span class="sxs-lookup"><span data-stu-id="c0b38-233">For example:</span></span>
> 
> - <span data-ttu-id="c0b38-234">`Request.Form` fornece valores de elementos dentro de enviado `<form>` elemento se a solicitação for uma `POST` solicitação.</span><span class="sxs-lookup"><span data-stu-id="c0b38-234">`Request.Form` gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - <span data-ttu-id="c0b38-235">`Request.QueryString` fornece apenas os valores na cadeia de caracteres de consulta da URL.</span><span class="sxs-lookup"><span data-stu-id="c0b38-235">`Request.QueryString` gives you just the values in the URL's query string.</span></span> <span data-ttu-id="c0b38-236">(Em uma URL como `http://mysite/myapp/page?searchGenre=action&page=2`, o `?searchGenre=action&page=2` seção da URL é a cadeia de caracteres de consulta.)</span><span class="sxs-lookup"><span data-stu-id="c0b38-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - <span data-ttu-id="c0b38-237">`Request.Cookies` coleção fornece acesso aos cookies que o navegador enviou.</span><span class="sxs-lookup"><span data-stu-id="c0b38-237">`Request.Cookies` collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="c0b38-238">Para obter um valor que você sabe que está no formulário enviado, você pode usar `Request["name"]`.</span><span class="sxs-lookup"><span data-stu-id="c0b38-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="c0b38-239">Como alternativa, você pode usar as versões mais específicas `Request.Form["name"]` (para `POST` solicitações) ou `Request.QueryString["name"]` (para `GET` solicitações).</span><span class="sxs-lookup"><span data-stu-id="c0b38-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="c0b38-240">É claro *nome* é o nome do item a ser obtido.</span><span class="sxs-lookup"><span data-stu-id="c0b38-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="c0b38-241">O nome do item que você deseja obter deve ser exclusivo dentro da coleção que você está usando.</span><span class="sxs-lookup"><span data-stu-id="c0b38-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="c0b38-242">É por isso que o `Request` objeto fornece, como os subconjuntos `Request.Form` e `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="c0b38-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="c0b38-243">Suponha que sua página contém um elemento de formulário chamado `userName` e *também* contém um cookie chamado `userName`.</span><span class="sxs-lookup"><span data-stu-id="c0b38-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="c0b38-244">Se você receber `Request["userName"]`, é ambíguo se deseja que o valor de formulário ou o cookie.</span><span class="sxs-lookup"><span data-stu-id="c0b38-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="c0b38-245">No entanto, se você obtiver `Request.Form["userName"]` ou `Request.Cookie["userName"]`, você está sendo explícito sobre qual valor a ser obtido.</span><span class="sxs-lookup"><span data-stu-id="c0b38-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="c0b38-246">Ele é uma boa prática para ser específico e usar o subconjunto de `Request` que você está interessado, como `Request.Form` ou `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="c0b38-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="c0b38-247">Para as páginas simples que você está criando neste tutorial, provavelmente não realmente faz alguma diferença.</span><span class="sxs-lookup"><span data-stu-id="c0b38-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="c0b38-248">No entanto, quando você cria páginas mais complexas, usando a versão explícita `Request.Form` ou `Request.QueryString` pode ajudá-lo a evitar problemas que podem surgir quando a página contém um formulário (ou vários formulários), cookies, valores de cadeia de caracteres de consulta e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="c0b38-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>


## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="c0b38-249">Criando uma consulta usando um termo de pesquisa</span><span class="sxs-lookup"><span data-stu-id="c0b38-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="c0b38-250">Agora que você sabe como obter o termo de pesquisa que o usuário inseriu, você pode criar uma consulta que usa a ele.</span><span class="sxs-lookup"><span data-stu-id="c0b38-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="c0b38-251">Lembre-se de que, para obter todos os itens de filme do banco de dados, você está usando uma consulta SQL que se parece com esta instrução:</span><span class="sxs-lookup"><span data-stu-id="c0b38-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="c0b38-252">Para obter somente determinados filmes, você precisa usar uma consulta que inclui um `Where` cláusula.</span><span class="sxs-lookup"><span data-stu-id="c0b38-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="c0b38-253">Essa cláusula permite definir uma condição em que as linhas são retornadas pela consulta.</span><span class="sxs-lookup"><span data-stu-id="c0b38-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="c0b38-254">Veja um exemplo:</span><span class="sxs-lookup"><span data-stu-id="c0b38-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="c0b38-255">O formato básico é `WHERE column = value`.</span><span class="sxs-lookup"><span data-stu-id="c0b38-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="c0b38-256">Você pode usar operadores diferentes, além de apenas `=`, como `>` (maior que), `<` (menor que), `<>` (igual a), `<=` (menor ou igual a), etc., dependendo o que você está procurando.</span><span class="sxs-lookup"><span data-stu-id="c0b38-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="c0b38-257">Caso você esteja se perguntando, instruções SQL não diferenciam maiusculas de minúsculas &mdash; `SELECT` é o mesmo que `Select` (ou até mesmo `select`).</span><span class="sxs-lookup"><span data-stu-id="c0b38-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="c0b38-258">No entanto, as pessoas geralmente capitalizar as palavras-chave em uma instrução SQL, como `SELECT` e `WHERE`, para facilitar a leitura.</span><span class="sxs-lookup"><span data-stu-id="c0b38-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="c0b38-259">Passando o termo de pesquisa como um parâmetro</span><span class="sxs-lookup"><span data-stu-id="c0b38-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="c0b38-260">Ao procurar por um gênero específico é muito fácil (`WHERE Genre = 'Action'`), mas você deseja ser capaz de procurar qualquer gênero inseridas pelo usuário.</span><span class="sxs-lookup"><span data-stu-id="c0b38-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="c0b38-261">Para fazer isso, você cria como a consulta SQL que inclui um espaço reservado para o valor para pesquisar.</span><span class="sxs-lookup"><span data-stu-id="c0b38-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="c0b38-262">Ele se parecerá com este comando:</span><span class="sxs-lookup"><span data-stu-id="c0b38-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="c0b38-263">O espaço reservado é a `@` caracteres seguidos por zero.</span><span class="sxs-lookup"><span data-stu-id="c0b38-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="c0b38-264">Como você pode imaginar, uma consulta pode conter vários espaços reservados e seria nomeados `@0`, `@1`, `@2`, etc.</span><span class="sxs-lookup"><span data-stu-id="c0b38-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="c0b38-265">Para configurar a consulta e passar para ele, na verdade, o valor, você deve usar o código semelhante ao seguinte:</span><span class="sxs-lookup"><span data-stu-id="c0b38-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="c0b38-266">Esse código é semelhante a o que já foi feito para exibir dados na grade.</span><span class="sxs-lookup"><span data-stu-id="c0b38-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="c0b38-267">As únicas diferenças são:</span><span class="sxs-lookup"><span data-stu-id="c0b38-267">The only differences are:</span></span>

- <span data-ttu-id="c0b38-268">A consulta contém um espaço reservado (`WHERE Genre = @0"`).</span><span class="sxs-lookup"><span data-stu-id="c0b38-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="c0b38-269">A consulta é colocada em uma variável (`selectCommand`); antes, a consulta é passado diretamente para o `db.Query` método.</span><span class="sxs-lookup"><span data-stu-id="c0b38-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="c0b38-270">Quando você chama o `db.Query` método, você passa a consulta e o valor a ser usado para o espaço reservado.</span><span class="sxs-lookup"><span data-stu-id="c0b38-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="c0b38-271">(Se a consulta tiver vários espaços reservados, você deve passá-los como valores separados para o método.)</span><span class="sxs-lookup"><span data-stu-id="c0b38-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="c0b38-272">Se você reunir todos esses elementos, você obterá o código a seguir:</span><span class="sxs-lookup"><span data-stu-id="c0b38-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> <span data-ttu-id="c0b38-273">**Importante!**</span><span class="sxs-lookup"><span data-stu-id="c0b38-273">**Important!**</span></span> <span data-ttu-id="c0b38-274">Uso de espaços reservados (como `@0`) passar valores para um comando SQL está *extremamente importante* para segurança.</span><span class="sxs-lookup"><span data-stu-id="c0b38-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="c0b38-275">A maneira que você vê-la aqui, com espaços reservados para dados da variável, é a única maneira de você deve construir comandos SQL.</span><span class="sxs-lookup"><span data-stu-id="c0b38-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="c0b38-276">Nunca construa uma instrução SQL reunindo valores que obter do usuário e texto literal (concatenar).</span><span class="sxs-lookup"><span data-stu-id="c0b38-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="c0b38-277">Concatenação de entrada do usuário em uma instrução SQL é aberta no seu site para um *ataque de injeção de SQL* em que um usuário mal-intencionado envia valores para sua página que hack seu banco de dados.</span><span class="sxs-lookup"><span data-stu-id="c0b38-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="c0b38-278">(Você pode ler mais no artigo [injeção de SQL](https://msdn.microsoft.com/library/ms161953.aspx) site do MSDN.)</span><span class="sxs-lookup"><span data-stu-id="c0b38-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) the MSDN website.)</span></span>


## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="c0b38-279">Atualizar a página de filmes com código de pesquisa</span><span class="sxs-lookup"><span data-stu-id="c0b38-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="c0b38-280">Agora você pode atualizar o código na *Movies.cshtml* arquivo.</span><span class="sxs-lookup"><span data-stu-id="c0b38-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="c0b38-281">Para começar, substitua o código no bloco de código na parte superior da página com este código:</span><span class="sxs-lookup"><span data-stu-id="c0b38-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="c0b38-282">A diferença aqui é que você os colocou a consulta para o `selectCommand` variável, que você passará para `db.Query` mais tarde.</span><span class="sxs-lookup"><span data-stu-id="c0b38-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="c0b38-283">Colocando a instrução SQL em uma variável permite alterar a instrução, que é o que você fará para executar a pesquisa.</span><span class="sxs-lookup"><span data-stu-id="c0b38-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="c0b38-284">Também removeu estas duas linhas, você vai colocar de volta no posteriormente:</span><span class="sxs-lookup"><span data-stu-id="c0b38-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="c0b38-285">Você não quiser executar a consulta ainda (ou seja, chame `db.Query`) e você não deseja inicializar o `WebGrid` auxiliar ainda qualquer um.</span><span class="sxs-lookup"><span data-stu-id="c0b38-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="c0b38-286">Depois que você descobriu quais instrução SQL deve ser executado, você fará essas coisas.</span><span class="sxs-lookup"><span data-stu-id="c0b38-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="c0b38-287">Após esse bloco reconfigurado, você pode adicionar a nova lógica para lidar com a pesquisa.</span><span class="sxs-lookup"><span data-stu-id="c0b38-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="c0b38-288">O código completo será a seguinte aparência.</span><span class="sxs-lookup"><span data-stu-id="c0b38-288">The completed code will look like the following.</span></span> <span data-ttu-id="c0b38-289">Atualize o código em sua página para que coincida com este exemplo:</span><span class="sxs-lookup"><span data-stu-id="c0b38-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="c0b38-290">A página agora funciona da seguinte maneira.</span><span class="sxs-lookup"><span data-stu-id="c0b38-290">The page now works like this.</span></span> <span data-ttu-id="c0b38-291">Sempre que a página é executada, o código abre o banco de dados e o `selectCommand` variável é definida como a instrução SQL que obtém todos os registros do `Movies` tabela.</span><span class="sxs-lookup"><span data-stu-id="c0b38-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="c0b38-292">O código também inicializa o `searchTerm` variável.</span><span class="sxs-lookup"><span data-stu-id="c0b38-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="c0b38-293">No entanto, se a solicitação atual inclui um valor para o `searchGenre` elemento, o código define `selectCommand` para uma consulta diferente — ou seja, para um que inclua o `Where` cláusula para procurar um gênero.</span><span class="sxs-lookup"><span data-stu-id="c0b38-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="c0b38-294">Ele também define `searchTerm` tudo o que foi passado para a caixa de pesquisa (que pode ser nada).</span><span class="sxs-lookup"><span data-stu-id="c0b38-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="c0b38-295">Independentemente de qual SQL instrução está em `selectCommand`, o código, em seguida, chama `db.Query` para executar a consulta, passando a ele qualquer é `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="c0b38-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="c0b38-296">Se não houver nada `searchTerm`, não importa, porque nesse caso, não há nenhum parâmetro para passar o valor para `selectCommand` mesmo assim.</span><span class="sxs-lookup"><span data-stu-id="c0b38-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="c0b38-297">Por fim, o código inicializa o `WebGrid` auxiliar usando os resultados da consulta, como antes.</span><span class="sxs-lookup"><span data-stu-id="c0b38-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="c0b38-298">Você pode ver que, colocando a instrução SQL e o termo de pesquisa em variáveis, você adicionou flexibilidade ao código.</span><span class="sxs-lookup"><span data-stu-id="c0b38-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="c0b38-299">Como você verá posteriormente no tutorial, você pode usar essa estrutura básica e Continue adicionando a lógica para diferentes tipos de pesquisas.</span><span class="sxs-lookup"><span data-stu-id="c0b38-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="c0b38-300">Testar o recurso de pesquisa por gênero</span><span class="sxs-lookup"><span data-stu-id="c0b38-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="c0b38-301">No WebMatrix, execute as *Movies.cshtml* página.</span><span class="sxs-lookup"><span data-stu-id="c0b38-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="c0b38-302">Você verá a página com a caixa de texto do gênero.</span><span class="sxs-lookup"><span data-stu-id="c0b38-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="c0b38-303">Insira um gênero que você inseriu para um dos seus registros de teste, clique **pesquisa**.</span><span class="sxs-lookup"><span data-stu-id="c0b38-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="c0b38-304">Neste momento, você verá uma listagem dos apenas filmes que correspondem daquele gênero:</span><span class="sxs-lookup"><span data-stu-id="c0b38-304">This time you see a listing of just the movies that match that genre:</span></span>

![Página de filmes listando depois de pesquisar por gênero 'Comedies'](form-basics/_static/image4.png)

<span data-ttu-id="c0b38-306">Insira um gênero diferente e pesquise novamente.</span><span class="sxs-lookup"><span data-stu-id="c0b38-306">Enter a different genre and search again.</span></span> <span data-ttu-id="c0b38-307">Tente inserir o gênero usando todas as letras em maiusculas ou minúsculas para que você possa ver que a pesquisa não diferencia maiusculas de minúsculas.</span><span class="sxs-lookup"><span data-stu-id="c0b38-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="c0b38-308">"Lembrar" o que o usuário inseriu</span><span class="sxs-lookup"><span data-stu-id="c0b38-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="c0b38-309">Você deve ter notado que depois que você inseriu um gênero e clicado **gênero pesquisa**, você viu uma listagem para daquele gênero.</span><span class="sxs-lookup"><span data-stu-id="c0b38-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="c0b38-310">No entanto, a caixa de texto de pesquisa estava vazia &mdash; em outras palavras, ele não Lembre-se você tivesse inserido.</span><span class="sxs-lookup"><span data-stu-id="c0b38-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="c0b38-311">É importante entender por que esse comportamento ocorre.</span><span class="sxs-lookup"><span data-stu-id="c0b38-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="c0b38-312">Quando você enviar uma página, o navegador envia uma solicitação ao servidor web.</span><span class="sxs-lookup"><span data-stu-id="c0b38-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="c0b38-313">Quando o ASP.NET obtém a solicitação, ele cria uma instância totalmente nova da página, executará o código e, em seguida, renderiza a página para o navegador novamente.</span><span class="sxs-lookup"><span data-stu-id="c0b38-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="c0b38-314">Na verdade, no entanto, a página não sabe que você estava trabalhando apenas com uma versão anterior de si mesmo.</span><span class="sxs-lookup"><span data-stu-id="c0b38-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="c0b38-315">Tudo o que ele sabe é que ele recebeu uma solicitação que tinha alguns dados de formulário nele.</span><span class="sxs-lookup"><span data-stu-id="c0b38-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="c0b38-316">Sempre que você solicita uma página &mdash; pela primeira vez ou enviando-o &mdash; você está obtendo uma nova página.</span><span class="sxs-lookup"><span data-stu-id="c0b38-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="c0b38-317">O servidor web não tem memória de sua última solicitação.</span><span class="sxs-lookup"><span data-stu-id="c0b38-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="c0b38-318">Também não o ASP.NET, e também não o navegador.</span><span class="sxs-lookup"><span data-stu-id="c0b38-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="c0b38-319">A única conexão entre essas instâncias separadas da página é uma todos os dados transmitidos entre eles.</span><span class="sxs-lookup"><span data-stu-id="c0b38-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="c0b38-320">Se você enviar uma página, por exemplo, a nova instância de página pode obter os dados de formulário que foi enviados pela instância anterior.</span><span class="sxs-lookup"><span data-stu-id="c0b38-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="c0b38-321">(É outra maneira de passar dados entre páginas use cookies.)</span><span class="sxs-lookup"><span data-stu-id="c0b38-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="c0b38-322">Uma maneira formal para descrever essa situação é dizer que as páginas da web estão *sem monitoração de estado*.</span><span class="sxs-lookup"><span data-stu-id="c0b38-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="c0b38-323">Servidores Web e as próprias páginas e os elementos na página não mantêm todas as informações sobre o estado anterior de uma página.</span><span class="sxs-lookup"><span data-stu-id="c0b38-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="c0b38-324">A web foi projetada dessa forma porque a manutenção do estado de solicitações individuais seria rapidamente esgotar os recursos dos servidores web, que geralmente manipulam milhares, talvez até mesmo centenas de milhares de solicitações por segundo.</span><span class="sxs-lookup"><span data-stu-id="c0b38-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="c0b38-325">É por isso que a caixa de texto estava vazia.</span><span class="sxs-lookup"><span data-stu-id="c0b38-325">So that's why the text box was empty.</span></span> <span data-ttu-id="c0b38-326">Depois que a página é enviada, o ASP.NET criado uma nova instância da página e foi executado por meio do código e marcação.</span><span class="sxs-lookup"><span data-stu-id="c0b38-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="c0b38-327">Não havia nada em que o código que disse ASP.NET para colocar um valor na caixa de texto.</span><span class="sxs-lookup"><span data-stu-id="c0b38-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="c0b38-328">Portanto, o ASP.NET não fez nada e a caixa de texto foi renderizada sem um valor nele.</span><span class="sxs-lookup"><span data-stu-id="c0b38-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="c0b38-329">Há, na verdade, uma maneira fácil de contornar esse problema.</span><span class="sxs-lookup"><span data-stu-id="c0b38-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="c0b38-330">O gênero que você inseriu na caixa de texto *está* disponíveis para você no código &mdash; está em `Request.QueryString["searchGenre"]`.</span><span class="sxs-lookup"><span data-stu-id="c0b38-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="c0b38-331">Atualize a marcação da caixa de texto, de modo que o `value` atributo obtém seu valor de `searchTerm`, como neste exemplo:</span><span class="sxs-lookup"><span data-stu-id="c0b38-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="c0b38-332">Nessa página, você pode ter também definir a `value` de atributo para o `searchTerm` variável, uma vez que essa variável também contém o gênero que você inseriu.</span><span class="sxs-lookup"><span data-stu-id="c0b38-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="c0b38-333">Mas o uso de `Request` objeto para definir o `value` atributo conforme mostrado aqui é o modo padrão para realizar essa tarefa.</span><span class="sxs-lookup"><span data-stu-id="c0b38-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="c0b38-334">(Supondo que você deseja fazer isso até mesmo &mdash; em algumas situações, você talvez queira processar a página *sem* valores nos campos.</span><span class="sxs-lookup"><span data-stu-id="c0b38-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="c0b38-335">Tudo depende o que está acontecendo com seu aplicativo.)</span><span class="sxs-lookup"><span data-stu-id="c0b38-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="c0b38-336">Você não pode "lembrar" o valor de uma caixa de texto que é usado para senhas.</span><span class="sxs-lookup"><span data-stu-id="c0b38-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="c0b38-337">Seria uma brecha de segurança para permitir que pessoas preencher um campo de senha por meio de código.</span><span class="sxs-lookup"><span data-stu-id="c0b38-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>


<span data-ttu-id="c0b38-338">Execute novamente a página, insira um gênero e clique em **gênero pesquisa**.</span><span class="sxs-lookup"><span data-stu-id="c0b38-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="c0b38-339">Desta vez não só você ver os resultados da pesquisa, mas a caixa de texto se lembra de que você inseriu última vez:</span><span class="sxs-lookup"><span data-stu-id="c0b38-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![Página mostrando que a caixa de texto tem 'lembradas' a entrada anterior](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="c0b38-341">Procurando por qualquer palavra no título</span><span class="sxs-lookup"><span data-stu-id="c0b38-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="c0b38-342">Agora você pode pesquisar qualquer gênero, mas você também poderá pesquisar um título.</span><span class="sxs-lookup"><span data-stu-id="c0b38-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="c0b38-343">É difícil de acertar um título exatamente quando você pesquisa, portanto, em vez disso, que você pode pesquisar uma palavra que aparece em qualquer lugar dentro de um título.</span><span class="sxs-lookup"><span data-stu-id="c0b38-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="c0b38-344">Para fazer isso no SQL, você deve usar o `LIKE` operador e a sintaxe semelhante ao seguinte:</span><span class="sxs-lookup"><span data-stu-id="c0b38-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="c0b38-345">Esse comando obtém todos os filmes cujos títulos contêm "adventure".</span><span class="sxs-lookup"><span data-stu-id="c0b38-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="c0b38-346">Quando você usa o `LIKE` operador, você incluir o caractere curinga `%` como parte do termo de pesquisa.</span><span class="sxs-lookup"><span data-stu-id="c0b38-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="c0b38-347">A pesquisa `LIKE 'adventure%'` significa "começando com 'adventure'".</span><span class="sxs-lookup"><span data-stu-id="c0b38-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="c0b38-348">(Tecnicamente, isso significa "A cadeia de caracteres 'adventure', seguida por qualquer coisa.") Da mesma forma, o termo de pesquisa `LIKE '%adventure'` significa que "nada seguido pela cadeia de caracteres 'adventure'", que é outra maneira de dizer "termina com 'adventure'".</span><span class="sxs-lookup"><span data-stu-id="c0b38-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="c0b38-349">O termo de pesquisa `LIKE '%adventure%'` , portanto, significa "com a"adventure' em qualquer lugar no título."</span><span class="sxs-lookup"><span data-stu-id="c0b38-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="c0b38-350">(Tecnicamente, "qualquer coisa no título, seguido de 'adventure', seguida por qualquer coisa".)</span><span class="sxs-lookup"><span data-stu-id="c0b38-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="c0b38-351">Dentro de `<form>` elemento, adicione a seguinte marcação imediatamente sob o fechamento `</div>` marca para a pesquisa de gênero (antes do fechamento `</form>` elemento):</span><span class="sxs-lookup"><span data-stu-id="c0b38-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="c0b38-352">O código para lidar com essa pesquisa é semelhante ao código para a pesquisa de gênero, exceto que você precisa que montar o `LIKE` pesquisa.</span><span class="sxs-lookup"><span data-stu-id="c0b38-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="c0b38-353">Dentro do bloco de código na parte superior da página, adicione `if` bloquear logo após o `if` bloco para a pesquisa de Gênero:</span><span class="sxs-lookup"><span data-stu-id="c0b38-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="c0b38-354">Esse código usa a mesma lógica que você viu anteriormente, exceto que a pesquisa usa um `LIKE` operador e coloca o código "`%`" antes e depois o termo de pesquisa.</span><span class="sxs-lookup"><span data-stu-id="c0b38-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="c0b38-355">Observe como ele foi fácil de adicionar outra pesquisa para a página.</span><span class="sxs-lookup"><span data-stu-id="c0b38-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="c0b38-356">Tudo o que você precisava fazer era:</span><span class="sxs-lookup"><span data-stu-id="c0b38-356">All you had to do was:</span></span>

- <span data-ttu-id="c0b38-357">Criar um `if` bloco testado para ver se a caixa de pesquisa relevantes tinha um valor.</span><span class="sxs-lookup"><span data-stu-id="c0b38-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="c0b38-358">Defina o `selectCommand` variável para uma nova instrução SQL.</span><span class="sxs-lookup"><span data-stu-id="c0b38-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="c0b38-359">Defina o `searchTerm` variável como o valor para passar para a consulta.</span><span class="sxs-lookup"><span data-stu-id="c0b38-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="c0b38-360">Aqui está o bloco de código completo, que contém a nova lógica de uma pesquisa de título:</span><span class="sxs-lookup"><span data-stu-id="c0b38-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="c0b38-361">Aqui está um resumo do que esse código faz:</span><span class="sxs-lookup"><span data-stu-id="c0b38-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="c0b38-362">As variáveis `searchTerm` e `selectCommand` são inicializados na parte superior.</span><span class="sxs-lookup"><span data-stu-id="c0b38-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="c0b38-363">Você vai definir essas variáveis para o termo de pesquisa apropriado (se houver) e o comando SQL apropriado com base em que o usuário faz na página.</span><span class="sxs-lookup"><span data-stu-id="c0b38-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="c0b38-364">A pesquisa padrão é o caso mais simples de obter todos os filmes de banco de dados.</span><span class="sxs-lookup"><span data-stu-id="c0b38-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="c0b38-365">Nos testes para `searchGenre` e `searchTitle`, o código define `searchTerm` para o valor que você deseja pesquisar.</span><span class="sxs-lookup"><span data-stu-id="c0b38-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="c0b38-366">Os blocos de código também definir `selectCommand` para um comando SQL apropriado para que a pesquisa.</span><span class="sxs-lookup"><span data-stu-id="c0b38-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="c0b38-367">O `db.Query` método é invocado apenas uma vez, usando qualquer outro comando SQL está em `selectedCommand` e o valor que está em `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="c0b38-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="c0b38-368">Se não houver nenhum termo de pesquisa (sem gênero e nenhuma palavra título), o valor de `searchTerm` é uma cadeia de caracteres vazia.</span><span class="sxs-lookup"><span data-stu-id="c0b38-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="c0b38-369">No entanto, isso não importa, porque nesse caso, a consulta não requer um parâmetro.</span><span class="sxs-lookup"><span data-stu-id="c0b38-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="c0b38-370">Testar o recurso de pesquisa de título</span><span class="sxs-lookup"><span data-stu-id="c0b38-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="c0b38-371">Agora você pode testar sua página de pesquisa concluída.</span><span class="sxs-lookup"><span data-stu-id="c0b38-371">Now you can test your completed search page.</span></span> <span data-ttu-id="c0b38-372">Execute *Movies.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="c0b38-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="c0b38-373">Insira um gênero e clique em **gênero pesquisa**.</span><span class="sxs-lookup"><span data-stu-id="c0b38-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="c0b38-374">A grade exibe os filmes daquele gênero, como antes.</span><span class="sxs-lookup"><span data-stu-id="c0b38-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="c0b38-375">Insira uma palavra de título e clique em **pesquisar título**.</span><span class="sxs-lookup"><span data-stu-id="c0b38-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="c0b38-376">A grade exibe filmes com essa palavra no título.</span><span class="sxs-lookup"><span data-stu-id="c0b38-376">The grid displays movies that have that word in the title.</span></span>

![Listagem depois de pesquisar por 'O' no título de página de filmes](form-basics/_static/image6.png)

<span data-ttu-id="c0b38-378">Deixe as caixas de texto em branco e clique em um dos botões.</span><span class="sxs-lookup"><span data-stu-id="c0b38-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="c0b38-379">A grade exibe todos os filmes.</span><span class="sxs-lookup"><span data-stu-id="c0b38-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="c0b38-380">Combinando as consultas</span><span class="sxs-lookup"><span data-stu-id="c0b38-380">Combining the Queries</span></span>

<span data-ttu-id="c0b38-381">Você pode observar que as pesquisas que você pode executar são exclusivas.</span><span class="sxs-lookup"><span data-stu-id="c0b38-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="c0b38-382">É possível pesquisar o título e o gênero ao mesmo tempo, mesmo que ambas as caixas de pesquisa têm valores neles.</span><span class="sxs-lookup"><span data-stu-id="c0b38-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="c0b38-383">Por exemplo, você não pode procurar todos os filmes de ação cujo título contém "Adventure".</span><span class="sxs-lookup"><span data-stu-id="c0b38-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="c0b38-384">(A página é codificado agora, se você inserir valores para o gênero e o título, a pesquisa de título obtém precedência). Para criar uma pesquisa que combina as condições, você precisaria criar uma consulta SQL que tem uma sintaxe semelhante ao seguinte:</span><span class="sxs-lookup"><span data-stu-id="c0b38-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="c0b38-385">E você teria de executar a consulta usando uma instrução semelhante ao seguinte (em termos gerais):</span><span class="sxs-lookup"><span data-stu-id="c0b38-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="c0b38-386">Criar a lógica para permitir que muitas permutações de critérios de pesquisa pode obter um pouco envolvido, como você pode ver.</span><span class="sxs-lookup"><span data-stu-id="c0b38-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="c0b38-387">Portanto, vamos parar aqui.</span><span class="sxs-lookup"><span data-stu-id="c0b38-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="c0b38-388">Próximo</span><span class="sxs-lookup"><span data-stu-id="c0b38-388">Coming Up Next</span></span>

<span data-ttu-id="c0b38-389">O próximo tutorial, você criará uma página que usa um formulário para permitir que usuários adicionem filmes no banco de dados.</span><span class="sxs-lookup"><span data-stu-id="c0b38-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="c0b38-390">Listagem completa para a página de filme (atualizada com a pesquisa)</span><span class="sxs-lookup"><span data-stu-id="c0b38-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="c0b38-391">Recursos adicionais</span><span class="sxs-lookup"><span data-stu-id="c0b38-391">Additional Resources</span></span>

- [<span data-ttu-id="c0b38-392">Introdução à programação Web do ASP.NET usando a sintaxe Razor</span><span class="sxs-lookup"><span data-stu-id="c0b38-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="c0b38-393">[Cláusula WHERE SQL](http://www.w3schools.com/sql/sql_where.asp) no site W3Schools</span><span class="sxs-lookup"><span data-stu-id="c0b38-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="c0b38-394">[As definições de método](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) artigo no site do W3C</span><span class="sxs-lookup"><span data-stu-id="c0b38-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="c0b38-395">[Anterior](displaying-data.md)
> [Próximo](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="c0b38-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>
