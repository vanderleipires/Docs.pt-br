---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
title: "Introdução a páginas da Web ASP.NET - atualização do banco de dados | Microsoft Docs"
author: tfitzmac
description: "Este tutorial mostra como atualizar entrada (alterar) um banco de dados quando você usa páginas da Web do ASP.NET (Razor). Ele pressupõe que você tenha concluído a série th..."
ms.author: aspnetcontent
manager: wpickett
ms.date: 01/02/2018
ms.topic: article
ms.assetid: ac86ec9c-6b69-485b-b9e0-8b9127b13e6b
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
msc.type: authoredcontent
ms.openlocfilehash: aabf572e254de9861719fdc502340353482919b4
ms.sourcegitcommit: 281f0c614543a6c3db565ea4655b70fe49b61d84
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 01/03/2018
---
<a name="introducing-aspnet-web-pages---updating-database-data"></a><span data-ttu-id="4a686-104">Introdução a páginas da Web ASP.NET - atualização do banco de dados</span><span class="sxs-lookup"><span data-stu-id="4a686-104">Introducing ASP.NET Web Pages - Updating Database Data</span></span>
====================
<span data-ttu-id="4a686-105">por [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="4a686-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="4a686-106">Este tutorial mostra como atualizar entrada (alterar) um banco de dados quando você usa páginas da Web do ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="4a686-106">This tutorial shows you how to update (change) an existing database entry when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="4a686-107">Ele pressupõe que você tenha concluído a série por meio de [inserindo dados pelo uso de formulários usando páginas da Web ASP.NET](entering-data.md).</span><span class="sxs-lookup"><span data-stu-id="4a686-107">It assumes you have completed the series through [Entering Data by Using Forms Using ASP.NET Web Pages](entering-data.md).</span></span>
> 
> <span data-ttu-id="4a686-108">O que você aprenderá:</span><span class="sxs-lookup"><span data-stu-id="4a686-108">What you'll learn:</span></span>
> 
> - <span data-ttu-id="4a686-109">Como selecionar um registro individual no `WebGrid` auxiliar.</span><span class="sxs-lookup"><span data-stu-id="4a686-109">How to select an individual record in the `WebGrid` helper.</span></span>
> - <span data-ttu-id="4a686-110">Como ler um único registro de um banco de dados.</span><span class="sxs-lookup"><span data-stu-id="4a686-110">How to read a single record from a database.</span></span>
> - <span data-ttu-id="4a686-111">Como pré-carregar um formulário com valores do registro do banco de dados.</span><span class="sxs-lookup"><span data-stu-id="4a686-111">How to preload a form with values from the database record.</span></span>
> - <span data-ttu-id="4a686-112">Como atualizar um registro existente em um banco de dados.</span><span class="sxs-lookup"><span data-stu-id="4a686-112">How to update an existing record in a database.</span></span>
> - <span data-ttu-id="4a686-113">Como armazenar informações na página sem exibi-lo.</span><span class="sxs-lookup"><span data-stu-id="4a686-113">How to store information in the page without displaying it.</span></span>
> - <span data-ttu-id="4a686-114">Como usar um campo oculto para armazenar informações.</span><span class="sxs-lookup"><span data-stu-id="4a686-114">How to use a hidden field to store information.</span></span>
>   
> 
> <span data-ttu-id="4a686-115">Recursos/tecnologias abordadas:</span><span class="sxs-lookup"><span data-stu-id="4a686-115">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="4a686-116">O `WebGrid` auxiliar.</span><span class="sxs-lookup"><span data-stu-id="4a686-116">The `WebGrid` helper.</span></span>
> - <span data-ttu-id="4a686-117">O SQL `Update` comando.</span><span class="sxs-lookup"><span data-stu-id="4a686-117">The SQL `Update` command.</span></span>
> - <span data-ttu-id="4a686-118">O método `Database.Execute`.</span><span class="sxs-lookup"><span data-stu-id="4a686-118">The `Database.Execute` method.</span></span>
> - <span data-ttu-id="4a686-119">Campos ocultos (`<input type="hidden">`).</span><span class="sxs-lookup"><span data-stu-id="4a686-119">Hidden fields (`<input type="hidden">`).</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="4a686-120">O que você vai criar</span><span class="sxs-lookup"><span data-stu-id="4a686-120">What You'll Build</span></span>

<span data-ttu-id="4a686-121">No tutorial anterior, você aprendeu como adicionar um registro de um banco de dados.</span><span class="sxs-lookup"><span data-stu-id="4a686-121">In the previous tutorial, you learned how to add a record to a database.</span></span> <span data-ttu-id="4a686-122">Aqui, você aprenderá como exibir um registro para edição.</span><span class="sxs-lookup"><span data-stu-id="4a686-122">Here, you'll learn how to display a record for editing.</span></span> <span data-ttu-id="4a686-123">No *filmes* página, você poderá atualizar o `WebGrid` auxiliar para que ele exibe um **editar** link ao lado de cada filme:</span><span class="sxs-lookup"><span data-stu-id="4a686-123">In the *Movies* page, you'll update the `WebGrid` helper so that it displays an **Edit** link next to each movie:</span></span>

![WebGrid exibir incluindo um link de 'Editar' para cada filme](updating-data/_static/image1.png)

<span data-ttu-id="4a686-125">Quando você clica o **editar** link, ele leva a uma página diferente, onde as informações de filme já estão em um formulário:</span><span class="sxs-lookup"><span data-stu-id="4a686-125">When you click the **Edit** link, it takes you to a different page, where the movie information is already in a form:</span></span>

![Editar página de filme mostrando filme a ser editado](updating-data/_static/image2.png)

<span data-ttu-id="4a686-127">Você pode alterar qualquer um dos valores.</span><span class="sxs-lookup"><span data-stu-id="4a686-127">You can change any of the values.</span></span> <span data-ttu-id="4a686-128">Quando você envia as alterações, o código na página atualiza o banco de dados e levará de volta para a listagem do filme.</span><span class="sxs-lookup"><span data-stu-id="4a686-128">When you submit the changes, the code in the page updates the database and takes you back to the movie listing.</span></span>

<span data-ttu-id="4a686-129">Esta parte do processo funciona quase exatamente como o *AddMovie.cshtml* página que você criou no tutorial anterior, para grande parte deste tutorial será familiar.</span><span class="sxs-lookup"><span data-stu-id="4a686-129">This part of the process works almost exactly like the *AddMovie.cshtml* page you created in the previous tutorial, so much of this tutorial will be familiar.</span></span>

<span data-ttu-id="4a686-130">Há várias maneiras que você pode implementar uma maneira para editar um filme individual.</span><span class="sxs-lookup"><span data-stu-id="4a686-130">There are several ways you could implement a way to edit an individual movie.</span></span> <span data-ttu-id="4a686-131">A abordagem mostrada foi escolhida porque é fácil de implementar e fácil de entender.</span><span class="sxs-lookup"><span data-stu-id="4a686-131">The approach shown was chosen because it's easy to implement and easy to understand.</span></span>

## <a name="adding-an-edit-link-to-the-movie-listing"></a><span data-ttu-id="4a686-132">Adicionando um Link de edição para a listagem de filme</span><span class="sxs-lookup"><span data-stu-id="4a686-132">Adding an Edit Link to the Movie Listing</span></span>

<span data-ttu-id="4a686-133">Para começar, você atualizará o *filmes* página de forma que cada filme listando também contém um **editar** link.</span><span class="sxs-lookup"><span data-stu-id="4a686-133">To begin, you'll update the *Movies* page so that each movie listing also contains an **Edit** link.</span></span>

<span data-ttu-id="4a686-134">Abra o *Movies.cshtml* arquivo.</span><span class="sxs-lookup"><span data-stu-id="4a686-134">Open the *Movies.cshtml* file.</span></span>

<span data-ttu-id="4a686-135">No corpo da página, altere o `WebGrid` marcação adicionando uma coluna.</span><span class="sxs-lookup"><span data-stu-id="4a686-135">In the body of the page, change the `WebGrid` markup by adding a column.</span></span> <span data-ttu-id="4a686-136">Aqui está a marcação modificada:</span><span class="sxs-lookup"><span data-stu-id="4a686-136">Here's the modified markup:</span></span>

[!code-html[Main](updating-data/samples/sample1.html?highlight=6)]

<span data-ttu-id="4a686-137">A nova coluna é este:</span><span class="sxs-lookup"><span data-stu-id="4a686-137">The new column is this one:</span></span>

[!code-html[Main](updating-data/samples/sample2.html)]

<span data-ttu-id="4a686-138">O ponto dessa coluna é para mostrar um link (`<a>` elemento) cujo texto diz "Editar".</span><span class="sxs-lookup"><span data-stu-id="4a686-138">The point of this column is to show a link (`<a>` element) whose text says "Edit".</span></span> <span data-ttu-id="4a686-139">O que estamos procurando é criar um link que é semelhante ao seguinte quando a página é executada, com o `id` valor diferente para cada filme:</span><span class="sxs-lookup"><span data-stu-id="4a686-139">What we're after is to create a link that looks like the following when the page runs, with the `id` value different for each movie:</span></span>

[!code-css[Main](updating-data/samples/sample3.css)]

<span data-ttu-id="4a686-140">Este link invocará uma página chamada *EditMovie*, e passe a cadeia de caracteres de consulta `?id=7` para a página.</span><span class="sxs-lookup"><span data-stu-id="4a686-140">This link will invoke a page named *EditMovie*, and it will pass the query string `?id=7` to that page.</span></span>

<span data-ttu-id="4a686-141">A sintaxe para a nova coluna pode parecer um pouco complexa, mas só porque ela reúne vários elementos.</span><span class="sxs-lookup"><span data-stu-id="4a686-141">The syntax for the new column might look a bit complex, but that's only because it puts together several elements.</span></span> <span data-ttu-id="4a686-142">Cada elemento individual é simples.</span><span class="sxs-lookup"><span data-stu-id="4a686-142">Each individual element is straightforward.</span></span> <span data-ttu-id="4a686-143">Se você se concentrar em apenas o `<a>` elemento, você verá essa marcação:</span><span class="sxs-lookup"><span data-stu-id="4a686-143">If you concentrate on just the `<a>` element, you see this markup:</span></span>

[!code-html[Main](updating-data/samples/sample4.html)]

<span data-ttu-id="4a686-144">Algumas informações básicas sobre como funciona a grade: a grade exibe linhas, uma para cada registro de banco de dados, e exibe colunas para cada campo no registro do banco de dados.</span><span class="sxs-lookup"><span data-stu-id="4a686-144">Some background about how the grid works: the grid displays rows, one for each database record, and it displays columns for each field in the database record.</span></span> <span data-ttu-id="4a686-145">Enquanto está sendo construída cada linha de grade, o `item` objeto contém o registro do banco de dados (item) para aquela linha.</span><span class="sxs-lookup"><span data-stu-id="4a686-145">While each grid row is being constructed, the `item` object contains the database record (item) for that row.</span></span> <span data-ttu-id="4a686-146">Além disso, ela oferece uma maneira em código para obter os dados para aquela linha.</span><span class="sxs-lookup"><span data-stu-id="4a686-146">This arrangement gives you a way in code to get at the data for that row.</span></span> <span data-ttu-id="4a686-147">É o que você vê aqui: a expressão `item.ID` está obtendo o valor da ID do item atual do banco de dados.</span><span class="sxs-lookup"><span data-stu-id="4a686-147">That's what you see here: the expression `item.ID` is getting the ID value of the current database item.</span></span> <span data-ttu-id="4a686-148">Você pode obter os valores do banco de dados (título, gênero ou ano) da mesma forma, usando `item.Title`, `item.Genre`, ou `item.Year`.</span><span class="sxs-lookup"><span data-stu-id="4a686-148">You could get any of the database values (title, genre, or year) the same way by using `item.Title`, `item.Genre`, or `item.Year`.</span></span>

<span data-ttu-id="4a686-149">A expressão `"~/EditMovie?id=@item.ID` combina a parte da URL de destino embutido (`~/EditMovie?id=`) com esta ID de dinamicamente derivada.</span><span class="sxs-lookup"><span data-stu-id="4a686-149">The expression `"~/EditMovie?id=@item.ID` combines the hard-coded part of the target URL (`~/EditMovie?id=`) with this dynamically derived ID.</span></span> <span data-ttu-id="4a686-150">(Você viu o `~` operador no tutorial anterior; é um operador do ASP.NET que representa a raiz do site atual.)</span><span class="sxs-lookup"><span data-stu-id="4a686-150">(You saw the `~` operator in the previous tutorial; it's an ASP.NET operator that represents the current website root.)</span></span>

<span data-ttu-id="4a686-151">O resultado é que essa parte da marcação na coluna simplesmente produz algo parecido com a seguinte marcação em tempo de execução:</span><span class="sxs-lookup"><span data-stu-id="4a686-151">The result is that this part of the markup in the column simply produces something like the following markup at run time:</span></span>

[!code-xml[Main](updating-data/samples/sample5.xml)]

<span data-ttu-id="4a686-152">Naturalmente, o valor real do `id` será diferente para cada linha.</span><span class="sxs-lookup"><span data-stu-id="4a686-152">Naturally, the actual value of `id` will be different for each row.</span></span>

## <a name="creating-a-custom-display-for-a-grid-column"></a><span data-ttu-id="4a686-153">Criando uma exibição personalizada para uma coluna de grade</span><span class="sxs-lookup"><span data-stu-id="4a686-153">Creating a Custom Display for a Grid Column</span></span>

<span data-ttu-id="4a686-154">Agora, de volta para a coluna de grade.</span><span class="sxs-lookup"><span data-stu-id="4a686-154">Now back to the grid column.</span></span> <span data-ttu-id="4a686-155">As três colunas você tinha originalmente nos grade exibido somente valores de dados (título, gênero e ano).</span><span class="sxs-lookup"><span data-stu-id="4a686-155">The three columns you originally had in the grid displayed only data values (title, genre, and year).</span></span> <span data-ttu-id="4a686-156">Você especificou essa exibição, passando o nome da coluna de banco de dados &mdash; por exemplo, `grid.Column("Title")`.</span><span class="sxs-lookup"><span data-stu-id="4a686-156">You specified this display by passing the name of the database column &mdash; for example, `grid.Column("Title")`.</span></span>

<span data-ttu-id="4a686-157">Essa nova **editar** link coluna é diferente.</span><span class="sxs-lookup"><span data-stu-id="4a686-157">This new **Edit** link column is different.</span></span> <span data-ttu-id="4a686-158">Em vez de especificar um nome de coluna, você está passando um `format` parâmetro.</span><span class="sxs-lookup"><span data-stu-id="4a686-158">Instead of specifying a column name, you're passing a `format` parameter.</span></span> <span data-ttu-id="4a686-159">Esse parâmetro permite que você defina marcação que o `WebGrid` auxiliar processará junto com o `item` valor para exibir os dados da coluna como negrito ou verde ou em qualquer formato que você deseja.</span><span class="sxs-lookup"><span data-stu-id="4a686-159">This parameter lets you define markup that the `WebGrid` helper will render along with the `item` value to display the column data as bold or green or in whatever format that you want.</span></span> <span data-ttu-id="4a686-160">Por exemplo, se você quiser que o título a ser exibido em negrito, pode criar uma coluna como neste exemplo:</span><span class="sxs-lookup"><span data-stu-id="4a686-160">For example, if you wanted the title to appear bold, you could create a column like this example:</span></span>

[!code-html[Main](updating-data/samples/sample6.html)]

<span data-ttu-id="4a686-161">(Os vários `@` caracteres do `format` propriedade marcar a transição entre a marcação e um valor de código.)</span><span class="sxs-lookup"><span data-stu-id="4a686-161">(The various `@` characters you see in the `format` property mark the transition between markup and a code value.)</span></span>

<span data-ttu-id="4a686-162">Depois que você conheça o `format` propriedade, é mais fácil entender como o novo **editar** coluna de link é reunida:</span><span class="sxs-lookup"><span data-stu-id="4a686-162">Once you know about the `format` property, it's easier to understand how the new **Edit** link column is put together:</span></span>

[!code-html[Main](updating-data/samples/sample7.html)]

<span data-ttu-id="4a686-163">A coluna consiste em *somente* da marcação que renderiza o link, além de algumas informações (a ID) que é extraído do registro do banco de dados para a linha.</span><span class="sxs-lookup"><span data-stu-id="4a686-163">The column consists *only* of the markup that renders the link, plus some information (the ID) that's extracted from the database record for the row.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="4a686-164">**Parâmetros nomeados e posicionais parâmetros para um método**</span><span class="sxs-lookup"><span data-stu-id="4a686-164">**Named Parameters and Positional Parameters for a Method**</span></span>
> 
> <span data-ttu-id="4a686-165">Muitas vezes quando você chamou um método e passados parâmetros para ele, simplesmente listados os valores de parâmetros separados por vírgulas.</span><span class="sxs-lookup"><span data-stu-id="4a686-165">Many times when you've called a method and passed parameters to it, you've simply listed the parameter values separated by commas.</span></span> <span data-ttu-id="4a686-166">Veja alguns exemplos:</span><span class="sxs-lookup"><span data-stu-id="4a686-166">Here are a couple of examples:</span></span>
> 
> `db.Execute(insertCommand, title, genre, year)`
> 
> `Validation.RequireField("title", "You must enter a title")`
> 
> <span data-ttu-id="4a686-167">Não mencionamos o problema quando você viu primeiro esse código, mas em cada caso, você está passando parâmetros para os métodos em uma ordem específica &mdash; , ou seja, a ordem na qual os parâmetros são definidos no método.</span><span class="sxs-lookup"><span data-stu-id="4a686-167">We didn't mention the issue when you first saw this code, but in each case, you're passing parameters to the methods in a specific order &mdash; namely, the order in which the parameters are defined in that method.</span></span> <span data-ttu-id="4a686-168">Para `db.Execute` e `Validation.RequireFields`, se você misto a ordem dos valores que você passa, você receberia uma mensagem de erro quando a página é executada, ou pelo menos alguns resultados estranhos.</span><span class="sxs-lookup"><span data-stu-id="4a686-168">For `db.Execute` and `Validation.RequireFields`, if you mixed up the order of the values you pass, you'd get an error message when the page runs, or at least some strange results.</span></span> <span data-ttu-id="4a686-169">Obviamente, você precisa saber a ordem para passar os parâmetros.</span><span class="sxs-lookup"><span data-stu-id="4a686-169">Clearly, you have to know the order to pass the parameters in.</span></span> <span data-ttu-id="4a686-170">(No WebMatrix, IntelliSense pode ajudá-lo a aprender descobrir o nome, tipo e ordem dos parâmetros.)</span><span class="sxs-lookup"><span data-stu-id="4a686-170">(In WebMatrix, IntelliSense can help you learn figure out the name, type, and order of the parameters.)</span></span>
> 
> <span data-ttu-id="4a686-171">Como alternativa para passar valores em ordem, você pode usar *parâmetros nomeados*.</span><span class="sxs-lookup"><span data-stu-id="4a686-171">As an alternative to passing values in order, you can use *named parameters*.</span></span> <span data-ttu-id="4a686-172">(Passando parâmetros na ordem é conhecido como usando *parâmetros posicionais*.) Parâmetros nomeados, você explicitamente incluir o nome do parâmetro ao passar seu valor.</span><span class="sxs-lookup"><span data-stu-id="4a686-172">(Passing parameters in order is known as using *positional parameters*.) For named parameters, you explicitly include the name of the parameter when passing its value.</span></span> <span data-ttu-id="4a686-173">Você usou parâmetros nomeados já um número de vezes nesses tutoriais.</span><span class="sxs-lookup"><span data-stu-id="4a686-173">You've used named parameters already a number of times in these tutorials.</span></span> <span data-ttu-id="4a686-174">Por exemplo:</span><span class="sxs-lookup"><span data-stu-id="4a686-174">For example:</span></span>
> 
> [!code-csharp[Main](updating-data/samples/sample8.cs)]
> 
> <span data-ttu-id="4a686-175">e</span><span class="sxs-lookup"><span data-stu-id="4a686-175">and</span></span>
> 
> [!code-css[Main](updating-data/samples/sample9.css)]
> 
> <span data-ttu-id="4a686-176">Parâmetros nomeados são úteis para algumas situações, especialmente quando um método usa muitos parâmetros.</span><span class="sxs-lookup"><span data-stu-id="4a686-176">Named parameters are handy for a couple of situations, especially when a method takes many parameters.</span></span> <span data-ttu-id="4a686-177">Um é quando você deseja passar apenas um ou dois parâmetros, mas não são os valores que você deseja passar entre as posições primeiro na lista de parâmetros.</span><span class="sxs-lookup"><span data-stu-id="4a686-177">One is when you want to pass only one or two parameters, but the values you want to pass are not among the first positions in the parameter list.</span></span> <span data-ttu-id="4a686-178">Outra situação é quando você deseja tornar seu código mais legível, passando os parâmetros na ordem em que faz mais sentido para você.</span><span class="sxs-lookup"><span data-stu-id="4a686-178">Another situation is when you want to make your code more readable by passing the parameters in the order that makes the most sense to you.</span></span>
> 
> <span data-ttu-id="4a686-179">Obviamente, para usar parâmetros nomeados, você precisa saber os nomes dos parâmetros.</span><span class="sxs-lookup"><span data-stu-id="4a686-179">Obviously, to use named parameters, you have to know the names of the parameters.</span></span> <span data-ttu-id="4a686-180">WebMatrix IntelliSense pode *Mostrar* você os nomes, mas ele não pode atualmente preenchê-los para você.</span><span class="sxs-lookup"><span data-stu-id="4a686-180">WebMatrix IntelliSense can *show* you the names, but it cannot currently fill them in for you.</span></span>


## <a name="creating-the-edit-page"></a><span data-ttu-id="4a686-181">Criar página de edição</span><span class="sxs-lookup"><span data-stu-id="4a686-181">Creating the Edit Page</span></span>

<span data-ttu-id="4a686-182">Agora você pode criar o *EditMovie* página.</span><span class="sxs-lookup"><span data-stu-id="4a686-182">Now you can create the *EditMovie* page.</span></span> <span data-ttu-id="4a686-183">Quando os usuários clicarem o **editar** link, ele acabará nesta página.</span><span class="sxs-lookup"><span data-stu-id="4a686-183">When users click the **Edit** link, they'll end up on this page.</span></span>

<span data-ttu-id="4a686-184">Crie uma página chamada *EditMovie.cshtml* e substitua o que está no arquivo com a seguinte marcação:</span><span class="sxs-lookup"><span data-stu-id="4a686-184">Create a page named *EditMovie.cshtml* and replace what's in the file with the following markup:</span></span>

[!code-cshtml[Main](updating-data/samples/sample10.cshtml)]

<span data-ttu-id="4a686-185">Essa marcação e o código é semelhante ao que você tem *AddMovie* página.</span><span class="sxs-lookup"><span data-stu-id="4a686-185">This markup and code is similar to what you have in the *AddMovie* page.</span></span> <span data-ttu-id="4a686-186">Há uma pequena diferença no texto do botão de envio.</span><span class="sxs-lookup"><span data-stu-id="4a686-186">There's a small difference in the text for the submit button.</span></span> <span data-ttu-id="4a686-187">Assim como acontece com o *AddMovie* página, não há um `Html.ValidationSummary` chamada que irá exibir erros de validação, se houver.</span><span class="sxs-lookup"><span data-stu-id="4a686-187">As with the *AddMovie* page, there's an `Html.ValidationSummary` call that will display validation errors if there are any.</span></span> <span data-ttu-id="4a686-188">Neste momento, está saindo chamadas para `Validation.Message`, uma vez que os erros serão exibidos no resumo de validação.</span><span class="sxs-lookup"><span data-stu-id="4a686-188">This time we're leaving out calls to `Validation.Message`, since errors will be displayed in the validation summary.</span></span> <span data-ttu-id="4a686-189">Conforme observado no tutorial anterior, você pode usar o resumo de validação e mensagens de erro individuais em várias combinações.</span><span class="sxs-lookup"><span data-stu-id="4a686-189">As noted in the previous tutorial, you can use the validation summary and the individual error messages in various combinations.</span></span>

<span data-ttu-id="4a686-190">Observe novamente que o `method` atributo o `<form>` é definido como `post`.</span><span class="sxs-lookup"><span data-stu-id="4a686-190">Notice again that the `method` attribute of the `<form>` element is set to `post`.</span></span> <span data-ttu-id="4a686-191">Assim como acontece com o *AddMovie.cshtml* página, esta página faz alterações no banco de dados.</span><span class="sxs-lookup"><span data-stu-id="4a686-191">As with the *AddMovie.cshtml* page, this page makes changes to the database.</span></span> <span data-ttu-id="4a686-192">Portanto, este formulário deve executar um `POST` operação.</span><span class="sxs-lookup"><span data-stu-id="4a686-192">Therefore, this form should perform a `POST` operation.</span></span> <span data-ttu-id="4a686-193">(Para obter mais informações sobre a diferença entre `GET` e `POST` operações, consulte o [GET, POST e HTTP verbo segurança](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) barra lateral do tutorial em formulários HTML.)</span><span class="sxs-lookup"><span data-stu-id="4a686-193">(For more about the difference between `GET` and `POST` operations, see the [GET, POST, and HTTP Verb Safety](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) sidebar in the tutorial on HTML forms.)</span></span>

<span data-ttu-id="4a686-194">Como você viu um tutorial anterior, o `value` atributos das caixas de texto estão sendo definidos com o código do Razor para pré-carregá-los.</span><span class="sxs-lookup"><span data-stu-id="4a686-194">As you saw in an earlier tutorial, the `value` attributes of the text boxes are being set with Razor code in order to preload them.</span></span> <span data-ttu-id="4a686-195">Neste momento, porém, você está usando variáveis como `title` e `genre` para essa tarefa, em vez de `Request.Form["title"]`:</span><span class="sxs-lookup"><span data-stu-id="4a686-195">This time, though, you're using variables like `title` and `genre` for that task instead of `Request.Form["title"]`:</span></span>

`<input type="text" name="title" value="@title" />`

<span data-ttu-id="4a686-196">Como antes, esta marcação será pré-carregar os valores da caixa de texto com os valores do filme.</span><span class="sxs-lookup"><span data-stu-id="4a686-196">As before, this markup will preload the text box values with the movie values.</span></span> <span data-ttu-id="4a686-197">Você verá em breve por que é útil para usar variáveis neste momento, em vez de usar o `Request` objeto.</span><span class="sxs-lookup"><span data-stu-id="4a686-197">You'll see in a moment why it's handy to use variables this time instead of using the `Request` object.</span></span>

<span data-ttu-id="4a686-198">Também há um `<input type="hidden">` elemento nesta página.</span><span class="sxs-lookup"><span data-stu-id="4a686-198">There's also a `<input type="hidden">` element on this page.</span></span> <span data-ttu-id="4a686-199">Esse elemento armazena a ID de filme sem torná-lo visível na página.</span><span class="sxs-lookup"><span data-stu-id="4a686-199">This element stores the movie ID without making it visible on the page.</span></span> <span data-ttu-id="4a686-200">A ID é inicialmente passada para a página, usando um valor de cadeia de caracteres de consulta (`?id=7` ou semelhante na URL).</span><span class="sxs-lookup"><span data-stu-id="4a686-200">The ID is initially passed to the page by using a query string value (`?id=7` or similar in the URL).</span></span> <span data-ttu-id="4a686-201">Colocando o valor de ID em um campo oculto, você pode garantir que ele esteja disponível quando o formulário é enviado, mesmo se você não tiver acesso à URL original que a página foi chamada com.</span><span class="sxs-lookup"><span data-stu-id="4a686-201">By putting the ID value into a hidden field, you can make sure that it's available when the form is submitted, even if you no longer have access to the original URL that the page was invoked with.</span></span>

<span data-ttu-id="4a686-202">Ao contrário de *AddMovie* página, o código para o *EditMovie* página tem duas funções diferentes.</span><span class="sxs-lookup"><span data-stu-id="4a686-202">Unlike the *AddMovie* page, the code for the *EditMovie* page has two distinct functions.</span></span> <span data-ttu-id="4a686-203">A primeira função é que, quando a página é exibida pela primeira vez (e *somente* , em seguida,), o código obtém a ID de filme da cadeia de consulta.</span><span class="sxs-lookup"><span data-stu-id="4a686-203">The first function is that when the page is displayed for the first time (and *only* then), the code gets the movie ID from the query string.</span></span> <span data-ttu-id="4a686-204">O código, em seguida, usa o ID para ler o filme correspondente do banco de dados e exibir (pré-carregar)-lo nas caixas de texto.</span><span class="sxs-lookup"><span data-stu-id="4a686-204">The code then uses the ID to read the corresponding movie out of the database and display (preload) it in the text boxes.</span></span>

<span data-ttu-id="4a686-205">A segunda função é que, quando o usuário clica o **enviar alterações** botão, o código precisa ler os valores das caixas de texto e validá-los.</span><span class="sxs-lookup"><span data-stu-id="4a686-205">The second function is that when the user clicks the **Submit Changes** button, the code has to read the values of the text boxes and validate them.</span></span> <span data-ttu-id="4a686-206">O código também deve atualizar o item de banco de dados com os novos valores.</span><span class="sxs-lookup"><span data-stu-id="4a686-206">The code also has to update the database item with the new values.</span></span> <span data-ttu-id="4a686-207">Essa técnica é semelhante à adição de um registro, como você viu na *AddMovie*.</span><span class="sxs-lookup"><span data-stu-id="4a686-207">This technique is similar to adding a record, as you saw in *AddMovie*.</span></span>

## <a name="adding-code-to-read-a-single-movie"></a><span data-ttu-id="4a686-208">Adicionando código para ler um filme único</span><span class="sxs-lookup"><span data-stu-id="4a686-208">Adding Code to Read a Single Movie</span></span>

<span data-ttu-id="4a686-209">Para executar a primeira função, adicione este código para a parte superior da página:</span><span class="sxs-lookup"><span data-stu-id="4a686-209">To perform the first function, add this code to the top of the page:</span></span>

[!code-cshtml[Main](updating-data/samples/sample11.cshtml)]

<span data-ttu-id="4a686-210">A maioria do código está dentro de um bloco que inicia `if(!IsPost)`.</span><span class="sxs-lookup"><span data-stu-id="4a686-210">Most of this code is inside a block that starts `if(!IsPost)`.</span></span> <span data-ttu-id="4a686-211">O `!` operador significa "não", portanto, a expressão significa *se esta solicitação não for um envio post*, que é uma maneira indireta de dizer *se essa solicitação é a primeira vez que esta página foi executada*.</span><span class="sxs-lookup"><span data-stu-id="4a686-211">The `!` operator means "not," so the expression means *if this request is not a post submission*, which is an indirect way of saying *if this request is the first time that this page has been run*.</span></span> <span data-ttu-id="4a686-212">Conforme observado anteriormente, esse código deve ser executado *somente* na primeira vez em que a página é executada.</span><span class="sxs-lookup"><span data-stu-id="4a686-212">As noted earlier, this code should run *only* the first time the page runs.</span></span> <span data-ttu-id="4a686-213">Se você não incluir o código em `if(!IsPost)`, ele seria executado sempre que a página é chamado, se a primeira vez ou em resposta a um botão, clique em.</span><span class="sxs-lookup"><span data-stu-id="4a686-213">If you didn't enclose the code in `if(!IsPost)`, it would run every time the page is invoked, whether the first time or in response to a button click.</span></span>

<span data-ttu-id="4a686-214">Observe que o código inclui um `else` bloquear neste momento.</span><span class="sxs-lookup"><span data-stu-id="4a686-214">Notice that the code includes an `else` block this time.</span></span> <span data-ttu-id="4a686-215">Como foi dito quando apresentamos `if` blocos, às vezes você deseja executar código alternativo, se a condição que você está testando não for true.</span><span class="sxs-lookup"><span data-stu-id="4a686-215">As we said when we introduced `if` blocks, sometimes you want to run alternative code if the condition you're testing isn't true.</span></span> <span data-ttu-id="4a686-216">Esse é o caso aqui.</span><span class="sxs-lookup"><span data-stu-id="4a686-216">That's the case here.</span></span> <span data-ttu-id="4a686-217">Se a condição for aprovada (ou seja, se a ID passada para a página é okey), você ler uma linha do banco de dados.</span><span class="sxs-lookup"><span data-stu-id="4a686-217">If the condition passes (that is, if the ID passed to the page is ok), you read a row from the database.</span></span> <span data-ttu-id="4a686-218">No entanto, se a condição não for aprovada, o `else` bloco é executado e o código define uma mensagem de erro.</span><span class="sxs-lookup"><span data-stu-id="4a686-218">However, if the condition doesn't pass, the `else` block runs and the code sets an error message.</span></span>

## <a name="validating-a-value-passed-to-the-page"></a><span data-ttu-id="4a686-219">Validar um valor passado para a página</span><span class="sxs-lookup"><span data-stu-id="4a686-219">Validating a Value Passed to the Page</span></span>

<span data-ttu-id="4a686-220">O código usa `Request.QueryString["id"]` para obter a ID que é passada para a página.</span><span class="sxs-lookup"><span data-stu-id="4a686-220">The code uses `Request.QueryString["id"]` to get the ID that's passed to the page.</span></span> <span data-ttu-id="4a686-221">O código torna-se de que, na verdade, foi passado um valor para a ID.</span><span class="sxs-lookup"><span data-stu-id="4a686-221">The code makes sure that a value was actually passed for the ID.</span></span> <span data-ttu-id="4a686-222">Se nenhum valor foi passado, o código define um erro de validação.</span><span class="sxs-lookup"><span data-stu-id="4a686-222">If no value was passed, the code sets a validation error.</span></span>

<span data-ttu-id="4a686-223">Esse código mostra uma maneira diferente de validar informações.</span><span class="sxs-lookup"><span data-stu-id="4a686-223">This code shows a different way to validate information.</span></span> <span data-ttu-id="4a686-224">No tutorial anterior, você trabalhou com o `Validation` auxiliar.</span><span class="sxs-lookup"><span data-stu-id="4a686-224">In the previous tutorial, you worked with the `Validation` helper.</span></span> <span data-ttu-id="4a686-225">Você registrou campos para validar e ASP.NET automaticamente fez a validação e exibidos erros usando `Html.ValidationMessage` e `Html.ValidationSummary`.</span><span class="sxs-lookup"><span data-stu-id="4a686-225">You registered fields to validate, and ASP.NET automatically did the validation and displayed errors by using `Html.ValidationMessage` and `Html.ValidationSummary`.</span></span> <span data-ttu-id="4a686-226">Nesse caso, no entanto, você está realmente não Validando entrada do usuário.</span><span class="sxs-lookup"><span data-stu-id="4a686-226">In this case, however, you're not really validating user input.</span></span> <span data-ttu-id="4a686-227">Em vez disso, você está validando um valor que foi passado para a página de qualquer outro lugar.</span><span class="sxs-lookup"><span data-stu-id="4a686-227">Instead, you're validating a value that was passed to the page from elsewhere.</span></span> <span data-ttu-id="4a686-228">O `Validation` auxiliar não faz isso para você.</span><span class="sxs-lookup"><span data-stu-id="4a686-228">The `Validation` helper doesn't do that for you.</span></span>

<span data-ttu-id="4a686-229">Portanto, é verificar o valor por conta própria, testando-o com `if(!Request.QueryString["ID"].IsEmpty()`).</span><span class="sxs-lookup"><span data-stu-id="4a686-229">Therefore, you check the value yourself, by testing it with `if(!Request.QueryString["ID"].IsEmpty()`).</span></span> <span data-ttu-id="4a686-230">Se houver um problema, você pode exibir o erro usando `Html.ValidationSummary`, como você fez com o `Validation` auxiliar.</span><span class="sxs-lookup"><span data-stu-id="4a686-230">If there's a problem, you can display the error by using `Html.ValidationSummary`, as you did with the `Validation` helper.</span></span> <span data-ttu-id="4a686-231">Para fazer isso, você deve chamar `Validation.AddFormError` e passá-lo em uma mensagem a ser exibida.</span><span class="sxs-lookup"><span data-stu-id="4a686-231">To do that, you call `Validation.AddFormError` and pass it a message to display.</span></span> <span data-ttu-id="4a686-232">`Validation.AddFormError`é um método interno que permite que você defina as mensagens personalizadas empatarem com o sistema de validação que você já estiver familiarizado com.</span><span class="sxs-lookup"><span data-stu-id="4a686-232">`Validation.AddFormError` is a built-in method that lets you define custom messages that tie in with the validation system you're already familiar with.</span></span> <span data-ttu-id="4a686-233">(Posteriormente neste tutorial falaremos sobre como tornar o processo de validação um pouco mais robusto.)</span><span class="sxs-lookup"><span data-stu-id="4a686-233">(Later in this tutorial we'll talk about how to make this validation process a little more robust.)</span></span>

<span data-ttu-id="4a686-234">Após certificar-se de que há uma ID de filme, o código lê o banco de dados, procurando por apenas um item único banco de dados.</span><span class="sxs-lookup"><span data-stu-id="4a686-234">After making sure that there's an ID for the movie, the code reads the database, looking for only a single database item.</span></span> <span data-ttu-id="4a686-235">(Você provavelmente notou o padrão geral para operações de banco de dados: Abra o banco de dados, definir uma instrução SQL e execute a instrução.) Neste momento, o SQL `Select` instrução inclui `WHERE ID = @0`.</span><span class="sxs-lookup"><span data-stu-id="4a686-235">(You probably have noticed the general pattern for database operations: open the database, define a SQL statement, and run the statement.) This time, the SQL `Select` statement includes `WHERE ID = @0`.</span></span> <span data-ttu-id="4a686-236">Porque a ID é exclusiva, apenas um registro pode ser retornado.</span><span class="sxs-lookup"><span data-stu-id="4a686-236">Because the ID is unique, only one record can be returned.</span></span>

<span data-ttu-id="4a686-237">A consulta é executada usando `db.QuerySingle` (não `db.Query`, conforme usado para a listagem de filme), e o código coloca o resultado para o `row` variável.</span><span class="sxs-lookup"><span data-stu-id="4a686-237">The query is performed by using `db.QuerySingle` (not `db.Query`, as you used for the movie listing), and the code puts the result into the `row` variable.</span></span> <span data-ttu-id="4a686-238">O nome `row` é arbitrário; você pode nomear as variáveis que desejar.</span><span class="sxs-lookup"><span data-stu-id="4a686-238">The name `row` is arbitrary; you can name the variables anything you like.</span></span> <span data-ttu-id="4a686-239">As variáveis inicializadas na parte superior, em seguida, são preenchidas com os detalhes do filme para que esses valores podem ser exibidos nas caixas de texto.</span><span class="sxs-lookup"><span data-stu-id="4a686-239">The variables initialized at the top are then filled with the movie details so that these values can be displayed in the text boxes.</span></span>

## <a name="testing-the-edit-page-so-far"></a><span data-ttu-id="4a686-240">Testando a página Editar (até)</span><span class="sxs-lookup"><span data-stu-id="4a686-240">Testing the Edit Page (So Far)</span></span>

<span data-ttu-id="4a686-241">Se você gostaria de sua página de teste, execute o *filmes* página agora e clique em uma **editar** link ao lado de qualquer filme.</span><span class="sxs-lookup"><span data-stu-id="4a686-241">If you'd like to test your page, run the *Movies* page now and click an **Edit** link next to any movie.</span></span> <span data-ttu-id="4a686-242">Você verá o *EditMovie* página com os detalhes preenchidos para o filme selecionado:</span><span class="sxs-lookup"><span data-stu-id="4a686-242">You'll see the *EditMovie* page with the details filled in for the movie you selected:</span></span>

![Editar página de filme mostrando filme a ser editado](updating-data/_static/image3.png)

<span data-ttu-id="4a686-244">Observe que a URL da página inclui algo como `?id=10` (ou algum outro número).</span><span class="sxs-lookup"><span data-stu-id="4a686-244">Notice that the URL of the page includes something like `?id=10` (or some other number).</span></span> <span data-ttu-id="4a686-245">Até você testou que **editar** links no *filme* página trabalho, se a página está lendo a ID da cadeia de consulta, e que o banco de dados de consulta para obter um registro único filme está funcionando.</span><span class="sxs-lookup"><span data-stu-id="4a686-245">So far you've tested that **Edit** links in the *Movie* page work, that your page is reading the ID from the query string, and that the database query to get a single movie record is working.</span></span>

<span data-ttu-id="4a686-246">Você pode alterar as informações de filme, mas nada acontece quando você clica em **enviar alterações**.</span><span class="sxs-lookup"><span data-stu-id="4a686-246">You can change the movie information, but nothing happens when you click **Submit Changes**.</span></span>

## <a name="adding-code-to-update-the-movie-with-the-users-changes"></a><span data-ttu-id="4a686-247">Adicionando código para atualizar o filme com as alterações do usuário</span><span class="sxs-lookup"><span data-stu-id="4a686-247">Adding Code to Update the Movie with the User's Changes</span></span>

<span data-ttu-id="4a686-248">No *EditMovie.cshtml* de arquivos, para implementar a segunda função (Salvar alterações), adicione o seguinte código dentro da chave de fechamento do `@` bloco.</span><span class="sxs-lookup"><span data-stu-id="4a686-248">In the *EditMovie.cshtml* file, to implement the second function (saving changes), add the following code just inside the closing brace of the `@` block.</span></span> <span data-ttu-id="4a686-249">(Se você não tiver certeza sobre onde colocar o código, você pode examinar o [completo listagem de código para a página Editar filme](#Complete_Page_Listing_for_EditMovie) que aparece no fim deste tutorial.)</span><span class="sxs-lookup"><span data-stu-id="4a686-249">(If you're not sure exactly where to put the code, you can look at the [complete code listing for the Edit Movie page](#Complete_Page_Listing_for_EditMovie) that appears at the end of this tutorial.)</span></span>

[!code-csharp[Main](updating-data/samples/sample12.cs)]

<span data-ttu-id="4a686-250">Novamente, esta marcação e o código é semelhante ao código *AddMovie*.</span><span class="sxs-lookup"><span data-stu-id="4a686-250">Again, this markup and code is similar to the code in *AddMovie*.</span></span> <span data-ttu-id="4a686-251">O código está em um `if(IsPost)` bloquear, porque esse código é executado somente quando o usuário clica o **enviar alterações** botão &mdash; ou seja, quando (e apenas quando) o formulário foi lançado.</span><span class="sxs-lookup"><span data-stu-id="4a686-251">The code is in an `if(IsPost)` block, because this code runs only when the user clicks the **Submit Changes** button &mdash; that is, when (and only when) the form has been posted.</span></span> <span data-ttu-id="4a686-252">Nesse caso, você não estiver usando um teste como `if(IsPost && Validation.IsValid())`— ou seja, você não está combinando os dois testes usando and.</span><span class="sxs-lookup"><span data-stu-id="4a686-252">In this case, you're not using a test like `if(IsPost && Validation.IsValid())`— that is, you're not combining both tests by using AND.</span></span> <span data-ttu-id="4a686-253">Nessa página, você primeiro determinar se há uma submissão de formulário (`if(IsPost)`) e só então registrar os campos para validação.</span><span class="sxs-lookup"><span data-stu-id="4a686-253">In this page, you first determine whether there's a form submission (`if(IsPost)`), and only then register the fields for validation.</span></span> <span data-ttu-id="4a686-254">Em seguida, você pode testar os resultados de validação (`if(Validation.IsValid()`).</span><span class="sxs-lookup"><span data-stu-id="4a686-254">Then you can test the validation results (`if(Validation.IsValid()`).</span></span> <span data-ttu-id="4a686-255">O fluxo é um pouco diferente no *AddMovie.cshtml* página, mas o efeito é o mesmo.</span><span class="sxs-lookup"><span data-stu-id="4a686-255">The flow is slightly different than in the *AddMovie.cshtml* page, but the effect is the same.</span></span>

<span data-ttu-id="4a686-256">Obter os valores das caixas de texto usando `Request.Form["title"]` e um código semelhante para os outros `<input>` elementos.</span><span class="sxs-lookup"><span data-stu-id="4a686-256">You get the values of the text boxes by using `Request.Form["title"]` and similar code for the other `<input>` elements.</span></span> <span data-ttu-id="4a686-257">Observe que esse tempo, o código obtém a ID de filme fora do campo oculto (`<input type="hidden">`).</span><span class="sxs-lookup"><span data-stu-id="4a686-257">Notice that this time, the code gets the movie ID out of the hidden field (`<input type="hidden">`).</span></span> <span data-ttu-id="4a686-258">Quando a página foi executado na primeira vez, o código tem a ID de cadeia de caracteres de consulta.</span><span class="sxs-lookup"><span data-stu-id="4a686-258">When the page ran the first time, the code got the ID out of the query string.</span></span> <span data-ttu-id="4a686-259">Você pode obter o valor do campo oculto para certificar-se de que você está obtendo a ID do filme que foi originalmente exibida, no caso de alguma forma, a cadeia de caracteres de consulta foi alterada desde então.</span><span class="sxs-lookup"><span data-stu-id="4a686-259">You get the value from the hidden field to make sure that you're getting the ID of the movie that was originally displayed, in case the query string was somehow altered since then.</span></span>

<span data-ttu-id="4a686-260">A diferença muito importante entre o *AddMovie* código e esse código é nesse código usar o SQL `Update` instrução, em vez do `Insert Into` instrução.</span><span class="sxs-lookup"><span data-stu-id="4a686-260">The really important difference between the *AddMovie* code and this code is that in this code you use the SQL `Update` statement instead of the `Insert Into` statement.</span></span> <span data-ttu-id="4a686-261">O exemplo a seguir mostra a sintaxe do SQL `Update` instrução:</span><span class="sxs-lookup"><span data-stu-id="4a686-261">The following example shows the syntax of the SQL `Update` statement:</span></span>

`UPDATE table SET col1="value", col2="value", col3="value" ... WHERE ID = value`

<span data-ttu-id="4a686-262">Você pode especificar qualquer coluna em qualquer ordem, e não necessariamente é necessário atualizar todas as colunas durante um `Update` operação.</span><span class="sxs-lookup"><span data-stu-id="4a686-262">You can specify any columns in any order, and you don't necessarily have to update every column during an `Update` operation.</span></span> <span data-ttu-id="4a686-263">(Você não pode atualizar a ID em si, porque em vigor que seria salvá-lo como um novo registro, e que não é permitido para um `Update` operação.)</span><span class="sxs-lookup"><span data-stu-id="4a686-263">(You cannot update the ID itself, because that would in effect save the record as a new record, and that's not allowed for an `Update` operation.)</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="4a686-264">**Importante** de `Where` cláusula com a ID é muito importante, porque é como o banco de dados sabe qual banco de dados de registro que você deseja atualizar.</span><span class="sxs-lookup"><span data-stu-id="4a686-264">**Important** The `Where` clause with the ID is very important, because that's how the database knows which database record you want to update.</span></span> <span data-ttu-id="4a686-265">Se você parou o `Where` cláusula, atualizará o banco de dados *cada* registros no banco de dados.</span><span class="sxs-lookup"><span data-stu-id="4a686-265">If you left off the `Where` clause, the database would update *every* record in the database.</span></span> <span data-ttu-id="4a686-266">Na maioria dos casos, isso seria um desastre.</span><span class="sxs-lookup"><span data-stu-id="4a686-266">In most cases, that would be a disaster.</span></span>


<span data-ttu-id="4a686-267">No código, para atualizar os valores são passados para a instrução SQL usando espaços reservados.</span><span class="sxs-lookup"><span data-stu-id="4a686-267">In the code, the values to update are passed to the SQL statement by using placeholders.</span></span> <span data-ttu-id="4a686-268">O que dissemos antes de repetir: por razões de segurança, *somente* usar espaços reservados para passar valores para uma instrução SQL.</span><span class="sxs-lookup"><span data-stu-id="4a686-268">To repeat what we've said before: for security reasons, *only* use placeholders to pass values to a SQL statement.</span></span>

<span data-ttu-id="4a686-269">Depois que o código usa `db.Execute` para executar o `Update` instrução, ele redireciona de volta para a página de listagem, onde você pode ver as alterações.</span><span class="sxs-lookup"><span data-stu-id="4a686-269">After the code uses `db.Execute` to run the `Update` statement, it redirects back to the listing page, where you can see the changes.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="4a686-270">**Instruções SQL diferente, métodos diferentes**</span><span class="sxs-lookup"><span data-stu-id="4a686-270">**Different SQL Statements, Different Methods**</span></span>
> 
> <span data-ttu-id="4a686-271">Você deve ter notado que usa métodos ligeiramente diferentes para executar instruções SQL diferentes.</span><span class="sxs-lookup"><span data-stu-id="4a686-271">You might have noticed that you use slightly different methods to run different SQL statements.</span></span> <span data-ttu-id="4a686-272">Para executar um `Select` que potencialmente consulta retorna vários registros, use o `Query` método.</span><span class="sxs-lookup"><span data-stu-id="4a686-272">To run a `Select` query that potentially returns multiple records, you use the `Query` method.</span></span> <span data-ttu-id="4a686-273">Para executar um `Select` a consulta que você sabe que irá retornar apenas um item de banco de dados, use o `QuerySingle` método.</span><span class="sxs-lookup"><span data-stu-id="4a686-273">To run a `Select` query that you know will return only one database item, you use the `QuerySingle` method.</span></span> <span data-ttu-id="4a686-274">Para executar comandos que fazer alterações, mas que não retornam os itens de banco de dados, você deve usar o `Execute` método.</span><span class="sxs-lookup"><span data-stu-id="4a686-274">To run commands that make changes but that don't return database items, you use the `Execute` method.</span></span>
> 
> <span data-ttu-id="4a686-275">Você precisa ter diferentes métodos porque cada um deles retorna resultados diferentes, como você já viu a diferença entre `Query` e `QuerySingle`.</span><span class="sxs-lookup"><span data-stu-id="4a686-275">You have to have different methods because each of them returns different results, as you saw already in the difference between `Query` and `QuerySingle`.</span></span> <span data-ttu-id="4a686-276">(O `Execute` método realmente retorna um valor também &mdash; , ou seja, o número de linhas do banco de dados que foram afetadas pelo comando &mdash; , mas você tiver sido ignorando que até o momento.)</span><span class="sxs-lookup"><span data-stu-id="4a686-276">(The `Execute` method actually returns a value also &mdash; namely, the number of database rows that were affected by the command &mdash; but you've been ignoring that so far.)</span></span>
> 
> <span data-ttu-id="4a686-277">Naturalmente, o `Query` método pode retornar apenas uma linha de banco de dados.</span><span class="sxs-lookup"><span data-stu-id="4a686-277">Of course, the `Query` method might return only one database row.</span></span> <span data-ttu-id="4a686-278">No entanto, o ASP.NET sempre trata os resultados do `Query` método como uma coleção.</span><span class="sxs-lookup"><span data-stu-id="4a686-278">However, ASP.NET always treats the results of the `Query` method as a collection.</span></span> <span data-ttu-id="4a686-279">Mesmo que o método retorna apenas uma linha, você precisa extrair aquela única linha da coleção.</span><span class="sxs-lookup"><span data-stu-id="4a686-279">Even if the method returns just one row, you have to extract that single row from the collection.</span></span> <span data-ttu-id="4a686-280">Portanto, em situações em que você *saber* você terá apenas uma linha, é um pouco mais conveniente usar `QuerySingle`.</span><span class="sxs-lookup"><span data-stu-id="4a686-280">Therefore, in situations where you *know* you'll get back only one row, it's a bit more convenient to use `QuerySingle`.</span></span>
> 
> <span data-ttu-id="4a686-281">Há alguns outros métodos que realizam a tipos específicos de operações de banco de dados.</span><span class="sxs-lookup"><span data-stu-id="4a686-281">There are a few other methods that perform specific types of database operations.</span></span> <span data-ttu-id="4a686-282">Você pode encontrar uma lista de métodos de banco de dados no [referência rápida de API de páginas da Web do ASP.NET](../../api-reference/asp-net-web-pages-api-reference.md#Data).</span><span class="sxs-lookup"><span data-stu-id="4a686-282">You can find a listing of database methods in the [ASP.NET Web Pages API Quick Reference](../../api-reference/asp-net-web-pages-api-reference.md#Data).</span></span>


## <a name="making-validation-for-the-id-more-robust"></a><span data-ttu-id="4a686-283">Fazer a validação de ID mais robusta</span><span class="sxs-lookup"><span data-stu-id="4a686-283">Making Validation for the ID More Robust</span></span>

<span data-ttu-id="4a686-284">Na primeira vez que a página é executada, você obtém a ID de filme de cadeia de caracteres de consulta para que você possa ir obter esse filme do banco de dados.</span><span class="sxs-lookup"><span data-stu-id="4a686-284">The first time that the page runs, you get the movie ID from the query string so that you can go get that movie from the database.</span></span> <span data-ttu-id="4a686-285">Certificar-se que, na verdade, não havia um valor para procurar, que você fez usando este código:</span><span class="sxs-lookup"><span data-stu-id="4a686-285">You made sure that there actually was a value to go look for, which you did by using this code:</span></span>

[!code-csharp[Main](updating-data/samples/sample13.cs)]

<span data-ttu-id="4a686-286">Você usou esse código para garantir que se um usuário obtém para o *EditMovies* página sem primeiro selecionar um filme no *filmes* página, a página será exibida uma mensagem de erro amigável.</span><span class="sxs-lookup"><span data-stu-id="4a686-286">You used this code to make sure that if a user gets to the *EditMovies* page without first selecting a movie in the *Movies* page, the page would display a user-friendly error message.</span></span> <span data-ttu-id="4a686-287">(Caso contrário, os usuários verá um erro que seria provavelmente apenas confundem-las.)</span><span class="sxs-lookup"><span data-stu-id="4a686-287">(Otherwise, users would see an error that would probably just confuse them.)</span></span>

<span data-ttu-id="4a686-288">No entanto, essa validação não é muito eficiente.</span><span class="sxs-lookup"><span data-stu-id="4a686-288">However, this validation isn't very robust.</span></span> <span data-ttu-id="4a686-289">A página também pode ser chamada com estes erros:</span><span class="sxs-lookup"><span data-stu-id="4a686-289">The page might also be invoked with these errors:</span></span>

- <span data-ttu-id="4a686-290">A ID não é um número.</span><span class="sxs-lookup"><span data-stu-id="4a686-290">The ID isn't a number.</span></span> <span data-ttu-id="4a686-291">Por exemplo, a página pode ser chamada com uma URL como `http://localhost:nnnnn/EditMovie?id=abc`.</span><span class="sxs-lookup"><span data-stu-id="4a686-291">For example, the page could be invoked with a URL like `http://localhost:nnnnn/EditMovie?id=abc`.</span></span>
- <span data-ttu-id="4a686-292">A ID é um número, mas faz referência a um filme que não existe (por exemplo, `http://localhost:nnnnn/EditMovie?id=100934`).</span><span class="sxs-lookup"><span data-stu-id="4a686-292">The ID is a number, but it references a movie that doesn't exist (for example, `http://localhost:nnnnn/EditMovie?id=100934`).</span></span>

<span data-ttu-id="4a686-293">Se você estiver curioso para ver os erros resultantes dessas URLs, executadas o *filmes* página.</span><span class="sxs-lookup"><span data-stu-id="4a686-293">If you're curious to see the errors that result from these URLs, run the *Movies* page.</span></span> <span data-ttu-id="4a686-294">Selecione um filme para editar e, em seguida, altere a URL do *EditMovie* página para uma URL que contém um alfabético ID ou a ID de um filme inexistente.</span><span class="sxs-lookup"><span data-stu-id="4a686-294">Select a movie to edit, and then change the URL of the *EditMovie* page to a URL that contains an alphabetic ID or the ID of a non-existent movie.</span></span>

<span data-ttu-id="4a686-295">Assim que você deve fazer?</span><span class="sxs-lookup"><span data-stu-id="4a686-295">So what should you do?</span></span> <span data-ttu-id="4a686-296">A primeira correção é certificar-se de que não só é uma ID passada para a página, mas a ID é um inteiro.</span><span class="sxs-lookup"><span data-stu-id="4a686-296">The first fix is to make sure that not only is an ID passed to the page, but that the ID is an integer.</span></span> <span data-ttu-id="4a686-297">Altere o código para o `!IsPost` teste para se parecer com este exemplo:</span><span class="sxs-lookup"><span data-stu-id="4a686-297">Change the code for the `!IsPost` test to look like this example:</span></span>

[!code-csharp[Main](updating-data/samples/sample14.cs)]

<span data-ttu-id="4a686-298">Você adicionou uma segunda condição para a `IsEmpty` teste, vinculado com `&&` (AND lógico):</span><span class="sxs-lookup"><span data-stu-id="4a686-298">You've added a second condition to the `IsEmpty` test, linked with `&&` (logical AND):</span></span>

[!code-csharp[Main](updating-data/samples/sample15.cs)]

<span data-ttu-id="4a686-299">Você deve se lembrar do [Introdução à programação de páginas da Web do ASP.NET](../introducing-razor-syntax-c.md) tutorial métodos como `AsBool` um `AsInt` converter uma cadeia de caracteres para outro tipo de dados.</span><span class="sxs-lookup"><span data-stu-id="4a686-299">You might remember from the [Introduction to ASP.NET Web Pages Programming](../introducing-razor-syntax-c.md) tutorial that methods like `AsBool` an `AsInt` convert a character string to some other data type.</span></span> <span data-ttu-id="4a686-300">O `IsInt` método (e outros, como `IsBool` e `IsDateTime`) são semelhantes.</span><span class="sxs-lookup"><span data-stu-id="4a686-300">The `IsInt` method (and others, like `IsBool` and `IsDateTime`) are similar.</span></span> <span data-ttu-id="4a686-301">No entanto, eles testam somente se você *pode* converter a cadeia de caracteres, sem realmente executar a conversão.</span><span class="sxs-lookup"><span data-stu-id="4a686-301">However, they test only whether you *can* convert the string, without actually performing the conversion.</span></span> <span data-ttu-id="4a686-302">Aqui você está dizendo essencialmente *se o valor de cadeia de caracteres de consulta pode ser convertido em um número inteiro...* .</span><span class="sxs-lookup"><span data-stu-id="4a686-302">So here you're essentially saying *If the query string value can be converted to an integer ...*.</span></span>

<span data-ttu-id="4a686-303">O outro problema está procurando um filme que não existe.</span><span class="sxs-lookup"><span data-stu-id="4a686-303">The other potential problem is looking for a movie that doesn't exist.</span></span> <span data-ttu-id="4a686-304">O código para obter um filme parece com este código:</span><span class="sxs-lookup"><span data-stu-id="4a686-304">The code to get a movie looks like this code:</span></span>

[!code-csharp[Main](updating-data/samples/sample16.cs)]

<span data-ttu-id="4a686-305">Se você passar um `movieId` o valor para o `QuerySingle` método que não corresponde a um filme real, nada será retornado e as instruções a seguir (por exemplo, `title=row.Title`) resultar em erros.</span><span class="sxs-lookup"><span data-stu-id="4a686-305">If you pass a `movieId` value to the `QuerySingle` method that doesn't correspond to an actual movie, nothing is returned and the statements that follow (for example, `title=row.Title`) result in errors.</span></span>

<span data-ttu-id="4a686-306">Novamente, há uma solução fácil.</span><span class="sxs-lookup"><span data-stu-id="4a686-306">Again there's an easy fix.</span></span> <span data-ttu-id="4a686-307">Se o `db.QuerySingle` método não retorna nenhum resultado, o `row` variável será nula.</span><span class="sxs-lookup"><span data-stu-id="4a686-307">If the `db.QuerySingle` method returns no results, the `row` variable will be null.</span></span> <span data-ttu-id="4a686-308">Por isso, você pode verificar se o `row` variável é nula antes de tentar obter valores dele.</span><span class="sxs-lookup"><span data-stu-id="4a686-308">So you can check whether the `row` variable is null before you try to get values from it.</span></span> <span data-ttu-id="4a686-309">O código a seguir adiciona uma `if` bloco de instruções que obtêm valores da `row` objeto:</span><span class="sxs-lookup"><span data-stu-id="4a686-309">The following code adds an `if` block around the statements that get the values out of the `row` object:</span></span>

[!code-csharp[Main](updating-data/samples/sample17.cs)]

<span data-ttu-id="4a686-310">Com esses dois testes de validação adicionais, a página se torna mais prova.</span><span class="sxs-lookup"><span data-stu-id="4a686-310">With these two additional validation tests, the page becomes more bullet-proof.</span></span> <span data-ttu-id="4a686-311">O código completo para o `!IsPost` ramificação agora parece semelhante a este exemplo:</span><span class="sxs-lookup"><span data-stu-id="4a686-311">The complete code for the `!IsPost` branch now looks like this example:</span></span>

[!code-csharp[Main](updating-data/samples/sample18.cs)]

<span data-ttu-id="4a686-312">Podemos observará mais uma vez que essa tarefa é um bom uso para um `else` bloco.</span><span class="sxs-lookup"><span data-stu-id="4a686-312">We'll note once more that this task is a good use for an `else` block.</span></span> <span data-ttu-id="4a686-313">Se não passarem nos testes, a `else` conjunto de blocos de mensagens de erro.</span><span class="sxs-lookup"><span data-stu-id="4a686-313">If the tests don't pass, the `else` blocks set error messages.</span></span>

## <a name="adding-a-link-to-return-to-the-movies-page"></a><span data-ttu-id="4a686-314">Adicionar um Link para retornar à página de filmes</span><span class="sxs-lookup"><span data-stu-id="4a686-314">Adding a Link to Return to the Movies Page</span></span>

<span data-ttu-id="4a686-315">Um detalhe final e útil é adicionar um link para o *filmes* página.</span><span class="sxs-lookup"><span data-stu-id="4a686-315">A final and helpful detail is to add a link back to the *Movies* page.</span></span> <span data-ttu-id="4a686-316">O fluxo normal de eventos, os usuários começarão ao *filmes* página e clique em uma **editar** link.</span><span class="sxs-lookup"><span data-stu-id="4a686-316">In the ordinary flow of events, users will start at the *Movies* page and click an **Edit** link.</span></span> <span data-ttu-id="4a686-317">Isso traz-los o *EditMovie* página, onde podem editar o filme e clique no botão.</span><span class="sxs-lookup"><span data-stu-id="4a686-317">That brings them to the *EditMovie* page, where they can edit the movie and click the button.</span></span> <span data-ttu-id="4a686-318">Depois que o código processa a alteração, ele redireciona de volta para o *filmes* página.</span><span class="sxs-lookup"><span data-stu-id="4a686-318">After the code processes the change, it redirects back to the *Movies* page.</span></span>

<span data-ttu-id="4a686-319">No entanto:</span><span class="sxs-lookup"><span data-stu-id="4a686-319">However:</span></span>

- <span data-ttu-id="4a686-320">O usuário pode decidir por não altera nada.</span><span class="sxs-lookup"><span data-stu-id="4a686-320">The user might decide not to change anything.</span></span>
- <span data-ttu-id="4a686-321">O usuário pode ter chegado desta página sem primeiro clicar em um **editar** link no *filmes* página.</span><span class="sxs-lookup"><span data-stu-id="4a686-321">The user might have gotten to this page without first clicking an **Edit** link in the *Movies* page.</span></span>

<span data-ttu-id="4a686-322">De qualquer forma, que você deseja que tornam mais fácil retornar à lista principal.</span><span class="sxs-lookup"><span data-stu-id="4a686-322">Either way, you want to make it easy for them to return to the main listing.</span></span> <span data-ttu-id="4a686-323">É uma solução fácil &mdash; adicione a seguinte marcação logo após o fechamento `</form>` marca na marcação:</span><span class="sxs-lookup"><span data-stu-id="4a686-323">It's an easy fix &mdash; add the following markup just after the closing `</form>` tag in the markup:</span></span>

[!code-html[Main](updating-data/samples/sample19.html)]

<span data-ttu-id="4a686-324">Essa marcação usa a mesma sintaxe para um `<a>` elemento que você viu em outro lugar.</span><span class="sxs-lookup"><span data-stu-id="4a686-324">This markup uses the same syntax for an `<a>` element that you've seen elsewhere.</span></span> <span data-ttu-id="4a686-325">A URL inclui `~` significando "raiz do site".</span><span class="sxs-lookup"><span data-stu-id="4a686-325">The URL includes `~` to mean "root of the website."</span></span>

## <a name="testing-the-movie-update-process"></a><span data-ttu-id="4a686-326">Testar o processo de atualização de filme</span><span class="sxs-lookup"><span data-stu-id="4a686-326">Testing the Movie Update Process</span></span>

<span data-ttu-id="4a686-327">Agora você pode testar.</span><span class="sxs-lookup"><span data-stu-id="4a686-327">Now you can test.</span></span> <span data-ttu-id="4a686-328">Execute o *filmes* página e, em seguida, clique em **editar** ao lado de um filme.</span><span class="sxs-lookup"><span data-stu-id="4a686-328">Run the *Movies* page, and click **Edit** next to a movie.</span></span> <span data-ttu-id="4a686-329">Quando o *EditMovie* página aparecer, fazer alterações de filme e clique em **enviar alterações**.</span><span class="sxs-lookup"><span data-stu-id="4a686-329">When the *EditMovie* page appears, make changes to the movie and click **Submit Changes**.</span></span> <span data-ttu-id="4a686-330">Quando a listagem do filme aparece, certifique-se de que as alterações são mostradas.</span><span class="sxs-lookup"><span data-stu-id="4a686-330">When the movie listing appears, make sure that your changes are shown.</span></span>

<span data-ttu-id="4a686-331">Para certificar-se de que a validação está funcionando, clique em **editar** para outro filme.</span><span class="sxs-lookup"><span data-stu-id="4a686-331">To make sure that validation is working, click **Edit** for another movie.</span></span> <span data-ttu-id="4a686-332">Quando chegar a *EditMovie* página, desmarque o **gênero** campo (ou **ano** campo, ou ambos) e tentar enviar suas alterações.</span><span class="sxs-lookup"><span data-stu-id="4a686-332">When you get to the *EditMovie* page, clear the **Genre** field (or **Year** field, or both) and try to submit your changes.</span></span> <span data-ttu-id="4a686-333">Você verá um erro, conforme o esperado:</span><span class="sxs-lookup"><span data-stu-id="4a686-333">You'll see an error, as you'd expect:</span></span>

![Editar página de filme mostrando erros de validação](updating-data/_static/image4.png)

<span data-ttu-id="4a686-335">Clique o **retornar à lista de filme** link para abandonar suas alterações e retornar para o *filmes* página.</span><span class="sxs-lookup"><span data-stu-id="4a686-335">Click the **Return to movie listing** link to abandon your changes and return to the *Movies* page.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="4a686-336">Próximo</span><span class="sxs-lookup"><span data-stu-id="4a686-336">Coming Up Next</span></span>

<span data-ttu-id="4a686-337">O seguinte tutorial, você verá como excluir um registro de filme.</span><span class="sxs-lookup"><span data-stu-id="4a686-337">In the next tutorial, you'll see how to delete a movie record.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-edit-links"></a><span data-ttu-id="4a686-338">Listagem completa para a página de filme (atualizada com Links de edição)</span><span class="sxs-lookup"><span data-stu-id="4a686-338">Complete Listing for Movie Page (Updated with Edit Links)</span></span>

[!code-cshtml[Main](updating-data/samples/sample20.cshtml)]

<a id="Complete_Page_Listing_for_EditMovie"></a>
## <a name="complete-page-listing-for-edit-movie-page"></a><span data-ttu-id="4a686-339">Listagem de página para a página de edição de filme completo</span><span class="sxs-lookup"><span data-stu-id="4a686-339">Complete Page Listing for Edit Movie Page</span></span>

[!code-cshtml[Main](updating-data/samples/sample21.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="4a686-340">Recursos adicionais</span><span class="sxs-lookup"><span data-stu-id="4a686-340">Additional Resources</span></span>

- [<span data-ttu-id="4a686-341">Introdução à programação da Web do ASP.NET usando a sintaxe do Razor</span><span class="sxs-lookup"><span data-stu-id="4a686-341">Introduction to ASP.NET Web Programming by Using the Razor Syntax</span></span>](introducing-razor-syntax-c.md)
- <span data-ttu-id="4a686-342">[Instrução de atualização SQL](http://www.w3schools.com/sql/sql_update.asp) no site W3Schools</span><span class="sxs-lookup"><span data-stu-id="4a686-342">[SQL UPDATE Statement](http://www.w3schools.com/sql/sql_update.asp) on the W3Schools site</span></span>

>[!div class="step-by-step"]
<span data-ttu-id="4a686-343">[Anterior](entering-data.md)
[Próximo](deleting-data.md)</span><span class="sxs-lookup"><span data-stu-id="4a686-343">[Previous](entering-data.md)
[Next](deleting-data.md)</span></span>
