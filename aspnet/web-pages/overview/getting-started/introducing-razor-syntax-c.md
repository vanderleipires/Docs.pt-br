---
uid: web-pages/overview/getting-started/introducing-razor-syntax-c
title: "Introdução à programação da Web do ASP.NET usando a sintaxe do Razor (c#) | Microsoft Docs"
author: tfitzmac
description: "Este capítulo fornece uma visão geral de programação com páginas da Web do ASP.NET usando a sintaxe do Razor. O ASP.NET é uma tecnologia da Microsoft para executar o pa web dinâmicos..."
ms.author: aspnetcontent
manager: wpickett
ms.date: 02/07/2014
ms.topic: article
ms.assetid: aa67d304-583b-4bf8-a231-195656cfb587
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-razor-syntax-c
msc.type: authoredcontent
ms.openlocfilehash: 32cdd8d524d783d7ccc3ab076de636ce4a868132
ms.sourcegitcommit: 060879fcf3f73d2366b5c811986f8695fff65db8
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 01/24/2018
---
<a name="introduction-to-aspnet-web-programming-using-the-razor-syntax-c"></a><span data-ttu-id="0d03c-104">Introdução à programação da Web do ASP.NET usando a sintaxe do Razor (c#)</span><span class="sxs-lookup"><span data-stu-id="0d03c-104">Introduction to ASP.NET Web Programming Using the Razor Syntax (C#)</span></span>
====================
<span data-ttu-id="0d03c-105">por [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="0d03c-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="0d03c-106">Este artigo fornece uma visão geral de programação com páginas da Web do ASP.NET usando a sintaxe do Razor.</span><span class="sxs-lookup"><span data-stu-id="0d03c-106">This article gives you an overview of programming with ASP.NET Web Pages using the Razor syntax.</span></span> <span data-ttu-id="0d03c-107">O ASP.NET é uma tecnologia da Microsoft para a execução de páginas da web dinâmicas em servidores web.</span><span class="sxs-lookup"><span data-stu-id="0d03c-107">ASP.NET is Microsoft's technology for running dynamic web pages on web servers.</span></span> <span data-ttu-id="0d03c-108">Este artigo enfoca usando a linguagem de programação c#.</span><span class="sxs-lookup"><span data-stu-id="0d03c-108">This articles focuses on using the C# programming language.</span></span>
> 
> <span data-ttu-id="0d03c-109">**Você aprenderá**:</span><span class="sxs-lookup"><span data-stu-id="0d03c-109">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="0d03c-110">Os 8 principais dicas para começar a trabalhar com o ASP.NET Web Pages com sintaxe Razor de programação de programação.</span><span class="sxs-lookup"><span data-stu-id="0d03c-110">The top 8 programming tips for getting started with programming ASP.NET Web Pages using Razor syntax.</span></span>
> - <span data-ttu-id="0d03c-111">Conceitos de programação, será necessário.</span><span class="sxs-lookup"><span data-stu-id="0d03c-111">Basic programming concepts you'll need.</span></span>
> - <span data-ttu-id="0d03c-112">O código de servidor do ASP.NET e a sintaxe do Razor é tudo sobre.</span><span class="sxs-lookup"><span data-stu-id="0d03c-112">What ASP.NET server code and the Razor syntax is all about.</span></span>
>   
> 
> ## <a name="software-versions"></a><span data-ttu-id="0d03c-113">Versões de software</span><span class="sxs-lookup"><span data-stu-id="0d03c-113">Software versions</span></span>
> 
> 
> - <span data-ttu-id="0d03c-114">Páginas da Web do ASP.NET (Razor) 3</span><span class="sxs-lookup"><span data-stu-id="0d03c-114">ASP.NET Web Pages (Razor) 3</span></span>
>   
> 
> <span data-ttu-id="0d03c-115">Este tutorial também funciona com 2 de páginas da Web do ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="0d03c-115">This tutorial also works with ASP.NET Web Pages 2.</span></span>


## <a name="the-top-8-programming-tips"></a><span data-ttu-id="0d03c-116">As dicas de programação 8 superior</span><span class="sxs-lookup"><span data-stu-id="0d03c-116">The Top 8 Programming Tips</span></span>

<span data-ttu-id="0d03c-117">Esta seção lista algumas dicas que é absolutamente necessário saber como começar a escrever código de servidor ASP.NET usando a sintaxe do Razor.</span><span class="sxs-lookup"><span data-stu-id="0d03c-117">This section lists a few tips that you absolutely need to know as you start writing ASP.NET server code using the Razor syntax.</span></span>

> [!NOTE]
> <span data-ttu-id="0d03c-118">A sintaxe do Razor é baseada na linguagem de programação c#, e que é a linguagem que é geralmente usada com páginas da Web do ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="0d03c-118">The Razor syntax is based on the C# programming language, and that's the language that's used most often with ASP.NET Web Pages.</span></span> <span data-ttu-id="0d03c-119">No entanto, a sintaxe do Razor também oferece suporte a linguagem Visual Basic e tudo o que você vê que você também pode fazer no Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="0d03c-119">However, the Razor syntax also supports the Visual Basic language, and everything you see you can also do in Visual Basic.</span></span> <span data-ttu-id="0d03c-120">Para obter detalhes, consulte o Apêndice [linguagem Visual Basic e a sintaxe](https://go.microsoft.com/fwlink/?LinkId=202908).</span><span class="sxs-lookup"><span data-stu-id="0d03c-120">For details, see the appendix [Visual Basic Language and Syntax](https://go.microsoft.com/fwlink/?LinkId=202908).</span></span>


<span data-ttu-id="0d03c-121">Você pode encontrar mais detalhes sobre a maioria dessas técnicas de programação posteriormente neste artigo.</span><span class="sxs-lookup"><span data-stu-id="0d03c-121">You can find more details about most of these programming techniques later in the article.</span></span>

### <a name="1-you-add-code-to-a-page-using-the--character"></a><span data-ttu-id="0d03c-122">1. Adicione código para uma página usando o caractere @</span><span class="sxs-lookup"><span data-stu-id="0d03c-122">1. You add code to a page using the @ character</span></span>

<span data-ttu-id="0d03c-123">O `@` caractere inicia expressões internas, blocos de instrução única e blocos de várias instruções:</span><span class="sxs-lookup"><span data-stu-id="0d03c-123">The `@` character starts inline expressions, single statement blocks, and multi-statement blocks:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample1.html)]

<span data-ttu-id="0d03c-124">Isso é que essas instruções aparência quando a página é executada em um navegador:</span><span class="sxs-lookup"><span data-stu-id="0d03c-124">This is what these statements look like when the page runs in a browser:</span></span>

![Razor-Img1](introducing-razor-syntax-c/_static/image1.jpg)

> [!TIP] 
> 
> <span data-ttu-id="0d03c-126">**Codificação HTML**</span><span class="sxs-lookup"><span data-stu-id="0d03c-126">**HTML Encoding**</span></span>
> 
> <span data-ttu-id="0d03c-127">Quando você exibe o conteúdo em uma página usando o `@` caractere, como nos exemplos anteriores, ASP.NET HTML codifica a saída.</span><span class="sxs-lookup"><span data-stu-id="0d03c-127">When you display content in a page using the `@` character, as in the preceding examples, ASP.NET HTML-encodes the output.</span></span> <span data-ttu-id="0d03c-128">Isso substitui caracteres reservados de HTML (como `<` e `>` e `&`) com códigos que permitem que os caracteres a serem exibidos como caracteres em uma página da web em vez de ser interpretado como marcas HTML ou entidades.</span><span class="sxs-lookup"><span data-stu-id="0d03c-128">This replaces reserved HTML characters (such as `<` and `>` and `&`) with codes that enable the characters to be displayed as characters in a web page instead of being interpreted as HTML tags or entities.</span></span> <span data-ttu-id="0d03c-129">Sem codificação HTML, a saída do seu código de servidor não seja exibido corretamente e pode expor uma página a riscos de segurança.</span><span class="sxs-lookup"><span data-stu-id="0d03c-129">Without HTML encoding, the output from your server code might not display correctly, and could expose a page to security risks.</span></span>
> 
> <span data-ttu-id="0d03c-130">Se sua meta é uma marcação HTML que renderiza marcações como marcação de saída (por exemplo `<p></p>` para um parágrafo ou `<em></em>` para enfatizar o texto), consulte a seção [combinação de texto, marcação e código em blocos de código](#BM_CombiningTextMarkupAndCode) posteriormente neste artigo.</span><span class="sxs-lookup"><span data-stu-id="0d03c-130">If your goal is to output HTML markup that renders tags as markup (for example `<p></p>` for a paragraph or `<em></em>` to emphasize text), see the section [Combining Text, Markup, and Code in Code Blocks](#BM_CombiningTextMarkupAndCode) later in this article.</span></span>
> 
> <span data-ttu-id="0d03c-131">Você pode ler mais sobre a codificação de HTML no [trabalhar com formulários](https://go.microsoft.com/fwlink/?LinkId=202892).</span><span class="sxs-lookup"><span data-stu-id="0d03c-131">You can read more about HTML encoding in [Working with Forms](https://go.microsoft.com/fwlink/?LinkId=202892).</span></span>


### <a name="2-you-enclose-code-blocks-in-braces"></a><span data-ttu-id="0d03c-132">2. Coloque os blocos de código entre chaves</span><span class="sxs-lookup"><span data-stu-id="0d03c-132">2. You enclose code blocks in braces</span></span>

<span data-ttu-id="0d03c-133">Um *bloco de código* inclui uma ou mais instruções de código e fica entre chaves.</span><span class="sxs-lookup"><span data-stu-id="0d03c-133">A *code block* includes one or more code statements and is enclosed in braces.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample2.html)]

<span data-ttu-id="0d03c-134">O resultado exibido em um navegador:</span><span class="sxs-lookup"><span data-stu-id="0d03c-134">The result displayed in a browser:</span></span>

![Razor-Img2](introducing-razor-syntax-c/_static/image2.jpg)

### <a name="3-inside-a-block-you-end-each-code-statement-with-a-semicolon"></a><span data-ttu-id="0d03c-136">3. Dentro de um bloco, você encerrar a cada instrução de código com um ponto e vírgula</span><span class="sxs-lookup"><span data-stu-id="0d03c-136">3. Inside a block, you end each code statement with a semicolon</span></span>

<span data-ttu-id="0d03c-137">Dentro de um bloco de código, cada instrução de código completo deve terminar com um ponto e vírgula.</span><span class="sxs-lookup"><span data-stu-id="0d03c-137">Inside a code block, each complete code statement must end with a semicolon.</span></span> <span data-ttu-id="0d03c-138">Expressões internas não terminam com um ponto e vírgula.</span><span class="sxs-lookup"><span data-stu-id="0d03c-138">Inline expressions don't end with a semicolon.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample3.html)]

### <a name="4-you-use-variables-to-store-values"></a><span data-ttu-id="0d03c-139">4. Usar variáveis para armazenar valores</span><span class="sxs-lookup"><span data-stu-id="0d03c-139">4. You use variables to store values</span></span>

<span data-ttu-id="0d03c-140">Você pode armazenar valores em uma *variável*, incluindo cadeias de caracteres, números e datas, etc. Criar uma nova variável usando o `var` palavra-chave.</span><span class="sxs-lookup"><span data-stu-id="0d03c-140">You can store values in a *variable*, including strings, numbers, and dates, etc. You create a new variable using the `var` keyword.</span></span> <span data-ttu-id="0d03c-141">Você pode inserir valores de variáveis diretamente em uma página usando `@`.</span><span class="sxs-lookup"><span data-stu-id="0d03c-141">You can insert variable values directly in a page using `@`.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample4.html)]

<span data-ttu-id="0d03c-142">O resultado exibido em um navegador:</span><span class="sxs-lookup"><span data-stu-id="0d03c-142">The result displayed in a browser:</span></span>

![Razor-Img3](introducing-razor-syntax-c/_static/image3.jpg)

<a id="ID_StringLiterals"></a>
### <a name="5-you-enclose-literal-string-values-in-double-quotation-marks"></a><span data-ttu-id="0d03c-144">5. Coloque os valores de cadeia de caracteres literal entre aspas duplas</span><span class="sxs-lookup"><span data-stu-id="0d03c-144">5. You enclose literal string values in double quotation marks</span></span>

<span data-ttu-id="0d03c-145">Um *cadeia de caracteres* é uma sequência de caracteres que são tratados como texto.</span><span class="sxs-lookup"><span data-stu-id="0d03c-145">A *string* is a sequence of characters that are treated as text.</span></span> <span data-ttu-id="0d03c-146">Para especificar uma cadeia de caracteres, coloque-o entre aspas duplas:</span><span class="sxs-lookup"><span data-stu-id="0d03c-146">To specify a string, you enclose it in double quotation marks:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample5.cshtml)]

<span data-ttu-id="0d03c-147">Se a cadeia de caracteres que você deseja exibir contém um caractere de barra invertida ( `\` ) ou aspas duplas ( `"` ), use um *literal de cadeia de caracteres textual* que é prefixado com o `@` operador.</span><span class="sxs-lookup"><span data-stu-id="0d03c-147">If the string that you want to display contains a backslash character ( `\` ) or double quotation marks ( `"` ), use a *verbatim string literal* that's prefixed with the `@` operator.</span></span> <span data-ttu-id="0d03c-148">(Em c#, o \ caractere tem um significado especial, a menos que você use uma cadeia de caracteres textual literal.)</span><span class="sxs-lookup"><span data-stu-id="0d03c-148">(In C#, the \ character has special meaning unless you use a verbatim string literal.)</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample6.html)]

<span data-ttu-id="0d03c-149">Inserir aspas duplas, use uma cadeia de caracteres textual literal e repita as aspas:</span><span class="sxs-lookup"><span data-stu-id="0d03c-149">To embed double quotation marks, use a verbatim string literal and repeat the quotation marks:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample7.html)]

<span data-ttu-id="0d03c-150">Aqui está o resultado do uso de ambos os exemplos em uma página:</span><span class="sxs-lookup"><span data-stu-id="0d03c-150">Here's the result of using both of these examples in a page:</span></span>

![Razor-Img4](introducing-razor-syntax-c/_static/image4.jpg)

> [!NOTE]
> <span data-ttu-id="0d03c-152">Observe que o `@` caractere é usado para marcar os literais de cadeia de caracteres textuais em c# e marcar o código em páginas ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="0d03c-152">Notice that the `@` character is used both to mark verbatim string literals in C# and to mark code in ASP.NET pages.</span></span>


### <a name="6-code-is-case-sensitive"></a><span data-ttu-id="0d03c-153">6. Código diferencia maiusculas de minúsculas</span><span class="sxs-lookup"><span data-stu-id="0d03c-153">6. Code is case sensitive</span></span>

<span data-ttu-id="0d03c-154">No c#, palavras-chave (como `var`, `true`, e `if`) e nomes de variáveis diferenciam maiusculas de minúsculas.</span><span class="sxs-lookup"><span data-stu-id="0d03c-154">In C#, keywords (like `var`, `true`, and `if`) and variable names are case sensitive.</span></span> <span data-ttu-id="0d03c-155">Linhas de código a seguir criam duas variáveis diferentes, `lastName` e`LastName.`</span><span class="sxs-lookup"><span data-stu-id="0d03c-155">The following lines of code create two different variables, `lastName` and `LastName.`</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample8.cshtml)]

<span data-ttu-id="0d03c-156">Se você declarar uma variável como `var lastName = "Smith";` e se você tentar fazer referência a essa variável em sua página como `@LastName`, ocorrerá um erro porque `LastName` não será reconhecida.</span><span class="sxs-lookup"><span data-stu-id="0d03c-156">If you declare a variable as `var lastName = "Smith";` and if you try to reference that variable in your page as `@LastName`, an error results because `LastName` won't be recognized.</span></span>

> [!NOTE]
> <span data-ttu-id="0d03c-157">No Visual Basic, variáveis e palavras-chave são *não* diferencia maiusculas de minúsculas.</span><span class="sxs-lookup"><span data-stu-id="0d03c-157">In Visual Basic, keywords and variables are *not* case sensitive.</span></span>


### <a name="7-much-of-your-coding-involves-objects"></a><span data-ttu-id="0d03c-158">7. Grande parte da sua codificação envolve objetos</span><span class="sxs-lookup"><span data-stu-id="0d03c-158">7. Much of your coding involves objects</span></span>

<span data-ttu-id="0d03c-159">Um *objeto* representa algo que você pode programar com &#8212; uma página, uma caixa de texto, um arquivo, uma imagem, uma solicitação da web, uma mensagem de email, um registro de cliente (linha de banco de dados), etc. Os objetos têm propriedades que descrevem suas características e que você pode ler ou alterar &#8212; um objeto de caixa de texto tem uma `Text` propriedade (entre outros), um objeto de solicitação tem uma `Url` propriedade, uma mensagem de email tem um `From` propriedade e um objeto de cliente tem um `FirstName` propriedade.</span><span class="sxs-lookup"><span data-stu-id="0d03c-159">An *object* represents a thing that you can program with &#8212; a page, a text box, a file, an image, a web request, an email message, a customer record (database row), etc. Objects have properties that describe their characteristics and that you can read or change &#8212; a text box object has a `Text` property (among others), a request object has a `Url` property, an email message has a `From` property, and a customer object has a `FirstName` property.</span></span> <span data-ttu-id="0d03c-160">Objetos também têm métodos que são o &quot;verbos&quot; eles podem executar.</span><span class="sxs-lookup"><span data-stu-id="0d03c-160">Objects also have methods that are the &quot;verbs&quot; they can perform.</span></span> <span data-ttu-id="0d03c-161">Os exemplos incluem um objeto de arquivo `Save` método, um objeto de imagem `Rotate` método e um objeto de email `Send` método.</span><span class="sxs-lookup"><span data-stu-id="0d03c-161">Examples include a file object's `Save` method, an image object's `Rotate` method, and an email object's `Send` method.</span></span>

<span data-ttu-id="0d03c-162">Geralmente, você trabalhará com o `Request` do objeto, que fornece informações como os valores de caixas de texto (campos de formulário) na página, o tipo de navegador fez a solicitação, a URL da página, a identidade do usuário, etc. O exemplo a seguir mostra como acessar as propriedades do `Request` objeto e como chamar o `MapPath` método o `Request` object, que fornece o caminho absoluto da página no servidor:</span><span class="sxs-lookup"><span data-stu-id="0d03c-162">You'll often work with the `Request` object, which gives you information like the values of text boxes (form fields) on the page, what type of browser made the request, the URL of the page, the user identity, etc. The following example shows how to access properties of the `Request` object and how to call the `MapPath` method of the `Request` object, which gives you the absolute path of the page on the server:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample9.html)]

<span data-ttu-id="0d03c-163">O resultado exibido em um navegador:</span><span class="sxs-lookup"><span data-stu-id="0d03c-163">The result displayed in a browser:</span></span>

![Razor-Img5](introducing-razor-syntax-c/_static/image5.jpg)

### <a name="8-you-can-write-code-that-makes-decisions"></a><span data-ttu-id="0d03c-165">8. Você pode escrever código que toma decisões</span><span class="sxs-lookup"><span data-stu-id="0d03c-165">8. You can write code that makes decisions</span></span>

<span data-ttu-id="0d03c-166">Um recurso importante de páginas da web dinâmicas é que você pode determinar o que fazer com base nas condições.</span><span class="sxs-lookup"><span data-stu-id="0d03c-166">A key feature of dynamic web pages is that you can determine what to do based on conditions.</span></span> <span data-ttu-id="0d03c-167">A maneira mais comum para isso é com o `if` instrução (e opcionais `else` instrução).</span><span class="sxs-lookup"><span data-stu-id="0d03c-167">The most common way to do this is with the `if` statement (and optional `else` statement).</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample10.cshtml)]

<span data-ttu-id="0d03c-168">A instrução `if(IsPost)` é uma forma abreviada de gravar `if(IsPost == true)`.</span><span class="sxs-lookup"><span data-stu-id="0d03c-168">The statement `if(IsPost)` is a shorthand way of writing `if(IsPost == true)`.</span></span> <span data-ttu-id="0d03c-169">Juntamente com `if` instruções, há uma variedade de maneiras de testar condições, repetição blocos de código, e assim por diante, que são descrito neste artigo.</span><span class="sxs-lookup"><span data-stu-id="0d03c-169">Along with `if` statements, there are a variety of ways to test conditions, repeat blocks of code, and so on, which are described later in this article.</span></span>

<span data-ttu-id="0d03c-170">O resultado exibido em um navegador (depois de clicar em **enviar**):</span><span class="sxs-lookup"><span data-stu-id="0d03c-170">The result displayed in a browser (after clicking **Submit**):</span></span>

![Razor-Img6](introducing-razor-syntax-c/_static/image6.jpg)

> [!TIP] 
> 
> <a id="SB_HttpGetPost"></a>
> ### <a name="http-get-and-post-methods-and-the-ispost-property"></a><span data-ttu-id="0d03c-172">HTTP GET e POST métodos e a propriedade IsPost</span><span class="sxs-lookup"><span data-stu-id="0d03c-172">HTTP GET and POST Methods and the IsPost Property</span></span>
> 
> <span data-ttu-id="0d03c-173">O protocolo usado para páginas da web (HTTP) oferece suporte a um número muito limitado de métodos (verbos) que são usados para fazer solicitações para o servidor.</span><span class="sxs-lookup"><span data-stu-id="0d03c-173">The protocol used for web pages (HTTP) supports a very limited number of methods (verbs) that are used to make requests to the server.</span></span> <span data-ttu-id="0d03c-174">Duas as mais comuns são GET, que é usado para ler uma página, e o POST, que é usado para enviar uma página.</span><span class="sxs-lookup"><span data-stu-id="0d03c-174">The two most common ones are GET, which is used to read a page, and POST, which is used to submit a page.</span></span> <span data-ttu-id="0d03c-175">Em geral, a primeira vez que um usuário solicita uma página, a página é solicitada usando GET.</span><span class="sxs-lookup"><span data-stu-id="0d03c-175">In general, the first time a user requests a page, the page is requested using GET.</span></span> <span data-ttu-id="0d03c-176">Se o usuário preenche um formulário e, em seguida, clica no botão Enviar, o navegador faz uma solicitação POST para o servidor.</span><span class="sxs-lookup"><span data-stu-id="0d03c-176">If the user fills in a form and then clicks a submit button, the browser makes a POST request to the server.</span></span>
> 
> <span data-ttu-id="0d03c-177">Programação da web, geralmente é útil saber se uma página está sendo solicitada como GET ou como um POST para que você sabe como processar a página.</span><span class="sxs-lookup"><span data-stu-id="0d03c-177">In web programming, it's often useful to know whether a page is being requested as a GET or as a POST so that you know how to process the page.</span></span> <span data-ttu-id="0d03c-178">Em páginas de Web do ASP.NET, você pode usar o `IsPost` propriedade para ver se uma solicitação é um GET ou um POST.</span><span class="sxs-lookup"><span data-stu-id="0d03c-178">In ASP.NET Web Pages, you can use the `IsPost` property to see whether a request is a GET or a POST.</span></span> <span data-ttu-id="0d03c-179">Se a solicitação for uma POSTAGEM, o `IsPost` propriedade retornará true, e você pode fazer coisas como ler os valores das caixas de texto em um formulário.</span><span class="sxs-lookup"><span data-stu-id="0d03c-179">If the request is a POST, the `IsPost` property will return true, and you can do things like read the values of text boxes on a form.</span></span> <span data-ttu-id="0d03c-180">Muitos exemplos, você verá mostram como processar a página de forma diferente dependendo do valor de `IsPost`.</span><span class="sxs-lookup"><span data-stu-id="0d03c-180">Many examples you'll see show you how to process the page differently depending on the value of `IsPost`.</span></span>


## <a name="a-simple-code-example"></a><span data-ttu-id="0d03c-181">Um exemplo de código simples</span><span class="sxs-lookup"><span data-stu-id="0d03c-181">A Simple Code Example</span></span>

<span data-ttu-id="0d03c-182">Este procedimento mostra como criar uma página que ilustra as técnicas básicas de programação.</span><span class="sxs-lookup"><span data-stu-id="0d03c-182">This procedure shows you how to create a page that illustrates basic programming techniques.</span></span> <span data-ttu-id="0d03c-183">No exemplo, você deve criar uma página que permite aos usuários inserir dois números, em seguida, adiciona-os e exibe o resultado.</span><span class="sxs-lookup"><span data-stu-id="0d03c-183">In the example, you create a page that lets users enter two numbers, then it adds them and displays the result.</span></span>

1. <span data-ttu-id="0d03c-184">Em seu editor, crie um novo arquivo e nomeie- *AddNumbers.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="0d03c-184">In your editor, create a new file and name it *AddNumbers.cshtml*.</span></span>
2. <span data-ttu-id="0d03c-185">Copie o seguinte código e marcação para a página, substituindo qualquer coisa já na página.</span><span class="sxs-lookup"><span data-stu-id="0d03c-185">Copy the following code and markup into the page, replacing anything already in the page.</span></span>  

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample11.cshtml)]

    <span data-ttu-id="0d03c-186">Aqui estão algumas coisas a observar:</span><span class="sxs-lookup"><span data-stu-id="0d03c-186">Here are some things for you to note:</span></span>

    - <span data-ttu-id="0d03c-187">O `@` caractere inicia o primeiro bloco de código na página, e ela precede o `totalMessage` variável que é inserido na parte inferior da página.</span><span class="sxs-lookup"><span data-stu-id="0d03c-187">The `@` character starts the first block of code in the page, and it precedes the `totalMessage` variable that's embedded near the bottom of the page.</span></span>
    - <span data-ttu-id="0d03c-188">O bloco na parte superior da página é colocado entre chaves.</span><span class="sxs-lookup"><span data-stu-id="0d03c-188">The block at the top of the page is enclosed in braces.</span></span>
    - <span data-ttu-id="0d03c-189">No bloco na parte superior, todas as linhas terminam com um ponto e vírgula.</span><span class="sxs-lookup"><span data-stu-id="0d03c-189">In the block at the top, all lines end with a semicolon.</span></span>
    - <span data-ttu-id="0d03c-190">As variáveis `total`, `num1`, `num2`, e `totalMessage` armazenar vários números e uma cadeia de caracteres.</span><span class="sxs-lookup"><span data-stu-id="0d03c-190">The variables `total`, `num1`, `num2`, and `totalMessage` store several numbers and a string.</span></span>
    - <span data-ttu-id="0d03c-191">O valor de cadeia de caracteres literal atribuído para o `totalMessage` variável está entre aspas duplas.</span><span class="sxs-lookup"><span data-stu-id="0d03c-191">The literal string value assigned to the `totalMessage` variable is in double quotation marks.</span></span>
    - <span data-ttu-id="0d03c-192">Porque o código diferencia maiusculas de minúsculas, quando o `totalMessage` variável é usada na parte inferior da página, o nome deve corresponder exatamente a variável na parte superior.</span><span class="sxs-lookup"><span data-stu-id="0d03c-192">Because the code is case-sensitive, when the `totalMessage` variable is used near the bottom of the page, its name must match the variable at the top exactly.</span></span>
    - <span data-ttu-id="0d03c-193">A expressão `num1.AsInt() + num2.AsInt()` mostra como trabalhar com objetos e métodos.</span><span class="sxs-lookup"><span data-stu-id="0d03c-193">The expression `num1.AsInt() + num2.AsInt()` shows how to work with objects and methods.</span></span> <span data-ttu-id="0d03c-194">O `AsInt` método em cada variável converte a cadeia de caracteres inserida por um usuário para um número (um inteiro) para que você possa realizar aritmética nele.</span><span class="sxs-lookup"><span data-stu-id="0d03c-194">The `AsInt` method on each variable converts the string entered by a user to a number (an integer) so that you can perform arithmetic on it.</span></span>
    - <span data-ttu-id="0d03c-195">O `<form>` marca inclui um `method="post"` atributo.</span><span class="sxs-lookup"><span data-stu-id="0d03c-195">The `<form>` tag includes a `method="post"` attribute.</span></span> <span data-ttu-id="0d03c-196">Isso especifica que quando o usuário clica **adicionar**, a página será enviada ao servidor usando o método HTTP POST.</span><span class="sxs-lookup"><span data-stu-id="0d03c-196">This specifies that when the user clicks **Add**, the page will be sent to the server using the HTTP POST method.</span></span> <span data-ttu-id="0d03c-197">Quando a página é enviada, o `if(IsPost)` teste seja avaliada como true e a condicional código é executado, exibindo o resultado da adição de números.</span><span class="sxs-lookup"><span data-stu-id="0d03c-197">When the page is submitted, the `if(IsPost)` test evaluates to true and the conditional code runs, displaying the result of adding the numbers.</span></span>
3. <span data-ttu-id="0d03c-198">Salve a página e executá-lo em um navegador.</span><span class="sxs-lookup"><span data-stu-id="0d03c-198">Save the page and run it in a browser.</span></span> <span data-ttu-id="0d03c-199">(Verifique se a página está selecionada no **arquivos** espaço de trabalho antes de você executá-lo.) Insira dois números inteiros e, em seguida, clique no **adicionar** botão.</span><span class="sxs-lookup"><span data-stu-id="0d03c-199">(Make sure the page is selected in the **Files** workspace before you run it.) Enter two whole numbers and then click the **Add** button.</span></span> 

    ![Razor-Img7](introducing-razor-syntax-c/_static/image7.jpg)

## <a name="basic-programming-concepts"></a><span data-ttu-id="0d03c-201">Conceitos básicos de programação</span><span class="sxs-lookup"><span data-stu-id="0d03c-201">Basic Programming Concepts</span></span>

<span data-ttu-id="0d03c-202">Este artigo fornece uma visão geral de programação da web ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="0d03c-202">This article provides you with an overview of ASP.NET web programming.</span></span> <span data-ttu-id="0d03c-203">Não é uma análise completa, um rápido tour por meio de conceitos de programação que você usará com mais frequência.</span><span class="sxs-lookup"><span data-stu-id="0d03c-203">It isn't an exhaustive examination, just a quick tour through the programming concepts you'll use most often.</span></span> <span data-ttu-id="0d03c-204">Mesmo assim, ele aborda quase tudo o que você precisará começar com páginas da Web do ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="0d03c-204">Even so, it covers almost everything you'll need to get started with ASP.NET Web Pages.</span></span>

<span data-ttu-id="0d03c-205">Mas primeiro, uma pouco técnicas básicas.</span><span class="sxs-lookup"><span data-stu-id="0d03c-205">But first, a little technical background.</span></span>

### <a name="the-razor-syntax-server-code-and-aspnet"></a><span data-ttu-id="0d03c-206">A sintaxe do Razor, o código do servidor e o ASP.NET</span><span class="sxs-lookup"><span data-stu-id="0d03c-206">The Razor Syntax, Server Code, and ASP.NET</span></span>

<span data-ttu-id="0d03c-207">A sintaxe do Razor é uma sintaxe de programação simple para inserir o código de servidor em uma página da web.</span><span class="sxs-lookup"><span data-stu-id="0d03c-207">Razor syntax is a simple programming syntax for embedding server-based code in a web page.</span></span> <span data-ttu-id="0d03c-208">Em uma página da web que usa a sintaxe do Razor, há dois tipos de conteúdo: código de conteúdo e o servidor do cliente.</span><span class="sxs-lookup"><span data-stu-id="0d03c-208">In a web page that uses the Razor syntax, there are two kinds of content: client content and server code.</span></span> <span data-ttu-id="0d03c-209">Conteúdo do cliente é coisas que você está acostumado a em páginas da web: a marcação HTML (elementos), estilo informações como CSS, talvez alguns scripts de cliente, como JavaScript e texto sem formatação.</span><span class="sxs-lookup"><span data-stu-id="0d03c-209">Client content is the stuff you're used to in web pages: HTML markup (elements), style information such as CSS, maybe some client script such as JavaScript, and plain text.</span></span>

<span data-ttu-id="0d03c-210">A sintaxe do Razor permite adicionar código de servidor para esse conteúdo de cliente.</span><span class="sxs-lookup"><span data-stu-id="0d03c-210">Razor syntax lets you add server code to this client content.</span></span> <span data-ttu-id="0d03c-211">Se houver código de servidor na página, o servidor executa código primeiro, antes de enviar a página para o navegador.</span><span class="sxs-lookup"><span data-stu-id="0d03c-211">If there's server code in the page, the server runs that code first, before it sends the page to the browser.</span></span> <span data-ttu-id="0d03c-212">Executando no servidor, o código pode executar tarefas que podem ser muito mais complexas fazer usando o conteúdo do cliente usado sozinho, como acessar bancos de dados baseados em servidor.</span><span class="sxs-lookup"><span data-stu-id="0d03c-212">By running on the server, the code can perform tasks that can be a lot more complex to do using client content alone, like accessing server-based databases.</span></span> <span data-ttu-id="0d03c-213">Mais importante, o código de servidor pode criar dinamicamente cliente conteúdo &#8212; ele pode gerar uma marcação HTML ou outro conteúdo em tempo real e, em seguida, enviá-lo para o navegador junto com qualquer HTML estático que a página pode conter.</span><span class="sxs-lookup"><span data-stu-id="0d03c-213">Most importantly, server code can dynamically create client content &#8212; it can generate HTML markup or other content on the fly and then send it to the browser along with any static HTML that the page might contain.</span></span> <span data-ttu-id="0d03c-214">Da perspectiva do navegador, conteúdo de cliente que é gerado pelo seu código de servidor não é diferente de qualquer outro conteúdo do cliente.</span><span class="sxs-lookup"><span data-stu-id="0d03c-214">From the browser's perspective, client content that's generated by your server code is no different than any other client content.</span></span> <span data-ttu-id="0d03c-215">Como já vimos, o código de servidor necessária é muito simple.</span><span class="sxs-lookup"><span data-stu-id="0d03c-215">As you've already seen, the server code that's required is quite simple.</span></span>

<span data-ttu-id="0d03c-216">Páginas da web ASP.NET que inclua a sintaxe do Razor têm uma extensão de arquivo especial (*. cshtml* ou *. vbhtml*).</span><span class="sxs-lookup"><span data-stu-id="0d03c-216">ASP.NET web pages that include the Razor syntax have a special file extension (*.cshtml* or *.vbhtml*).</span></span> <span data-ttu-id="0d03c-217">O servidor reconhece essas extensões, executa o código que é marcado com a sintaxe do Razor e, em seguida, envia a página para o navegador.</span><span class="sxs-lookup"><span data-stu-id="0d03c-217">The server recognizes these extensions, runs the code that's marked with Razor syntax, and then sends the page to the browser.</span></span>

### <a name="where-does-aspnet-fit-in"></a><span data-ttu-id="0d03c-218">Onde entra a ASP.NET?</span><span class="sxs-lookup"><span data-stu-id="0d03c-218">Where does ASP.NET fit in?</span></span>

<span data-ttu-id="0d03c-219">A sintaxe do Razor baseia-se em uma tecnologia da Microsoft chamada de ASP.NET, que por sua vez, baseia-se no Microsoft .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="0d03c-219">Razor syntax is based on a technology from Microsoft called ASP.NET, which in turn is based on the Microsoft .NET Framework.</span></span> <span data-ttu-id="0d03c-220">O.NET Framework é uma estrutura de programação grande e abrangente da Microsoft para o desenvolvimento de praticamente qualquer tipo de aplicativo de computador.</span><span class="sxs-lookup"><span data-stu-id="0d03c-220">The.NET Framework is a big, comprehensive programming framework from Microsoft for developing virtually any type of computer application.</span></span> <span data-ttu-id="0d03c-221">O ASP.NET é parte do .NET Framework que é projetado especificamente para a criação de aplicativos web.</span><span class="sxs-lookup"><span data-stu-id="0d03c-221">ASP.NET is the part of the .NET Framework that's specifically designed for creating web applications.</span></span> <span data-ttu-id="0d03c-222">Os desenvolvedores usaram ASP.NET para criar muitos sites maiores e mais alto tráfego no mundo.</span><span class="sxs-lookup"><span data-stu-id="0d03c-222">Developers have used ASP.NET to create many of the largest and highest-traffic websites in the world.</span></span> <span data-ttu-id="0d03c-223">(Qualquer momento você ver a extensão de nome de arquivo *. aspx* como parte da URL em um site, você saberá que o site foi escrito usando o ASP.NET.)</span><span class="sxs-lookup"><span data-stu-id="0d03c-223">(Any time you see the file-name extension *.aspx* as part of the URL in a site, you'll know that the site was written using ASP.NET.)</span></span>

<span data-ttu-id="0d03c-224">A sintaxe do Razor fornece todo o poder do ASP.NET, mas usando uma sintaxe simplificada, que é mais fácil saber se você for iniciante e que torna mais produtivo, se você é um especialista.</span><span class="sxs-lookup"><span data-stu-id="0d03c-224">The Razor syntax gives you all the power of ASP.NET, but using a simplified syntax that's easier to learn if you're a beginner and that makes you more productive if you're an expert.</span></span> <span data-ttu-id="0d03c-225">Embora essa sintaxe é simple de usar, sua relação família com ASP.NET e o .NET Framework significa como seus sites se tornam mais sofisticados, você tem a capacidade do maior frameworks disponíveis para você.</span><span class="sxs-lookup"><span data-stu-id="0d03c-225">Even though this syntax is simple to use, its family relationship to ASP.NET and the .NET Framework means that as your websites become more sophisticated, you have the power of the larger frameworks available to you.</span></span>

![Razor-Img8](introducing-razor-syntax-c/_static/image8.jpg)

> [!TIP] 
> 
> <span data-ttu-id="0d03c-227">**Classes e instâncias**</span><span class="sxs-lookup"><span data-stu-id="0d03c-227">**Classes and Instances**</span></span>
> 
> <span data-ttu-id="0d03c-228">Código de servidor ASP.NET usa objetos, que por sua vez são criados em conceito de classes.</span><span class="sxs-lookup"><span data-stu-id="0d03c-228">ASP.NET server code uses objects, which are in turn built on the idea of classes.</span></span> <span data-ttu-id="0d03c-229">A classe é a definição ou modelo para um objeto.</span><span class="sxs-lookup"><span data-stu-id="0d03c-229">The class is the definition or template for an object.</span></span> <span data-ttu-id="0d03c-230">Por exemplo, um aplicativo pode conter um `Customer` classe que define as propriedades e métodos que precisa de qualquer objeto do cliente.</span><span class="sxs-lookup"><span data-stu-id="0d03c-230">For example, an application might contain a `Customer` class that defines the properties and methods that any customer object needs.</span></span>
> 
> <span data-ttu-id="0d03c-231">Quando o aplicativo precisa para trabalhar com as informações do cliente real, ele cria uma instância do (ou *instancia*) um objeto de cliente.</span><span class="sxs-lookup"><span data-stu-id="0d03c-231">When the application needs to work with actual customer information, it creates an instance of (or *instantiates*) a customer object.</span></span> <span data-ttu-id="0d03c-232">Cada cliente individual é uma instância separada do `Customer` classe.</span><span class="sxs-lookup"><span data-stu-id="0d03c-232">Each individual customer is a separate instance of the `Customer` class.</span></span> <span data-ttu-id="0d03c-233">Cada instância suporta as mesmas propriedades e métodos, mas os valores de propriedade para cada instância são normalmente diferentes, porque cada objeto de cliente é exclusivo.</span><span class="sxs-lookup"><span data-stu-id="0d03c-233">Every instance supports the same properties and methods, but the property values for each instance are typically different, because each customer object is unique.</span></span> <span data-ttu-id="0d03c-234">No objeto de um cliente, o `LastName` propriedade pode ser "Smith"; em outro objeto de cliente, o `LastName` propriedade pode ser "Jones".</span><span class="sxs-lookup"><span data-stu-id="0d03c-234">In one customer object, the `LastName` property might be "Smith"; in another customer object, the `LastName` property might be "Jones."</span></span>
> 
> <span data-ttu-id="0d03c-235">Da mesma forma, qualquer página da web individuais em seu site é um `Page` que é uma instância do objeto de `Page` classe.</span><span class="sxs-lookup"><span data-stu-id="0d03c-235">Similarly, any individual web page in your site is a `Page` object that's an instance of the `Page` class.</span></span> <span data-ttu-id="0d03c-236">Um botão na página é uma `Button` que é uma instância do objeto o `Button` classe e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="0d03c-236">A button on the page is a `Button` object that is an instance of the `Button` class, and so on.</span></span> <span data-ttu-id="0d03c-237">Cada instância tem características próprias, mas todos eles são com base no que é especificado na definição de classe do objeto.</span><span class="sxs-lookup"><span data-stu-id="0d03c-237">Each instance has its own characteristics, but they all are based on what's specified in the object's class definition.</span></span>


## <a name="basic-syntax"></a><span data-ttu-id="0d03c-238">Sintaxe básica</span><span class="sxs-lookup"><span data-stu-id="0d03c-238">Basic Syntax</span></span>

<span data-ttu-id="0d03c-239">Anteriormente, você viu um exemplo básico de como criar uma página da Web do ASP.NET e como você pode adicionar o código do servidor para marcação HTML.</span><span class="sxs-lookup"><span data-stu-id="0d03c-239">Earlier you saw a basic example of how to create an ASP.NET Web Pages page, and how you can add server code to HTML markup.</span></span> <span data-ttu-id="0d03c-240">Aqui, você aprenderá os conceitos básicos de escrever código de servidor ASP.NET usando a sintaxe do Razor &#8212; ou seja, as regras linguagem de programação.</span><span class="sxs-lookup"><span data-stu-id="0d03c-240">Here you'll learn the basics of writing ASP.NET server code using the Razor syntax &#8212; that is, the programming language rules.</span></span>

<span data-ttu-id="0d03c-241">Se você estiver familiarizado com a programação (especialmente se você tiver usado o C, C++, c#, Visual Basic ou JavaScript), muitas das quais você ler aqui serão familiares.</span><span class="sxs-lookup"><span data-stu-id="0d03c-241">If you're experienced with programming (especially if you've used C, C++, C#, Visual Basic, or JavaScript), much of what you read here will be familiar.</span></span> <span data-ttu-id="0d03c-242">Você provavelmente precisará familiarizar-se apenas com como o código do servidor é adicionado à marcação em *. cshtml* arquivos.</span><span class="sxs-lookup"><span data-stu-id="0d03c-242">You'll probably need to familiarize yourself only with how server code is added to markup in *.cshtml* files.</span></span>

<a id="BM_CombiningTextMarkupAndCode"></a>
### <a name="combining-text-markup-and-code-in-code-blocks"></a><span data-ttu-id="0d03c-243">A combinação de texto, marcação e código em blocos de código</span><span class="sxs-lookup"><span data-stu-id="0d03c-243">Combining Text, Markup, and Code in Code Blocks</span></span>

<span data-ttu-id="0d03c-244">Em blocos de código do servidor, você geralmente deseja saída texto ou marcação (ou ambos) para a página.</span><span class="sxs-lookup"><span data-stu-id="0d03c-244">In server code blocks, you often want to output text or markup (or both) to the page.</span></span> <span data-ttu-id="0d03c-245">Se um bloco de código do servidor contém texto que não é código e que em vez disso, deve ser renderizado como é, o ASP.NET precisa ser capaz de distinguir o texto do código.</span><span class="sxs-lookup"><span data-stu-id="0d03c-245">If a server code block contains text that's not code and that instead should be rendered as is, ASP.NET needs to be able to distinguish that text from code.</span></span> <span data-ttu-id="0d03c-246">Há várias maneiras de fazer isso.</span><span class="sxs-lookup"><span data-stu-id="0d03c-246">There are several ways to do this.</span></span>

- <span data-ttu-id="0d03c-247">Coloque o texto em um elemento HTML como `<p></p>` ou `<em></em>`:</span><span class="sxs-lookup"><span data-stu-id="0d03c-247">Enclose the text in an HTML element like `<p></p>` or `<em></em>`:</span></span>   

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample12.cshtml)]

    <span data-ttu-id="0d03c-248">O elemento HTML pode incluir texto, elementos HTML adicionais e expressões de código do servidor.</span><span class="sxs-lookup"><span data-stu-id="0d03c-248">The HTML element can include text, additional HTML elements, and server-code expressions.</span></span> <span data-ttu-id="0d03c-249">Quando o ASP.NET vê a marca HTML de abertura (por exemplo, `<p>`), ele processa tudo, incluindo o elemento e seu conteúdo como para o navegador, resolvendo expressões de código do servidor quando ele passa.</span><span class="sxs-lookup"><span data-stu-id="0d03c-249">When ASP.NET sees the opening HTML tag (for example, `<p>`), it renders everything including the element and its content as is to the browser, resolving server-code expressions as it goes.</span></span>
- <span data-ttu-id="0d03c-250">Use o `@:` operador ou a `<text>` elemento.</span><span class="sxs-lookup"><span data-stu-id="0d03c-250">Use the `@:` operator or the `<text>` element.</span></span> <span data-ttu-id="0d03c-251">O `@:` gera uma única linha de conteúdo que contém o texto sem formatação ou marcas HTML não correspondentes; o `<text>` elemento abrange várias linhas de saída.</span><span class="sxs-lookup"><span data-stu-id="0d03c-251">The `@:` outputs a single line of content containing plain text or unmatched HTML tags; the `<text>` element encloses multiple lines to output.</span></span> <span data-ttu-id="0d03c-252">Essas opções são úteis quando você não deseja renderizar um elemento HTML como parte da saída.</span><span class="sxs-lookup"><span data-stu-id="0d03c-252">These options are useful when you don't want to render an HTML element as part of the output.</span></span>  

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample13.cshtml)]

    <span data-ttu-id="0d03c-253">Se você deseja produzir várias linhas de texto ou marcas HTML não correspondentes, você pode preceder cada linha com `@:`, ou você pode colocar a linha em uma `<text>` elemento.</span><span class="sxs-lookup"><span data-stu-id="0d03c-253">If you want to output multiple lines of text or unmatched HTML tags, you can precede each line with `@:`, or you can enclose the line in a `<text>` element.</span></span> <span data-ttu-id="0d03c-254">Como o `@:` operador,`<text>` marcas são usadas pelo ASP.NET para identificar o conteúdo de texto e nunca são renderizadas na saída da página.</span><span class="sxs-lookup"><span data-stu-id="0d03c-254">Like the `@:` operator,`<text>` tags are used by ASP.NET to identify text content and are never rendered in the page output.</span></span>

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample14.cshtml)]

    <span data-ttu-id="0d03c-255">O primeiro exemplo repete o exemplo anterior, mas usa um único par de `<text>` marcas para incluir o texto para processar.</span><span class="sxs-lookup"><span data-stu-id="0d03c-255">The first example repeats the previous example but uses a single pair of `<text>` tags to enclose the text to render.</span></span> <span data-ttu-id="0d03c-256">No segundo exemplo, o `<text>` e `</text>` marcas Coloque três linhas, que têm algum texto contido e marcas HTML não correspondentes (`<br />`), junto com o código do servidor e as marcas HTML correspondentes.</span><span class="sxs-lookup"><span data-stu-id="0d03c-256">In the second example, the `<text>` and `</text>` tags enclose three lines, all of which have some uncontained text and unmatched HTML tags (`<br />`), along with server code and matched HTML tags.</span></span> <span data-ttu-id="0d03c-257">Novamente, você também pode preceder cada linha individualmente com a `@:` operador; qualquer funciona de maneira.</span><span class="sxs-lookup"><span data-stu-id="0d03c-257">Again, you could also precede each line individually with the `@:` operator; either way works.</span></span>

    > [!NOTE]
    > <span data-ttu-id="0d03c-258">Quando você saída texto conforme mostrado nesta seção &#8212; usando um elemento HTML, o `@:` operador, ou o `<text>` elemento &#8212; ASP.NET não codificação HTML a saída.</span><span class="sxs-lookup"><span data-stu-id="0d03c-258">When you output text as shown in this section &#8212; using an HTML element, the `@:` operator, or the `<text>` element &#8212; ASP.NET doesn't HTML-encode the output.</span></span> <span data-ttu-id="0d03c-259">(Como observado anteriormente, o ASP.NET codificar a saída de expressões de código do servidor e blocos de código de servidor que são precedidos por `@`, exceto nos casos especiais observados nesta seção.)</span><span class="sxs-lookup"><span data-stu-id="0d03c-259">(As noted earlier, ASP.NET does encode the output of server code expressions and server code blocks that are preceded by `@`, except in the special cases noted in this section.)</span></span>

### <a name="whitespace"></a><span data-ttu-id="0d03c-260">Whitespace</span><span class="sxs-lookup"><span data-stu-id="0d03c-260">Whitespace</span></span>

<span data-ttu-id="0d03c-261">A instrução não afetam os espaços adicionais em uma instrução (e fora de uma literal de cadeia de caracteres):</span><span class="sxs-lookup"><span data-stu-id="0d03c-261">Extra spaces in a statement (and outside of a string literal) don't affect the statement:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample15.cshtml)]

<span data-ttu-id="0d03c-262">Uma quebra de linha em uma instrução não tem nenhum efeito na instrução, e você pode encapsular instruções para facilitar a leitura.</span><span class="sxs-lookup"><span data-stu-id="0d03c-262">A line break in a statement has no effect on the statement, and you can wrap statements for readability.</span></span> <span data-ttu-id="0d03c-263">As instruções a seguir são os mesmos:</span><span class="sxs-lookup"><span data-stu-id="0d03c-263">The following statements are the same:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample16.cshtml)]

<span data-ttu-id="0d03c-264">No entanto, você não pode encapsular uma linha no meio de uma cadeia de caracteres literal.</span><span class="sxs-lookup"><span data-stu-id="0d03c-264">However, you can't wrap a line in the middle of a string literal.</span></span> <span data-ttu-id="0d03c-265">O exemplo a seguir não funciona:</span><span class="sxs-lookup"><span data-stu-id="0d03c-265">The following example doesn't work:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample17.cshtml)]

<span data-ttu-id="0d03c-266">Para combinar uma cadeia de caracteres longa é quebrada em várias linhas com o código acima, há duas opções.</span><span class="sxs-lookup"><span data-stu-id="0d03c-266">To combine a long string that wraps to multiple lines like the above code, there are two options.</span></span> <span data-ttu-id="0d03c-267">Você pode usar o operador de concatenação (`+`), que você verá neste artigo.</span><span class="sxs-lookup"><span data-stu-id="0d03c-267">You can use the concatenation operator (`+`), which you'll see later in this article.</span></span> <span data-ttu-id="0d03c-268">Você também pode usar o `@` caractere para criar uma cadeia de caracteres textual literal, como você viu neste artigo.</span><span class="sxs-lookup"><span data-stu-id="0d03c-268">You can also use the `@` character to create a verbatim string literal, as you saw earlier in this article.</span></span> <span data-ttu-id="0d03c-269">Você pode dividir a literais de cadeia de caracteres textuais em linhas:</span><span class="sxs-lookup"><span data-stu-id="0d03c-269">You can break verbatim string literals across lines:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample18.cshtml)]

### <a name="code-and-markup-comments"></a><span data-ttu-id="0d03c-270">Código (e a marcação) comentários</span><span class="sxs-lookup"><span data-stu-id="0d03c-270">Code (and Markup) Comments</span></span>

<span data-ttu-id="0d03c-271">Comentários permitem que você deixe anotações para você ou outras pessoas.</span><span class="sxs-lookup"><span data-stu-id="0d03c-271">Comments let you leave notes for yourself or others.</span></span> <span data-ttu-id="0d03c-272">Eles também permitem que você desabilite (*comentar*) uma seção de código ou marcação que você não deseja executar, mas deseja manter em sua página por enquanto.</span><span class="sxs-lookup"><span data-stu-id="0d03c-272">They also allow you to disable (*comment out*) a section of code or markup that you don't want to run but want to keep in your page for the time being.</span></span>

<span data-ttu-id="0d03c-273">Há diferentes comentando sintaxe código Razor para marcação HTML.</span><span class="sxs-lookup"><span data-stu-id="0d03c-273">There's different commenting syntax for Razor code and for HTML markup.</span></span> <span data-ttu-id="0d03c-274">Assim como acontece com todo o código Razor, Razor comentários são processados (e, em seguida, removidos) no servidor antes da página é enviada para o navegador.</span><span class="sxs-lookup"><span data-stu-id="0d03c-274">As with all Razor code, Razor comments are processed (and then removed) on the server before the page is sent to the browser.</span></span> <span data-ttu-id="0d03c-275">Portanto, a sintaxe de comentário do Razor permite que você coloque comentários no código (ou até mesmo para a marcação) que você pode ver quando você editar o arquivo, mas que os usuários não veem, mesmo na fonte de página.</span><span class="sxs-lookup"><span data-stu-id="0d03c-275">Therefore, the Razor commenting syntax lets you put comments into the code (or even into the markup) that you can see when you edit the file, but that users don't see, even in the page source.</span></span>

<span data-ttu-id="0d03c-276">Para comentários de ASP.NET Razor, iniciar o comentário com `@*` e terminar com `*@`.</span><span class="sxs-lookup"><span data-stu-id="0d03c-276">For ASP.NET Razor comments, you start the comment with `@*` and end it with `*@`.</span></span> <span data-ttu-id="0d03c-277">O comentário pode estar em uma ou várias linhas:</span><span class="sxs-lookup"><span data-stu-id="0d03c-277">The comment can be on one line or multiple lines:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample19.cshtml)]

<span data-ttu-id="0d03c-278">Aqui está um comentário dentro de um bloco de código:</span><span class="sxs-lookup"><span data-stu-id="0d03c-278">Here is a comment within a code block:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample20.cshtml)]

<span data-ttu-id="0d03c-279">Aqui está o mesmo bloco de código, com a linha de código comentado para que ele não será executado:</span><span class="sxs-lookup"><span data-stu-id="0d03c-279">Here is the same block of code, with the line of code commented out so that it won't run:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample21.cshtml)]

<span data-ttu-id="0d03c-280">Dentro de um bloco de código, como uma alternativa ao uso da sintaxe de comentário do Razor, você pode usar a sintaxe de comentário da linguagem de programação que você está usando, como c#:</span><span class="sxs-lookup"><span data-stu-id="0d03c-280">Inside a code block, as an alternative to using Razor comment syntax, you can use the commenting syntax of the programming language you're using, such as C#:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample22.cshtml)]

<span data-ttu-id="0d03c-281">No c#, comentários de linha única são precedidos pelo `//` caracteres e comentários de várias linhas que começam com `/*` e terminar com `*/`.</span><span class="sxs-lookup"><span data-stu-id="0d03c-281">In C#, single-line comments are preceded by the `//` characters, and multi-line comments begin with `/*` and end with `*/`.</span></span> <span data-ttu-id="0d03c-282">(Assim como acontece com comentários Razor, c# comentários não são processados no navegador.)</span><span class="sxs-lookup"><span data-stu-id="0d03c-282">(As with Razor comments, C# comments are not rendered to the browser.)</span></span>

<span data-ttu-id="0d03c-283">Para marcação, como você deve saber, você pode criar um comentário HTML:</span><span class="sxs-lookup"><span data-stu-id="0d03c-283">For markup, as you probably know, you can create an HTML comment:</span></span>

[!code-xml[Main](introducing-razor-syntax-c/samples/sample23.xml)]

<span data-ttu-id="0d03c-284">Comentários HTML iniciar com `<!--` caracteres e terminar com `-->`.</span><span class="sxs-lookup"><span data-stu-id="0d03c-284">HTML comments start with `<!--` characters and end with `-->`.</span></span> <span data-ttu-id="0d03c-285">Você pode usar comentários HTML ao redor não apenas texto, mas também qualquer marcação HTML que você talvez queira manter na página, mas não deseja processar.</span><span class="sxs-lookup"><span data-stu-id="0d03c-285">You can use HTML comments to surround not only text, but also any HTML markup that you may want to keep in the page but don't want to render.</span></span> <span data-ttu-id="0d03c-286">Este comentário HTML ocultará todo o conteúdo de marcas e o texto que contêm:</span><span class="sxs-lookup"><span data-stu-id="0d03c-286">This HTML comment will hide the entire content of the tags and the text they contain:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample24.html)]

<span data-ttu-id="0d03c-287">Ao contrário de comentários do Razor, comentários HTML *são* processado para a página e o usuário pode vê-los ao exibir a origem da página.</span><span class="sxs-lookup"><span data-stu-id="0d03c-287">Unlike Razor comments, HTML comments *are* rendered to the page and the user can see them by viewing the page source.</span></span>

<span data-ttu-id="0d03c-288">Razor tem limitações em blocos aninhados do c#.</span><span class="sxs-lookup"><span data-stu-id="0d03c-288">Razor has limitations on nested blocks of C#.</span></span> <span data-ttu-id="0d03c-289">Para obter mais informações, consulte [chamado c# variáveis e aninhados blocos gerar desfeitos código](http://aspnetwebstack.codeplex.com/workitem/1914)</span><span class="sxs-lookup"><span data-stu-id="0d03c-289">For more information see [Named C# Variables and Nested Blocks Generate Broken Code](http://aspnetwebstack.codeplex.com/workitem/1914)</span></span>

## <a name="variables"></a><span data-ttu-id="0d03c-290">Variáveis</span><span class="sxs-lookup"><span data-stu-id="0d03c-290">Variables</span></span>

<span data-ttu-id="0d03c-291">Uma variável é um objeto nomeado que você usa para armazenar dados.</span><span class="sxs-lookup"><span data-stu-id="0d03c-291">A variable is a named object that you use to store data.</span></span> <span data-ttu-id="0d03c-292">Você pode nomear variáveis, mas o nome deve começar com um caractere alfabético e não pode conter espaço em branco ou caracteres reservados.</span><span class="sxs-lookup"><span data-stu-id="0d03c-292">You can name variables anything, but the name must begin with an alphabetic character and it cannot contain whitespace or reserved characters.</span></span>

### <a name="variables-and-data-types"></a><span data-ttu-id="0d03c-293">Tipos de dados e variáveis</span><span class="sxs-lookup"><span data-stu-id="0d03c-293">Variables and Data Types</span></span>

<span data-ttu-id="0d03c-294">Uma variável pode ter um tipo de dados específico, que indica o tipo de dados é armazenado na variável.</span><span class="sxs-lookup"><span data-stu-id="0d03c-294">A variable can have a specific data type, which indicates what kind of data is stored in the variable.</span></span> <span data-ttu-id="0d03c-295">Você pode ter variáveis de cadeia de caracteres que armazenam valores de cadeia de caracteres (como &quot;Olá, mundo&quot;), variáveis de inteiro que armazenam valores de número inteiro (como 3 ou 79) e variáveis de data que armazenam valores de data em uma variedade de formatos (como 12/4/2012 ou de março de 2009 ).</span><span class="sxs-lookup"><span data-stu-id="0d03c-295">You can have string variables that store string values (like &quot;Hello world&quot;), integer variables that store whole-number values (like 3 or 79), and date variables that store date values in a variety of formats (like 4/12/2012 or March 2009).</span></span> <span data-ttu-id="0d03c-296">E há muitos outros tipos de dados que você pode usar.</span><span class="sxs-lookup"><span data-stu-id="0d03c-296">And there are many other data types you can use.</span></span>

<span data-ttu-id="0d03c-297">No entanto, você geralmente não precisa especificar um tipo para uma variável.</span><span class="sxs-lookup"><span data-stu-id="0d03c-297">However, you generally don't have to specify a type for a variable.</span></span> <span data-ttu-id="0d03c-298">Na maioria das vezes, ASP.NET pode descobrir o tipo com base em como os dados na variável está sendo usados.</span><span class="sxs-lookup"><span data-stu-id="0d03c-298">Most of the time, ASP.NET can figure out the type based on how the data in the variable is being used.</span></span> <span data-ttu-id="0d03c-299">(Ocasionalmente, você deve especificar um tipo, você verá exemplos em que isso é verdadeiro.)</span><span class="sxs-lookup"><span data-stu-id="0d03c-299">(Occasionally you must specify a type; you'll see examples where this is true.)</span></span>

<span data-ttu-id="0d03c-300">Você declara uma variável usando o `var` palavra-chave (se não desejar especificar um tipo) ou usando o nome do tipo:</span><span class="sxs-lookup"><span data-stu-id="0d03c-300">You declare a variable using the `var` keyword (if you don't want to specify a type) or by using the name of the type:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample25.cshtml)]

<span data-ttu-id="0d03c-301">O exemplo a seguir mostra alguns usos típicos de variáveis em uma página da web:</span><span class="sxs-lookup"><span data-stu-id="0d03c-301">The following example shows some typical uses of variables in a web page:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample26.cshtml)]

<span data-ttu-id="0d03c-302">Se você combinar os exemplos anteriores em uma página, você verá exibidas em um navegador:</span><span class="sxs-lookup"><span data-stu-id="0d03c-302">If you combine the previous examples in a page, you see this displayed in a browser:</span></span>

![Razor-Img9](introducing-razor-syntax-c/_static/image9.jpg)

### <a name="converting-and-testing-data-types"></a><span data-ttu-id="0d03c-304">Convertendo e tipos de dados de teste</span><span class="sxs-lookup"><span data-stu-id="0d03c-304">Converting and Testing Data Types</span></span>

<span data-ttu-id="0d03c-305">Embora o ASP.NET geralmente pode determinar automaticamente um tipo de dados, às vezes, não pode ser.</span><span class="sxs-lookup"><span data-stu-id="0d03c-305">Although ASP.NET can usually determine a data type automatically, sometimes it can't.</span></span> <span data-ttu-id="0d03c-306">Portanto, você precisará ajudar ASP.NET executando uma conversão explícita.</span><span class="sxs-lookup"><span data-stu-id="0d03c-306">Therefore, you might need to help ASP.NET out by performing an explicit conversion.</span></span> <span data-ttu-id="0d03c-307">Mesmo se você não precisa converter os tipos de, às vezes é útil testar para ver o tipo de dados você pode estar trabalhando com.</span><span class="sxs-lookup"><span data-stu-id="0d03c-307">Even if you don't have to convert types, sometimes it's helpful to test to see what type of data you might be working with.</span></span>

<span data-ttu-id="0d03c-308">O caso mais comum é que você precisa converter uma cadeia de caracteres para outro tipo, como em uma data ou um número inteiro.</span><span class="sxs-lookup"><span data-stu-id="0d03c-308">The most common case is that you have to convert a string to another type, such as to an integer or date.</span></span> <span data-ttu-id="0d03c-309">O exemplo a seguir mostra um caso comum em que você deve converter uma cadeia de caracteres em um número.</span><span class="sxs-lookup"><span data-stu-id="0d03c-309">The following example shows a typical case where you must convert a string to a number.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample27.cshtml)]

<span data-ttu-id="0d03c-310">Como regra, entrada do usuário é fornecida a você como cadeias de caracteres.</span><span class="sxs-lookup"><span data-stu-id="0d03c-310">As a rule, user input comes to you as strings.</span></span> <span data-ttu-id="0d03c-311">Mesmo que você tenha solicitado aos usuários inserir um número, e mesmo que foi inserido um dígito, quando a entrada do usuário é enviada e lê-lo no código, os dados estão em formato de cadeia de caracteres.</span><span class="sxs-lookup"><span data-stu-id="0d03c-311">Even if you've prompted users to enter a number, and even if they've entered a digit, when user input is submitted and you read it in code, the data is in string format.</span></span> <span data-ttu-id="0d03c-312">Portanto, você deve converter a cadeia de caracteres em um número.</span><span class="sxs-lookup"><span data-stu-id="0d03c-312">Therefore, you must convert the string to a number.</span></span> <span data-ttu-id="0d03c-313">No exemplo, se você tentar executar cálculos nos valores sem convertê-los, o seguinte erro resulta, porque o ASP.NET não é possível adicionar duas cadeias de caracteres:</span><span class="sxs-lookup"><span data-stu-id="0d03c-313">In the example, if you try to perform arithmetic on the values without converting them, the following error results, because ASP.NET cannot add two strings:</span></span>

<span data-ttu-id="0d03c-314">*Não é possível converter implicitamente o tipo 'string' para 'int'.*</span><span class="sxs-lookup"><span data-stu-id="0d03c-314">*Cannot implicitly convert type 'string' to 'int'.*</span></span>

<span data-ttu-id="0d03c-315">Para converter os valores inteiros, você deve chamar o `AsInt` método.</span><span class="sxs-lookup"><span data-stu-id="0d03c-315">To convert the values to integers, you call the `AsInt` method.</span></span> <span data-ttu-id="0d03c-316">Se a conversão for bem-sucedida, você pode adicionar os números.</span><span class="sxs-lookup"><span data-stu-id="0d03c-316">If the conversion is successful, you can then add the numbers.</span></span>

<span data-ttu-id="0d03c-317">A tabela a seguir lista alguns métodos comuns de conversão e teste para variáveis.</span><span class="sxs-lookup"><span data-stu-id="0d03c-317">The following table lists some common conversion and test methods for variables.</span></span>

| <span data-ttu-id="0d03c-318">**Método**</span><span class="sxs-lookup"><span data-stu-id="0d03c-318">**Method**</span></span> | <span data-ttu-id="0d03c-319">**Descrição**</span><span class="sxs-lookup"><span data-stu-id="0d03c-319">**Description**</span></span> | <span data-ttu-id="0d03c-320">**Exemplo**</span><span class="sxs-lookup"><span data-stu-id="0d03c-320">**Example**</span></span> |
| --- | --- | --- |
| `AsInt(), IsInt()` | <span data-ttu-id="0d03c-321">Converte uma cadeia de caracteres que representa um número inteiro (como "593") para um número inteiro.</span><span class="sxs-lookup"><span data-stu-id="0d03c-321">Converts a string that represents a whole number (like "593") to an integer.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample28.cs)] |
| `AsBool(), IsBool()` | <span data-ttu-id="0d03c-322">Converte uma cadeia de caracteres como &quot;true&quot; ou &quot;false&quot; para um tipo Boolean.</span><span class="sxs-lookup"><span data-stu-id="0d03c-322">Converts a string like &quot;true&quot; or &quot;false&quot; to a Boolean type.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample29.cs)] |
| `AsFloat(), IsFloat()` | <span data-ttu-id="0d03c-323">Converte uma cadeia de caracteres que tem um valor decimal como &quot;1.3&quot; ou &quot;7.439&quot; para um número de ponto flutuante.</span><span class="sxs-lookup"><span data-stu-id="0d03c-323">Converts a string that has a decimal value like &quot;1.3&quot; or &quot;7.439&quot; to a floating-point number.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample30.cs)] |
| `AsDecimal(), IsDecimal()` | <span data-ttu-id="0d03c-324">Converte uma cadeia de caracteres que tem um valor decimal como &quot;1.3&quot; ou &quot;7.439&quot; para um número decimal.</span><span class="sxs-lookup"><span data-stu-id="0d03c-324">Converts a string that has a decimal value like &quot;1.3&quot; or &quot;7.439&quot; to a decimal number.</span></span> <span data-ttu-id="0d03c-325">(No ASP.NET, um número decimal é mais preciso do que um número de ponto flutuante.)</span><span class="sxs-lookup"><span data-stu-id="0d03c-325">(In ASP.NET, a decimal number is more precise than a floating-point number.)</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample31.cs)] |
| `AsDateTime(), IsDateTime()` | <span data-ttu-id="0d03c-326">Converte uma cadeia de caracteres que representa um valor de data e hora para o ASP.NET `DateTime` tipo.</span><span class="sxs-lookup"><span data-stu-id="0d03c-326">Converts a string that represents a date and time value to the ASP.NET `DateTime` type.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample32.cs)] |
| `ToString()` | <span data-ttu-id="0d03c-327">Converte qualquer tipo de dados em uma cadeia de caracteres.</span><span class="sxs-lookup"><span data-stu-id="0d03c-327">Converts any other data type to a string.</span></span> | [!code-javascript[Main](introducing-razor-syntax-c/samples/sample33.js)] |

## <a name="operators"></a><span data-ttu-id="0d03c-328">Operadores</span><span class="sxs-lookup"><span data-stu-id="0d03c-328">Operators</span></span>

<span data-ttu-id="0d03c-329">Um operador é uma palavra-chave ou um caractere que diz ao ASP.NET que tipo de comando para executar em uma expressão.</span><span class="sxs-lookup"><span data-stu-id="0d03c-329">An operator is a keyword or character that tells ASP.NET what kind of command to perform in an expression.</span></span> <span data-ttu-id="0d03c-330">A linguagem c# (e a sintaxe do Razor que se baseia) dá suporte a muitos operadores, mas você precisa apenas reconhecer alguns para começar.</span><span class="sxs-lookup"><span data-stu-id="0d03c-330">The C# language (and the Razor syntax that's based on it) supports many operators, but you only need to recognize a few to get started.</span></span> <span data-ttu-id="0d03c-331">A tabela a seguir resume os operadores mais comuns.</span><span class="sxs-lookup"><span data-stu-id="0d03c-331">The following table summarizes the most common operators.</span></span>

| <span data-ttu-id="0d03c-332">**Operador**</span><span class="sxs-lookup"><span data-stu-id="0d03c-332">**Operator**</span></span> | <span data-ttu-id="0d03c-333">**Descrição**</span><span class="sxs-lookup"><span data-stu-id="0d03c-333">**Description**</span></span> | <span data-ttu-id="0d03c-334">**Exemplos**</span><span class="sxs-lookup"><span data-stu-id="0d03c-334">**Examples**</span></span> |
| --- | --- | --- |
| <span data-ttu-id="0d03c-335">`+` `-` `*` `/`</span><span class="sxs-lookup"><span data-stu-id="0d03c-335">`+` `-` `*` `/`</span></span> | <span data-ttu-id="0d03c-336">Operadores matemáticos usados em expressões numéricas.</span><span class="sxs-lookup"><span data-stu-id="0d03c-336">Math operators used in numerical expressions.</span></span> | [!code-css[Main](introducing-razor-syntax-c/samples/sample34.css)] |
| `=` | <span data-ttu-id="0d03c-337">Atribuição.</span><span class="sxs-lookup"><span data-stu-id="0d03c-337">Assignment.</span></span> <span data-ttu-id="0d03c-338">Atribui o valor à direita de uma instrução para o objeto no lado esquerdo.</span><span class="sxs-lookup"><span data-stu-id="0d03c-338">Assigns the value on the right side of a statement to the object on the left side.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample35.cs)] |
| `==` | <span data-ttu-id="0d03c-339">Igualdade.</span><span class="sxs-lookup"><span data-stu-id="0d03c-339">Equality.</span></span> <span data-ttu-id="0d03c-340">Retorna `true` se os valores são iguais.</span><span class="sxs-lookup"><span data-stu-id="0d03c-340">Returns `true` if the values are equal.</span></span> <span data-ttu-id="0d03c-341">(Observe a diferença entre o `=` operador e o `==` operador.)</span><span class="sxs-lookup"><span data-stu-id="0d03c-341">(Notice the distinction between the `=` operator and the `==` operator.)</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample36.cs)] |
| `!=` | <span data-ttu-id="0d03c-342">Desigualdade.</span><span class="sxs-lookup"><span data-stu-id="0d03c-342">Inequality.</span></span> <span data-ttu-id="0d03c-343">Retorna `true` se os valores não forem iguais.</span><span class="sxs-lookup"><span data-stu-id="0d03c-343">Returns `true` if the values are not equal.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample37.cs)] |
| `< > <= >=` | <span data-ttu-id="0d03c-344">Menos-que, maior-que less-than-or-equal e maior-than-or-equal.</span><span class="sxs-lookup"><span data-stu-id="0d03c-344">Less-than, greater-than, less-than-or-equal, and greater-than-or-equal.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample38.cs)] |
| `+` | <span data-ttu-id="0d03c-345">Concatenação, que é usada para unir cadeias de caracteres.</span><span class="sxs-lookup"><span data-stu-id="0d03c-345">Concatenation, which is used to join strings.</span></span> <span data-ttu-id="0d03c-346">ASP.NET sabe a diferença entre esse operador e o operador de adição com base no tipo de dados da expressão.</span><span class="sxs-lookup"><span data-stu-id="0d03c-346">ASP.NET knows the difference between this operator and the addition operator based on the data type of the expression.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample39.cs)] |
| <span data-ttu-id="0d03c-347">`+=` `-=`</span><span class="sxs-lookup"><span data-stu-id="0d03c-347">`+=` `-=`</span></span> | <span data-ttu-id="0d03c-348">Os operadores de incremento e de decremento, adicionam e subtrair 1 (respectivamente) de uma variável.</span><span class="sxs-lookup"><span data-stu-id="0d03c-348">The increment and decrement operators, which add and subtract 1 (respectively) from a variable.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample40.cs)] |
| `.` | <span data-ttu-id="0d03c-349">Ponto.</span><span class="sxs-lookup"><span data-stu-id="0d03c-349">Dot.</span></span> <span data-ttu-id="0d03c-350">Usado para distinguir os objetos e suas propriedades e métodos.</span><span class="sxs-lookup"><span data-stu-id="0d03c-350">Used to distinguish objects and their properties and methods.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample41.cs)] |
| `()` | <span data-ttu-id="0d03c-351">Parênteses.</span><span class="sxs-lookup"><span data-stu-id="0d03c-351">Parentheses.</span></span> <span data-ttu-id="0d03c-352">Usado para agrupar expressões e passar parâmetros para métodos.</span><span class="sxs-lookup"><span data-stu-id="0d03c-352">Used to group expressions and to pass parameters to methods.</span></span> | [!code-javascript[Main](introducing-razor-syntax-c/samples/sample42.js)] |
| `[]` | <span data-ttu-id="0d03c-353">Colchetes.</span><span class="sxs-lookup"><span data-stu-id="0d03c-353">Brackets.</span></span> <span data-ttu-id="0d03c-354">Usado para acessar valores em matrizes ou coleções.</span><span class="sxs-lookup"><span data-stu-id="0d03c-354">Used for accessing values in arrays or collections.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample43.cs)] |
| `!` | <span data-ttu-id="0d03c-355">Não.</span><span class="sxs-lookup"><span data-stu-id="0d03c-355">Not.</span></span> <span data-ttu-id="0d03c-356">Inverte uma `true` valor `false` e vice-versa.</span><span class="sxs-lookup"><span data-stu-id="0d03c-356">Reverses a `true` value to `false` and vice versa.</span></span> <span data-ttu-id="0d03c-357">Normalmente usado como uma forma abreviada para testar o `false` (ou seja, para não `true`).</span><span class="sxs-lookup"><span data-stu-id="0d03c-357">Typically used as a shorthand way to test for `false` (that is, for not `true`).</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample44.cs)] |
| <span data-ttu-id="0d03c-358">`&&` <code>&#124;&#124;</code></span><span class="sxs-lookup"><span data-stu-id="0d03c-358">`&&` <code>&#124;&#124;</code></span></span> | <span data-ttu-id="0d03c-359">AND lógico e, que são usados para vincular condições ou juntos.</span><span class="sxs-lookup"><span data-stu-id="0d03c-359">Logical AND and OR, which are used to link conditions together.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample45.cs)] |

<a id="ID_WorkingWithFileAndFolderPaths"></a>
## <a name="working-with-file-and-folder-paths-in-code"></a><span data-ttu-id="0d03c-360">Trabalhar com caminhos de arquivo e pasta no código</span><span class="sxs-lookup"><span data-stu-id="0d03c-360">Working with File and Folder Paths in Code</span></span>

<span data-ttu-id="0d03c-361">Geralmente, você trabalhará com os caminhos de arquivo e pasta no seu código.</span><span class="sxs-lookup"><span data-stu-id="0d03c-361">You'll often work with file and folder paths in your code.</span></span> <span data-ttu-id="0d03c-362">Aqui está um exemplo da estrutura de pasta física para um site, como pode aparecer no computador de desenvolvimento:</span><span class="sxs-lookup"><span data-stu-id="0d03c-362">Here is an example of physical folder structure for a website as it might appear on your development computer:</span></span>

`C:\WebSites\MyWebSite default.cshtml datafile.txt \images Logo.jpg \styles Styles.css`

<span data-ttu-id="0d03c-363">Aqui estão alguns detalhes essenciais sobre URLs e caminhos:</span><span class="sxs-lookup"><span data-stu-id="0d03c-363">Here are some essential details about URLs and paths:</span></span>

- <span data-ttu-id="0d03c-364">Uma URL começa com a um nome de domínio (`http://www.example.com`) ou um nome de servidor (`http://localhost`, `http://mycomputer`).</span><span class="sxs-lookup"><span data-stu-id="0d03c-364">A URL begins with either a domain name (`http://www.example.com`) or a server name (`http://localhost`, `http://mycomputer`).</span></span>
- <span data-ttu-id="0d03c-365">Uma URL corresponde a um caminho físico em um computador host.</span><span class="sxs-lookup"><span data-stu-id="0d03c-365">A URL corresponds to a physical path on a host computer.</span></span> <span data-ttu-id="0d03c-366">Por exemplo, `http://myserver` deve corresponder à pasta *C:\websites\mywebsite* no servidor.</span><span class="sxs-lookup"><span data-stu-id="0d03c-366">For example, `http://myserver` might correspond to the folder *C:\websites\mywebsite* on the server.</span></span>
- <span data-ttu-id="0d03c-367">Um caminho virtual é abreviado para representar os caminhos de código sem a necessidade de especificar o caminho completo.</span><span class="sxs-lookup"><span data-stu-id="0d03c-367">A virtual path is shorthand to represent paths in code without having to specify the full path.</span></span> <span data-ttu-id="0d03c-368">Ele inclui a parte de uma URL que segue o nome de domínio ou servidor.</span><span class="sxs-lookup"><span data-stu-id="0d03c-368">It includes the portion of a URL that follows the domain or server name.</span></span> <span data-ttu-id="0d03c-369">Quando você usa caminhos virtuais, você pode mover seu código para um domínio diferente ou servidor sem ter que atualizar os caminhos.</span><span class="sxs-lookup"><span data-stu-id="0d03c-369">When you use virtual paths, you can move your code to a different domain or server without having to update the paths.</span></span>

<span data-ttu-id="0d03c-370">Aqui está um exemplo para ajudá-lo a entender as diferenças:</span><span class="sxs-lookup"><span data-stu-id="0d03c-370">Here's an example to help you understand the differences:</span></span>

| <span data-ttu-id="0d03c-371">URL completa</span><span class="sxs-lookup"><span data-stu-id="0d03c-371">Complete URL</span></span> | `http://mycompanyserver/humanresources/CompanyPolicy.htm` |
| --- | --- |
| <span data-ttu-id="0d03c-372">Nome do servidor</span><span class="sxs-lookup"><span data-stu-id="0d03c-372">Server name</span></span> | <span data-ttu-id="0d03c-373">*mycompanyserver*</span><span class="sxs-lookup"><span data-stu-id="0d03c-373">*mycompanyserver*</span></span> |
| <span data-ttu-id="0d03c-374">Caminho virtual</span><span class="sxs-lookup"><span data-stu-id="0d03c-374">Virtual path</span></span> | <span data-ttu-id="0d03c-375">*/humanresources/CompanyPolicy.htm*</span><span class="sxs-lookup"><span data-stu-id="0d03c-375">*/humanresources/CompanyPolicy.htm*</span></span> |
| <span data-ttu-id="0d03c-376">Caminho físico</span><span class="sxs-lookup"><span data-stu-id="0d03c-376">Physical path</span></span> | <span data-ttu-id="0d03c-377">*C:\mywebsites\humanresources\CompanyPolicy.htm*</span><span class="sxs-lookup"><span data-stu-id="0d03c-377">*C:\mywebsites\humanresources\CompanyPolicy.htm*</span></span> |

<span data-ttu-id="0d03c-378">É a raiz virtual /, assim como a raiz da unidade c: unidade é \.</span><span class="sxs-lookup"><span data-stu-id="0d03c-378">The virtual root is /, just like the root of your C: drive is \.</span></span> <span data-ttu-id="0d03c-379">(Os caminhos de pasta virtual sempre usam barras "/"). O caminho virtual de uma pasta não precisa ter o mesmo nome como a pasta física; é um alias.</span><span class="sxs-lookup"><span data-stu-id="0d03c-379">(Virtual folder paths always use forward slashes.) The virtual path of a folder doesn't have to have the same name as the physical folder; it can be an alias.</span></span> <span data-ttu-id="0d03c-380">(Em servidores de produção, o caminho virtual raramente corresponde a um caminho físico exato.)</span><span class="sxs-lookup"><span data-stu-id="0d03c-380">(On production servers, the virtual path rarely matches an exact physical path.)</span></span>

<span data-ttu-id="0d03c-381">Quando você trabalha com arquivos e pastas no código, às vezes você precisa referenciar o caminho físico e, às vezes, um caminho virtual, dependendo de quais objetos você está trabalhando.</span><span class="sxs-lookup"><span data-stu-id="0d03c-381">When you work with files and folders in code, sometimes you need to reference the physical path and sometimes a virtual path, depending on what objects you're working with.</span></span> <span data-ttu-id="0d03c-382">ASP.NET oferece essas ferramentas para trabalhar com caminhos de arquivo e pasta no código: o `Server.MapPath` método e o `~` operador e `Href` método.</span><span class="sxs-lookup"><span data-stu-id="0d03c-382">ASP.NET gives you these tools for working with file and folder paths in code: the `Server.MapPath` method, and the `~` operator and `Href` method.</span></span>

### <a name="converting-virtual-to-physical-paths-the-servermappath-method"></a><span data-ttu-id="0d03c-383">Conversão de caminhos virtuais físicos: o método MapPath</span><span class="sxs-lookup"><span data-stu-id="0d03c-383">Converting virtual to physical paths: the Server.MapPath method</span></span>

<span data-ttu-id="0d03c-384">O `Server.MapPath` método converte um caminho virtual (como */default.cshtml*) em um caminho físico absoluto (como *C:\WebSites\MyWebSiteFolder\default.cshtml*).</span><span class="sxs-lookup"><span data-stu-id="0d03c-384">The `Server.MapPath` method converts a virtual path (like */default.cshtml*) to an absolute physical path (like *C:\WebSites\MyWebSiteFolder\default.cshtml*).</span></span> <span data-ttu-id="0d03c-385">Você usar esse método sempre que você precisa de um caminho físico completo.</span><span class="sxs-lookup"><span data-stu-id="0d03c-385">You use this method any time you need a complete physical path.</span></span> <span data-ttu-id="0d03c-386">Um exemplo típico é ao ler ou gravar um arquivo de texto ou imagem no servidor web.</span><span class="sxs-lookup"><span data-stu-id="0d03c-386">A typical example is when you're reading or writing a text file or image file on the web server.</span></span>

<span data-ttu-id="0d03c-387">Normalmente você não souber o caminho físico absoluto do seu site no servidor de hospedagem do site, para que esse método pode converter o caminho você souber — o caminho virtual — para o caminho correspondente no servidor para você.</span><span class="sxs-lookup"><span data-stu-id="0d03c-387">You typically don't know the absolute physical path of your site on a hosting site's server, so this method can convert the path you do know — the virtual path — to the corresponding path on the server for you.</span></span> <span data-ttu-id="0d03c-388">Passe o caminho virtual para um arquivo ou pasta para o método e retorna o caminho físico:</span><span class="sxs-lookup"><span data-stu-id="0d03c-388">You pass the virtual path to a file or folder to the method, and it returns the physical path:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample46.cshtml)]

### <a name="referencing-the-virtual-root-the--operator-and-href-method"></a><span data-ttu-id="0d03c-389">Fazendo referência a raiz virtual: o ~ operador e o método de Href</span><span class="sxs-lookup"><span data-stu-id="0d03c-389">Referencing the virtual root: the ~ operator and Href method</span></span>

<span data-ttu-id="0d03c-390">Em um *. cshtml* ou *. vbhtml* arquivo, você pode referenciar o caminho raiz virtual usando o `~` operador.</span><span class="sxs-lookup"><span data-stu-id="0d03c-390">In a *.cshtml* or *.vbhtml* file, you can reference the virtual root path using the `~` operator.</span></span> <span data-ttu-id="0d03c-391">Isso é muito útil porque você pode se mover páginas em um site, e os links para outras páginas contêm não será interrompidos.</span><span class="sxs-lookup"><span data-stu-id="0d03c-391">This is very handy because you can move pages around in a site, and any links they contain to other pages won't be broken.</span></span> <span data-ttu-id="0d03c-392">Também é útil no caso de você nunca mover seu site para um local diferente.</span><span class="sxs-lookup"><span data-stu-id="0d03c-392">It's also handy in case you ever move your website to a different location.</span></span> <span data-ttu-id="0d03c-393">Estes são alguns exemplos:</span><span class="sxs-lookup"><span data-stu-id="0d03c-393">Here are some examples:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample47.cshtml)]

<span data-ttu-id="0d03c-394">Se o site for `http://myserver/myapp`, aqui está como ASP.NET tratará esses caminhos quando a página é executada:</span><span class="sxs-lookup"><span data-stu-id="0d03c-394">If the website is `http://myserver/myapp`, here's how ASP.NET will treat these paths when the page runs:</span></span>

- <span data-ttu-id="0d03c-395">`myImagesFolder`: `http://myserver/myapp/images`</span><span class="sxs-lookup"><span data-stu-id="0d03c-395">`myImagesFolder`: `http://myserver/myapp/images`</span></span>
- <span data-ttu-id="0d03c-396">`myStyleSheet` : `http://myserver/myapp/styles/Stylesheet.css`</span><span class="sxs-lookup"><span data-stu-id="0d03c-396">`myStyleSheet` : `http://myserver/myapp/styles/Stylesheet.css`</span></span>

<span data-ttu-id="0d03c-397">(Esses caminhos, na verdade, não verá como os valores da variável, mas o ASP.NET tratará os caminhos como se o que é o que eles foram).</span><span class="sxs-lookup"><span data-stu-id="0d03c-397">(You won't actually see these paths as the values of the variable, but ASP.NET will treat the paths as if that's what they were.)</span></span>

<span data-ttu-id="0d03c-398">Você pode usar o `~` operador no código do servidor (como acima) e na marcação, como este:</span><span class="sxs-lookup"><span data-stu-id="0d03c-398">You can use the `~` operator both in server code (as above) and in markup, like this:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample48.html)]

<span data-ttu-id="0d03c-399">Na marcação, você deve usar o `~` operador para criar caminhos para recursos, como arquivos CSS, outras páginas da web e arquivos de imagem.</span><span class="sxs-lookup"><span data-stu-id="0d03c-399">In markup, you use the `~` operator to create paths to resources like image files, other web pages, and CSS files.</span></span> <span data-ttu-id="0d03c-400">Quando a página é executada, o ASP.NET procura por meio da página (código e marcação) e resolve todos os `~` referências ao caminho adequado.</span><span class="sxs-lookup"><span data-stu-id="0d03c-400">When the page runs, ASP.NET looks through the page (both code and markup) and resolves all the `~` references to the appropriate path.</span></span>

## <a name="conditional-logic-and-loops"></a><span data-ttu-id="0d03c-401">Loops e lógica condicional</span><span class="sxs-lookup"><span data-stu-id="0d03c-401">Conditional Logic and Loops</span></span>

<span data-ttu-id="0d03c-402">Código de servidor do ASP.NET permite executar tarefas com base nas condições e escrever código que se repete instruções um número específico de vezes (ou seja, código que executa um loop).</span><span class="sxs-lookup"><span data-stu-id="0d03c-402">ASP.NET server code lets you perform tasks based on conditions and write code that repeats statements a specific number of times (that is, code that runs a loop).</span></span>

### <a name="testing-conditions"></a><span data-ttu-id="0d03c-403">Condições de teste</span><span class="sxs-lookup"><span data-stu-id="0d03c-403">Testing Conditions</span></span>

<span data-ttu-id="0d03c-404">Para testar uma condição simple é usar o `if` instrução, que retorna true ou false com base em um teste que você especificar:</span><span class="sxs-lookup"><span data-stu-id="0d03c-404">To test a simple condition you use the `if` statement, which returns true or false based on a test you specify:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample49.cshtml)]

<span data-ttu-id="0d03c-405">O `if` palavra-chave inicia um bloco.</span><span class="sxs-lookup"><span data-stu-id="0d03c-405">The `if` keyword starts a block.</span></span> <span data-ttu-id="0d03c-406">O teste real (condição) está entre parênteses e retorna true ou false.</span><span class="sxs-lookup"><span data-stu-id="0d03c-406">The actual test (condition) is in parentheses and returns true or false.</span></span> <span data-ttu-id="0d03c-407">As instruções que são executados quando o teste é true são colocadas entre chaves.</span><span class="sxs-lookup"><span data-stu-id="0d03c-407">The statements that run if the test is true are enclosed in braces.</span></span> <span data-ttu-id="0d03c-408">Um `if` instrução pode incluir um `else` bloco que especifica as instruções a serem executadas se a condição for false:</span><span class="sxs-lookup"><span data-stu-id="0d03c-408">An `if` statement can include an `else` block that specifies statements to run if the condition is false:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample50.cshtml)]

<span data-ttu-id="0d03c-409">Você pode adicionar várias condições usando um `else if` bloco:</span><span class="sxs-lookup"><span data-stu-id="0d03c-409">You can add multiple conditions using an `else if` block:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample51.cshtml)]

<span data-ttu-id="0d03c-410">Neste exemplo, se a primeira condição no se bloco não for true, o `else if` condição é verificada.</span><span class="sxs-lookup"><span data-stu-id="0d03c-410">In this example, if the first condition in the if block is not true, the `else if` condition is checked.</span></span> <span data-ttu-id="0d03c-411">Se essa condição for atendida, as instruções de `else if` bloco são executados.</span><span class="sxs-lookup"><span data-stu-id="0d03c-411">If that condition is met, the statements in the `else if` block are executed.</span></span> <span data-ttu-id="0d03c-412">Se nenhuma das condições forem atendidas, as instruções de `else` bloco são executados.</span><span class="sxs-lookup"><span data-stu-id="0d03c-412">If none of the conditions are met, the statements in the `else` block are executed.</span></span> <span data-ttu-id="0d03c-413">Você pode adicionar qualquer número de senão se bloqueia e, em seguida, feche com um `else` bloquear como o &quot;tudo&quot; condição.</span><span class="sxs-lookup"><span data-stu-id="0d03c-413">You can add any number of else if blocks, and then close with an `else` block as the &quot;everything else&quot; condition.</span></span>

<span data-ttu-id="0d03c-414">Para testar um grande número de condições, use um `switch` bloco:</span><span class="sxs-lookup"><span data-stu-id="0d03c-414">To test a large number of conditions, use a `switch` block:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample52.cshtml)]

<span data-ttu-id="0d03c-415">O valor a ser testado está entre parênteses (no exemplo, o `weekday` variável).</span><span class="sxs-lookup"><span data-stu-id="0d03c-415">The value to test is in parentheses (in the example, the `weekday` variable).</span></span> <span data-ttu-id="0d03c-416">Cada teste individual usa um `case` instrução que termina com dois pontos (:).</span><span class="sxs-lookup"><span data-stu-id="0d03c-416">Each individual test uses a `case` statement that ends with a colon (:).</span></span> <span data-ttu-id="0d03c-417">Se o valor de um `case` instrução corresponde ao valor de teste, o código no bloco caso é executado.</span><span class="sxs-lookup"><span data-stu-id="0d03c-417">If the value of a `case` statement matches the test value, the code in that case block is executed.</span></span> <span data-ttu-id="0d03c-418">Feche cada instrução case com um `break` instrução.</span><span class="sxs-lookup"><span data-stu-id="0d03c-418">You close each case statement with a `break` statement.</span></span> <span data-ttu-id="0d03c-419">(Se você se esquecer de incluir quebra em cada `case` bloquear, o código do próximo `case` instrução será executada também.) Um `switch` bloco geralmente tem um `default` instrução como o último caso para um &quot;tudo&quot; opção que é executado se nenhum dos outros casos é true.</span><span class="sxs-lookup"><span data-stu-id="0d03c-419">(If you forget to include break in each `case` block, the code from the next `case` statement will run also.) A `switch` block often has a `default` statement as the last case for an &quot;everything else&quot; option that runs if none of the other cases are true.</span></span>

<span data-ttu-id="0d03c-420">O resultado dos últimos dois blocos condicionais exibido em um navegador:</span><span class="sxs-lookup"><span data-stu-id="0d03c-420">The result of the last two conditional blocks displayed in a browser:</span></span>

![Razor-Img10](introducing-razor-syntax-c/_static/image10.jpg)

### <a name="looping-code"></a><span data-ttu-id="0d03c-422">Código de loop</span><span class="sxs-lookup"><span data-stu-id="0d03c-422">Looping Code</span></span>

<span data-ttu-id="0d03c-423">Muitas vezes, é preciso executar as mesmas instruções repetidamente.</span><span class="sxs-lookup"><span data-stu-id="0d03c-423">You often need to run the same statements repeatedly.</span></span> <span data-ttu-id="0d03c-424">Para fazer isso, um loop.</span><span class="sxs-lookup"><span data-stu-id="0d03c-424">You do this by looping.</span></span> <span data-ttu-id="0d03c-425">Por exemplo, você sempre execute as mesmas instruções para cada item em uma coleção de dados.</span><span class="sxs-lookup"><span data-stu-id="0d03c-425">For example, you often run the same statements for each item in a collection of data.</span></span> <span data-ttu-id="0d03c-426">Se você souber exatamente quantas vezes desejar executar um loop, você pode usar um `for` loop.</span><span class="sxs-lookup"><span data-stu-id="0d03c-426">If you know exactly how many times you want to loop, you can use a `for` loop.</span></span> <span data-ttu-id="0d03c-427">Esse tipo de loop é especialmente útil para de contagem ou contagem regressiva:</span><span class="sxs-lookup"><span data-stu-id="0d03c-427">This kind of loop is especially useful for counting up or counting down:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample53.html)]

<span data-ttu-id="0d03c-428">O loop começa com o `for` palavra-chave, seguido de três instruções entre parênteses, cada um foi encerrado com um ponto e vírgula.</span><span class="sxs-lookup"><span data-stu-id="0d03c-428">The loop begins with the `for` keyword, followed by three statements in parentheses, each terminated with a semicolon.</span></span>

- <span data-ttu-id="0d03c-429">Dentro dos parênteses, a primeira instrução (`var i=10;`) cria um contador e inicializa a 10.</span><span class="sxs-lookup"><span data-stu-id="0d03c-429">Inside the parentheses, the first statement (`var i=10;`) creates a counter and initializes it to 10.</span></span> <span data-ttu-id="0d03c-430">Você não tem o nome do contador `i` &#8212; você pode usar qualquer variável.</span><span class="sxs-lookup"><span data-stu-id="0d03c-430">You don't have to name the counter `i` &#8212; you can use any variable.</span></span> <span data-ttu-id="0d03c-431">Quando o `for` loop é executado, o contador é incrementado automaticamente.</span><span class="sxs-lookup"><span data-stu-id="0d03c-431">When the `for` loop runs, the counter is automatically incremented.</span></span>
- <span data-ttu-id="0d03c-432">A segunda instrução (`i < 21;`) define a condição para a distância em que você deseja contar.</span><span class="sxs-lookup"><span data-stu-id="0d03c-432">The second statement (`i < 21;`) sets the condition for how far you want to count.</span></span> <span data-ttu-id="0d03c-433">Nesse caso, você quiser ir para um máximo de 20 (ou seja, continuar enquanto o contador é menos de 21).</span><span class="sxs-lookup"><span data-stu-id="0d03c-433">In this case, you want it to go to a maximum of 20 (that is, keep going while the counter is less than 21).</span></span>
- <span data-ttu-id="0d03c-434">A terceira instrução (`i++` ) usa um operador de incremento, que só especifica que o contador deve ter 1 adicionado a cada vez que o loop é executado.</span><span class="sxs-lookup"><span data-stu-id="0d03c-434">The third statement (`i++` ) uses an increment operator, which simply specifies that the counter should have 1 added to it each time the loop runs.</span></span>

<span data-ttu-id="0d03c-435">Dentro das chaves é o código que será executado em cada iteração do loop.</span><span class="sxs-lookup"><span data-stu-id="0d03c-435">Inside the braces is the code that will run for each iteration of the loop.</span></span> <span data-ttu-id="0d03c-436">A marcação cria um novo parágrafo (`<p>` elemento) cada vez e adiciona uma linha para a saída, exibindo o valor de `i` (o contador).</span><span class="sxs-lookup"><span data-stu-id="0d03c-436">The markup creates a new paragraph (`<p>` element) each time and adds a line to the output, displaying the value of `i` (the counter).</span></span> <span data-ttu-id="0d03c-437">Quando você executa esta página, o exemplo cria 11 linhas exibindo a saída, com o texto de cada linha que indica o número de item.</span><span class="sxs-lookup"><span data-stu-id="0d03c-437">When you run this page, the example creates 11 lines displaying the output, with the text in each line indicating the item number.</span></span>

![Razor-Img11](introducing-razor-syntax-c/_static/image11.jpg)

<span data-ttu-id="0d03c-439">Se você estiver trabalhando com uma coleção ou matriz, você geralmente usa um `foreach` loop.</span><span class="sxs-lookup"><span data-stu-id="0d03c-439">If you're working with a collection or array, you often use a `foreach` loop.</span></span> <span data-ttu-id="0d03c-440">Uma coleção é um grupo de objetos semelhantes e o `foreach` loop permite a você executa uma tarefa em cada item na coleção.</span><span class="sxs-lookup"><span data-stu-id="0d03c-440">A collection is a group of similar objects, and the `foreach` loop lets you carry out a task on each item in the collection.</span></span> <span data-ttu-id="0d03c-441">Esse tipo de loop é conveniente para coleções, porque Diferentemente de uma `for` loop, você não precisa incrementar o contador ou definir um limite.</span><span class="sxs-lookup"><span data-stu-id="0d03c-441">This type of loop is convenient for collections, because unlike a `for` loop, you don't have to increment the counter or set a limit.</span></span> <span data-ttu-id="0d03c-442">Em vez disso, o `foreach` loop código simplesmente passa a coleção até que ela seja concluída.</span><span class="sxs-lookup"><span data-stu-id="0d03c-442">Instead, the `foreach` loop code simply proceeds through the collection until it's finished.</span></span>

<span data-ttu-id="0d03c-443">Por exemplo, o código a seguir retorna os itens a `Request.ServerVariables` coleção, que é um objeto que contém informações sobre seu servidor web.</span><span class="sxs-lookup"><span data-stu-id="0d03c-443">For example, the following code returns the items in the `Request.ServerVariables` collection, which is an object that contains information about your web server.</span></span> <span data-ttu-id="0d03c-444">Ele usa um `foreac` h loop para exibir o nome de cada item, criando um novo `<li>` elemento em uma lista com marcadores de HTML.</span><span class="sxs-lookup"><span data-stu-id="0d03c-444">It uses a `foreac` h loop to display the name of each item by creating a new `<li>` element in an HTML bulleted list.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample54.html)]

<span data-ttu-id="0d03c-445">O `foreach` palavra-chave é seguido por parênteses, onde você pode declarar uma variável que representa um único item na coleção (no exemplo, `var item`), seguido de `in` palavra-chave, seguido de coleção que você deseja fazer o loop.</span><span class="sxs-lookup"><span data-stu-id="0d03c-445">The `foreach` keyword is followed by parentheses where you declare a variable that represents a single item in the collection (in the example, `var item`), followed by the `in` keyword, followed by the collection you want to loop through.</span></span> <span data-ttu-id="0d03c-446">No corpo do `foreach` loop, você pode acessar o item atual usando a variável que é declarado anteriormente.</span><span class="sxs-lookup"><span data-stu-id="0d03c-446">In the body of the `foreach` loop, you can access the current item using the variable that you declared earlier.</span></span>

![Razor-Img12](introducing-razor-syntax-c/_static/image12.jpg)

<span data-ttu-id="0d03c-448">Para criar um loop mais geral, use o `while` instrução:</span><span class="sxs-lookup"><span data-stu-id="0d03c-448">To create a more general-purpose loop, use the `while` statement:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample55.cshtml)]

<span data-ttu-id="0d03c-449">Um `while` loop começa com o `while` palavra-chave, seguido de parênteses em que você especificar quanto tempo o loop continua (aqui, para desde que `countNum` é menor que 50), em seguida, o bloco para repetir.</span><span class="sxs-lookup"><span data-stu-id="0d03c-449">A `while` loop begins with the `while` keyword, followed by parentheses where you specify how long the loop continues (here, for as long as `countNum` is less than 50), then the block to repeat.</span></span> <span data-ttu-id="0d03c-450">Loops normalmente incrementam (Adicionar a) ou de decremento (subtrair de) uma variável ou o objeto usado para contagem.</span><span class="sxs-lookup"><span data-stu-id="0d03c-450">Loops typically increment (add to) or decrement (subtract from) a variable or object used for counting.</span></span> <span data-ttu-id="0d03c-451">No exemplo, o `+=` operador adiciona 1 à `countNum` cada vez que o loop é executado.</span><span class="sxs-lookup"><span data-stu-id="0d03c-451">In the example, the `+=` operator adds 1 to `countNum` each time the loop runs.</span></span> <span data-ttu-id="0d03c-452">(Para diminuir a uma variável em um loop que conta para baixo, você usaria o operador de decremento `-=`).</span><span class="sxs-lookup"><span data-stu-id="0d03c-452">(To decrement a variable in a loop that counts down, you would use the decrement operator `-=`).</span></span>

## <a name="objects-and-collections"></a><span data-ttu-id="0d03c-453">Objetos e coleções</span><span class="sxs-lookup"><span data-stu-id="0d03c-453">Objects and Collections</span></span>

<span data-ttu-id="0d03c-454">Quase tudo em um site ASP.NET é um objeto, incluindo a própria página da web.</span><span class="sxs-lookup"><span data-stu-id="0d03c-454">Nearly everything in an ASP.NET website is an object, including the web page itself.</span></span> <span data-ttu-id="0d03c-455">Esta seção discute alguns objetos importantes que você trabalhará com frequência em seu código.</span><span class="sxs-lookup"><span data-stu-id="0d03c-455">This section discusses some important objects you'll work with frequently in your code.</span></span>

### <a name="page-objects"></a><span data-ttu-id="0d03c-456">Objetos de página</span><span class="sxs-lookup"><span data-stu-id="0d03c-456">Page Objects</span></span>

<span data-ttu-id="0d03c-457">O objeto mais básico no ASP.NET é a página.</span><span class="sxs-lookup"><span data-stu-id="0d03c-457">The most basic object in ASP.NET is the page.</span></span> <span data-ttu-id="0d03c-458">Você pode acessar as propriedades do objeto page diretamente sem nenhum objeto qualificado.</span><span class="sxs-lookup"><span data-stu-id="0d03c-458">You can access properties of the page object directly without any qualifying object.</span></span> <span data-ttu-id="0d03c-459">O código a seguir obtém o caminho do arquivo da página, usando o `Request` objeto da página:</span><span class="sxs-lookup"><span data-stu-id="0d03c-459">The following code gets the page's file path, using the `Request` object of the page:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample56.cshtml)]

<span data-ttu-id="0d03c-460">Para tornar claro que você está fazendo referência a propriedades e métodos no objeto da página atual, você também pode usar a palavra-chave `this` para representar o objeto de página em seu código.</span><span class="sxs-lookup"><span data-stu-id="0d03c-460">To make it clear that you're referencing properties and methods on the current page object, you can optionally use the keyword `this` to represent the page object in your code.</span></span> <span data-ttu-id="0d03c-461">Aqui está o exemplo de código anterior, com `this` adicionado para representar a página:</span><span class="sxs-lookup"><span data-stu-id="0d03c-461">Here is the previous code example, with `this` added to represent the page:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample57.cshtml)]

<span data-ttu-id="0d03c-462">Você pode usar propriedades do `Page` objeto para obter muitas informações, como:</span><span class="sxs-lookup"><span data-stu-id="0d03c-462">You can use properties of the `Page` object to get a lot of information, such as:</span></span>

- <span data-ttu-id="0d03c-463">`Request`.</span><span class="sxs-lookup"><span data-stu-id="0d03c-463">`Request`.</span></span> <span data-ttu-id="0d03c-464">Como você já viu, esta é uma coleção de informações sobre a solicitação atual, incluindo o tipo de navegador fez a solicitação, a URL da página, a identidade do usuário, etc.</span><span class="sxs-lookup"><span data-stu-id="0d03c-464">As you've already seen, this is a collection of information about the current request, including what type of browser made the request, the URL of the page, the user identity, etc.</span></span>
- <span data-ttu-id="0d03c-465">`Response`.</span><span class="sxs-lookup"><span data-stu-id="0d03c-465">`Response`.</span></span> <span data-ttu-id="0d03c-466">Esta é uma coleção de informações sobre a resposta (página) que será enviada ao navegador quando o código de servidor concluiu a execução.</span><span class="sxs-lookup"><span data-stu-id="0d03c-466">This is a collection of information about the response (page) that will be sent to the browser when the server code has finished running.</span></span> <span data-ttu-id="0d03c-467">Por exemplo, você pode usar essa propriedade para gravar informações na resposta.</span><span class="sxs-lookup"><span data-stu-id="0d03c-467">For example, you can use this property to write information into the response.</span></span> 

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample58.cshtml)]

<a id="ID_CollectionsAndObjects"></a>
### <a name="collection-objects-arrays-and-dictionaries"></a><span data-ttu-id="0d03c-468">Objetos de coleção (matrizes e dicionários)</span><span class="sxs-lookup"><span data-stu-id="0d03c-468">Collection Objects (Arrays and Dictionaries)</span></span>

<span data-ttu-id="0d03c-469">Um *coleção* é um grupo de objetos do mesmo tipo, como uma coleção de `Customer` objetos de banco de dados.</span><span class="sxs-lookup"><span data-stu-id="0d03c-469">A *collection* is a group of objects of the same type, such as a collection of `Customer` objects from a database.</span></span> <span data-ttu-id="0d03c-470">O ASP.NET contém várias coleções internas, como o `Request.Files` coleção.</span><span class="sxs-lookup"><span data-stu-id="0d03c-470">ASP.NET contains many built-in collections, like the `Request.Files` collection.</span></span>

<span data-ttu-id="0d03c-471">Geralmente, você trabalhará com dados em coleções.</span><span class="sxs-lookup"><span data-stu-id="0d03c-471">You'll often work with data in collections.</span></span> <span data-ttu-id="0d03c-472">Dois tipos de coleção comuns a *matriz* e *dicionário*.</span><span class="sxs-lookup"><span data-stu-id="0d03c-472">Two common collection types are the *array* and the *dictionary*.</span></span> <span data-ttu-id="0d03c-473">Uma matriz é útil quando você deseja armazenar uma coleção de itens semelhantes, mas não deseja criar uma variável separada para conter cada item:</span><span class="sxs-lookup"><span data-stu-id="0d03c-473">An array is useful when you want to store a collection of similar items but don't want to create a separate variable to hold each item:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample59.cshtml)]

<span data-ttu-id="0d03c-474">Com matrizes, você declara um tipo de dados específico, como `string`, `int`, ou `DateTime`.</span><span class="sxs-lookup"><span data-stu-id="0d03c-474">With arrays, you declare a specific data type, such as `string`, `int`, or `DateTime`.</span></span> <span data-ttu-id="0d03c-475">Para indicar que a variável pode conter uma matriz, adicione parênteses à declaração (como `string[]` ou `int[]`).</span><span class="sxs-lookup"><span data-stu-id="0d03c-475">To indicate that the variable can contain an array, you add brackets to the declaration (such as `string[]` or `int[]`).</span></span> <span data-ttu-id="0d03c-476">Você pode acessar itens em uma matriz usando sua posição (índice) ou usando o `foreach` instrução.</span><span class="sxs-lookup"><span data-stu-id="0d03c-476">You can access items in an array using their position (index) or by using the `foreach` statement.</span></span> <span data-ttu-id="0d03c-477">Matriz de índices são com base em zero &#8212; Isto é, o primeiro item está na posição 0, o segundo item está na posição 1 e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="0d03c-477">Array indexes are zero-based &#8212; that is, the first item is at position 0, the second item is at position 1, and so on.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample60.cshtml)]

<span data-ttu-id="0d03c-478">Você pode determinar o número de itens em uma matriz obtendo seus `Length` propriedade.</span><span class="sxs-lookup"><span data-stu-id="0d03c-478">You can determine the number of items in an array by getting its `Length` property.</span></span> <span data-ttu-id="0d03c-479">Para obter a posição de um item específico na matriz (para a matriz de pesquisa), use o `Array.IndexOf` método.</span><span class="sxs-lookup"><span data-stu-id="0d03c-479">To get the position of a specific item in the array (to search the array), use the `Array.IndexOf` method.</span></span> <span data-ttu-id="0d03c-480">Você também pode fazer coisas como reverter o conteúdo de uma matriz (a `Array.Reverse` método) ou classificar o conteúdo (o `Array.Sort` método).</span><span class="sxs-lookup"><span data-stu-id="0d03c-480">You can also do things like reverse the contents of an array (the `Array.Reverse` method) or sort the contents (the `Array.Sort` method).</span></span>

<span data-ttu-id="0d03c-481">A saída do código de matriz de cadeia de caracteres exibido em um navegador:</span><span class="sxs-lookup"><span data-stu-id="0d03c-481">The output of the string array code displayed in a browser:</span></span>

![Razor-Img13](introducing-razor-syntax-c/_static/image13.jpg)

<span data-ttu-id="0d03c-483">Um dicionário é uma coleção de pares chave/valor, em que você fornecer a chave (ou nome) para definir ou recuperar o valor correspondente:</span><span class="sxs-lookup"><span data-stu-id="0d03c-483">A dictionary is a collection of key/value pairs, where you provide the key (or name) to set or retrieve the corresponding value:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample61.cshtml)]

<span data-ttu-id="0d03c-484">Para criar um dicionário, use o `new` palavra-chave para indicar que você está criando um novo objeto de dicionário.</span><span class="sxs-lookup"><span data-stu-id="0d03c-484">To create a dictionary, you use the `new` keyword to indicate that you're creating a new dictionary object.</span></span> <span data-ttu-id="0d03c-485">Você pode atribuir um dicionário para uma variável usando o `var` palavra-chave.</span><span class="sxs-lookup"><span data-stu-id="0d03c-485">You can assign a dictionary to a variable using the `var` keyword.</span></span> <span data-ttu-id="0d03c-486">Indicar os tipos de dados dos itens no dicionário usando colchetes angulares ( `< >` ).</span><span class="sxs-lookup"><span data-stu-id="0d03c-486">You indicate the data types of the items in the dictionary using angle brackets ( `< >` ).</span></span> <span data-ttu-id="0d03c-487">No final da declaração, você deve adicionar um par de parênteses, porque isso é, na verdade, um método que cria um novo dicionário.</span><span class="sxs-lookup"><span data-stu-id="0d03c-487">At the end of the declaration, you must add a pair of parentheses, because this is actually a method that creates a new dictionary.</span></span>

<span data-ttu-id="0d03c-488">Para adicionar itens ao dicionário, você pode chamar o `Add` método da variável de dicionário (`myScores` nesse caso) e, em seguida, especifique uma chave e um valor.</span><span class="sxs-lookup"><span data-stu-id="0d03c-488">To add items to the dictionary, you can call the `Add` method of the dictionary variable (`myScores` in this case), and then specify a key and a value.</span></span> <span data-ttu-id="0d03c-489">Como alternativa, você pode usar colchetes para indicar a chave e fazer uma atribuição simple, como no exemplo a seguir:</span><span class="sxs-lookup"><span data-stu-id="0d03c-489">Alternatively, you can use square brackets to indicate the key and do a simple assignment, as in the following example:</span></span>

[!code-csharp[Main](introducing-razor-syntax-c/samples/sample62.cs)]

<span data-ttu-id="0d03c-490">Para obter um valor do dicionário, você pode especificar a chave entre colchetes:</span><span class="sxs-lookup"><span data-stu-id="0d03c-490">To get a value from the dictionary, you specify the key in brackets:</span></span>

[!code-csharp[Main](introducing-razor-syntax-c/samples/sample63.cs)]

## <a name="calling-methods-with-parameters"></a><span data-ttu-id="0d03c-491">Chamando métodos com parâmetros</span><span class="sxs-lookup"><span data-stu-id="0d03c-491">Calling Methods with Parameters</span></span>

<span data-ttu-id="0d03c-492">Durante a leitura deste artigo, os objetos que você programar com podem ter métodos.</span><span class="sxs-lookup"><span data-stu-id="0d03c-492">As you read earlier in this article, the objects that you program with can have methods.</span></span> <span data-ttu-id="0d03c-493">Por exemplo, um `Database` objeto pode ter um `Database.Connect` método.</span><span class="sxs-lookup"><span data-stu-id="0d03c-493">For example, a `Database` object might have a `Database.Connect` method.</span></span> <span data-ttu-id="0d03c-494">Muitos métodos também tem um ou mais parâmetros.</span><span class="sxs-lookup"><span data-stu-id="0d03c-494">Many methods also have one or more parameters.</span></span> <span data-ttu-id="0d03c-495">Um *parâmetro* é um valor que você passa para um método para habilitar o método concluir a tarefa.</span><span class="sxs-lookup"><span data-stu-id="0d03c-495">A *parameter* is a value that you pass to a method to enable the method to complete its task.</span></span> <span data-ttu-id="0d03c-496">Por exemplo, veja uma declaração para o `Request.MapPath` método, que usa três parâmetros:</span><span class="sxs-lookup"><span data-stu-id="0d03c-496">For example, look at a declaration for the `Request.MapPath` method, which takes three parameters:</span></span>

[!code-csharp[Main](introducing-razor-syntax-c/samples/sample64.cs)]

<span data-ttu-id="0d03c-497">(A linha foi quebrada para torná-lo mais legível.</span><span class="sxs-lookup"><span data-stu-id="0d03c-497">(The line has been wrapped to make it more readable.</span></span> <span data-ttu-id="0d03c-498">Lembre-se de que você pode colocar as quebras de linha quase qualquer local, exceto interna cadeias de caracteres são colocados entre aspas.)</span><span class="sxs-lookup"><span data-stu-id="0d03c-498">Remember that you can put line breaks almost any place except inside strings that are enclosed in quotation marks.)</span></span>

<span data-ttu-id="0d03c-499">Esse método retorna o caminho físico no servidor que corresponde a um caminho virtual especificado.</span><span class="sxs-lookup"><span data-stu-id="0d03c-499">This method returns the physical path on the server that corresponds to a specified virtual path.</span></span> <span data-ttu-id="0d03c-500">Os três parâmetros para o método são `virtualPath`, `baseVirtualDir`, e `allowCrossAppMapping`.</span><span class="sxs-lookup"><span data-stu-id="0d03c-500">The three parameters for the method are `virtualPath`, `baseVirtualDir`, and `allowCrossAppMapping`.</span></span> <span data-ttu-id="0d03c-501">(Observe que na declaração, os parâmetros são listados com os tipos de dados dos dados que eles aceitará). Quando você chamar esse método, você deve fornecer valores para todos os três parâmetros.</span><span class="sxs-lookup"><span data-stu-id="0d03c-501">(Notice that in the declaration, the parameters are listed with the data types of the data that they'll accept.) When you call this method, you must supply values for all three parameters.</span></span>

<span data-ttu-id="0d03c-502">A sintaxe do Razor fornece duas opções para passar parâmetros para um método: *parâmetros posicionais* e *parâmetros nomeados*.</span><span class="sxs-lookup"><span data-stu-id="0d03c-502">The Razor syntax gives you two options for passing parameters to a method: *positional parameters* and *named parameters*.</span></span> <span data-ttu-id="0d03c-503">Para chamar um método usando parâmetros posicionais, você pode passar os parâmetros em uma ordem estrita que é especificada na declaração de método.</span><span class="sxs-lookup"><span data-stu-id="0d03c-503">To call a method using positional parameters, you pass the parameters in a strict order that's specified in the method declaration.</span></span> <span data-ttu-id="0d03c-504">(Você normalmente saberia nesta ordem lendo a documentação do método.) Você deve seguir a ordem, e você não pode ignorar qualquer um dos parâmetros &#8212; Se necessário, você passar uma cadeia de caracteres vazia (`""`) ou `null` para um parâmetro posicional que você não tem um valor.</span><span class="sxs-lookup"><span data-stu-id="0d03c-504">(You would typically know this order by reading documentation for the method.) You must follow the order, and you can't skip any of the parameters &#8212; if necessary, you pass an empty string (`""`) or `null` for a positional parameter that you don't have a value for.</span></span>

<span data-ttu-id="0d03c-505">O exemplo a seguir supõe que você tem uma pasta chamada *scripts* no seu site.</span><span class="sxs-lookup"><span data-stu-id="0d03c-505">The following example assumes you have a folder named *scripts* on your website.</span></span> <span data-ttu-id="0d03c-506">O código chama o `Request.MapPath` método e passa valores para os três parâmetros na ordem correta.</span><span class="sxs-lookup"><span data-stu-id="0d03c-506">The code calls the `Request.MapPath` method and passes values for the three parameters in the correct order.</span></span> <span data-ttu-id="0d03c-507">Em seguida, ele exibe o caminho de mapeada resultante.</span><span class="sxs-lookup"><span data-stu-id="0d03c-507">It then displays the resulting mapped path.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample65.cshtml)]

<span data-ttu-id="0d03c-508">Quando um método tem muitos parâmetros, você pode manter seu código mais legível usando parâmetros nomeados.</span><span class="sxs-lookup"><span data-stu-id="0d03c-508">When a method has many parameters, you can keep your code more readable by using named parameters.</span></span> <span data-ttu-id="0d03c-509">Para chamar um método usando parâmetros nomeados, você especificar o nome do parâmetro seguido por dois-pontos (:) e, em seguida, o valor.</span><span class="sxs-lookup"><span data-stu-id="0d03c-509">To call a method using named parameters, you specify the parameter name followed by a colon (:), and then the value.</span></span> <span data-ttu-id="0d03c-510">A vantagem de parâmetros nomeados é que você pode passá-los em qualquer ordem desejada.</span><span class="sxs-lookup"><span data-stu-id="0d03c-510">The advantage of named parameters is that you can pass them in any order you want.</span></span> <span data-ttu-id="0d03c-511">(Uma desvantagem é que a chamada do método não é tão compacta).</span><span class="sxs-lookup"><span data-stu-id="0d03c-511">(A disadvantage is that the method call is not as compact.)</span></span>

<span data-ttu-id="0d03c-512">O exemplo a seguir chama o método mesmo que acima, mas usa a parâmetros para fornecer os valores nomeados:</span><span class="sxs-lookup"><span data-stu-id="0d03c-512">The following example calls the same method as above, but uses named parameters to supply the values:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample66.cshtml)]

<span data-ttu-id="0d03c-513">Como você pode ver, os parâmetros são passados em uma ordem diferente.</span><span class="sxs-lookup"><span data-stu-id="0d03c-513">As you can see, the parameters are passed in a different order.</span></span> <span data-ttu-id="0d03c-514">No entanto, se você executar o exemplo anterior e neste exemplo, eles irá retornar o mesmo valor.</span><span class="sxs-lookup"><span data-stu-id="0d03c-514">However, if you run the previous example and this example, they'll return the same value.</span></span>

<a id="ID_HandlingErrors"></a>
## <a name="handling-errors"></a><span data-ttu-id="0d03c-515">Manipulando erros</span><span class="sxs-lookup"><span data-stu-id="0d03c-515">Handling Errors</span></span>

### <a name="try-catch-statements"></a><span data-ttu-id="0d03c-516">Instruções Try-Catch</span><span class="sxs-lookup"><span data-stu-id="0d03c-516">Try-Catch Statements</span></span>

<span data-ttu-id="0d03c-517">Geralmente, você terá instruções no seu código que pode falhar por razões de fora de seu controle.</span><span class="sxs-lookup"><span data-stu-id="0d03c-517">You'll often have statements in your code that might fail for reasons outside your control.</span></span> <span data-ttu-id="0d03c-518">Por exemplo:</span><span class="sxs-lookup"><span data-stu-id="0d03c-518">For example:</span></span>

- <span data-ttu-id="0d03c-519">Se seu código tentar criar ou acessar um arquivo, todos os tipos de erros podem ocorrer.</span><span class="sxs-lookup"><span data-stu-id="0d03c-519">If your code tries to create or access a file, all sorts of errors might occur.</span></span> <span data-ttu-id="0d03c-520">O arquivo que você deseja pode não existir, ele pode ser bloqueado, o código não pode ter permissões e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="0d03c-520">The file you want might not exist, it might be locked, the code might not have permissions, and so on.</span></span>
- <span data-ttu-id="0d03c-521">Da mesma forma, se seu código tentar atualizar registros em um banco de dados, pode haver problemas de permissões, a conexão ao banco de dados pode ser descartado, os dados ao salvar podem ser inválida e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="0d03c-521">Similarly, if your code tries to update records in a database, there can be permissions issues, the connection to the database might be dropped, the data to save might be invalid, and so on.</span></span>

<span data-ttu-id="0d03c-522">Em termos de programação, essas situações são chamadas *exceções*.</span><span class="sxs-lookup"><span data-stu-id="0d03c-522">In programming terms, these situations are called *exceptions*.</span></span> <span data-ttu-id="0d03c-523">Se seu código encontra uma exceção, ele gera (lança) uma mensagem de erro que 's, na melhor das hipóteses, irritantes aos usuários:</span><span class="sxs-lookup"><span data-stu-id="0d03c-523">If your code encounters an exception, it generates (throws) an error message that's, at best, annoying to users:</span></span>

![Razor-Img14](introducing-razor-syntax-c/_static/image14.jpg)

<span data-ttu-id="0d03c-525">Em situações em que o seu código pode encontrar exceções e para evitar esse tipo de mensagem de erro, você pode usar `try/catch` instruções.</span><span class="sxs-lookup"><span data-stu-id="0d03c-525">In situations where your code might encounter exceptions, and in order to avoid error messages of this type, you can use `try/catch` statements.</span></span> <span data-ttu-id="0d03c-526">No `try` instrução, você executa o código que você está verificando.</span><span class="sxs-lookup"><span data-stu-id="0d03c-526">In the `try` statement, you run the code that you're checking.</span></span> <span data-ttu-id="0d03c-527">Em um ou mais `catch` instruções, você pode procurar específico erros (tipos específicos de exceções) que possam ter ocorrido.</span><span class="sxs-lookup"><span data-stu-id="0d03c-527">In one or more `catch` statements, you can look for specific errors (specific types of exceptions) that might have occurred.</span></span> <span data-ttu-id="0d03c-528">Você pode incluir tantos `catch` instruções de como você precisam procurar erros antecipando a você.</span><span class="sxs-lookup"><span data-stu-id="0d03c-528">You can include as many `catch` statements as you need to look for errors that you are anticipating.</span></span>

> [!NOTE]
> <span data-ttu-id="0d03c-529">É recomendável que você evite usar o `Response.Redirect` método `try/catch` instruções, porque ele pode causar uma exceção em sua página.</span><span class="sxs-lookup"><span data-stu-id="0d03c-529">We recommend that you avoid using the `Response.Redirect` method in `try/catch` statements, because it can cause an exception in your page.</span></span>


<span data-ttu-id="0d03c-530">O exemplo a seguir mostra uma página que cria um arquivo de texto na primeira solicitação e, em seguida, exibe um botão que permite que o usuário abra o arquivo.</span><span class="sxs-lookup"><span data-stu-id="0d03c-530">The following example shows a page that creates a text file on the first request and then displays a button that lets the user open the file.</span></span> <span data-ttu-id="0d03c-531">O exemplo deliberadamente usa um nome de arquivo inválido para que ela fará com que uma exceção.</span><span class="sxs-lookup"><span data-stu-id="0d03c-531">The example deliberately uses a bad file name so that it will cause an exception.</span></span> <span data-ttu-id="0d03c-532">Inclui o código `catch` instruções para duas exceções possíveis: `FileNotFoundException`, que ocorre se o nome de arquivo for inválido, e `DirectoryNotFoundException`, que ocorre se o ASP.NET ainda não é possível localizar a pasta.</span><span class="sxs-lookup"><span data-stu-id="0d03c-532">The code includes `catch` statements for two possible exceptions: `FileNotFoundException`, which occurs if the file name is bad, and `DirectoryNotFoundException`, which occurs if ASP.NET can't even find the folder.</span></span> <span data-ttu-id="0d03c-533">(Você pode remover o comentário uma instrução no exemplo para ver como ele é executado quando tudo está funcionando corretamente.)</span><span class="sxs-lookup"><span data-stu-id="0d03c-533">(You can uncomment a statement in the example in order to see how it runs when everything works properly.)</span></span>

<span data-ttu-id="0d03c-534">Se seu código não lidar com a exceção, você verá uma página de erro como a captura de tela anterior.</span><span class="sxs-lookup"><span data-stu-id="0d03c-534">If your code didn't handle the exception, you would see an error page like the previous screen shot.</span></span> <span data-ttu-id="0d03c-535">No entanto, o `try/catch` seção ajuda a impedir que o usuário ver esses tipos de erros.</span><span class="sxs-lookup"><span data-stu-id="0d03c-535">However, the `try/catch` section helps prevent the user from seeing these types of errors.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample67.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="0d03c-536">Recursos adicionais</span><span class="sxs-lookup"><span data-stu-id="0d03c-536">Additional Resources</span></span>

<span data-ttu-id="0d03c-537">**Programação com o Visual Basic**</span><span class="sxs-lookup"><span data-stu-id="0d03c-537">**Programming with Visual Basic**</span></span>


[<span data-ttu-id="0d03c-538">Apêndice: linguagem Visual Basic e a sintaxe</span><span class="sxs-lookup"><span data-stu-id="0d03c-538">Appendix: Visual Basic Language and Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkId=202908)


<span data-ttu-id="0d03c-539">**Documentação de referência**</span><span class="sxs-lookup"><span data-stu-id="0d03c-539">**Reference Documentation**</span></span>


[<span data-ttu-id="0d03c-540">ASP.NET</span><span class="sxs-lookup"><span data-stu-id="0d03c-540">ASP.NET</span></span>](https://msdn.microsoft.com/library/ee532866.aspx)

[<span data-ttu-id="0d03c-541">Linguagem c#</span><span class="sxs-lookup"><span data-stu-id="0d03c-541">C# Language</span></span>](https://msdn.microsoft.com/library/kx37x362.aspx)
