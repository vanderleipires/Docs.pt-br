---
uid: signalr/overview/older-versions/introduction-to-security
title: Introdução à segurança do SignalR (SignalR 1.x) | Microsoft Docs
author: pfletcher
description: Descreve os problemas de segurança, que você deve considerar ao desenvolver um aplicativo do SignalR.
ms.author: aspnetcontent
manager: wpickett
ms.date: 10/17/2013
ms.topic: article
ms.assetid: 715a4059-d307-4631-abbb-c789c95d6eb4
ms.technology: dotnet-signalr
msc.legacyurl: /signalr/overview/older-versions/introduction-to-security
msc.type: authoredcontent
ms.openlocfilehash: b50b96bd4d1bf84c431f53f18747fe26a629d98a
ms.sourcegitcommit: 953ff9ea4369f154d6fd0239599279ddd3280009
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 07/03/2018
ms.locfileid: "37370504"
---
<a name="introduction-to-signalr-security-signalr-1x"></a><span data-ttu-id="51bb3-103">Introdução à segurança do SignalR (SignalR 1.x)</span><span class="sxs-lookup"><span data-stu-id="51bb3-103">Introduction to SignalR Security (SignalR 1.x)</span></span>
====================
<span data-ttu-id="51bb3-104">por [Patrick Fletcher](https://github.com/pfletcher), [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="51bb3-104">by [Patrick Fletcher](https://github.com/pfletcher), [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="51bb3-105">Este artigo descreve os problemas de segurança, que você deve considerar ao desenvolver um aplicativo do SignalR.</span><span class="sxs-lookup"><span data-stu-id="51bb3-105">This article describes the security issues you must consider when developing a SignalR application.</span></span>


## <a name="overview"></a><span data-ttu-id="51bb3-106">Visão geral</span><span class="sxs-lookup"><span data-stu-id="51bb3-106">Overview</span></span>

<span data-ttu-id="51bb3-107">Este documento contém as seguintes seções:</span><span class="sxs-lookup"><span data-stu-id="51bb3-107">This document contains the following sections:</span></span>

- [<span data-ttu-id="51bb3-108">Conceitos de segurança do SignalR</span><span class="sxs-lookup"><span data-stu-id="51bb3-108">SignalR Security Concepts</span></span>](#concepts)

    - [<span data-ttu-id="51bb3-109">Autenticação e autorização</span><span class="sxs-lookup"><span data-stu-id="51bb3-109">Authentication and authorization</span></span>](#authentication)
    - [<span data-ttu-id="51bb3-110">Token de Conexão</span><span class="sxs-lookup"><span data-stu-id="51bb3-110">Connection token</span></span>](#connectiontoken)
    - [<span data-ttu-id="51bb3-111">Incluir novamente grupos ao reconectar</span><span class="sxs-lookup"><span data-stu-id="51bb3-111">Rejoining groups when reconnecting</span></span>](#rejoingroup)
- [<span data-ttu-id="51bb3-112">Como o SignalR impede que a falsificação de solicitação entre sites</span><span class="sxs-lookup"><span data-stu-id="51bb3-112">How SignalR prevents Cross-Site Request Forgery</span></span>](#csrf)
- [<span data-ttu-id="51bb3-113">Recomendações de segurança do SignalR</span><span class="sxs-lookup"><span data-stu-id="51bb3-113">SignalR Security Recommendations</span></span>](#recommendations)

    - [<span data-ttu-id="51bb3-114">Protocolo seguro de camadas de soquete (SSL)</span><span class="sxs-lookup"><span data-stu-id="51bb3-114">Secure Socket Layers (SSL) protocol</span></span>](#ssl)
    - [<span data-ttu-id="51bb3-115">Não use grupos como um mecanismo de segurança</span><span class="sxs-lookup"><span data-stu-id="51bb3-115">Do not use groups as a security mechanism</span></span>](#groupsecurity)
    - [<span data-ttu-id="51bb3-116">Manipulando a entrada de clientes com segurança</span><span class="sxs-lookup"><span data-stu-id="51bb3-116">Safely handling input from clients</span></span>](#input)
    - [<span data-ttu-id="51bb3-117">Reconciliando uma alteração no status de usuário com uma conexão ativa</span><span class="sxs-lookup"><span data-stu-id="51bb3-117">Reconciling a change in user status with an active connection</span></span>](#reconcile)
    - [<span data-ttu-id="51bb3-118">Arquivos de proxy JavaScript gerados automaticamente</span><span class="sxs-lookup"><span data-stu-id="51bb3-118">Automatically generated JavaScript proxy files</span></span>](#autogen)
    - [<span data-ttu-id="51bb3-119">Exceções</span><span class="sxs-lookup"><span data-stu-id="51bb3-119">Exceptions</span></span>](#exceptions)

<a id="concepts"></a>

## <a name="signalr-security-concepts"></a><span data-ttu-id="51bb3-120">Conceitos de segurança do SignalR</span><span class="sxs-lookup"><span data-stu-id="51bb3-120">SignalR Security Concepts</span></span>

<a id="authentication"></a>

### <a name="authentication-and-authorization"></a><span data-ttu-id="51bb3-121">Autenticação e autorização</span><span class="sxs-lookup"><span data-stu-id="51bb3-121">Authentication and authorization</span></span>

<span data-ttu-id="51bb3-122">O SignalR é projetado para ser integrados a estrutura existente de autenticação para um aplicativo.</span><span class="sxs-lookup"><span data-stu-id="51bb3-122">SignalR is designed to be integrated into the existing authentication structure for an application.</span></span> <span data-ttu-id="51bb3-123">Ele fornece recursos para autenticar usuários.</span><span class="sxs-lookup"><span data-stu-id="51bb3-123">It does not provide any features for authenticating users.</span></span> <span data-ttu-id="51bb3-124">Em vez disso, você autenticar os usuários, como você faria normalmente em seu aplicativo e, em seguida, trabalhar com os resultados da autenticação em seu código do SignalR.</span><span class="sxs-lookup"><span data-stu-id="51bb3-124">Instead, you authenticate users as you would normally in your application, and then work with the results of the authentication in your SignalR code.</span></span> <span data-ttu-id="51bb3-125">Por exemplo, você pode autenticar os usuários com a autenticação de formulários do ASP.NET e, em seguida, no hub, impor que os usuários ou funções têm autorização para chamar um método.</span><span class="sxs-lookup"><span data-stu-id="51bb3-125">For example, you might authenticate your users with ASP.NET forms authentication, and then in your hub, enforce which users or roles are authorized to call a method.</span></span> <span data-ttu-id="51bb3-126">Em seu hub, você também pode passar informações de autenticação, como nome de usuário ou se um usuário pertence a uma função, para o cliente.</span><span class="sxs-lookup"><span data-stu-id="51bb3-126">In your hub, you can also pass authentication information, such as user name or whether a user belongs to a role, to the client.</span></span>

<span data-ttu-id="51bb3-127">O SignalR fornece o [autorizar](https://msdn.microsoft.com/library/microsoft.aspnet.signalr.authorizeattribute(v=vs.111).aspx) atributo para especificar quais usuários têm acesso a um hub ou método.</span><span class="sxs-lookup"><span data-stu-id="51bb3-127">SignalR provides the [Authorize](https://msdn.microsoft.com/library/microsoft.aspnet.signalr.authorizeattribute(v=vs.111).aspx) attribute to specify which users have access to a hub or method.</span></span> <span data-ttu-id="51bb3-128">Você pode aplicar o atributo Authorize para um hub ou determinados métodos em um hub.</span><span class="sxs-lookup"><span data-stu-id="51bb3-128">You apply the Authorize attribute to either a hub or particular methods in a hub.</span></span> <span data-ttu-id="51bb3-129">Sem o atributo Authorize, todos os métodos públicos no hub estão disponíveis para um cliente que está conectado ao hub.</span><span class="sxs-lookup"><span data-stu-id="51bb3-129">Without the Authorize attribute, all public methods on the hub are available to a client that is connected to the hub.</span></span> <span data-ttu-id="51bb3-130">Para obter mais informações sobre os hubs, consulte [autenticação e autorização para Hubs do SignalR](../security/hub-authorization.md).</span><span class="sxs-lookup"><span data-stu-id="51bb3-130">For more information about hubs, see [Authentication and Authorization for SignalR Hubs](../security/hub-authorization.md).</span></span>

<span data-ttu-id="51bb3-131">O `Authorize` atributo é usado somente com hubs.</span><span class="sxs-lookup"><span data-stu-id="51bb3-131">The `Authorize` attribute is used only with hubs.</span></span> <span data-ttu-id="51bb3-132">Para impor regras de autorização ao usar um `PersistentConnection` você deve substituir o `AuthorizeRequest` método.</span><span class="sxs-lookup"><span data-stu-id="51bb3-132">To enforce authorization rules when using a `PersistentConnection` you must override the `AuthorizeRequest` method.</span></span> <span data-ttu-id="51bb3-133">Para obter mais informações sobre conexões persistentes, consulte [autenticação e autorização para conexões persistentes do SignalR](../security/persistent-connection-authorization.md).</span><span class="sxs-lookup"><span data-stu-id="51bb3-133">For more information about persistent connections, see [Authentication and Authorization for SignalR Persistent Connections](../security/persistent-connection-authorization.md).</span></span>

<a id="connectiontoken"></a>

### <a name="connection-token"></a><span data-ttu-id="51bb3-134">Token de Conexão</span><span class="sxs-lookup"><span data-stu-id="51bb3-134">Connection token</span></span>

<span data-ttu-id="51bb3-135">O SignalR minimiza o risco da execução de comandos mal-intencionados ao validar a identidade do remetente.</span><span class="sxs-lookup"><span data-stu-id="51bb3-135">SignalR mitigates the risk of executing malicious commands by validating the identity of the sender.</span></span> <span data-ttu-id="51bb3-136">Um token de conexão, que contém a id de conexão e o nome de usuário para usuários autenticados, é passado entre o cliente e servidor para cada solicitação.</span><span class="sxs-lookup"><span data-stu-id="51bb3-136">A connection token, containing the connection id and username for authenticated users, is passed between the client and server for each request.</span></span> <span data-ttu-id="51bb3-137">A id de conexão é um identificador exclusivo que é gerado aleatoriamente pelo servidor quando uma nova conexão é criada e é mantido durante a conexão.</span><span class="sxs-lookup"><span data-stu-id="51bb3-137">The connection id is a unique identifier that is randomly generated by the server when a new connection is created, and is persisted for the duration of the connection.</span></span> <span data-ttu-id="51bb3-138">O nome de usuário é fornecida pelo mecanismo de autenticação para o aplicativo web.</span><span class="sxs-lookup"><span data-stu-id="51bb3-138">The username is provided by the authentication mechanism for the web application.</span></span> <span data-ttu-id="51bb3-139">O token de conexão é protegido com uma assinatura digital e criptografia.</span><span class="sxs-lookup"><span data-stu-id="51bb3-139">The connection token is protected with encryption and a digital signature.</span></span>

![](introduction-to-security/_static/image2.png)

<span data-ttu-id="51bb3-140">Para cada solicitação, o servidor valida o conteúdo do token para garantir que a solicitação é proveniente do usuário especificado.</span><span class="sxs-lookup"><span data-stu-id="51bb3-140">For each request, the server validates the contents of the token to ensure that the request is coming from the specified user.</span></span> <span data-ttu-id="51bb3-141">O nome de usuário deve corresponder à id de conexão. Validando a id de conexão e o nome de usuário, o SignalR impede que um usuário mal-intencionado facilmente representando outro usuário.</span><span class="sxs-lookup"><span data-stu-id="51bb3-141">The username must correspond to the connection id. By validating both the connection id and the username, SignalR prevents a malicious user from easily impersonating another user.</span></span> <span data-ttu-id="51bb3-142">Se o servidor não é possível validar o token de conexão, a solicitação falhará.</span><span class="sxs-lookup"><span data-stu-id="51bb3-142">If the server cannot validate the connection token, the request fails.</span></span>

![](introduction-to-security/_static/image4.png)

<span data-ttu-id="51bb3-143">Porque a id de conexão é parte do processo de verificação, você não deve revelar a id de conexão de um usuário a outros usuários ou armazena o valor no cliente, como em um cookie.</span><span class="sxs-lookup"><span data-stu-id="51bb3-143">Because the connection id is part of the verification process, you should not reveal one user's connection id to other users or store the value on the client, such as in a cookie.</span></span>

<a id="rejoingroup"></a>

### <a name="rejoining-groups-when-reconnecting"></a><span data-ttu-id="51bb3-144">Incluir novamente grupos ao reconectar</span><span class="sxs-lookup"><span data-stu-id="51bb3-144">Rejoining groups when reconnecting</span></span>

<span data-ttu-id="51bb3-145">Por padrão, o aplicativo SignalR novamente atribuirá automaticamente um usuário aos grupos apropriados ao reconectar-se de uma interrupção temporária, como quando uma conexão será descartada e restabelecida antes que a conexão expire. Ao reconectar, o cliente passa um token de grupo que inclui a id de conexão e os grupos atribuídos.</span><span class="sxs-lookup"><span data-stu-id="51bb3-145">By default, the SignalR application will automatically re-assign a user to the appropriate groups when reconnecting from a temporary disruption, such as when a connection is dropped and re-established before the connection times out. When reconnecting, the client passes a group token that includes the connection id and the assigned groups.</span></span> <span data-ttu-id="51bb3-146">O token de grupo é digitalmente assinado e criptografado.</span><span class="sxs-lookup"><span data-stu-id="51bb3-146">The group token is digitally signed and encrypted.</span></span> <span data-ttu-id="51bb3-147">O cliente retém a mesma id de conexão após uma reconexão. Portanto, a id de conexão passada do cliente reconectado deve corresponder a id de conexão anterior usada pelo cliente.</span><span class="sxs-lookup"><span data-stu-id="51bb3-147">The client retains the same connection id after a reconnection; therefore, the connection id passed from the reconnected client must match the previous connection id used by the client.</span></span> <span data-ttu-id="51bb3-148">Essa verificação impede que um usuário mal-intencionado passar as solicitações para ingressar em grupos de não autorizados ao reconectar.</span><span class="sxs-lookup"><span data-stu-id="51bb3-148">This verification prevents a malicious user from passing requests to join unauthorized groups when reconnecting.</span></span>

<span data-ttu-id="51bb3-149">No entanto, é importante observar que o token de grupo não expira.</span><span class="sxs-lookup"><span data-stu-id="51bb3-149">However, it is important to note, that the group token does not expire.</span></span> <span data-ttu-id="51bb3-150">Se um usuário pertencia a um grupo no passado, mas foi banido do grupo, que o usuário pode ser capaz de simular um token de grupo que inclui o grupo proibido.</span><span class="sxs-lookup"><span data-stu-id="51bb3-150">If a user belonged to a group in the past, but was banned from that group, that user may be able to mimic a group token that includes the prohibited group.</span></span> <span data-ttu-id="51bb3-151">Se você precisar gerenciar com segurança os usuários que pertencem a quais grupos, você precisa armazenar os dados no servidor, como em um banco de dados.</span><span class="sxs-lookup"><span data-stu-id="51bb3-151">If you need to securely manage which users belong to which groups, you need to store that data on the server, such as in a database.</span></span> <span data-ttu-id="51bb3-152">Em seguida, adicione lógica ao seu aplicativo que verifica no servidor, se um usuário pertence a um grupo.</span><span class="sxs-lookup"><span data-stu-id="51bb3-152">Then, add logic to your application that verifies on the server whether a user belongs to a group.</span></span> <span data-ttu-id="51bb3-153">Para obter um exemplo de verificação de associação de grupo, consulte [trabalhando com grupos de](../guide-to-the-api/working-with-groups.md).</span><span class="sxs-lookup"><span data-stu-id="51bb3-153">For an example of verifying group membership, see [Working with groups](../guide-to-the-api/working-with-groups.md).</span></span>

<span data-ttu-id="51bb3-154">Automaticamente incluir novamente grupos se aplica somente quando uma conexão é reconectada, após uma interrupção temporária.</span><span class="sxs-lookup"><span data-stu-id="51bb3-154">Automatically rejoining groups only applies when a connection is reconnected after a temporary disruption.</span></span> <span data-ttu-id="51bb3-155">Se um usuário se desconecta navegar para fora do aplicativo ou o aplicativo ser reiniciado, o aplicativo deve tratar como adicionar esse usuário a grupos corretos.</span><span class="sxs-lookup"><span data-stu-id="51bb3-155">If a user disconnects by navigating away from the application or the application restarts, your application must handle how to add that user to the correct groups.</span></span> <span data-ttu-id="51bb3-156">Para obter mais informações, consulte [trabalhando com grupos de](../guide-to-the-api/working-with-groups.md).</span><span class="sxs-lookup"><span data-stu-id="51bb3-156">For more information, see [Working with groups](../guide-to-the-api/working-with-groups.md).</span></span>

<a id="csrf"></a>

## <a name="how-signalr-prevents-cross-site-request-forgery"></a><span data-ttu-id="51bb3-157">Como o SignalR impede que a falsificação de solicitação entre sites</span><span class="sxs-lookup"><span data-stu-id="51bb3-157">How SignalR prevents Cross-Site Request Forgery</span></span>

<span data-ttu-id="51bb3-158">Falsificação de solicitação entre sites (CSRF) é um ataque em que um site mal-intencionado envia uma solicitação para um site vulnerável no qual o usuário fez logon.</span><span class="sxs-lookup"><span data-stu-id="51bb3-158">Cross-Site Request Forgery (CSRF) is an attack where a malicious site sends a request to a vulnerable site where the user is currently logged in.</span></span> <span data-ttu-id="51bb3-159">O SignalR impede CSRF, tornando extremamente improvável de um site mal-intencionado para criar uma solicitação válida para o seu aplicativo do SignalR.</span><span class="sxs-lookup"><span data-stu-id="51bb3-159">SignalR prevents CSRF by making it extremely unlikely for a malicious site to create a valid request for your SignalR application.</span></span>

### <a name="description-of-csrf-attack"></a><span data-ttu-id="51bb3-160">Descrição de ataque CSRF</span><span class="sxs-lookup"><span data-stu-id="51bb3-160">Description of CSRF attack</span></span>

<span data-ttu-id="51bb3-161">Aqui está um exemplo de um ataque CSRF:</span><span class="sxs-lookup"><span data-stu-id="51bb3-161">Here is an example of a CSRF attack:</span></span>

1. <span data-ttu-id="51bb3-162">Um usuário faz logon em www.example.com, usando a autenticação de formulários.</span><span class="sxs-lookup"><span data-stu-id="51bb3-162">A user logs into www.example.com, using forms authentication.</span></span>
2. <span data-ttu-id="51bb3-163">O servidor autentica o usuário.</span><span class="sxs-lookup"><span data-stu-id="51bb3-163">The server authenticates the user.</span></span> <span data-ttu-id="51bb3-164">A resposta do servidor inclui um cookie de autenticação.</span><span class="sxs-lookup"><span data-stu-id="51bb3-164">The response from the server includes an authentication cookie.</span></span>
3. <span data-ttu-id="51bb3-165">Sem fazer logoff, o usuário visitar um site mal-intencionado.</span><span class="sxs-lookup"><span data-stu-id="51bb3-165">Without logging out, the user visits a malicious web site.</span></span> <span data-ttu-id="51bb3-166">Este site mal-intencionado contém o formulário HTML a seguir:</span><span class="sxs-lookup"><span data-stu-id="51bb3-166">This malicious site contains the following HTML form:</span></span> 

    [!code-html[Main](introduction-to-security/samples/sample1.html)]

   <span data-ttu-id="51bb3-167">Observe que a ação de formulário faz a postagem para o site vulnerável, não para o site mal-intencionado.</span><span class="sxs-lookup"><span data-stu-id="51bb3-167">Notice that the form action posts to the vulnerable site, not to the malicious site.</span></span> <span data-ttu-id="51bb3-168">Essa é a parte de "site cruzado" de CSRF.</span><span class="sxs-lookup"><span data-stu-id="51bb3-168">This is the "cross-site" part of CSRF.</span></span>
4. <span data-ttu-id="51bb3-169">O usuário clica no botão Enviar.</span><span class="sxs-lookup"><span data-stu-id="51bb3-169">The user clicks the submit button.</span></span> <span data-ttu-id="51bb3-170">O navegador inclui o cookie de autenticação com a solicitação.</span><span class="sxs-lookup"><span data-stu-id="51bb3-170">The browser includes the authentication cookie with the request.</span></span>
5. <span data-ttu-id="51bb3-171">A solicitação é executado no servidor de example.com com o contexto de autenticação do usuário e pode fazer qualquer coisa que um usuário autenticado tem permissão para fazer.</span><span class="sxs-lookup"><span data-stu-id="51bb3-171">The request runs on the example.com server with the user's authentication context, and can do anything that an authenticated user is allowed to do.</span></span>

<span data-ttu-id="51bb3-172">Embora este exemplo requer que o usuário clicar no botão do formulário, a página mal-intencionada pode apenas executar facilmente um script que envia uma solicitação AJAX ao seu aplicativo do SignalR.</span><span class="sxs-lookup"><span data-stu-id="51bb3-172">Although this example requires the user to click the form button, the malicious page could just as easily run a script that sends an AJAX request to your SignalR application.</span></span> <span data-ttu-id="51bb3-173">Além disso, usando o SSL não impede que um ataque CSRF, porque o site mal-intencionado pode enviar uma solicitação de "https://".</span><span class="sxs-lookup"><span data-stu-id="51bb3-173">Moreover, using SSL does not prevent a CSRF attack, because the malicious site can send an "https://" request.</span></span>

<span data-ttu-id="51bb3-174">Normalmente, ataques de CSRF são possíveis em relação a sites da web que usam cookies para autenticação, porque os navegadores enviam a todos os cookies relevantes para o site de destino.</span><span class="sxs-lookup"><span data-stu-id="51bb3-174">Typically, CSRF attacks are possible against web sites that use cookies for authentication, because browsers send all relevant cookies to the destination web site.</span></span> <span data-ttu-id="51bb3-175">No entanto, ataques de CSRF não são limitados a exploração de cookies.</span><span class="sxs-lookup"><span data-stu-id="51bb3-175">However, CSRF attacks are not limited to exploiting cookies.</span></span> <span data-ttu-id="51bb3-176">Por exemplo, a autenticação básica e Digest também são vulneráveis.</span><span class="sxs-lookup"><span data-stu-id="51bb3-176">For example, Basic and Digest authentication are also vulnerable.</span></span> <span data-ttu-id="51bb3-177">Depois que um usuário faz logon com a autenticação básica ou Digest, o navegador automaticamente envia as credenciais até que a sessão termina.</span><span class="sxs-lookup"><span data-stu-id="51bb3-177">After a user logs in with Basic or Digest authentication, the browser automatically sends the credentials until the session ends.</span></span>

### <a name="csrf-mitigations-taken-by-signalr"></a><span data-ttu-id="51bb3-178">Atenuações de CSRF tomadas pelo SignalR</span><span class="sxs-lookup"><span data-stu-id="51bb3-178">CSRF mitigations taken by SignalR</span></span>

<span data-ttu-id="51bb3-179">O SignalR realiza as seguintes etapas para impedir que um site mal-intencionado criando solicitações válidas para seu aplicativo do SignalR.</span><span class="sxs-lookup"><span data-stu-id="51bb3-179">SignalR takes the following steps to prevent a malicious site from creating valid requests to your SignalR application.</span></span> <span data-ttu-id="51bb3-180">Essas etapas são executadas por padrão e não exigem nenhuma ação em seu código.</span><span class="sxs-lookup"><span data-stu-id="51bb3-180">These steps are taken by default and do not require any action in your code.</span></span>

- <span data-ttu-id="51bb3-181">**Desabilite as solicitações entre domínios**</span><span class="sxs-lookup"><span data-stu-id="51bb3-181">**Disable cross domain requests**</span></span>  
 <span data-ttu-id="51bb3-182">Por padrão, entre domínios solicitações estão desabilitadas em um aplicativo do SignalR para impedir que usuários chamando um ponto de extremidade do SignalR de um domínio externo.</span><span class="sxs-lookup"><span data-stu-id="51bb3-182">By default, cross domain requests are disabled in a SignalR application to prevent users from calling a SignalR endpoint from an external domain.</span></span> <span data-ttu-id="51bb3-183">Qualquer solicitação que vem de um domínio externo é automaticamente considerada inválida e será bloqueada.</span><span class="sxs-lookup"><span data-stu-id="51bb3-183">Any request that comes from an external domain is automatically considered invalid and is blocked.</span></span> <span data-ttu-id="51bb3-184">É recomendável que você mantenha esse comportamento padrão; Caso contrário, um site mal-intencionado pode enganar os usuários para enviar comandos ao seu site.</span><span class="sxs-lookup"><span data-stu-id="51bb3-184">It is recommended that you keep this default behavior; otherwise, a malicious site could trick users into sending commands to your site.</span></span> <span data-ttu-id="51bb3-185">Se você precisar usar solicitações de domínio cruzado, consulte [como estabelecer uma conexão entre domínios](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain) .</span><span class="sxs-lookup"><span data-stu-id="51bb3-185">If you need to use cross domain requests, see     [How to establish a cross-domain connection](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain) .</span></span>
- <span data-ttu-id="51bb3-186">**Passe o token de conexão na cadeia de caracteres de consulta, não o cookie**</span><span class="sxs-lookup"><span data-stu-id="51bb3-186">**Pass connection token in query string, not cookie**</span></span>  
 <span data-ttu-id="51bb3-187">O SignalR passa o token de conexão como um valor de cadeia de caracteres de consulta, em vez de como um cookie.</span><span class="sxs-lookup"><span data-stu-id="51bb3-187">SignalR passes the connection token as a query string value, instead of as a cookie.</span></span> <span data-ttu-id="51bb3-188">Por não armazenar o token de conexão como um cookie, o token de conexão não é inadvertidamente encaminhado pelo navegador quando o código mal-intencionado é encontrado.</span><span class="sxs-lookup"><span data-stu-id="51bb3-188">By not storing the connection token as a cookie, the connection token is not inadvertently forwarded by the browser when malicious code is encountered.</span></span> <span data-ttu-id="51bb3-189">Além disso, o token de conexão não persiste além a conexão atual.</span><span class="sxs-lookup"><span data-stu-id="51bb3-189">Also, the connection token is not persisted beyond the current connection.</span></span> <span data-ttu-id="51bb3-190">Portanto, um usuário mal-intencionado não pode fazer uma solicitação de credenciais de autenticação de outro usuário.</span><span class="sxs-lookup"><span data-stu-id="51bb3-190">Therefore, a malicious user cannot make a request under another user's authentication credentials.</span></span>
- <span data-ttu-id="51bb3-191">**Verifique se o token de conexão**</span><span class="sxs-lookup"><span data-stu-id="51bb3-191">**Verify connection token**</span></span>  
 <span data-ttu-id="51bb3-192">Conforme descrito na [token de Conexão](#connectiontoken) seção, o servidor sabe qual id de conexão é associado a cada usuário autenticado.</span><span class="sxs-lookup"><span data-stu-id="51bb3-192">As described in the [Connection token](#connectiontoken) section, the server knows which connection id is associated with each authenticated user.</span></span> <span data-ttu-id="51bb3-193">O servidor não processar qualquer solicitação de uma id de conexão que não coincide com o nome de usuário.</span><span class="sxs-lookup"><span data-stu-id="51bb3-193">The server does not process any request from a connection id that does not match the user name.</span></span> <span data-ttu-id="51bb3-194">É improvável que um usuário mal-intencionado poderia imaginar uma solicitação válida porque o usuário mal-intencionado precisaria saber o nome de usuário e a id de conexão gerada aleatoriamente atual. Essa id de conexão se torna inválido, assim que a conexão será encerrada.</span><span class="sxs-lookup"><span data-stu-id="51bb3-194">It is unlikely a malicious user could guess a valid request because the malicious user would have to know the user name and the current randomly-generated connection id. That connection id becomes invalid as soon as the connection is ended.</span></span> <span data-ttu-id="51bb3-195">Os usuários anônimos não devem ter acesso a informações confidenciais.</span><span class="sxs-lookup"><span data-stu-id="51bb3-195">Anonymous users should not have access to any sensitive information.</span></span>

<a id="recommendations"></a>

## <a name="signalr-security-recommendations"></a><span data-ttu-id="51bb3-196">Recomendações de segurança do SignalR</span><span class="sxs-lookup"><span data-stu-id="51bb3-196">SignalR Security Recommendations</span></span>

<a id="ssl"></a>

### <a name="secure-socket-layers-ssl-protocol"></a><span data-ttu-id="51bb3-197">Protocolo seguro de camadas de soquete (SSL)</span><span class="sxs-lookup"><span data-stu-id="51bb3-197">Secure Socket Layers (SSL) protocol</span></span>

<span data-ttu-id="51bb3-198">O protocolo SSL usa criptografia para proteger o transporte de dados entre um cliente e servidor.</span><span class="sxs-lookup"><span data-stu-id="51bb3-198">The SSL protocol uses encryption to secure the transport of data between a client and server.</span></span> <span data-ttu-id="51bb3-199">Se seu aplicativo SignalR transmite informações sigilosas entre o cliente e servidor, use o SSL para o transporte.</span><span class="sxs-lookup"><span data-stu-id="51bb3-199">If your SignalR application transmits sensitive information between the client and server, use SSL for the transport.</span></span> <span data-ttu-id="51bb3-200">Para obter mais informações sobre como configurar o SSL, consulte [como configurar o SSL no IIS 7](https://www.iis.net/learn/manage/configuring-security/how-to-set-up-ssl-on-iis).</span><span class="sxs-lookup"><span data-stu-id="51bb3-200">For more information about setting up SSL, see [How to set up SSL on IIS 7](https://www.iis.net/learn/manage/configuring-security/how-to-set-up-ssl-on-iis).</span></span>

<a id="groupsecurity"></a>

### <a name="do-not-use-groups-as-a-security-mechanism"></a><span data-ttu-id="51bb3-201">Não use grupos como um mecanismo de segurança</span><span class="sxs-lookup"><span data-stu-id="51bb3-201">Do not use groups as a security mechanism</span></span>

<span data-ttu-id="51bb3-202">Grupos são uma maneira conveniente de coleta de usuários relacionados, mas eles não são um mecanismo seguro para limitar o acesso a informações confidenciais.</span><span class="sxs-lookup"><span data-stu-id="51bb3-202">Groups are a convenient way of collecting related users, but they are not a secure mechanism for limiting access to sensitive information.</span></span> <span data-ttu-id="51bb3-203">Isso é especialmente verdadeiro quando os usuários podem automaticamente incluir grupos novamente durante uma reconexão.</span><span class="sxs-lookup"><span data-stu-id="51bb3-203">This is especially true when users can automatically rejoin groups during a reconnect.</span></span> <span data-ttu-id="51bb3-204">Em vez disso, considere a adição de usuários com privilégios para uma função e limitando o acesso a um método de hub para somente os membros dessa função.</span><span class="sxs-lookup"><span data-stu-id="51bb3-204">Instead, consider adding privileged users to a role and limiting access to a hub method to only members of that role.</span></span> <span data-ttu-id="51bb3-205">Para obter um exemplo de restringir o acesso com base em uma função, consulte [autenticação e autorização para Hubs do SignalR](../security/hub-authorization.md).</span><span class="sxs-lookup"><span data-stu-id="51bb3-205">For an example of restricting access based on a role, see [Authentication and Authorization for SignalR Hubs](../security/hub-authorization.md).</span></span> <span data-ttu-id="51bb3-206">Para obter um exemplo de verificação de acesso do usuário aos grupos ao reconectar, consulte [trabalhando com grupos de](../guide-to-the-api/working-with-groups.md).</span><span class="sxs-lookup"><span data-stu-id="51bb3-206">For an example of checking user access to groups when reconnecting, see [Working with groups](../guide-to-the-api/working-with-groups.md).</span></span>

<a id="input"></a>

### <a name="safely-handling-input-from-clients"></a><span data-ttu-id="51bb3-207">Manipulando a entrada de clientes com segurança</span><span class="sxs-lookup"><span data-stu-id="51bb3-207">Safely handling input from clients</span></span>

<span data-ttu-id="51bb3-208">Todas as entradas de clientes que se destina para difusão de outros clientes devem ser codificada para garantir que um usuário mal-intencionado não enviar script para outros usuários.</span><span class="sxs-lookup"><span data-stu-id="51bb3-208">All input from clients that is intended for broadcast to other clients must be encoded to ensure that a malicious user does not send script to other users.</span></span> <span data-ttu-id="51bb3-209">É melhor codificar mensagens sobre os clientes de recebimento em vez do servidor, como seu aplicativo SignalR pode ter muitos tipos diferentes de clientes.</span><span class="sxs-lookup"><span data-stu-id="51bb3-209">It is best to encode messages on the receiving clients rather than the server, because your SignalR application may have many different types of clients.</span></span> <span data-ttu-id="51bb3-210">Portanto, a codificação HTML funciona para um cliente da web, mas não para outros tipos de clientes.</span><span class="sxs-lookup"><span data-stu-id="51bb3-210">Therefore, HTML-encoding works for a web client, but not for other types of clients.</span></span> <span data-ttu-id="51bb3-211">Por exemplo, um método de cliente da web para exibir uma mensagem de bate-papo lidaria com segurança com o nome de usuário e a mensagem chamando o `html()` função.</span><span class="sxs-lookup"><span data-stu-id="51bb3-211">For example, a web client method to display a chat message would safely handle the user name and message by calling the `html()` function.</span></span>

[!code-html[Main](introduction-to-security/samples/sample2.html?highlight=3-4)]

<a id="reconcile"></a>

### <a name="reconciling-a-change-in-user-status-with-an-active-connection"></a><span data-ttu-id="51bb3-212">Reconciliando uma alteração no status de usuário com uma conexão ativa</span><span class="sxs-lookup"><span data-stu-id="51bb3-212">Reconciling a change in user status with an active connection</span></span>

<span data-ttu-id="51bb3-213">Se o status de autenticação do usuário for alterada enquanto existe uma conexão ativa, o usuário receberá um erro que afirma, "não é possível alterar a identidade do usuário durante uma conexão SignalR Active Directory."</span><span class="sxs-lookup"><span data-stu-id="51bb3-213">If a user's authentication status changes while an active connection exists, the user will receive an error that states, "The user identity cannot change during an active SignalR connection."</span></span> <span data-ttu-id="51bb3-214">Nesse caso, seu aplicativo deve se conectar novamente ao servidor para certificar-se de que a id de conexão e o nome de usuário são coordenados.</span><span class="sxs-lookup"><span data-stu-id="51bb3-214">In that case, your application should re-connect to the server to make sure the connection id and username are coordinated.</span></span> <span data-ttu-id="51bb3-215">Por exemplo, se seu aplicativo permite que o usuário faça logoff enquanto existe uma conexão ativa, o nome de usuário para a conexão não corresponderá ao nome que é passado para a próxima solicitação.</span><span class="sxs-lookup"><span data-stu-id="51bb3-215">For example, if your application allows the user to log out while an active connection exists, the username for the connection will no longer match the name that is passed in for the next request.</span></span> <span data-ttu-id="51bb3-216">Você deseja interromper a conexão antes do usuário faz logoff e, em seguida, reiniciá-lo.</span><span class="sxs-lookup"><span data-stu-id="51bb3-216">You will want to stop the connection before the user logs out, and then restart it.</span></span>

<span data-ttu-id="51bb3-217">No entanto, é importante observar que a maioria dos aplicativos não precisará parar e iniciar a conexão manualmente.</span><span class="sxs-lookup"><span data-stu-id="51bb3-217">However, it is important to note that most applications will not need to manually stop and start the connection.</span></span> <span data-ttu-id="51bb3-218">Se seu aplicativo redireciona os usuários para uma página separada depois de fazer logon, como o comportamento padrão em um aplicativo de Web Forms ou MVC, ou atualiza a página atual após o logoff, a conexão ativa será automaticamente interrompida e não exigem nenhuma ação adicional.</span><span class="sxs-lookup"><span data-stu-id="51bb3-218">If your application redirects users to a separate page after logging out, such as the default behavior in a Web Forms application or MVC application, or refreshes the current page after logging out, the active connection is automatically disconnected and does not require any additional action.</span></span>

<span data-ttu-id="51bb3-219">O exemplo a seguir mostra como parar e iniciar uma conexão quando o status do usuário foi alterado.</span><span class="sxs-lookup"><span data-stu-id="51bb3-219">The following example shows how to stop and start a connection when the user status has changed.</span></span>

[!code-html[Main](introduction-to-security/samples/sample3.html)]

<span data-ttu-id="51bb3-220">Ou, o status de autenticação do usuário poderá ser alterado se o seu site usa a expiração deslizante com autenticação de formulários, e não há nenhuma atividade para manter o cookie de autenticação válido.</span><span class="sxs-lookup"><span data-stu-id="51bb3-220">Or, the user's authentication status may change if your site uses sliding expiration with Forms Authentication, and there is no activity to keep the authentication cookie valid.</span></span> <span data-ttu-id="51bb3-221">Nesse caso, o usuário será desconectado e o nome de usuário não corresponderá ao nome de usuário no token de conexão.</span><span class="sxs-lookup"><span data-stu-id="51bb3-221">In that case, the user will be logged out and the user name will no longer match the user name in the connection token.</span></span> <span data-ttu-id="51bb3-222">Você pode corrigir esse problema adicionando um script que periodicamente solicita um recurso no servidor web para manter o cookie de autenticação válido.</span><span class="sxs-lookup"><span data-stu-id="51bb3-222">You can fix this problem by adding some script that periodically requests a resource on the web server to keep the authentication cookie valid.</span></span> <span data-ttu-id="51bb3-223">O exemplo a seguir mostra como solicitar um recurso a cada 30 minutos.</span><span class="sxs-lookup"><span data-stu-id="51bb3-223">The following example shows how to request a resource every 30 minutes.</span></span>

[!code-javascript[Main](introduction-to-security/samples/sample4.js)]

<a id="autogen"></a>

### <a name="automatically-generated-javascript-proxy-files"></a><span data-ttu-id="51bb3-224">Arquivos de proxy JavaScript gerados automaticamente</span><span class="sxs-lookup"><span data-stu-id="51bb3-224">Automatically generated JavaScript proxy files</span></span>

<span data-ttu-id="51bb3-225">Se você não deseja incluir todos os hubs e métodos no arquivo de proxy JavaScript para cada usuário, você pode desabilitar a geração automática do arquivo.</span><span class="sxs-lookup"><span data-stu-id="51bb3-225">If you do not want to include all of the hubs and methods in the JavaScript proxy file for each user, you can disable the automatic generation of the file.</span></span> <span data-ttu-id="51bb3-226">Você pode escolher essa opção se você tiver vários hubs e métodos, mas não quiser que todos os usuários a serem consideradas todos os métodos.</span><span class="sxs-lookup"><span data-stu-id="51bb3-226">You might choose this option if you have multiple hubs and methods, but do not want every user to be aware of all of the methods.</span></span> <span data-ttu-id="51bb3-227">Desativar a geração automática, definindo **EnableJavaScriptProxies** à **falso**.</span><span class="sxs-lookup"><span data-stu-id="51bb3-227">You disable automatic generation by setting **EnableJavaScriptProxies** to **false**.</span></span>

[!code-csharp[Main](introduction-to-security/samples/sample5.cs)]

<span data-ttu-id="51bb3-228">Para obter mais informações sobre os arquivos de proxy do JavaScript, consulte [proxy gerado e o que ele faz para você](../guide-to-the-api/hubs-api-guide-javascript-client.md#genproxy).</span><span class="sxs-lookup"><span data-stu-id="51bb3-228">For more information about the JavaScript proxy files, see [The generated proxy and what it does for you](../guide-to-the-api/hubs-api-guide-javascript-client.md#genproxy).</span></span> <a id="exceptions"></a>

### <a name="exceptions"></a><span data-ttu-id="51bb3-229">Exceções</span><span class="sxs-lookup"><span data-stu-id="51bb3-229">Exceptions</span></span>

<span data-ttu-id="51bb3-230">Você deve evitar passar objetos de exceção para os clientes porque os objetos podem expor informações confidenciais para os clientes.</span><span class="sxs-lookup"><span data-stu-id="51bb3-230">You should avoid passing exception objects to clients because the objects may expose sensitive information to the clients.</span></span> <span data-ttu-id="51bb3-231">Em vez disso, chame um método no cliente que exibe a mensagem de erro relevantes.</span><span class="sxs-lookup"><span data-stu-id="51bb3-231">Instead, call a method on the client that displays the relevant error message.</span></span>

[!code-csharp[Main](introduction-to-security/samples/sample6.cs)]
