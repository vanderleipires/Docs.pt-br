---
uid: mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-vb
title: Noções básicas sobre filtros de ação (VB) | Microsoft Docs
author: microsoft
description: O objetivo deste tutorial é explicar os filtros de ação. Um filtro de ação é um atributo que você pode aplicar a uma ação do controlador – ou um controlador inteiro...
ms.author: riande
ms.date: 10/16/2008
ms.assetid: e83812f2-c53e-4a43-a7c1-d64c59ecf694
msc.legacyurl: /mvc/overview/older-versions-1/controllers-and-routing/understanding-action-filters-vb
msc.type: authoredcontent
ms.openlocfilehash: 0116306afdf21cb24a374013bb54ada54e5699ea
ms.sourcegitcommit: 45ac74e400f9f2b7dbded66297730f6f14a4eb25
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 08/16/2018
ms.locfileid: "41830505"
---
<a name="understanding-action-filters-vb"></a><span data-ttu-id="344ef-104">Noções básicas sobre filtros de ação (VB)</span><span class="sxs-lookup"><span data-stu-id="344ef-104">Understanding Action Filters (VB)</span></span>
====================
<span data-ttu-id="344ef-105">por [Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="344ef-105">by [Microsoft](https://github.com/microsoft)</span></span>

[<span data-ttu-id="344ef-106">Baixar PDF</span><span class="sxs-lookup"><span data-stu-id="344ef-106">Download PDF</span></span>](http://download.microsoft.com/download/e/f/3/ef3f2ff6-7424-48f7-bdaa-180ef64c3490/ASPNET_MVC_Tutorial_14_VB.pdf)

> <span data-ttu-id="344ef-107">O objetivo deste tutorial é explicar os filtros de ação.</span><span class="sxs-lookup"><span data-stu-id="344ef-107">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="344ef-108">Um filtro de ação é um atributo que você pode aplicar uma ação do controlador – ou um controlador inteiro – que modifica a maneira na qual a ação é executada.</span><span class="sxs-lookup"><span data-stu-id="344ef-108">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span>


## <a name="understanding-action-filters"></a><span data-ttu-id="344ef-109">Noções básicas sobre filtros de ação</span><span class="sxs-lookup"><span data-stu-id="344ef-109">Understanding Action Filters</span></span>

<span data-ttu-id="344ef-110">O objetivo deste tutorial é explicar os filtros de ação.</span><span class="sxs-lookup"><span data-stu-id="344ef-110">The goal of this tutorial is to explain action filters.</span></span> <span data-ttu-id="344ef-111">Um filtro de ação é um atributo que você pode aplicar uma ação do controlador – ou um controlador inteiro – que modifica a maneira na qual a ação é executada.</span><span class="sxs-lookup"><span data-stu-id="344ef-111">An action filter is an attribute that you can apply to a controller action -- or an entire controller -- that modifies the way in which the action is executed.</span></span> <span data-ttu-id="344ef-112">A estrutura ASP.NET MVC inclui vários filtros de ação:</span><span class="sxs-lookup"><span data-stu-id="344ef-112">The ASP.NET MVC framework includes several action filters:</span></span>

- <span data-ttu-id="344ef-113">OutputCache – esse filtro de ação armazena em cache a saída de uma ação do controlador para um período de tempo especificado.</span><span class="sxs-lookup"><span data-stu-id="344ef-113">OutputCache – This action filter caches the output of a controller action for a specified amount of time.</span></span>
- <span data-ttu-id="344ef-114">HandleError – esse filtro de ação manipula erros gerados quando uma ação do controlador é executado.</span><span class="sxs-lookup"><span data-stu-id="344ef-114">HandleError – This action filter handles errors raised when a controller action executes.</span></span>
- <span data-ttu-id="344ef-115">Autorizar – esse filtro de ação lhe permite restringir o acesso a um determinado usuário ou função.</span><span class="sxs-lookup"><span data-stu-id="344ef-115">Authorize – This action filter enables you to restrict access to a particular user or role.</span></span>

<span data-ttu-id="344ef-116">Você também pode criar seus próprios filtros de ação personalizada.</span><span class="sxs-lookup"><span data-stu-id="344ef-116">You also can create your own custom action filters.</span></span> <span data-ttu-id="344ef-117">Por exemplo, você talvez queira criar um filtro de ação personalizada para implementar um sistema de autenticação personalizada.</span><span class="sxs-lookup"><span data-stu-id="344ef-117">For example, you might want to create a custom action filter in order to implement a custom authentication system.</span></span> <span data-ttu-id="344ef-118">Ou, você talvez queira criar um filtro de ação que modifica os dados de exibição retornados por uma ação do controlador.</span><span class="sxs-lookup"><span data-stu-id="344ef-118">Or, you might want to create an action filter that modifies the view data returned by a controller action.</span></span>

<span data-ttu-id="344ef-119">Neste tutorial, você aprenderá como criar um filtro de ação desde o.</span><span class="sxs-lookup"><span data-stu-id="344ef-119">In this tutorial, you learn how to build an action filter from the ground up.</span></span> <span data-ttu-id="344ef-120">Podemos criar um filtro de ação de Log que registra os diferentes estágios do processamento de uma ação para a janela de saída do Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="344ef-120">We create a Log action filter that logs different stages of the processing of an action to the Visual Studio Output window.</span></span>

### <a name="using-an-action-filter"></a><span data-ttu-id="344ef-121">Usando um filtro de ação</span><span class="sxs-lookup"><span data-stu-id="344ef-121">Using an Action Filter</span></span>

<span data-ttu-id="344ef-122">Um filtro de ação é um atributo.</span><span class="sxs-lookup"><span data-stu-id="344ef-122">An action filter is an attribute.</span></span> <span data-ttu-id="344ef-123">Você pode aplicar a maioria dos filtros de ação para uma ação de controlador individual ou um controlador inteiro.</span><span class="sxs-lookup"><span data-stu-id="344ef-123">You can apply most action filters to either an individual controller action or an entire controller.</span></span>

<span data-ttu-id="344ef-124">Por exemplo, o controlador de dados na listagem 1 expõe uma ação chamada `Index()` que retorna a hora atual.</span><span class="sxs-lookup"><span data-stu-id="344ef-124">For example, the Data controller in Listing 1 exposes an action named `Index()` that returns the current time.</span></span> <span data-ttu-id="344ef-125">Essa ação é decorada com o `OutputCache` filtro de ação.</span><span class="sxs-lookup"><span data-stu-id="344ef-125">This action is decorated with the `OutputCache` action filter.</span></span> <span data-ttu-id="344ef-126">Esse filtro faz com que o valor retornado pela ação a ser armazenado em cache por 10 segundos.</span><span class="sxs-lookup"><span data-stu-id="344ef-126">This filter causes the value returned by the action to be cached for 10 seconds.</span></span>

<span data-ttu-id="344ef-127">**Listagem 1 – `Controllers\DataController.vb`**</span><span class="sxs-lookup"><span data-stu-id="344ef-127">**Listing 1 – `Controllers\DataController.vb`**</span></span>

[!code-vb[Main](understanding-action-filters-vb/samples/sample1.vb)]

<span data-ttu-id="344ef-128">Se você invocar repetidamente a `Index()` ação inserindo a URL/dados/índice na barra de endereços do navegador e atingir a atualização do botão várias vezes, em seguida, você verá o mesmo tempo para 10 segundos.</span><span class="sxs-lookup"><span data-stu-id="344ef-128">If you repeatedly invoke the `Index()` action by entering the URL /Data/Index into the address bar of your browser and hitting the Refresh button multiple times, then you will see the same time for 10 seconds.</span></span> <span data-ttu-id="344ef-129">A saída a `Index()` ação é armazenado em cache por 10 segundos (veja a Figura 1).</span><span class="sxs-lookup"><span data-stu-id="344ef-129">The output of the `Index()` action is cached for 10 seconds (see Figure 1).</span></span>


<span data-ttu-id="344ef-130">[![Tempo em cache](understanding-action-filters-vb/_static/image2.png)](understanding-action-filters-vb/_static/image1.png)</span><span class="sxs-lookup"><span data-stu-id="344ef-130">[![Cached time](understanding-action-filters-vb/_static/image2.png)](understanding-action-filters-vb/_static/image1.png)</span></span>

<span data-ttu-id="344ef-131">**Figura 01**: armazenado em cache o tempo ([clique para exibir a imagem em tamanho normal](understanding-action-filters-vb/_static/image3.png))</span><span class="sxs-lookup"><span data-stu-id="344ef-131">**Figure 01**: Cached time ([Click to view full-size image](understanding-action-filters-vb/_static/image3.png))</span></span>


<span data-ttu-id="344ef-132">Na listagem 1, um filtro de ação única – o `OutputCache` filtro de ação – é aplicada para o `Index()` método.</span><span class="sxs-lookup"><span data-stu-id="344ef-132">In Listing 1, a single action filter – the `OutputCache` action filter – is applied to the `Index()` method.</span></span> <span data-ttu-id="344ef-133">Se você precisar, você pode aplicar vários filtros de ação para a mesma ação.</span><span class="sxs-lookup"><span data-stu-id="344ef-133">If you need, you can apply multiple action filters to the same action.</span></span> <span data-ttu-id="344ef-134">Por exemplo, você pode querer aplicar ambas as `OutputCache` e `HandleError` filtros de ação para a mesma ação.</span><span class="sxs-lookup"><span data-stu-id="344ef-134">For example, you might want to apply both the `OutputCache` and `HandleError` action filters to the same action.</span></span>

<span data-ttu-id="344ef-135">Na listagem 1, o `OutputCache` ação de filtro é aplicada a `Index()` ação.</span><span class="sxs-lookup"><span data-stu-id="344ef-135">In Listing 1, the `OutputCache` action filter is applied to the `Index()` action.</span></span> <span data-ttu-id="344ef-136">Você também pode aplicar esse atributo para o `DataController` própria classe.</span><span class="sxs-lookup"><span data-stu-id="344ef-136">You also could apply this attribute to the `DataController` class itself.</span></span> <span data-ttu-id="344ef-137">Nesse caso, o resultado retornado por qualquer ação exposta pelo controlador deve ser armazenado em cache por 10 segundos.</span><span class="sxs-lookup"><span data-stu-id="344ef-137">In that case, the result returned by any action exposed by the controller would be cached for 10 seconds.</span></span>

### <a name="the-different-types-of-filters"></a><span data-ttu-id="344ef-138">Os diferentes tipos de filtros</span><span class="sxs-lookup"><span data-stu-id="344ef-138">The Different Types of Filters</span></span>

<span data-ttu-id="344ef-139">O ASP.NET MVC framework oferece suporte a quatro tipos diferentes de filtros:</span><span class="sxs-lookup"><span data-stu-id="344ef-139">The ASP.NET MVC framework supports four different types of filters:</span></span>

1. <span data-ttu-id="344ef-140">Filtros de autorização – implementa o `IAuthorizationFilter` atributo.</span><span class="sxs-lookup"><span data-stu-id="344ef-140">Authorization filters – Implements the `IAuthorizationFilter` attribute.</span></span>
2. <span data-ttu-id="344ef-141">Filtros de ação – implementa o `IActionFilter` atributo.</span><span class="sxs-lookup"><span data-stu-id="344ef-141">Action filters – Implements the `IActionFilter` attribute.</span></span>
3. <span data-ttu-id="344ef-142">Filtros – implementa de resultado a `IResultFilter` atributo.</span><span class="sxs-lookup"><span data-stu-id="344ef-142">Result filters – Implements the `IResultFilter` attribute.</span></span>
4. <span data-ttu-id="344ef-143">Filtros de exceção – implementa o `IExceptionFilter` atributo.</span><span class="sxs-lookup"><span data-stu-id="344ef-143">Exception filters – Implements the `IExceptionFilter` attribute.</span></span>

<span data-ttu-id="344ef-144">Filtros são executados na ordem listada acima.</span><span class="sxs-lookup"><span data-stu-id="344ef-144">Filters are executed in the order listed above.</span></span> <span data-ttu-id="344ef-145">Por exemplo, filtros de autorização são sempre executados antes dos filtros de ação e os filtros de exceção são sempre executados após todos os outros tipos de filtro.</span><span class="sxs-lookup"><span data-stu-id="344ef-145">For example, authorization filters are always executed before action filters and exception filters are always executed after every other type of filter.</span></span>

<span data-ttu-id="344ef-146">Filtros de autorização são usados para implementar a autenticação e autorização para ações do controlador.</span><span class="sxs-lookup"><span data-stu-id="344ef-146">Authorization filters are used to implement authentication and authorization for controller actions.</span></span> <span data-ttu-id="344ef-147">Por exemplo, o filtro Authorize é um exemplo de um filtro de autorização.</span><span class="sxs-lookup"><span data-stu-id="344ef-147">For example, the Authorize filter is an example of an Authorization filter.</span></span>

<span data-ttu-id="344ef-148">Filtros de ação contêm a lógica que é executada antes e depois executa uma ação do controlador.</span><span class="sxs-lookup"><span data-stu-id="344ef-148">Action filters contain logic that is executed before and after a controller action executes.</span></span> <span data-ttu-id="344ef-149">Você pode usar um filtro de ação, por exemplo, para modificar os dados de exibição que retorna uma ação do controlador.</span><span class="sxs-lookup"><span data-stu-id="344ef-149">You can use an action filter, for instance, to modify the view data that a controller action returns.</span></span>

<span data-ttu-id="344ef-150">Filtros de resultado contêm a lógica que é executada antes e depois que um resultado de exibição é executado.</span><span class="sxs-lookup"><span data-stu-id="344ef-150">Result filters contain logic that is executed before and after a view result is executed.</span></span> <span data-ttu-id="344ef-151">Por exemplo, você talvez queira modificar o modo de exibição resultado imediatamente antes que a exibição é renderizada no navegador.</span><span class="sxs-lookup"><span data-stu-id="344ef-151">For example, you might want to modify a view result right before the view is rendered to the browser.</span></span>

<span data-ttu-id="344ef-152">Filtros de exceção são o último tipo de filtro a ser executado.</span><span class="sxs-lookup"><span data-stu-id="344ef-152">Exception filters are the last type of filter to run.</span></span> <span data-ttu-id="344ef-153">Você pode usar um filtro de exceção para tratar erros gerados por seus resultados de ação do controlador ou ações do controlador.</span><span class="sxs-lookup"><span data-stu-id="344ef-153">You can use an exception filter to handle errors raised by either your controller actions or controller action results.</span></span> <span data-ttu-id="344ef-154">Você também pode usar filtros de exceção para erros de log.</span><span class="sxs-lookup"><span data-stu-id="344ef-154">You also can use exception filters to log errors.</span></span>

<span data-ttu-id="344ef-155">Todos os diferentes tipos de filtro é executado em uma ordem específica.</span><span class="sxs-lookup"><span data-stu-id="344ef-155">Each different type of filter is executed in a particular order.</span></span> <span data-ttu-id="344ef-156">Se você quiser controlar a ordem na qual os filtros do mesmo tipo são executados, em seguida, você pode definir a propriedade de ordem de um filtro.</span><span class="sxs-lookup"><span data-stu-id="344ef-156">If you want to control the order in which filters of the same type are executed then you can set a filter's Order property.</span></span>

<span data-ttu-id="344ef-157">A classe base para todos os filtros de ação é o `System.Web.Mvc.FilterAttribute` classe.</span><span class="sxs-lookup"><span data-stu-id="344ef-157">The base class for all action filters is the `System.Web.Mvc.FilterAttribute` class.</span></span> <span data-ttu-id="344ef-158">Se você quiser implementar um determinado tipo de filtro, em seguida, você precisará criar uma classe que herda da classe base do filtro e implementa uma ou mais do IAuthorizationFilter, IActionFilter e IResultFilter, ou interfaces de ExceptionFilter.</span><span class="sxs-lookup"><span data-stu-id="344ef-158">If you want to implement a particular type of filter, then you need to create a class that inherits from the base Filter class and implements one or more of the IAuthorizationFilter, IActionFilter, IResultFilter, or ExceptionFilter interfaces.</span></span>

### <a name="the-base-actionfilterattribute-class"></a><span data-ttu-id="344ef-159">A classe de ActionFilterAttribute Base</span><span class="sxs-lookup"><span data-stu-id="344ef-159">The Base ActionFilterAttribute Class</span></span>

<span data-ttu-id="344ef-160">Para tornar mais fácil de implementar um filtro de ação personalizada, a estrutura ASP.NET MVC inclui uma base de `ActionFilterAttribute` classe.</span><span class="sxs-lookup"><span data-stu-id="344ef-160">In order to make it easier for you to implement a custom action filter, the ASP.NET MVC framework includes a base `ActionFilterAttribute` class.</span></span> <span data-ttu-id="344ef-161">Essa classe implementa ambos os `IActionFilter` e `IResultFilter` interfaces e herda o `Filter` classe.</span><span class="sxs-lookup"><span data-stu-id="344ef-161">This class implements both the `IActionFilter` and `IResultFilter` interfaces and inherits from the `Filter` class.</span></span>

<span data-ttu-id="344ef-162">A terminologia usada aqui não é totalmente consistente.</span><span class="sxs-lookup"><span data-stu-id="344ef-162">The terminology here is not entirely consistent.</span></span> <span data-ttu-id="344ef-163">Tecnicamente, uma classe que herda da classe ActionFilterAttribute é um filtro de ação e um filtro de resultado.</span><span class="sxs-lookup"><span data-stu-id="344ef-163">Technically, a class that inherits from the ActionFilterAttribute class is both an action filter and a result filter.</span></span> <span data-ttu-id="344ef-164">No entanto, no sentido flexível, o filtro de ação do word é usado para se referir a qualquer tipo de filtro na estrutura do ASP.NET MVC.</span><span class="sxs-lookup"><span data-stu-id="344ef-164">However, in the loose sense, the word action filter is used to refer to any type of filter in the ASP.NET MVC framework.</span></span>

<span data-ttu-id="344ef-165">A classe de ActionFilterAttribute base tem os seguintes métodos que você pode substituir:</span><span class="sxs-lookup"><span data-stu-id="344ef-165">The base ActionFilterAttribute class has the following methods that you can override:</span></span>

- <span data-ttu-id="344ef-166">OnActionExecuting – esse método é chamado antes de uma ação do controlador é executada.</span><span class="sxs-lookup"><span data-stu-id="344ef-166">OnActionExecuting – This method is called before a controller action is executed.</span></span>
- <span data-ttu-id="344ef-167">OnActionExecuted – esse método é chamado depois que uma ação do controlador é executada.</span><span class="sxs-lookup"><span data-stu-id="344ef-167">OnActionExecuted – This method is called after a controller action is executed.</span></span>
- <span data-ttu-id="344ef-168">OnResultExecuting – esse método é chamado antes de um resultado de ação do controlador é executado.</span><span class="sxs-lookup"><span data-stu-id="344ef-168">OnResultExecuting – This method is called before a controller action result is executed.</span></span>
- <span data-ttu-id="344ef-169">OnResultExecuted – esse método é chamado depois que um resultado de ação do controlador é executado.</span><span class="sxs-lookup"><span data-stu-id="344ef-169">OnResultExecuted – This method is called after a controller action result is executed.</span></span>

<span data-ttu-id="344ef-170">Na próxima seção, veremos como você pode implementar cada um desses métodos diferentes.</span><span class="sxs-lookup"><span data-stu-id="344ef-170">In the next section, we'll see how you can implement each of these different methods.</span></span>

### <a name="creating-a-log-action-filter"></a><span data-ttu-id="344ef-171">Criando um filtro de ação do Log</span><span class="sxs-lookup"><span data-stu-id="344ef-171">Creating a Log Action Filter</span></span>

<span data-ttu-id="344ef-172">Para ilustrar como você pode criar um filtro de ação personalizada, vamos criar um filtro de ação personalizada que registra em log os estágios do processamento de uma ação do controlador para a janela de saída do Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="344ef-172">In order to illustrate how you can build a custom action filter, we'll create a custom action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span> <span data-ttu-id="344ef-173">Nosso `LogActionFilter` está contida na listagem 2.</span><span class="sxs-lookup"><span data-stu-id="344ef-173">Our `LogActionFilter` is contained in Listing 2.</span></span>

<span data-ttu-id="344ef-174">**Listagem 2 – `ActionFilters\LogActionFilter.vb`**</span><span class="sxs-lookup"><span data-stu-id="344ef-174">**Listing 2 – `ActionFilters\LogActionFilter.vb`**</span></span>

[!code-vb[Main](understanding-action-filters-vb/samples/sample2.vb)]

<span data-ttu-id="344ef-175">Na listagem 2, o `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`, e `OnResultExecuted()` todos os métodos de chamar o `Log()` método.</span><span class="sxs-lookup"><span data-stu-id="344ef-175">In Listing 2, the `OnActionExecuting()`, `OnActionExecuted()`, `OnResultExecuting()`, and `OnResultExecuted()` methods all call the `Log()` method.</span></span> <span data-ttu-id="344ef-176">O nome do método e os dados de rota atuais é passado para o `Log()` método.</span><span class="sxs-lookup"><span data-stu-id="344ef-176">The name of the method and the current route data is passed to the `Log()` method.</span></span> <span data-ttu-id="344ef-177">O `Log()` método grava uma mensagem na janela de saída do Visual Studio (veja a Figura 2).</span><span class="sxs-lookup"><span data-stu-id="344ef-177">The `Log()` method writes a message to the Visual Studio Output window (see Figure 2).</span></span>


<span data-ttu-id="344ef-178">[![Escrever na janela de saída do Visual Studio](understanding-action-filters-vb/_static/image5.png)](understanding-action-filters-vb/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="344ef-178">[![Writing to the Visual Studio Output window](understanding-action-filters-vb/_static/image5.png)](understanding-action-filters-vb/_static/image4.png)</span></span>

<span data-ttu-id="344ef-179">**Figura 02**: escrevendo para a janela de saída do Visual Studio ([clique para exibir a imagem em tamanho normal](understanding-action-filters-vb/_static/image6.png))</span><span class="sxs-lookup"><span data-stu-id="344ef-179">**Figure 02**: Writing to the Visual Studio Output window ([Click to view full-size image](understanding-action-filters-vb/_static/image6.png))</span></span>


<span data-ttu-id="344ef-180">O controlador Home na listagem 3 ilustra como você pode aplicar o filtro de ação do Log para uma classe de controlador inteiro.</span><span class="sxs-lookup"><span data-stu-id="344ef-180">The Home controller in Listing 3 illustrates how you can apply the Log action filter to an entire controller class.</span></span> <span data-ttu-id="344ef-181">Sempre que qualquer uma das ações expostas pelo controlador Home são invocados – ou o `Index()` método ou o `About()` método – os estágios do processamento de ação são registradas para a janela de saída do Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="344ef-181">Whenever any of the actions exposed by the Home controller are invoked – either the `Index()` method or the `About()` method – the stages of processing the action are logged to the Visual Studio Output window.</span></span>

<span data-ttu-id="344ef-182">**Listagem 3 – `Controllers\HomeController.vb`**</span><span class="sxs-lookup"><span data-stu-id="344ef-182">**Listing 3 – `Controllers\HomeController.vb`**</span></span>

[!code-vb[Main](understanding-action-filters-vb/samples/sample3.vb)]

### <a name="summary"></a><span data-ttu-id="344ef-183">Resumo</span><span class="sxs-lookup"><span data-stu-id="344ef-183">Summary</span></span>

<span data-ttu-id="344ef-184">Neste tutorial, você foi apresentado para filtros de ação do ASP.NET MVC.</span><span class="sxs-lookup"><span data-stu-id="344ef-184">In this tutorial, you were introduced to ASP.NET MVC action filters.</span></span> <span data-ttu-id="344ef-185">Você aprendeu sobre os quatro tipos diferentes de filtros: filtros de autorização, filtros de ação, filtros de resultado e filtros de exceção.</span><span class="sxs-lookup"><span data-stu-id="344ef-185">You learned about the four different types of filters: authorization filters, action filters, result filters, and exception filters.</span></span> <span data-ttu-id="344ef-186">Você também aprendeu sobre a base `ActionFilterAttribute` classe.</span><span class="sxs-lookup"><span data-stu-id="344ef-186">You also learned about the base `ActionFilterAttribute` class.</span></span>

<span data-ttu-id="344ef-187">Por fim, você aprendeu como implementar um filtro de ação simples.</span><span class="sxs-lookup"><span data-stu-id="344ef-187">Finally, you learned how to implement a simple action filter.</span></span> <span data-ttu-id="344ef-188">Criamos um filtro de ação de Log que registra em log os estágios do processamento de uma ação do controlador para a janela de saída do Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="344ef-188">We created a Log action filter that logs the stages of processing a controller action to the Visual Studio Output window.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="344ef-189">[Anterior](asp-net-mvc-routing-overview-vb.md)
> [Próximo](improving-performance-with-output-caching-vb.md)</span><span class="sxs-lookup"><span data-stu-id="344ef-189">[Previous](asp-net-mvc-routing-overview-vb.md)
[Next](improving-performance-with-output-caching-vb.md)</span></span>
