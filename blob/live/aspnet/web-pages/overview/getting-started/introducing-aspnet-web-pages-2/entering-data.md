---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/entering-data
title: "Introdução a páginas da Web ASP.NET - inserindo dados de banco de dados usando formulários | Microsoft Docs"
author: tfitzmac
description: "Este tutorial mostra como criar um formulário de entrada e, em seguida, insira os dados que você obtém do formulário em uma tabela de banco de dados quando você usa o ASP.NET Web Pages (..."
ms.author: aspnetcontent
manager: wpickett
ms.date: 05/28/2015
ms.topic: article
ms.assetid: d37c93fc-25fd-4e94-8671-0d437beef206
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/entering-data
msc.type: authoredcontent
ms.openlocfilehash: b74eecb16b2c4695bb417816b90f701f724cc9d0
ms.sourcegitcommit: 9a9483aceb34591c97451997036a9120c3fe2baf
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 11/10/2017
---
<a name="introducing-aspnet-web-pages---entering-database-data-by-using-forms"></a><span data-ttu-id="000f9-103">Introdução a páginas da Web ASP.NET - inserindo dados de banco de dados usando formulários</span><span class="sxs-lookup"><span data-stu-id="000f9-103">Introducing ASP.NET Web Pages - Entering Database Data by Using Forms</span></span>
====================
<span data-ttu-id="000f9-104">por [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="000f9-104">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="000f9-105">Este tutorial mostra como criar um formulário de entrada e, em seguida, insira os dados que você obtém do formulário em uma tabela de banco de dados quando você usa páginas da Web do ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="000f9-105">This tutorial shows you how to create an entry form and then enter the data that you get from the form into a database table when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="000f9-106">Ele pressupõe que você tenha concluído a série por meio de [Noções básicas de formulários de HTML em páginas da Web do ASP.NET](https://go.microsoft.com/fwlink/?LinkId=251581).</span><span class="sxs-lookup"><span data-stu-id="000f9-106">It assumes you have completed the series through [Basics of HTML Forms in ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=251581).</span></span>
> 
> <span data-ttu-id="000f9-107">O que você aprenderá:</span><span class="sxs-lookup"><span data-stu-id="000f9-107">What you'll learn:</span></span>
> 
> - <span data-ttu-id="000f9-108">Mais informações sobre como processar formulários de entrada.</span><span class="sxs-lookup"><span data-stu-id="000f9-108">More about how to process entry forms.</span></span>
> - <span data-ttu-id="000f9-109">Como adicionar (inserir) dados em um banco de dados.</span><span class="sxs-lookup"><span data-stu-id="000f9-109">How to add (insert) data in a database.</span></span>
> - <span data-ttu-id="000f9-110">Como certificar-se de que os usuários tem inserido um valor obrigatório em um formulário (como validar a entrada do usuário).</span><span class="sxs-lookup"><span data-stu-id="000f9-110">How to make sure that users have entered a required value in a form (how to validate user input).</span></span>
> - <span data-ttu-id="000f9-111">Como exibir erros de validação.</span><span class="sxs-lookup"><span data-stu-id="000f9-111">How to display validation errors.</span></span>
> - <span data-ttu-id="000f9-112">Como saltar para outra página da página atual.</span><span class="sxs-lookup"><span data-stu-id="000f9-112">How to jump to another page from the current page.</span></span>
>   
> 
> <span data-ttu-id="000f9-113">Recursos/tecnologias abordadas:</span><span class="sxs-lookup"><span data-stu-id="000f9-113">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="000f9-114">O método `Database.Execute`.</span><span class="sxs-lookup"><span data-stu-id="000f9-114">The `Database.Execute` method.</span></span>
> - <span data-ttu-id="000f9-115">O SQL `Insert Into` instrução</span><span class="sxs-lookup"><span data-stu-id="000f9-115">The SQL `Insert Into` statement</span></span>
> - <span data-ttu-id="000f9-116">O `Validation` auxiliar.</span><span class="sxs-lookup"><span data-stu-id="000f9-116">The `Validation` helper.</span></span>
> - <span data-ttu-id="000f9-117">O método `Response.Redirect`.</span><span class="sxs-lookup"><span data-stu-id="000f9-117">The `Response.Redirect` method.</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="000f9-118">O que você vai criar</span><span class="sxs-lookup"><span data-stu-id="000f9-118">What You'll Build</span></span>

<span data-ttu-id="000f9-119">No tutorial anterior que mostrou como criar um banco de dados, você inseriu dados do banco de dados editando o banco de dados diretamente no WebMatrix, trabalhando no **banco de dados** espaço de trabalho.</span><span class="sxs-lookup"><span data-stu-id="000f9-119">In the tutorial earlier that showed you how to create a database, you entered database data by editing the database directly in WebMatrix, working in the **Database** workspace.</span></span> <span data-ttu-id="000f9-120">Na maioria dos aplicativos, que não é uma maneira prática de colocar dados no banco de dados, embora.</span><span class="sxs-lookup"><span data-stu-id="000f9-120">In most apps, that's not a practical way to put data into the database, though.</span></span> <span data-ttu-id="000f9-121">Portanto, neste tutorial, você criará uma interface baseada na web que permite que você ou qualquer pessoa que inserir dados e salvá-lo no banco de dados.</span><span class="sxs-lookup"><span data-stu-id="000f9-121">So in this tutorial, you'll create a web-based interface that lets you or anyone enter data and save it to the database.</span></span>

<span data-ttu-id="000f9-122">Você criará uma página onde você pode inserir novos filmes.</span><span class="sxs-lookup"><span data-stu-id="000f9-122">You'll create a page where you can enter new movies.</span></span> <span data-ttu-id="000f9-123">A página conterá um formulário de entrada que tem campos (caixas de texto) de onde você pode inserir um título do filme, gênero e ano.</span><span class="sxs-lookup"><span data-stu-id="000f9-123">The page will contain an entry form that has fields (text boxes) where you can enter a movie title, genre, and year.</span></span> <span data-ttu-id="000f9-124">A página será semelhante a esta página:</span><span class="sxs-lookup"><span data-stu-id="000f9-124">The page will look like this page:</span></span>

!['Adicionar filme' page no navegador](entering-data/_static/image1.png)

<span data-ttu-id="000f9-126">As caixas de texto será HTML `<input>` elementos parecerá essa marcação, como:</span><span class="sxs-lookup"><span data-stu-id="000f9-126">The text boxes will be HTML `<input>` elements that will look like this markup:</span></span>

`<input type="text" name="genre" value="" />`

## <a name="creating-the-basic-entry-form"></a><span data-ttu-id="000f9-127">Criando o formulário de entrada básico</span><span class="sxs-lookup"><span data-stu-id="000f9-127">Creating the Basic Entry Form</span></span>

<span data-ttu-id="000f9-128">Crie uma página chamada *AddMovie.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="000f9-128">Create a page named *AddMovie.cshtml*.</span></span>

<span data-ttu-id="000f9-129">Substitua o que está no arquivo com a seguinte marcação.</span><span class="sxs-lookup"><span data-stu-id="000f9-129">Replace what's in the file with the following markup.</span></span> <span data-ttu-id="000f9-130">Substituir tudo. Você adicionará um bloco de código na parte superior em breve.</span><span class="sxs-lookup"><span data-stu-id="000f9-130">Overwrite everything; you'll add a code block at the top shortly.</span></span>

[!code-cshtml[Main](entering-data/samples/sample1.cshtml)]

<span data-ttu-id="000f9-131">Este exemplo mostra o HTML típico para a criação de um formulário.</span><span class="sxs-lookup"><span data-stu-id="000f9-131">This example shows typical HTML for creating a form.</span></span> <span data-ttu-id="000f9-132">Ele usa `<input>` elementos para as caixas de texto e no botão Enviar.</span><span class="sxs-lookup"><span data-stu-id="000f9-132">It uses `<input>` elements for the text boxes and for the submit button.</span></span> <span data-ttu-id="000f9-133">As legendas para caixas de texto são criadas usando o padrão `<label>` elementos.</span><span class="sxs-lookup"><span data-stu-id="000f9-133">The captions for the text boxes are created by using standard `<label>` elements.</span></span> <span data-ttu-id="000f9-134">O `<fieldset>` e `<legend>` elementos colocar uma caixa adequada ao redor do formulário.</span><span class="sxs-lookup"><span data-stu-id="000f9-134">The `<fieldset>` and `<legend>` elements put a nice box around the form.</span></span>

<span data-ttu-id="000f9-135">Observe que nessa página, o `<form>` elemento usa `post` como o valor para o `method` atributo.</span><span class="sxs-lookup"><span data-stu-id="000f9-135">Notice that in this page, the `<form>` element uses `post` as the value for the `method` attribute.</span></span> <span data-ttu-id="000f9-136">No tutorial anterior, você criou um formulário que é usado o `get` método.</span><span class="sxs-lookup"><span data-stu-id="000f9-136">In the previous tutorial, you created a form that used the `get` method.</span></span> <span data-ttu-id="000f9-137">Isso é correto, porque embora valores para o servidor de enviar o formulário, a solicitação não fez alterações.</span><span class="sxs-lookup"><span data-stu-id="000f9-137">That was correct, because although the form submitted values to the server, the request did not make any changes.</span></span> <span data-ttu-id="000f9-138">Tudo o que foi buscar dados de maneiras diferentes.</span><span class="sxs-lookup"><span data-stu-id="000f9-138">All it did was fetch data in different ways.</span></span> <span data-ttu-id="000f9-139">No entanto, nesta página que você *será* fazer alterações, você vai adicionar novos registros de banco de dados.</span><span class="sxs-lookup"><span data-stu-id="000f9-139">However, in this page you *will* make changes—you're going to add new database records.</span></span> <span data-ttu-id="000f9-140">Portanto, este formulário deve usar o `post` método.</span><span class="sxs-lookup"><span data-stu-id="000f9-140">Therefore, this form should use the `post` method.</span></span> <span data-ttu-id="000f9-141">(Para obter mais informações sobre a diferença entre `GET` e `POST` operações, consulte o[GET, POST e HTTP verbo segurança](https://go.microsoft.com/fwlink/?LinkId=251581#GET,_POST,_and_HTTP_Verb_Safety) lateral no tutorial anterior.)</span><span class="sxs-lookup"><span data-stu-id="000f9-141">(For more about the difference between `GET` and `POST` operations, see the[GET, POST, and HTTP Verb Safety](https://go.microsoft.com/fwlink/?LinkId=251581#GET,_POST,_and_HTTP_Verb_Safety) sidebar in the previous tutorial.)</span></span>

<span data-ttu-id="000f9-142">Observe que cada caixa de texto tem uma `name` elemento (`title`, `genre`, `year`).</span><span class="sxs-lookup"><span data-stu-id="000f9-142">Note that each text box has a `name` element (`title`, `genre`, `year`).</span></span> <span data-ttu-id="000f9-143">Como você viu no tutorial anterior, esses nomes são importantes, porque você deve ter esses nomes para que você possa obter a entrada do usuário mais tarde.</span><span class="sxs-lookup"><span data-stu-id="000f9-143">As you saw in the previous tutorial, these names are important because you must have those names so you can get the user's input later.</span></span> <span data-ttu-id="000f9-144">Você pode usar qualquer nome.</span><span class="sxs-lookup"><span data-stu-id="000f9-144">You can use any names.</span></span> <span data-ttu-id="000f9-145">É útil usar nomes significativos que ajudam você lembre-se os dados que você está trabalhando.</span><span class="sxs-lookup"><span data-stu-id="000f9-145">It's helpful to use meaningful names that help you remember what data you're working with.</span></span>

<span data-ttu-id="000f9-146">O `value` atributo de cada `<input>` elemento contém um trecho de código do Razor (por exemplo, `Request.Form["title"]`).</span><span class="sxs-lookup"><span data-stu-id="000f9-146">The `value` attribute of each `<input>` element contains a bit of Razor code (for example, `Request.Form["title"]`).</span></span> <span data-ttu-id="000f9-147">Você aprendeu uma versão desse truque no tutorial anterior para preservar o valor inserido na caixa de texto (se houver) depois que o formulário é enviado.</span><span class="sxs-lookup"><span data-stu-id="000f9-147">You learned a version of this trick in the previous tutorial to preserve the value entered into the text box (if any) after the form has been submitted.</span></span>

## <a name="getting-the-form-values"></a><span data-ttu-id="000f9-148">Obter os valores de formulário</span><span class="sxs-lookup"><span data-stu-id="000f9-148">Getting the Form Values</span></span>

<span data-ttu-id="000f9-149">Em seguida, você adiciona o código que processa o formulário.</span><span class="sxs-lookup"><span data-stu-id="000f9-149">Next, you add code that processes the form.</span></span> <span data-ttu-id="000f9-150">Na estrutura de tópicos, você fará o seguinte:</span><span class="sxs-lookup"><span data-stu-id="000f9-150">In outline, you'll do the following:</span></span>

1. <span data-ttu-id="000f9-151">Verifique se a página está sendo lançada (enviado).</span><span class="sxs-lookup"><span data-stu-id="000f9-151">Check whether the page is being posted (was submitted).</span></span> <span data-ttu-id="000f9-152">Você deseja para seu código executar somente quando os usuários clicaram no botão, não quando a página é executada.</span><span class="sxs-lookup"><span data-stu-id="000f9-152">You want to your code to run only when users have clicked the button, not when the page first runs.</span></span>
2. <span data-ttu-id="000f9-153">Obter os valores que o usuário inseriu nas caixas de texto.</span><span class="sxs-lookup"><span data-stu-id="000f9-153">Get the values that the user entered into the text boxes.</span></span> <span data-ttu-id="000f9-154">Nesse caso, porque o formulário está usando o `POST` verbo, você obtém os valores de formulário do `Request.Form` coleção.</span><span class="sxs-lookup"><span data-stu-id="000f9-154">In this case, because the form is using the `POST` verb, you get the form values from the `Request.Form` collection.</span></span>
3. <span data-ttu-id="000f9-155">Insira os valores como um novo registro no *filmes* tabela de banco de dados.</span><span class="sxs-lookup"><span data-stu-id="000f9-155">Insert the values as a new record in the *Movies* database table.</span></span>

<span data-ttu-id="000f9-156">Na parte superior do arquivo, adicione o seguinte código:</span><span class="sxs-lookup"><span data-stu-id="000f9-156">At the top of the file, add the following code:</span></span>

[!code-cshtml[Main](entering-data/samples/sample2.cshtml)]

<span data-ttu-id="000f9-157">As primeiras linhas criam variáveis (`title`, `genre`, e `year`) para manter os valores de caixas de texto.</span><span class="sxs-lookup"><span data-stu-id="000f9-157">The first few lines create variables (`title`, `genre`, and `year`) to hold the values from the text boxes.</span></span> <span data-ttu-id="000f9-158">A linha `if(IsPost)` certifica-se de que as variáveis são definidas *somente* quando os usuários clicarem o **Adicionar filme** botão — ou seja, quando o formulário foi lançado.</span><span class="sxs-lookup"><span data-stu-id="000f9-158">The line `if(IsPost)` makes sure that the variables are set *only* when users click the **Add Movie** button — that is, when the form has been posted.</span></span>

<span data-ttu-id="000f9-159">Como você viu um tutorial anterior, você obter o valor de uma caixa de texto usando uma expressão como `Request.Form["name"]`, onde *nome* é o nome do `<input>` elemento.</span><span class="sxs-lookup"><span data-stu-id="000f9-159">As you saw in an earlier tutorial, you get the value of a text box by using an expression like `Request.Form["name"]`, where *name* is the name of the `<input>` element.</span></span>

<span data-ttu-id="000f9-160">Os nomes das variáveis (`title`, `genre`, e `year`) são arbitrários.</span><span class="sxs-lookup"><span data-stu-id="000f9-160">The names of the variables (`title`, `genre`, and `year`) are arbitrary.</span></span> <span data-ttu-id="000f9-161">Como os nomes que você atribui a `<input>` elementos, você pode chamá-los que desejar.</span><span class="sxs-lookup"><span data-stu-id="000f9-161">Like the names that you assign to `<input>` elements, you can call them anything you like.</span></span> <span data-ttu-id="000f9-162">(Os nomes das variáveis não precisam coincidir com os atributos de nome de `<input>` elementos no formulário.) Mas, assim como acontece com o `<input>` elementos, é uma boa ideia usar nomes de variáveis que refletem os dados que eles contêm.</span><span class="sxs-lookup"><span data-stu-id="000f9-162">(The names of the variables don't have to match the name attributes of `<input>` elements on the form.) But as with the `<input>` elements, it's a good idea to use variable names that reflect the data that they contain.</span></span> <span data-ttu-id="000f9-163">Quando você escreve código, nomes consistentes torná-lo mais fácil de lembrar os dados que você está trabalhando.</span><span class="sxs-lookup"><span data-stu-id="000f9-163">When you write code, consistent names make it easier for you to remember what data you're working with.</span></span>

## <a name="adding-data-to-the-database"></a><span data-ttu-id="000f9-164">Adicionando dados ao banco de dados</span><span class="sxs-lookup"><span data-stu-id="000f9-164">Adding Data to the Database</span></span>

<span data-ttu-id="000f9-165">No código do bloco você recém-adicionada, apenas *dentro de* a chave de fechamento ( `}` ) da `if` bloco (não apenas dentro do bloco de código), adicione o seguinte código:</span><span class="sxs-lookup"><span data-stu-id="000f9-165">In the code block you just added, just *inside* the closing brace ( `}` ) of the `if` block (not just inside the code block), add the following code:</span></span>

[!code-csharp[Main](entering-data/samples/sample3.cs)]

<span data-ttu-id="000f9-166">Este exemplo é semelhante ao código usado em um tutorial anterior para busca e exibir os dados.</span><span class="sxs-lookup"><span data-stu-id="000f9-166">This example is similar to the code you used in a previous tutorial to fetch and display data.</span></span> <span data-ttu-id="000f9-167">A linha que começa com `db =` abre o banco de dados, como antes e a próxima linha define uma instrução SQL, novamente como você viu antes.</span><span class="sxs-lookup"><span data-stu-id="000f9-167">The line that starts with `db =` opens the database, like before, and the next line defines a SQL statement, again as you saw before.</span></span> <span data-ttu-id="000f9-168">No entanto, neste momento ele define um SQL `Insert Into` instrução.</span><span class="sxs-lookup"><span data-stu-id="000f9-168">However, this time it defines a SQL `Insert Into` statement.</span></span> <span data-ttu-id="000f9-169">O exemplo a seguir mostra a sintaxe geral do `Insert Into` instrução:</span><span class="sxs-lookup"><span data-stu-id="000f9-169">The following example shows the general syntax of the `Insert Into` statement:</span></span>

`INSERT INTO table (column1, column2, column3, ...) VALUES (value1, value2, value3, ...)`

<span data-ttu-id="000f9-170">Em outras palavras, você especificar a tabela para inserir, lista as colunas para inserir e, em seguida, lista de valores a serem inseridos.</span><span class="sxs-lookup"><span data-stu-id="000f9-170">In other words, you specify the table to insert into, then list the columns to insert into, and then list the values to insert.</span></span> <span data-ttu-id="000f9-171">(Conforme observado anteriormente, o SQL não diferencia maiusculas de minúsculas, mas algumas pessoas aproveitar as palavras-chave para facilitar a leitura do comando.)</span><span class="sxs-lookup"><span data-stu-id="000f9-171">(As noted before, SQL is not case sensitive but some people capitalize the keywords to make it easier to read the command.)</span></span>

<span data-ttu-id="000f9-172">As colunas que você está inserindo já estão listadas no comando — `(Title, Genre, Year)`.</span><span class="sxs-lookup"><span data-stu-id="000f9-172">The columns that you're inserting into are already listed in the command — `(Title, Genre, Year)`.</span></span> <span data-ttu-id="000f9-173">A parte interessante é como obter os valores de caixas de texto para o `VALUES` faz parte do comando.</span><span class="sxs-lookup"><span data-stu-id="000f9-173">The interesting part is how you get the values from the text boxes into the `VALUES` part of the command.</span></span> <span data-ttu-id="000f9-174">Em vez de valores reais, você verá `@0`, `@1`, e `@2`, é claro que são espaços reservados.</span><span class="sxs-lookup"><span data-stu-id="000f9-174">Instead of actual values, you see `@0`, `@1`, and `@2`, which are of course placeholders.</span></span> <span data-ttu-id="000f9-175">Quando você executar o comando (no `db.Execute` linha), você passa os valores que você obteve as caixas de texto.</span><span class="sxs-lookup"><span data-stu-id="000f9-175">When you run the command (on the `db.Execute` line), you pass the values that you got from the text boxes.</span></span>

<span data-ttu-id="000f9-176">**Importante!**</span><span class="sxs-lookup"><span data-stu-id="000f9-176">**Important!**</span></span> <span data-ttu-id="000f9-177">Lembre-se de que a única maneira de você nunca deve incluir dados inseridos on-line por um usuário em uma instrução SQL é usar espaços reservados, como você vê aqui (`VALUES(@0, @1, @2)`).</span><span class="sxs-lookup"><span data-stu-id="000f9-177">Remember that the only way you should ever include data entered online by a user in a SQL statement is to use placeholders, as you see here (`VALUES(@0, @1, @2)`).</span></span> <span data-ttu-id="000f9-178">Se você concatenar a entrada do usuário em uma instrução SQL, abrir-se a um ataque de injeção de SQL, conforme explicado em [Noções básicas de formulário em páginas da Web do ASP.NET](https://go.microsoft.com/fwlink/?LinkId=251581) (tutorial anterior).</span><span class="sxs-lookup"><span data-stu-id="000f9-178">If you concatenate user input into a SQL statement, you open yourself to a SQL injection attack, as explained in [Form Basics in ASP.NET Web Pages](https://go.microsoft.com/fwlink/?LinkId=251581) (the previous tutorial).</span></span>

<span data-ttu-id="000f9-179">Ainda dentro de `if` em bloco, adicione a seguinte linha após a `db.Execute` linha:</span><span class="sxs-lookup"><span data-stu-id="000f9-179">Still inside the `if` block, add the following line after the `db.Execute` line:</span></span>

[!code-css[Main](entering-data/samples/sample4.css)]

<span data-ttu-id="000f9-180">Depois que o novo filme tenha sido inserido no banco de dados, essa linha é (redirecionamentos) salta para o *filmes* página para ver o filme que você acabou de digitar.</span><span class="sxs-lookup"><span data-stu-id="000f9-180">After the new movie has been inserted into the database, this line jumps you (redirects) to the *Movies* page so you can see the movie you just entered.</span></span> <span data-ttu-id="000f9-181">O `~` operador significa "raiz do site".</span><span class="sxs-lookup"><span data-stu-id="000f9-181">The `~` operator means "root of the website."</span></span> <span data-ttu-id="000f9-182">(O `~` operador geralmente funciona apenas em páginas do ASP.NET, não em HTML.)</span><span class="sxs-lookup"><span data-stu-id="000f9-182">(The `~` operator works only in ASP.NET pages, not in HTML generally.)</span></span>

<span data-ttu-id="000f9-183">O bloco de código completo é semelhante a este exemplo:</span><span class="sxs-lookup"><span data-stu-id="000f9-183">The complete code block looks like this example:</span></span>

[!code-cshtml[Main](entering-data/samples/sample5.cshtml)]

## <a name="testing-the-insert-command-so-far"></a><span data-ttu-id="000f9-184">Teste o comando Insert (até)</span><span class="sxs-lookup"><span data-stu-id="000f9-184">Testing the Insert Command (So Far)</span></span>

<span data-ttu-id="000f9-185">Você não ainda terminou, mas agora é um bom momento para testar.</span><span class="sxs-lookup"><span data-stu-id="000f9-185">You're not done yet, but now is a good time to test.</span></span>

<span data-ttu-id="000f9-186">Na exibição de árvore de arquivos no WebMatrix, clique com botão direito do *AddMovie.cshtml* página e, em seguida, clique em **iniciar no navegador**.</span><span class="sxs-lookup"><span data-stu-id="000f9-186">In the tree view of files in WebMatrix, right-click the *AddMovie.cshtml* page and then click **Launch in browser**.</span></span>

!['Adicionar filme' page no navegador](entering-data/_static/image2.png)

<span data-ttu-id="000f9-188">(Se você acabará com uma página diferente no navegador, certifique-se de que a URL é `http://localhost:nnnnn/AddMovie`), onde  *nnnnn*  é o número da porta que você está usando.)</span><span class="sxs-lookup"><span data-stu-id="000f9-188">(If you end up with a different page in the browser, make sure that the URL is `http://localhost:nnnnn/AddMovie`), where *nnnnn* is the port number that you're using.)</span></span>

<span data-ttu-id="000f9-189">Você obteve uma página de erro?</span><span class="sxs-lookup"><span data-stu-id="000f9-189">Did you get an error page?</span></span> <span data-ttu-id="000f9-190">Em caso afirmativo, leia com atenção e certifique-se de que o código é exatamente o que estava listado anteriormente.</span><span class="sxs-lookup"><span data-stu-id="000f9-190">If so, read it carefully and make sure that the code looks exactly what was listed earlier.</span></span>

<span data-ttu-id="000f9-191">Insira um filme no formato &mdash; , por exemplo, use "Cidadão Kane", "Drama" e "1941".</span><span class="sxs-lookup"><span data-stu-id="000f9-191">Enter a movie in the form &mdash; for example, use "Citizen Kane", "Drama", and "1941".</span></span> <span data-ttu-id="000f9-192">(Ou qualquer outro.) Em seguida, clique em **Adicionar filme**.</span><span class="sxs-lookup"><span data-stu-id="000f9-192">(Or whatever.) Then click **Add Movie**.</span></span>

<span data-ttu-id="000f9-193">Se tudo correr bem, você será redirecionado para a *filmes* página.</span><span class="sxs-lookup"><span data-stu-id="000f9-193">If all goes well, you're redirected to the *Movies* page.</span></span> <span data-ttu-id="000f9-194">Certifique-se de que seu novo filme está listado.</span><span class="sxs-lookup"><span data-stu-id="000f9-194">Make sure that your new movie is listed.</span></span>

![Página de filmes mostrando recentemente adicionada filme](entering-data/_static/image3.png)

## <a name="validating-user-input"></a><span data-ttu-id="000f9-196">Validando a entrada do usuário</span><span class="sxs-lookup"><span data-stu-id="000f9-196">Validating User Input</span></span>

<span data-ttu-id="000f9-197">Volte para o *AddMovie* página ou executá-lo novamente.</span><span class="sxs-lookup"><span data-stu-id="000f9-197">Go back to the *AddMovie* page, or run it again.</span></span> <span data-ttu-id="000f9-198">Digite outro filme, mas desta vez, digite somente o título &mdash; por exemplo, digite "Entrando ' no chuva".</span><span class="sxs-lookup"><span data-stu-id="000f9-198">Enter another movie, but this time, enter only the title &mdash; for example, enter "Singin' in the Rain".</span></span> <span data-ttu-id="000f9-199">Em seguida, clique em **Adicionar filme**.</span><span class="sxs-lookup"><span data-stu-id="000f9-199">Then click **Add Movie**.</span></span>

<span data-ttu-id="000f9-200">Você será redirecionado para a *filmes* página novamente.</span><span class="sxs-lookup"><span data-stu-id="000f9-200">You're redirected to the *Movies* page again.</span></span> <span data-ttu-id="000f9-201">Você pode encontrar o novo filme, mas está incompleto.</span><span class="sxs-lookup"><span data-stu-id="000f9-201">You can find the new movie, but it's incomplete.</span></span>

![Página de filmes mostrando novo filme que alguns valores está ausentes](entering-data/_static/image4.png)

<span data-ttu-id="000f9-203">Quando você criou o *filmes* tabela, você explicitamente disse que nenhum dos campos pode ser nulo.</span><span class="sxs-lookup"><span data-stu-id="000f9-203">When you created the *Movies* table, you explicitly said that none of the fields could be null.</span></span> <span data-ttu-id="000f9-204">Aqui você pode ter um formulário de entrada de novos filmes e está saindo campos em branco.</span><span class="sxs-lookup"><span data-stu-id="000f9-204">Here you have an entry form for new movies, and you're leaving fields blank.</span></span> <span data-ttu-id="000f9-205">Que é um erro.</span><span class="sxs-lookup"><span data-stu-id="000f9-205">That's an error.</span></span>

<span data-ttu-id="000f9-206">Nesse caso, o banco de dados, na verdade, não gerar (ou *gerar*) um erro.</span><span class="sxs-lookup"><span data-stu-id="000f9-206">In this case, the database didn't actually raise (or *throw*) an error.</span></span> <span data-ttu-id="000f9-207">Você não forneceu um gênero ou ano, portanto o código a *AddMovie* página tratado esses valores como chamados *cadeias de caracteres vazias*.</span><span class="sxs-lookup"><span data-stu-id="000f9-207">You didn't supply a genre or year, so the code in the *AddMovie* page treated those values as so-called *empty strings*.</span></span> <span data-ttu-id="000f9-208">Quando o SQL `Insert Into` comando foi executado, os campos de gênero e ano não possuem dados úteis, mas eles não eram nulos.</span><span class="sxs-lookup"><span data-stu-id="000f9-208">When the SQL `Insert Into` command ran, the genre and year fields didn't have useful data in them, but they weren't null.</span></span>

<span data-ttu-id="000f9-209">Obviamente, você não deseja permitir que os usuários inserir informações de filme metade vazio no banco de dados.</span><span class="sxs-lookup"><span data-stu-id="000f9-209">Obviously, you don't want to let users enter half-empty movie information into the database.</span></span> <span data-ttu-id="000f9-210">A solução é validar a entrada do usuário.</span><span class="sxs-lookup"><span data-stu-id="000f9-210">The solution is to validate the user's input.</span></span> <span data-ttu-id="000f9-211">Inicialmente, a validação simplesmente garantirá que o usuário inseriu um valor para todos os campos (ou seja, nenhuma delas contém uma cadeia de caracteres vazia).</span><span class="sxs-lookup"><span data-stu-id="000f9-211">Initially, the validation will simply make sure that the user has entered a value for all of the fields (that is, that none of them contains an empty string).</span></span>

> [!TIP] 
> 
> <span data-ttu-id="000f9-212">**Cadeias de caracteres nulas e vazias**</span><span class="sxs-lookup"><span data-stu-id="000f9-212">**Null and Empty Strings**</span></span>
> 
> <span data-ttu-id="000f9-213">Em programação, há uma distinção entre Noções diferentes de "sem valor".</span><span class="sxs-lookup"><span data-stu-id="000f9-213">In programming, there's a distinction between different notions of "no value."</span></span> <span data-ttu-id="000f9-214">Em geral, um valor é *nulo* se ela nunca foi definida ou inicializada de forma alguma.</span><span class="sxs-lookup"><span data-stu-id="000f9-214">In general, a value is *null* if it has never been set or initialized in any way.</span></span> <span data-ttu-id="000f9-215">Em contraste, uma variável que espera dados de caractere (cadeias de caracteres) pode ser definida como um *cadeia de caracteres vazia*.</span><span class="sxs-lookup"><span data-stu-id="000f9-215">In contrast, a variable that expects character data (strings) can be set to an *empty string*.</span></span> <span data-ttu-id="000f9-216">Nesse caso, o valor não é nulo. ele é apenas foi explicitamente definido como uma cadeia de caracteres cujo tamanho é zero.</span><span class="sxs-lookup"><span data-stu-id="000f9-216">In that case, the value is not null; it's just been explicitly set to a string of characters whose length is zero.</span></span> <span data-ttu-id="000f9-217">Essas duas instruções mostram a diferença:</span><span class="sxs-lookup"><span data-stu-id="000f9-217">These two statements show the difference:</span></span>
> 
> [!code-csharp[Main](entering-data/samples/sample6.cs)]
> 
> <span data-ttu-id="000f9-218">É um pouco mais complicado do que o, mas o ponto importante é que `null` representa um tipo de estado indeterminado.</span><span class="sxs-lookup"><span data-stu-id="000f9-218">It's a little more complicated than that, but the important point is that `null` represents a sort of undetermined state.</span></span>
> 
> <span data-ttu-id="000f9-219">Agora e, em seguida, é importante entender exatamente quando um valor é nulo e quando ele é apenas uma cadeia de caracteres vazia.</span><span class="sxs-lookup"><span data-stu-id="000f9-219">Now and then it's important to understand exactly when a value is null and when it's just an empty string.</span></span> <span data-ttu-id="000f9-220">No código para o *AddMovie* página, você obter os valores das caixas de texto usando `Request.Form["title"]` e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="000f9-220">In the code for the *AddMovie* page, you get the values of the text boxes by using `Request.Form["title"]` and so on.</span></span> <span data-ttu-id="000f9-221">Quando a página executa pela primeira vez (antes de você clicar no botão), o valor de `Request.Form["title"]` é nulo.</span><span class="sxs-lookup"><span data-stu-id="000f9-221">When the page first runs (before you click the button), the value of `Request.Form["title"]` is null.</span></span> <span data-ttu-id="000f9-222">Porém, quando você enviar o formulário, `Request.Form["title"]` obtém o valor da `title` caixa de texto.</span><span class="sxs-lookup"><span data-stu-id="000f9-222">But when you submit the form, `Request.Form["title"]` gets the value of the `title` text box.</span></span> <span data-ttu-id="000f9-223">Não é óbvio, mas uma caixa de texto vazia não é nula. Ele apenas tem uma cadeia de caracteres vazia.</span><span class="sxs-lookup"><span data-stu-id="000f9-223">It's not obvious, but an empty text box is not null; it just has an empty string in it.</span></span> <span data-ttu-id="000f9-224">Portanto, quando o código é executado em resposta ao botão clique, `Request.Form["title"]` exibe uma cadeia de caracteres vazia.</span><span class="sxs-lookup"><span data-stu-id="000f9-224">So when the code runs in response to the button click, `Request.Form["title"]` has an empty string in it.</span></span>
> 
> <span data-ttu-id="000f9-225">Por essa distinção é importante?</span><span class="sxs-lookup"><span data-stu-id="000f9-225">Why is this distinction important?</span></span> <span data-ttu-id="000f9-226">Quando você criou o *filmes* tabela, você explicitamente disse que nenhum dos campos pode ser nulo.</span><span class="sxs-lookup"><span data-stu-id="000f9-226">When you created the *Movies* table, you explicitly said that none of the fields could be null.</span></span> <span data-ttu-id="000f9-227">Mas aqui é ter um formulário de entrada de novos filmes e está saindo campos em branco.</span><span class="sxs-lookup"><span data-stu-id="000f9-227">But here you have an entry form for new movies, and you're leaving fields blank.</span></span> <span data-ttu-id="000f9-228">Você esperaria razoavelmente o banco de dados para reclamar quando você tentou salvar novos filmes que não têm valores de gênero ou ano.</span><span class="sxs-lookup"><span data-stu-id="000f9-228">You would reasonably expect the database to complain when you tried to save new movies that didn't have values for genre or year.</span></span> <span data-ttu-id="000f9-229">Mas isso é o ponto de &mdash; mesmo se você deixar as caixas de texto, os valores não nulos; elas estão cadeias de caracteres vazias.</span><span class="sxs-lookup"><span data-stu-id="000f9-229">But that's the point &mdash; even if you leave those text boxes blank, the values aren't null; they're empty strings.</span></span> <span data-ttu-id="000f9-230">Como resultado, você é capaz de salvar novos filmes em banco de dados com essas colunas vazias &mdash; , mas não nulo!</span><span class="sxs-lookup"><span data-stu-id="000f9-230">As a result, you're able to save new movies to the database with these columns empty &mdash; but not null!</span></span> <span data-ttu-id="000f9-231">valores &mdash;.</span><span class="sxs-lookup"><span data-stu-id="000f9-231">&mdash; values.</span></span> <span data-ttu-id="000f9-232">Portanto, você precisa certificar-se de que os usuários não enviarem uma cadeia de caracteres vazia, o que pode ser feito por meio da validação de entrada do usuário.</span><span class="sxs-lookup"><span data-stu-id="000f9-232">Therefore, you have to make sure that users don't submit an empty string, which you can do by validating the user's input.</span></span>


### <a name="the-validation-helper"></a><span data-ttu-id="000f9-233">O auxiliar de validação</span><span class="sxs-lookup"><span data-stu-id="000f9-233">The Validation Helper</span></span>

<span data-ttu-id="000f9-234">Páginas da Web ASP.NET inclui um auxiliar &mdash; o `Validation` auxiliar &mdash; que você pode usar para certificar-se de que os usuários insiram dados que atenda às suas necessidades.</span><span class="sxs-lookup"><span data-stu-id="000f9-234">ASP.NET Web Pages includes a helper &mdash; the `Validation` helper &mdash; that you can use to make sure that users enter data that meets your requirements.</span></span> <span data-ttu-id="000f9-235">O `Validation` auxiliar é um dos auxiliares que é interno para o ASP.NET Web Pages, não é necessário instalá-lo como um pacote usando o NuGet, da maneira que você instalou o auxiliar Gravatar um tutorial anterior.</span><span class="sxs-lookup"><span data-stu-id="000f9-235">The `Validation` helper is one of the helpers that's built in to ASP.NET Web Pages, so you don't have to install it as a package by using NuGet, the way you installed the Gravatar helper in an earlier tutorial.</span></span>

<span data-ttu-id="000f9-236">Para validar a entrada do usuário, você fará o seguinte:</span><span class="sxs-lookup"><span data-stu-id="000f9-236">To validate the user's input, you'll do the following:</span></span>

- <span data-ttu-id="000f9-237">Use o código para especificar que você deseja exigir valores nas caixas de texto na página.</span><span class="sxs-lookup"><span data-stu-id="000f9-237">Use code to specify that you want to require values in the text boxes on the page.</span></span>
- <span data-ttu-id="000f9-238">Colocar um teste no código para que as informações de filme são adicionadas ao banco de dados somente se tudo for validado corretamente.</span><span class="sxs-lookup"><span data-stu-id="000f9-238">Put a test into the code so that the movie information is added to the database only if everything validates properly.</span></span>
- <span data-ttu-id="000f9-239">Adicione código para a marcação para exibir mensagens de erro.</span><span class="sxs-lookup"><span data-stu-id="000f9-239">Add code into the markup to display error messages.</span></span>

<span data-ttu-id="000f9-240">No bloco de código a *AddMovie* página, direita para cima na parte superior antes de declarações de variável, adicione o seguinte código:</span><span class="sxs-lookup"><span data-stu-id="000f9-240">In the code block in the *AddMovie* page, right up at the top before the variable declarations, add the following code:</span></span>

[!code-csharp[Main](entering-data/samples/sample7.cs)]

<span data-ttu-id="000f9-241">Você chamar `Validation.RequireField` uma vez para cada campo (`<input>` elemento) onde você deseja exigir uma entrada.</span><span class="sxs-lookup"><span data-stu-id="000f9-241">You call `Validation.RequireField` once for each field (`<input>` element) where you want to require an entry.</span></span> <span data-ttu-id="000f9-242">Você também pode adicionar uma mensagem de erro personalizada para cada chamada, como você vê aqui.</span><span class="sxs-lookup"><span data-stu-id="000f9-242">You can also add a custom error message for each call, like you see here.</span></span> <span data-ttu-id="000f9-243">(Nós variadas as mensagens apenas para mostrar que você pode colocar qualquer existe).</span><span class="sxs-lookup"><span data-stu-id="000f9-243">(We varied the messages just to show that you can put anything you like there.)</span></span>

<span data-ttu-id="000f9-244">Se houver um problema, você deseja impedir que as novas informações de filme seja inserido no banco de dados.</span><span class="sxs-lookup"><span data-stu-id="000f9-244">If there's a problem, you want to prevent the new movie information from being inserted into the database.</span></span> <span data-ttu-id="000f9-245">No `if(IsPost)` em bloco, use `&&` (AND lógico) para adicionar outra condição que testa `Validation.IsValid()`.</span><span class="sxs-lookup"><span data-stu-id="000f9-245">In the `if(IsPost)` block, use `&&` (logical AND) to add another condition that tests `Validation.IsValid()`.</span></span> <span data-ttu-id="000f9-246">Quando você terminar, todo o `if(IsPost)` bloco é parecido com este código:</span><span class="sxs-lookup"><span data-stu-id="000f9-246">When you're done, the whole `if(IsPost)` block looks like this code:</span></span>

[!code-csharp[Main](entering-data/samples/sample8.cs)]

<span data-ttu-id="000f9-247">Se houver um erro de validação com qualquer um dos campos que você registrou usando o `Validation` auxiliar, o `Validation.IsValid` método retornará false.</span><span class="sxs-lookup"><span data-stu-id="000f9-247">If there's a validation error with any of the fields that you registered by using the `Validation` helper, the `Validation.IsValid` method returns false.</span></span> <span data-ttu-id="000f9-248">E, nesse caso, nenhum código no bloco serão executados, portanto, não há entradas de filme inválido serão inseridas no banco de dados.</span><span class="sxs-lookup"><span data-stu-id="000f9-248">And in that case, none of the code in that block will run, so no invalid movie entries will be inserted into the database.</span></span> <span data-ttu-id="000f9-249">E obviamente você não será redirecionado para a *filmes* página.</span><span class="sxs-lookup"><span data-stu-id="000f9-249">And of course you're not redirected to the *Movies* page.</span></span>

<span data-ttu-id="000f9-250">O bloco de código completo, incluindo o código de validação, agora se parece com este exemplo:</span><span class="sxs-lookup"><span data-stu-id="000f9-250">The complete code block, including the validation code, now looks like this example:</span></span>

[!code-cshtml[Main](entering-data/samples/sample9.cshtml?highlight=10)]

## <a name="displaying-validation-errors"></a><span data-ttu-id="000f9-251">Exibindo erros de validação</span><span class="sxs-lookup"><span data-stu-id="000f9-251">Displaying Validation Errors</span></span>

<span data-ttu-id="000f9-252">A última etapa é exibir as mensagens de erro.</span><span class="sxs-lookup"><span data-stu-id="000f9-252">The last step is to display any error messages.</span></span> <span data-ttu-id="000f9-253">Você pode exibir as mensagens individuais para cada erro de validação, ou você pode exibir um resumo ou ambos.</span><span class="sxs-lookup"><span data-stu-id="000f9-253">You can display individual messages for each validation error, or you can display a summary, or both.</span></span> <span data-ttu-id="000f9-254">Para este tutorial, você realizará ambos para que você possa ver como ele funciona.</span><span class="sxs-lookup"><span data-stu-id="000f9-254">For this tutorial, you'll do both so that you can see how it works.</span></span>

<span data-ttu-id="000f9-255">Ao lado de cada `<input>` elemento que você está validando, chame o `Html.ValidationMessage` método e passe o nome do `<input>` elemento sendo validada.</span><span class="sxs-lookup"><span data-stu-id="000f9-255">Next to each `<input>` element that you're validating, call the `Html.ValidationMessage` method and pass it the name of the `<input>` element you're validating.</span></span> <span data-ttu-id="000f9-256">Colocar o `Html.ValidationMessage` direita do método onde você deseja que a mensagem de erro.</span><span class="sxs-lookup"><span data-stu-id="000f9-256">You put the `Html.ValidationMessage` method right where you want the error message to appear.</span></span> <span data-ttu-id="000f9-257">Quando a página é executada, o `Html.ValidationMessage` método renderiza um `<span>` elemento aonde o erro de validação.</span><span class="sxs-lookup"><span data-stu-id="000f9-257">When the page runs, the `Html.ValidationMessage` method renders a `<span>` element where the validation error will go.</span></span> <span data-ttu-id="000f9-258">(Se não houver nenhum erro, o `<span>` elemento é processado, mas não há nenhum texto nele.)</span><span class="sxs-lookup"><span data-stu-id="000f9-258">(If there's no error, the `<span>` element is rendered, but there's no text in it.)</span></span>

<span data-ttu-id="000f9-259">Alterar a marcação na página de modo que ele inclui um `Html.ValidationMessage` método para cada um dos três `<input>` elementos na página, como neste exemplo:</span><span class="sxs-lookup"><span data-stu-id="000f9-259">Change the markup in the page so that it includes an `Html.ValidationMessage` method for each of the three `<input>` elements on the page, like this example:</span></span>

[!code-cshtml[Main](entering-data/samples/sample10.cshtml?highlight=3,8,13)]

<span data-ttu-id="000f9-260">Para ver como funciona o resumo, também adicione a seguinte marcação e código logo após o `<h1>Add a Movie</h1>` elemento na página:</span><span class="sxs-lookup"><span data-stu-id="000f9-260">To see how the summary works, also add the following markup and code right after the `<h1>Add a Movie</h1>` element on the page:</span></span>

[!code-cshtml[Main](entering-data/samples/sample11.cshtml)]

<span data-ttu-id="000f9-261">Por padrão, o `Html.ValidationSummary` método exibe todas as mensagens de validação em uma lista (um `<ul>` elemento que está dentro de um `<div>` elemento).</span><span class="sxs-lookup"><span data-stu-id="000f9-261">By default, the `Html.ValidationSummary` method displays all the validation messages in a list (a `<ul>` element that's inside a `<div>` element).</span></span> <span data-ttu-id="000f9-262">Assim como acontece com o `Html.ValidationMessage` método, a marcação para o resumo de validação é sempre renderizada; se não houver nenhum erro, nenhum item de lista é renderizados.</span><span class="sxs-lookup"><span data-stu-id="000f9-262">As with the `Html.ValidationMessage` method, the markup for the validation summary is always rendered; if there are no errors, no list items are rendered.</span></span>

<span data-ttu-id="000f9-263">O resumo pode ser uma maneira alternativa de exibir mensagens de validação em vez de por meio de `Html.ValidationMessage` método para exibir cada erro de campo específicos.</span><span class="sxs-lookup"><span data-stu-id="000f9-263">The summary can be an alternative way to display validation messages instead of by using the `Html.ValidationMessage` method to display each field-specific error.</span></span> <span data-ttu-id="000f9-264">Ou você pode usar um resumo e os detalhes.</span><span class="sxs-lookup"><span data-stu-id="000f9-264">Or you can use both a summary and the details.</span></span> <span data-ttu-id="000f9-265">Ou você pode usar o `Html.ValidationSummary` método para exibir um erro genérico e, em seguida, usar individuais `Html.ValidationMessage` chamadas para exibir os detalhes.</span><span class="sxs-lookup"><span data-stu-id="000f9-265">Or you can use the `Html.ValidationSummary` method to display a generic error and then use individual `Html.ValidationMessage` calls to display details.</span></span>

<span data-ttu-id="000f9-266">A página concluída agora se parece com este exemplo:</span><span class="sxs-lookup"><span data-stu-id="000f9-266">The complete page now looks like this example:</span></span>

[!code-cshtml[Main](entering-data/samples/sample12.cshtml)]

<span data-ttu-id="000f9-267">É isso.</span><span class="sxs-lookup"><span data-stu-id="000f9-267">That's it.</span></span> <span data-ttu-id="000f9-268">Agora você pode testar a página pela adição de um filme, mas omitindo um ou mais campos.</span><span class="sxs-lookup"><span data-stu-id="000f9-268">You can now test the page by adding a movie but leaving out one or more of the fields.</span></span> <span data-ttu-id="000f9-269">Quando você fizer isso, consulte o seguinte erro de exibição:</span><span class="sxs-lookup"><span data-stu-id="000f9-269">When you do, you see the following error display:</span></span>

![Adicionar página de filme mostra mensagens de erro de validação](entering-data/_static/image5.png)

## <a name="styling-the-validation-error-messages"></a><span data-ttu-id="000f9-271">As mensagens de erro de validação de estilo</span><span class="sxs-lookup"><span data-stu-id="000f9-271">Styling the Validation Error Messages</span></span>

<span data-ttu-id="000f9-272">Você pode ver que há mensagens de erro, mas eles não realmente destacam muito bem.</span><span class="sxs-lookup"><span data-stu-id="000f9-272">You can see that there are error messages, but they don't really stand out very well.</span></span> <span data-ttu-id="000f9-273">Há uma maneira fácil para definir o estilo de mensagens de erro, embora.</span><span class="sxs-lookup"><span data-stu-id="000f9-273">There's an easy way to style the error messages, though.</span></span>

<span data-ttu-id="000f9-274">Para definir o estilo de mensagens de erro individuais que são exibidas por `Html.ValidationMessage`, crie uma classe de estilo CSS denominada `field-validation-error`.</span><span class="sxs-lookup"><span data-stu-id="000f9-274">To style the individual error messages that are displayed by `Html.ValidationMessage`, create a CSS style class named `field-validation-error`.</span></span> <span data-ttu-id="000f9-275">Para definir a aparência para o resumo de validação, crie uma classe de estilo CSS denominada `validation-summary-errors`.</span><span class="sxs-lookup"><span data-stu-id="000f9-275">To define the look for the validation summary, create a CSS style class named `validation-summary-errors`.</span></span>

<span data-ttu-id="000f9-276">Para ver como essa técnica funciona, adicione um `<style>` elemento dentro do `<head>` seção da página.</span><span class="sxs-lookup"><span data-stu-id="000f9-276">To see how this technique works, add a `<style>` element inside the `<head>` section of the page.</span></span> <span data-ttu-id="000f9-277">Em seguida, definir classes de estilo nomeadas `field-validation-error` e `validation-summary-errors` que contêm as seguintes regras:</span><span class="sxs-lookup"><span data-stu-id="000f9-277">Then define style classes named `field-validation-error` and `validation-summary-errors` that contain the following rules:</span></span>

[!code-cshtml[Main](entering-data/samples/sample13.cshtml?highlight=4-17)]

<span data-ttu-id="000f9-278">Normalmente você provavelmente seria colocar informações de estilo em uma separada *. CSS* arquivo, mas para simplificar você pode colocá-los na página agora.</span><span class="sxs-lookup"><span data-stu-id="000f9-278">Normally you'd probably put style information into a separate *.css* file, but for simplicity you can put them in the page for now.</span></span> <span data-ttu-id="000f9-279">(Posteriormente neste tutorial conjunto, você moverá as regras de CSS para um separado *. CSS* arquivo.)</span><span class="sxs-lookup"><span data-stu-id="000f9-279">(Later in this tutorial set, you'll move the CSS rules to a separate *.css* file.)</span></span>

<span data-ttu-id="000f9-280">Se houver um erro de validação, o `Html.ValidationMessage` método renderiza um `<span>` elemento inclui `class="field-validation-error"`.</span><span class="sxs-lookup"><span data-stu-id="000f9-280">If there's a validation error, the `Html.ValidationMessage` method renders a `<span>` element that includes `class="field-validation-error"`.</span></span> <span data-ttu-id="000f9-281">Adicionando uma definição de estilo para a classe, você pode configurar a aparência de mensagem.</span><span class="sxs-lookup"><span data-stu-id="000f9-281">By adding a style definition for that class, you can configure what the message looks like.</span></span> <span data-ttu-id="000f9-282">Se houver erros, o `ValidationSummary` método renderiza da mesma forma dinamicamente o atributo `class="validation-summary-errors"`.</span><span class="sxs-lookup"><span data-stu-id="000f9-282">If there are errors, the `ValidationSummary` method likewise dynamically renders the attribute `class="validation-summary-errors"`.</span></span>

<span data-ttu-id="000f9-283">Execute novamente a página e deixe deliberadamente alguns dos campos.</span><span class="sxs-lookup"><span data-stu-id="000f9-283">Run the page again and deliberately leave out a couple of the fields.</span></span> <span data-ttu-id="000f9-284">Agora, os erros são mais notáveis.</span><span class="sxs-lookup"><span data-stu-id="000f9-284">The errors are now more noticeable.</span></span> <span data-ttu-id="000f9-285">(Na verdade, eles estiverem já a terá desobedecidos, mas isso é apenas para mostrar o que você pode fazer.)</span><span class="sxs-lookup"><span data-stu-id="000f9-285">(In fact, they're overdone, but that's just to show what you can do.)</span></span>

![Adicionar página de filme mostrando erros de validação que tenham sido estilo](entering-data/_static/image6.png)

## <a name="adding-a-link-to-the-movies-page"></a><span data-ttu-id="000f9-287">Adicionar um Link para a página de filmes</span><span class="sxs-lookup"><span data-stu-id="000f9-287">Adding a Link to the Movies Page</span></span>

<span data-ttu-id="000f9-288">A etapa final é torná-lo conveniente para obter o *AddMovie* página da listagem de filme original.</span><span class="sxs-lookup"><span data-stu-id="000f9-288">One final step is to make it convenient to get to the *AddMovie* page from the original movie listing.</span></span>

<span data-ttu-id="000f9-289">Abra o *filmes* página novamente.</span><span class="sxs-lookup"><span data-stu-id="000f9-289">Open the *Movies* page again.</span></span> <span data-ttu-id="000f9-290">Após o fechamento `</div>` marca que segue o `WebGrid` auxiliar, adicione a seguinte marcação:</span><span class="sxs-lookup"><span data-stu-id="000f9-290">After the closing `</div>` tag that follows the `WebGrid` helper, add the following markup:</span></span>

[!code-cshtml[Main](entering-data/samples/sample14.cshtml)]

<span data-ttu-id="000f9-291">Como você viu antes, o ASP.NET interpreta o `~` operador como a raiz do site.</span><span class="sxs-lookup"><span data-stu-id="000f9-291">As you saw before, ASP.NET interprets the `~` operator as the root of the website.</span></span> <span data-ttu-id="000f9-292">Você não precisa usar o `~` operador; você pode usar a marcação `<a href="./AddMovie">Add a movie</a>` ou alguma outra maneira para definir o caminho que reconheça o HTML.</span><span class="sxs-lookup"><span data-stu-id="000f9-292">You don't have to use the `~` operator; you could use the markup `<a href="./AddMovie">Add a movie</a>` or some other way to define the path that HTML understands.</span></span> <span data-ttu-id="000f9-293">Mas o `~` operador é uma boa abordagem geral quando você criar links para páginas Razor, pois ele torna o site mais flexível — se você mover a página atual para uma subpasta, o link ainda irá para o *AddMovie* página.</span><span class="sxs-lookup"><span data-stu-id="000f9-293">But the `~` operator is a good general approach when you create links for Razor pages, because it makes the site more flexible — if you move the current page to a subfolder, the link will still go to the *AddMovie* page.</span></span> <span data-ttu-id="000f9-294">(Lembre-se de que o `~` operador só funciona *. cshtml* páginas.</span><span class="sxs-lookup"><span data-stu-id="000f9-294">(Remember that the `~` operator only works in *.cshtml* pages.</span></span> <span data-ttu-id="000f9-295">Entenda ASP.NET, mas não é HTML padrão.)</span><span class="sxs-lookup"><span data-stu-id="000f9-295">ASP.NET understands it, but it's not standard HTML.)</span></span>

<span data-ttu-id="000f9-296">Quando terminar, execute o *filmes* página.</span><span class="sxs-lookup"><span data-stu-id="000f9-296">When you're done, run the *Movies* page.</span></span> <span data-ttu-id="000f9-297">Ele será semelhante a esta página:</span><span class="sxs-lookup"><span data-stu-id="000f9-297">It will look like this page:</span></span>

![Página de filmes com link para a página 'Adicionar filmes'](entering-data/_static/image7.png)

<span data-ttu-id="000f9-299">Clique o **adicionar um filme** link para certificar-se de que ele passa para o *AddMovie* página.</span><span class="sxs-lookup"><span data-stu-id="000f9-299">Click the **Add a movie** link to make sure that it goes to the *AddMovie* page.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="000f9-300">Próximo</span><span class="sxs-lookup"><span data-stu-id="000f9-300">Coming Up Next</span></span>

<span data-ttu-id="000f9-301">O seguinte tutorial, você aprenderá como permitir aos usuários editar dados que já está no banco de dados.</span><span class="sxs-lookup"><span data-stu-id="000f9-301">In the next tutorial, you'll learn how to let users edit data that's already in the database.</span></span>

## <a name="complete-listing-for-addmovie-page"></a><span data-ttu-id="000f9-302">Lista completa de página AddMovie</span><span class="sxs-lookup"><span data-stu-id="000f9-302">Complete Listing for AddMovie Page</span></span>

[!code-cshtml[Main](entering-data/samples/sample15.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="000f9-303">Recursos adicionais</span><span class="sxs-lookup"><span data-stu-id="000f9-303">Additional Resources</span></span>

- [<span data-ttu-id="000f9-304">Introdução à programação da Web do ASP.NET usando a sintaxe do Razor</span><span class="sxs-lookup"><span data-stu-id="000f9-304">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="000f9-305">[Inserir na instrução SQL](http://www.w3schools.com/sql/sql_insert.asp) no site W3Schools</span><span class="sxs-lookup"><span data-stu-id="000f9-305">[SQL INSERT INTO Statement](http://www.w3schools.com/sql/sql_insert.asp) on the W3Schools site</span></span>
- <span data-ttu-id="000f9-306">[Validando a entrada do usuário da Web do ASP.NET páginas Sites](https://go.microsoft.com/fwlink/?LinkId=253002).</span><span class="sxs-lookup"><span data-stu-id="000f9-306">[Validating User Input in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=253002).</span></span> <span data-ttu-id="000f9-307">Para obter mais informações sobre como trabalhar com o `Validation` auxiliar.</span><span class="sxs-lookup"><span data-stu-id="000f9-307">More information about working with the `Validation` helper.</span></span>

>[!div class="step-by-step"]
<span data-ttu-id="000f9-308">[Anterior](form-basics.md)
[Próximo](updating-data.md)</span><span class="sxs-lookup"><span data-stu-id="000f9-308">[Previous](form-basics.md)
[Next](updating-data.md)</span></span>
