---
title: "Roteamento no núcleo do ASP.NET"
author: ardalis
description: 
keywords: ASP.NET Core,
ms.author: riande
manager: wpickett
ms.date: 10/14/2016
ms.topic: article
ms.assetid: bbbcf9e4-3c4c-4f50-b91e-175fe9cae4e2
ms.technology: aspnet
ms.prod: asp.net-core
uid: fundamentals/routing
ms.openlocfilehash: 431b837dc93abdf305b77615409883fd54b99455
ms.sourcegitcommit: 9cdbfd0d670d70b9c354216aabee260c52dad5ee
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 09/12/2017
---
# <a name="routing-in-aspnet-core"></a><span data-ttu-id="d89dd-103">Roteamento no núcleo do ASP.NET</span><span class="sxs-lookup"><span data-stu-id="d89dd-103">Routing in ASP.NET Core</span></span>

<span data-ttu-id="d89dd-104">Por [Ryan Nowak](https://github.com/rynowak), [Steve Smith](https://ardalis.com/), e [Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="d89dd-104">By [Ryan Nowak](https://github.com/rynowak), [Steve Smith](https://ardalis.com/), and [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

<span data-ttu-id="d89dd-105">Funcionalidade de roteamento é responsável por uma solicitação de entrada de mapeamento para um manipulador de rota.</span><span class="sxs-lookup"><span data-stu-id="d89dd-105">Routing functionality is responsible for mapping an incoming request to a route handler.</span></span> <span data-ttu-id="d89dd-106">Rotas são definidas no aplicativo do ASP.NET e configuradas quando o aplicativo é iniciado.</span><span class="sxs-lookup"><span data-stu-id="d89dd-106">Routes are defined in the ASP.NET app and configured when the app starts up.</span></span> <span data-ttu-id="d89dd-107">Uma rota opcionalmente pode extrair os valores da URL contida na solicitação, e esses valores, em seguida, podem ser usados para o processamento da solicitação.</span><span class="sxs-lookup"><span data-stu-id="d89dd-107">A route can optionally extract values from the URL contained in the request, and these values can then be used for request processing.</span></span> <span data-ttu-id="d89dd-108">Usando informações de rota do aplicativo ASP.NET, a funcionalidade de roteamento também é capaz de gerar URLs que são mapeados para manipuladores de rotas.</span><span class="sxs-lookup"><span data-stu-id="d89dd-108">Using route information from the ASP.NET app, the routing functionality is also able to generate URLs that map to route handlers.</span></span> <span data-ttu-id="d89dd-109">Portanto, o roteamento pode encontrar um manipulador de rota com base em uma URL ou a URL correspondente a um manipulador de rota em questão com base nas informações do manipulador de rota.</span><span class="sxs-lookup"><span data-stu-id="d89dd-109">Therefore, routing can find a route handler based on a URL, or the URL corresponding to a given route handler based on route handler information.</span></span>

>[!IMPORTANT]
> <span data-ttu-id="d89dd-110">Este documento abrange o nível inferior ASP.NET Core roteamento.</span><span class="sxs-lookup"><span data-stu-id="d89dd-110">This document covers the low level ASP.NET Core routing.</span></span> <span data-ttu-id="d89dd-111">Para o roteamento MVC do ASP.NET Core, consulte [roteamento para ações do controlador](../mvc/controllers/routing.md)</span><span class="sxs-lookup"><span data-stu-id="d89dd-111">For ASP.NET Core MVC routing, see [Routing to Controller Actions](../mvc/controllers/routing.md)</span></span>

[<span data-ttu-id="d89dd-112">Exibir ou baixar o código de exemplo</span><span class="sxs-lookup"><span data-stu-id="d89dd-112">View or download sample code</span></span>](https://github.com/aspnet/Docs/tree/master/aspnetcore/fundamentals/routing/sample)

## <a name="routing-basics"></a><span data-ttu-id="d89dd-113">Noções básicas sobre roteamento</span><span class="sxs-lookup"><span data-stu-id="d89dd-113">Routing basics</span></span>

<span data-ttu-id="d89dd-114">Usa roteamento *rotas* (implementações de [IRouter](https://docs.microsoft.com/aspnet/core/api/microsoft.aspnetcore.routing.irouter)) para:</span><span class="sxs-lookup"><span data-stu-id="d89dd-114">Routing uses *routes* (implementations of [IRouter](https://docs.microsoft.com/aspnet/core/api/microsoft.aspnetcore.routing.irouter)) to:</span></span>

* <span data-ttu-id="d89dd-115">solicitações de entrada do mapa *manipuladores de rota*</span><span class="sxs-lookup"><span data-stu-id="d89dd-115">map incoming requests to *route handlers*</span></span>

* <span data-ttu-id="d89dd-116">gerar as URLs usadas nas respostas</span><span class="sxs-lookup"><span data-stu-id="d89dd-116">generate URLs used in responses</span></span>

<span data-ttu-id="d89dd-117">Em geral, um aplicativo tem uma única coleção de rotas.</span><span class="sxs-lookup"><span data-stu-id="d89dd-117">Generally, an app has a single collection of routes.</span></span> <span data-ttu-id="d89dd-118">Quando uma solicitação chega, a coleção de rotas é processada na ordem.</span><span class="sxs-lookup"><span data-stu-id="d89dd-118">When a request arrives, the route collection is processed in order.</span></span> <span data-ttu-id="d89dd-119">A solicitação de entrada procura uma rota que corresponde à URL de solicitação chamando o `RouteAsync` método em cada rota disponível na coleção de rotas.</span><span class="sxs-lookup"><span data-stu-id="d89dd-119">The incoming request looks for a route that matches the request URL by calling the `RouteAsync` method on each available route in the route collection.</span></span> <span data-ttu-id="d89dd-120">Por outro lado, uma resposta pode usar o roteamento para gerar URLs (por exemplo, para redirecionamento ou links) com base nas informações de rota e, assim, evite ter URLs de embutir em código, que ajuda a facilidade de manutenção.</span><span class="sxs-lookup"><span data-stu-id="d89dd-120">By contrast, a response can use routing to generate URLs (for example, for redirection or links) based on route information, and thus avoid having to hard-code URLs, which helps maintainability.</span></span>

<span data-ttu-id="d89dd-121">Roteamento está conectado a [middleware](middleware.md) pipeline pela `RouterMiddleware` classe.</span><span class="sxs-lookup"><span data-stu-id="d89dd-121">Routing is connected to the [middleware](middleware.md) pipeline by the `RouterMiddleware` class.</span></span> <span data-ttu-id="d89dd-122">[ASP.NET MVC](../mvc/overview.md) adiciona o roteamento para o pipeline de middleware como parte da sua configuração.</span><span class="sxs-lookup"><span data-stu-id="d89dd-122">[ASP.NET MVC](../mvc/overview.md) adds routing to the middleware pipeline as part of its configuration.</span></span> <span data-ttu-id="d89dd-123">Para obter informações sobre como usar o roteamento como um componente autônomo, consulte [middleware de roteamento usando](#using-routing-middleware).</span><span class="sxs-lookup"><span data-stu-id="d89dd-123">To learn about using routing as a standalone component, see [using-routing-middleware](#using-routing-middleware).</span></span>

<a name=url-matching-ref></a>

### <a name="url-matching"></a><span data-ttu-id="d89dd-124">Correspondência de URL</span><span class="sxs-lookup"><span data-stu-id="d89dd-124">URL matching</span></span>

<span data-ttu-id="d89dd-125">Correspondência de URL é o processo pelo qual expede roteamento uma entrada de solicitação para um *manipulador*.</span><span class="sxs-lookup"><span data-stu-id="d89dd-125">URL matching is the process by which routing dispatches an incoming request to a *handler*.</span></span> <span data-ttu-id="d89dd-126">Esse processo geralmente com base nos dados no caminho da URL, mas pode ser estendido para considerar todos os dados na solicitação.</span><span class="sxs-lookup"><span data-stu-id="d89dd-126">This process is generally based on data in the URL path, but can be extended to consider any data in the request.</span></span> <span data-ttu-id="d89dd-127">A capacidade de enviar solicitações para separar manipuladores é fundamental para dimensionar o tamanho e a complexidade de um aplicativo.</span><span class="sxs-lookup"><span data-stu-id="d89dd-127">The ability to dispatch requests to separate handlers is key to scaling the size and complexity of an application.</span></span>

<span data-ttu-id="d89dd-128">Insira as solicitações de entrada a `RouterMiddleware`, que chama o `RouteAsync` método em cada rota na sequência.</span><span class="sxs-lookup"><span data-stu-id="d89dd-128">Incoming requests enter the `RouterMiddleware`, which calls the `RouteAsync` method on each route in sequence.</span></span> <span data-ttu-id="d89dd-129">O `IRouter` instância escolhe se deseja *tratar* a solicitação, definindo o `RouteContext` `Handler` para não-nulo `RequestDelegate`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-129">The `IRouter` instance chooses whether to *handle* the request by setting the `RouteContext` `Handler` to a non-null `RequestDelegate`.</span></span> <span data-ttu-id="d89dd-130">Se uma rota define um manipulador para a solicitação, o processamento será interrompido e o manipulador de rota será invocada para processar a solicitação.</span><span class="sxs-lookup"><span data-stu-id="d89dd-130">If a route sets a handler for the request, route processing stops and the handler will be invoked to process the request.</span></span> <span data-ttu-id="d89dd-131">Se todas as rotas serão tentadas e nenhum manipulador foi encontrado para a solicitação, o middleware chama *próximo* e o próximo middleware no pipeline de solicitação é invocado.</span><span class="sxs-lookup"><span data-stu-id="d89dd-131">If all routes are tried and no handler is found for the request, the middleware calls *next* and the next middleware in the request pipeline is invoked.</span></span>

<span data-ttu-id="d89dd-132">A entrada principal para `RouteAsync` é o `RouteContext` `HttpContext` associado à solicitação atual.</span><span class="sxs-lookup"><span data-stu-id="d89dd-132">The primary input to `RouteAsync` is the `RouteContext` `HttpContext` associated with the current request.</span></span> <span data-ttu-id="d89dd-133">O `RouteContext.Handler` e `RouteContext` `RouteData` saídas que serão definidos depois que uma rota corresponde.</span><span class="sxs-lookup"><span data-stu-id="d89dd-133">The `RouteContext.Handler` and `RouteContext` `RouteData` are outputs that will be set after a route matches.</span></span>

<span data-ttu-id="d89dd-134">Uma correspondência durante `RouteAsync` também definirá as propriedades do `RouteContext.RouteData` para os valores apropriados com base no processamento de solicitação concluído até o momento.</span><span class="sxs-lookup"><span data-stu-id="d89dd-134">A match during `RouteAsync` will also set the properties of the `RouteContext.RouteData` to appropriate values based on the request processing done so far.</span></span> <span data-ttu-id="d89dd-135">Se uma rota corresponde a uma solicitação, o `RouteContext.RouteData` conterá informações de estado importantes sobre o *resultado*.</span><span class="sxs-lookup"><span data-stu-id="d89dd-135">If a route matches a request, the `RouteContext.RouteData` will contain important state information about the *result*.</span></span>

<span data-ttu-id="d89dd-136">`RouteData``Values` é um dicionário de *valores de rota* produzidos de rota.</span><span class="sxs-lookup"><span data-stu-id="d89dd-136">`RouteData` `Values` is a dictionary of *route values* produced from the route.</span></span> <span data-ttu-id="d89dd-137">Esses valores geralmente são determinados pelo gerar tokens para a URL e podem ser usados para aceitar a entrada do usuário ou para tomar decisões mais expedição dentro do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="d89dd-137">These values are usually determined by tokenizing the URL, and can be used to accept user input, or to make further dispatching decisions inside the application.</span></span>

<span data-ttu-id="d89dd-138">`RouteData``DataTokens` é um recipiente de propriedades de dados adicionais relacionados à rota correspondente.</span><span class="sxs-lookup"><span data-stu-id="d89dd-138">`RouteData` `DataTokens`  is a property bag of additional data related to the matched route.</span></span> <span data-ttu-id="d89dd-139">`DataTokens`são fornecidos para oferecer suporte ao estado associando dados com cada rota para que o aplicativo possa tomar decisões mais tarde com base em qual rota correspondente.</span><span class="sxs-lookup"><span data-stu-id="d89dd-139">`DataTokens` are provided to support associating state data with each route so the application can make decisions later based on which route matched.</span></span> <span data-ttu-id="d89dd-140">Esses valores são definidos pelo desenvolvedor e **não** afetam o comportamento de roteamento de qualquer forma.</span><span class="sxs-lookup"><span data-stu-id="d89dd-140">These values are developer-defined and do **not** affect the behavior of routing in any way.</span></span> <span data-ttu-id="d89dd-141">Além disso, os valores armazenados nos tokens de dados podem ser de qualquer tipo, em contraste com valores de rota, que deve ser conversível em cadeias de caracteres.</span><span class="sxs-lookup"><span data-stu-id="d89dd-141">Additionally, values stashed in data tokens can be of any type, in contrast to route values, which must be easily convertible to and from strings.</span></span>

<span data-ttu-id="d89dd-142">`RouteData``Routers` é uma lista das rotas que participou na correspondência com êxito a solicitação.</span><span class="sxs-lookup"><span data-stu-id="d89dd-142">`RouteData` `Routers` is a list of the routes that took part in successfully matching the request.</span></span> <span data-ttu-id="d89dd-143">Rotas podem ser aninhadas dentro de um do outro e o `Routers` propriedade reflete o caminho até a árvore lógica de rotas que resultou em uma correspondência.</span><span class="sxs-lookup"><span data-stu-id="d89dd-143">Routes can be nested inside one another, and the `Routers` property reflects the path through the logical tree of routes that resulted in a match.</span></span> <span data-ttu-id="d89dd-144">Geralmente o primeiro item na `Routers` é a coleção de rotas e deve ser usada para geração de URL.</span><span class="sxs-lookup"><span data-stu-id="d89dd-144">Generally the first item in `Routers` is the route collection, and should be used for URL generation.</span></span> <span data-ttu-id="d89dd-145">O último item no `Routers` é o manipulador de rota correspondente.</span><span class="sxs-lookup"><span data-stu-id="d89dd-145">The last item in `Routers` is the route handler that matched.</span></span>

### <a name="url-generation"></a><span data-ttu-id="d89dd-146">Geração de URL</span><span class="sxs-lookup"><span data-stu-id="d89dd-146">URL generation</span></span>

<span data-ttu-id="d89dd-147">Geração de URL é o processo pelo qual o roteamento pode criar um caminho de URL com base em um conjunto de valores de rota.</span><span class="sxs-lookup"><span data-stu-id="d89dd-147">URL generation is the process by which routing can create a URL path based on a set of route values.</span></span> <span data-ttu-id="d89dd-148">Isso permite uma separação lógica entre seus manipuladores e as URLs que acessá-los.</span><span class="sxs-lookup"><span data-stu-id="d89dd-148">This allows for a logical separation between your handlers and the URLs that access them.</span></span>

<span data-ttu-id="d89dd-149">Geração de URL segue um processo iterativo semelhante, mas começa com o código de usuário ou do framework chamando o `GetVirtualPath` método de coleção de rotas.</span><span class="sxs-lookup"><span data-stu-id="d89dd-149">URL generation follows a similar iterative process, but starts with user or framework code calling into the `GetVirtualPath` method of the route collection.</span></span> <span data-ttu-id="d89dd-150">Cada *rota* terá seu `GetVirtualPath` método chamado em sequência até não null `VirtualPathData` é retornado.</span><span class="sxs-lookup"><span data-stu-id="d89dd-150">Each *route* will then have its `GetVirtualPath` method called in sequence until a non-null `VirtualPathData` is returned.</span></span>

<span data-ttu-id="d89dd-151">O principal entradas para `GetVirtualPath` são:</span><span class="sxs-lookup"><span data-stu-id="d89dd-151">The primary inputs to `GetVirtualPath` are:</span></span>

* <span data-ttu-id="d89dd-152">`VirtualPathContext` `HttpContext`</span><span class="sxs-lookup"><span data-stu-id="d89dd-152">`VirtualPathContext` `HttpContext`</span></span>

* <span data-ttu-id="d89dd-153">`VirtualPathContext` `Values`</span><span class="sxs-lookup"><span data-stu-id="d89dd-153">`VirtualPathContext` `Values`</span></span>

* <span data-ttu-id="d89dd-154">`VirtualPathContext` `AmbientValues`</span><span class="sxs-lookup"><span data-stu-id="d89dd-154">`VirtualPathContext` `AmbientValues`</span></span>

<span data-ttu-id="d89dd-155">Rotas usem principalmente os valores de rota fornecidos pelo `Values` e `AmbientValues` para decidir onde é possível gerar uma URL e os valores a serem incluídos.</span><span class="sxs-lookup"><span data-stu-id="d89dd-155">Routes primarily use the route values provided by the `Values` and `AmbientValues` to decide where it is possible to generate a URL and what values to include.</span></span> <span data-ttu-id="d89dd-156">O `AmbientValues` são o conjunto de valores de rota que foram produzidas pela correspondência a solicitação atual com o sistema de roteamento.</span><span class="sxs-lookup"><span data-stu-id="d89dd-156">The `AmbientValues` are the set of route values that were produced from matching the current request with the routing system.</span></span> <span data-ttu-id="d89dd-157">Por outro lado, `Values` são os valores de rota que especificam como gerar a URL desejada para a operação atual.</span><span class="sxs-lookup"><span data-stu-id="d89dd-157">In contrast, `Values` are the route values that specify how to generate the desired URL for the current operation.</span></span> <span data-ttu-id="d89dd-158">O `HttpContext` é fornecido no caso de uma rota precisa obter serviços ou dados adicionais associados ao contexto atual.</span><span class="sxs-lookup"><span data-stu-id="d89dd-158">The `HttpContext` is provided in case a route needs to get services or additional data associated with the current context.</span></span>

<span data-ttu-id="d89dd-159">Dica: Pensar `Values` como sendo um conjunto de substituições para o `AmbientValues`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-159">Tip: Think of `Values` as being a set of overrides for the `AmbientValues`.</span></span> <span data-ttu-id="d89dd-160">Geração de URL tenta reutilizar valores de rota da solicitação atual para tornar mais fácil gerar URLs para links usando o mesmo roteiro ou valores de rota.</span><span class="sxs-lookup"><span data-stu-id="d89dd-160">URL generation tries to reuse route values from the current request to make it easy to generate URLs for links using the same route or route values.</span></span>

<span data-ttu-id="d89dd-161">A saída de `GetVirtualPath` é um `VirtualPathData`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-161">The output of `GetVirtualPath` is a `VirtualPathData`.</span></span> <span data-ttu-id="d89dd-162">`VirtualPathData`é um paralelo do `RouteData`; ele contém o `VirtualPath` para a URL de saída, bem como algumas propriedades adicionais que devem ser definidas pela rota.</span><span class="sxs-lookup"><span data-stu-id="d89dd-162">`VirtualPathData` is a parallel of `RouteData`; it contains the `VirtualPath` for the output URL as well as the some additional properties that should be set by the route.</span></span>

<span data-ttu-id="d89dd-163">O `VirtualPathData` `VirtualPath` propriedade contém o *caminho virtual* produzido pela rota.</span><span class="sxs-lookup"><span data-stu-id="d89dd-163">The `VirtualPathData` `VirtualPath` property contains the *virtual path* produced by the route.</span></span> <span data-ttu-id="d89dd-164">Dependendo de suas necessidades, talvez seja necessário processar ainda mais o caminho.</span><span class="sxs-lookup"><span data-stu-id="d89dd-164">Depending on your needs you may need to process the path further.</span></span> <span data-ttu-id="d89dd-165">Por exemplo, se você deseja processar a URL gerada em HTML, você precisa colocar o caminho base do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="d89dd-165">For instance, if you want to render the generated URL in HTML you need to prepend the base path of the application.</span></span>

<span data-ttu-id="d89dd-166">O `VirtualPathData` `Router` é uma referência para a rota que é gerado com êxito a URL.</span><span class="sxs-lookup"><span data-stu-id="d89dd-166">The `VirtualPathData` `Router` is a reference to the route that successfully generated the URL.</span></span>

<span data-ttu-id="d89dd-167">O `VirtualPathData` `DataTokens` propriedades é um dicionário de dados adicionais relacionados à rota que gerou a URL.</span><span class="sxs-lookup"><span data-stu-id="d89dd-167">The `VirtualPathData` `DataTokens` properties is a dictionary of additional data related to the route that generated the URL.</span></span> <span data-ttu-id="d89dd-168">Isso é paralelo de `RouteData.DataTokens`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-168">This is the parallel of `RouteData.DataTokens`.</span></span>

### <a name="creating-routes"></a><span data-ttu-id="d89dd-169">Criar rotas</span><span class="sxs-lookup"><span data-stu-id="d89dd-169">Creating routes</span></span>

<span data-ttu-id="d89dd-170">O roteamento fornece o `Route` classe como a implementação padrão de `IRouter`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-170">Routing provides the `Route` class as the standard implementation of `IRouter`.</span></span> <span data-ttu-id="d89dd-171">`Route`usa o *modelo de rota* sintaxe para definir padrões que fará a correspondência com o caminho de URL quando `RouteAsync` é chamado.</span><span class="sxs-lookup"><span data-stu-id="d89dd-171">`Route` uses the *route template* syntax to define patterns that will match against the URL path when `RouteAsync` is called.</span></span> <span data-ttu-id="d89dd-172">`Route`usará o mesmo modelo de rota para gerar uma URL quando `GetVirtualPath` é chamado.</span><span class="sxs-lookup"><span data-stu-id="d89dd-172">`Route` will use the same route template to generate a URL when `GetVirtualPath` is called.</span></span>

<span data-ttu-id="d89dd-173">A maioria dos aplicativos criará rotas chamando `MapRoute` ou um dos métodos de extensão semelhante definidos em `IRouteBuilder`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-173">Most applications will create routes by calling `MapRoute` or one of the similar extension methods defined on `IRouteBuilder`.</span></span> <span data-ttu-id="d89dd-174">Todos esses métodos criará uma instância de `Route` e adicioná-la à coleção de rotas.</span><span class="sxs-lookup"><span data-stu-id="d89dd-174">All of these methods will create an instance of `Route` and add it to the route collection.</span></span>

<span data-ttu-id="d89dd-175">Observação: `MapRoute` não tem um parâmetro de manipulador de rota - ela apenas adiciona as rotas que serão tratadas pelo `DefaultHandler`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-175">Note: `MapRoute` doesn't take a route handler parameter - it only adds routes that will be handled by the `DefaultHandler`.</span></span> <span data-ttu-id="d89dd-176">Como o manipulador padrão é um `IRouter`, ele pode decidir por não manipular a solicitação.</span><span class="sxs-lookup"><span data-stu-id="d89dd-176">Since the default handler is an `IRouter`, it may decide not to handle the request.</span></span> <span data-ttu-id="d89dd-177">Por exemplo, ASP.NET MVC normalmente é configurado como um manipulador padrão que só trata as solicitações que correspondem um controlador disponível e a ação.</span><span class="sxs-lookup"><span data-stu-id="d89dd-177">For example, ASP.NET MVC is typically configured as a default handler that only handles requests that match an available controller and action.</span></span> <span data-ttu-id="d89dd-178">Para saber mais sobre o roteamento para MVC, consulte [roteamento para ações do controlador](../mvc/controllers/routing.md).</span><span class="sxs-lookup"><span data-stu-id="d89dd-178">To learn more about routing to MVC, see [Routing to Controller Actions](../mvc/controllers/routing.md).</span></span>

<span data-ttu-id="d89dd-179">Este é um exemplo de um `MapRoute` chamada usada por uma definição de rota ASP.NET MVC típica:</span><span class="sxs-lookup"><span data-stu-id="d89dd-179">This is an example of a `MapRoute` call used by a typical ASP.NET MVC route definition:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id?}");
```

<span data-ttu-id="d89dd-180">Este modelo corresponderá a um caminho de URL como `/Products/Details/17` e extrair os valores de rota `{ controller = Products, action = Details, id = 17 }`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-180">This template will match a URL path like `/Products/Details/17` and extract the route values `{ controller = Products, action = Details, id = 17 }`.</span></span> <span data-ttu-id="d89dd-181">Os valores de rota são determinados pelo dividir o caminho da URL em segmentos e correspondência de cada segmento com o *parâmetro de rota* nome do modelo de rota.</span><span class="sxs-lookup"><span data-stu-id="d89dd-181">The route values are determined by splitting the URL path into segments, and matching each segment with the *route parameter* name in the route template.</span></span> <span data-ttu-id="d89dd-182">Parâmetros de rota são nomeados.</span><span class="sxs-lookup"><span data-stu-id="d89dd-182">Route parameters are named.</span></span> <span data-ttu-id="d89dd-183">Eles são definidos, colocando o nome do parâmetro chaves `{ }`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-183">They are defined by enclosing the parameter name in braces `{ }`.</span></span>

<span data-ttu-id="d89dd-184">O modelo acima também pode corresponder ao caminho da URL `/` e produzir valores `{ controller = Home, action = Index }`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-184">The template above could also match the URL path `/` and would produce the values `{ controller = Home, action = Index }`.</span></span> <span data-ttu-id="d89dd-185">Isso acontece porque o `{controller}` e `{action}` parâmetros de rota têm valores padrão e o `id` parâmetro de rota é opcional.</span><span class="sxs-lookup"><span data-stu-id="d89dd-185">This happens because the `{controller}` and `{action}` route parameters have default values, and the `id` route parameter is optional.</span></span> <span data-ttu-id="d89dd-186">Igual a `=` sinal seguido por um valor após o nome do parâmetro de rota define um valor padrão para o parâmetro.</span><span class="sxs-lookup"><span data-stu-id="d89dd-186">An equals `=` sign followed by a value after the route parameter name defines a default value for the parameter.</span></span> <span data-ttu-id="d89dd-187">Um ponto de interrogação `?` depois que o nome do parâmetro de rota define o parâmetro como opcional.</span><span class="sxs-lookup"><span data-stu-id="d89dd-187">A question mark `?` after the route parameter name defines the parameter as optional.</span></span> <span data-ttu-id="d89dd-188">Parâmetros com um valor padrão de rota *sempre* produzir um valor de rota quando a rota corresponde - parâmetros opcionais não produzir um valor de rota se não houver nenhum segmento de caminho de URL correspondente.</span><span class="sxs-lookup"><span data-stu-id="d89dd-188">Route parameters with a default value *always* produce a route value when the route matches - optional parameters will not produce a route value if there was no corresponding URL path segment.</span></span>

<span data-ttu-id="d89dd-189">Consulte [referência de modelo de rota](#route-template-reference) para obter uma descrição completa dos recursos de modelo de rota e sintaxe.</span><span class="sxs-lookup"><span data-stu-id="d89dd-189">See [route-template-reference](#route-template-reference) for a thorough description of route template features and syntax.</span></span>

<span data-ttu-id="d89dd-190">Este exemplo inclui uma *restrição de rota*:</span><span class="sxs-lookup"><span data-stu-id="d89dd-190">This example includes a *route constraint*:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id:int}");
```

<span data-ttu-id="d89dd-191">Este modelo corresponderá a um caminho de URL como `/Products/Details/17`, mas não `/Products/Details/Apples`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-191">This template will match a URL path like `/Products/Details/17`, but not `/Products/Details/Apples`.</span></span> <span data-ttu-id="d89dd-192">A definição do parâmetro de rota `{id:int}` define uma *restrição de rota* para o `id` parâmetro de rota.</span><span class="sxs-lookup"><span data-stu-id="d89dd-192">The route parameter definition `{id:int}` defines a *route constraint* for the `id` route parameter.</span></span> <span data-ttu-id="d89dd-193">Implementam restrições da rota `IRouteConstraint` e inspecionar os valores de rota para verificá-los.</span><span class="sxs-lookup"><span data-stu-id="d89dd-193">Route constraints implement `IRouteConstraint` and inspect route values to verify them.</span></span> <span data-ttu-id="d89dd-194">Neste exemplo, o valor de rota `id` deve ser conversível em inteiro.</span><span class="sxs-lookup"><span data-stu-id="d89dd-194">In this example the route value `id` must be convertible to an integer.</span></span> <span data-ttu-id="d89dd-195">Consulte [referência de restrição de rota](#route-constraint-reference) para obter uma explicação mais detalhada de restrições de rota que são fornecidos pela estrutura.</span><span class="sxs-lookup"><span data-stu-id="d89dd-195">See [route-constraint-reference](#route-constraint-reference) for a more detailed explanation of route constraints that are provided by the framework.</span></span>

<span data-ttu-id="d89dd-196">Sobrecargas adicionais de `MapRoute` aceitam valores de `constraints`, `dataTokens`, e `defaults`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-196">Additional overloads of `MapRoute` accept values for `constraints`, `dataTokens`, and `defaults`.</span></span> <span data-ttu-id="d89dd-197">Esses parâmetros adicionais de `MapRoute` são definidos como tipo `object`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-197">These additional parameters of `MapRoute` are defined as type `object`.</span></span> <span data-ttu-id="d89dd-198">O uso típico desses parâmetros é passar um objeto tipo anônimos, onde os nomes de propriedade da correspondência de tipo anônimo nomes de parâmetro de rota.</span><span class="sxs-lookup"><span data-stu-id="d89dd-198">The typical usage of these parameters is to pass an anonymously typed object, where the property names of the anonymous type match route parameter names.</span></span>

<span data-ttu-id="d89dd-199">Os exemplos a seguir criam rotas equivalentes:</span><span class="sxs-lookup"><span data-stu-id="d89dd-199">The following two examples create equivalent routes:</span></span>

```csharp
routes.MapRoute(
    name: "default_route",
    template: "{controller}/{action}/{id?}",
    defaults: new { controller = "Home", action = "Index" });

routes.MapRoute(
    name: "default_route",
    template: "{controller=Home}/{action=Index}/{id?}");
```

<span data-ttu-id="d89dd-200">Dica: A sintaxe embutida para definir padrões e restrições pode ser mais conveniente para rotas simples.</span><span class="sxs-lookup"><span data-stu-id="d89dd-200">Tip: The inline syntax for defining constraints and defaults can be more convenient for simple routes.</span></span> <span data-ttu-id="d89dd-201">No entanto, existem recursos, como tokens de dados que não são suportados pela sintaxe embutida.</span><span class="sxs-lookup"><span data-stu-id="d89dd-201">However, there are features such as data tokens which are not supported by inline syntax.</span></span>

<span data-ttu-id="d89dd-202">Este exemplo demonstra alguns dos recursos mais:</span><span class="sxs-lookup"><span data-stu-id="d89dd-202">This example demonstrates a few more features:</span></span>

```csharp
routes.MapRoute(
  name: "blog",
  template: "Blog/{*article}",
  defaults: new { controller = "Blog", action = "ReadArticle" });
```

<span data-ttu-id="d89dd-203">Este modelo corresponderá a um caminho de URL como `/Blog/All-About-Routing/Introduction` e extrairá os valores `{ controller = Blog, action = ReadArticle, article = All-About-Routing/Introduction }`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-203">This template will match a URL path like `/Blog/All-About-Routing/Introduction` and will extract the values `{ controller = Blog, action = ReadArticle, article = All-About-Routing/Introduction }`.</span></span> <span data-ttu-id="d89dd-204">Valores de rota padrão para `controller` e `action` são produzidas pela rota, mesmo que não haja nenhum parâmetro de rota correspondente no modelo.</span><span class="sxs-lookup"><span data-stu-id="d89dd-204">The default route values for `controller` and `action` are produced by the route even though there are no corresponding route parameters in the template.</span></span> <span data-ttu-id="d89dd-205">Valores padrão podem ser especificados no modelo de rota.</span><span class="sxs-lookup"><span data-stu-id="d89dd-205">Default values can be specified in the route template.</span></span> <span data-ttu-id="d89dd-206">O `article` parâmetro de rota é definido como um *pega-tudo* pela aparência de um asterisco `*` antes do nome de parâmetro de rota.</span><span class="sxs-lookup"><span data-stu-id="d89dd-206">The `article` route parameter is defined as a *catch-all* by the appearance of an asterisk `*` before the route parameter name.</span></span> <span data-ttu-id="d89dd-207">Parâmetros de rota pega-tudo capturar o restante do caminho da URL e também podem corresponder a cadeia de caracteres vazia.</span><span class="sxs-lookup"><span data-stu-id="d89dd-207">Catch-all route parameters capture the remainder of the URL path, and can also match the empty string.</span></span>

<span data-ttu-id="d89dd-208">Este exemplo adiciona tokens de dados e restrições da rota:</span><span class="sxs-lookup"><span data-stu-id="d89dd-208">This example adds route constraints and data tokens:</span></span>

```csharp
routes.MapRoute(
    name: "us_english_products",
    template: "en-US/Products/{id}",
    defaults: new { controller = "Products", action = "Details" },
    constraints: new { id = new IntRouteConstraint() },
    dataTokens: new { locale = "en-US" });
```

<span data-ttu-id="d89dd-209">Este modelo corresponderá a um caminho de URL como `/Products/5` e extrairá os valores `{ controller = Products, action = Details, id = 5 }` e os tokens de dados `{ locale = en-US }`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-209">This template will match a URL path like `/Products/5` and will extract the values `{ controller = Products, action = Details, id = 5 }` and the data tokens `{ locale = en-US }`.</span></span>

![Tokens do Windows locais](routing/_static/tokens.png)

<a name=id1></a>

### <a name="url-generation"></a><span data-ttu-id="d89dd-211">Geração de URL</span><span class="sxs-lookup"><span data-stu-id="d89dd-211">URL generation</span></span>

<span data-ttu-id="d89dd-212">O `Route` classe também pode executar a geração de URL, a combinação de um conjunto de valores de rota com seu modelo de rota.</span><span class="sxs-lookup"><span data-stu-id="d89dd-212">The `Route` class can also perform URL generation by combining a set of route values with its route template.</span></span> <span data-ttu-id="d89dd-213">Logicamente, este é o processo inverso de correspondência, o caminho da URL.</span><span class="sxs-lookup"><span data-stu-id="d89dd-213">This is logically the reverse process of matching the URL path.</span></span>

<span data-ttu-id="d89dd-214">Dica: Para entender melhor a geração de URL, imagine que você deseja gerar e, em seguida, considere como um modelo de rota corresponderia a essa URL de URL.</span><span class="sxs-lookup"><span data-stu-id="d89dd-214">Tip: To better understand URL generation, imagine what URL you want to generate and then think about how a route template would match that URL.</span></span> <span data-ttu-id="d89dd-215">Quais valores deve ser produzido?</span><span class="sxs-lookup"><span data-stu-id="d89dd-215">What values would be produced?</span></span> <span data-ttu-id="d89dd-216">Este é o equivalente de como funciona a geração de URL a `Route` classe.</span><span class="sxs-lookup"><span data-stu-id="d89dd-216">This is the rough equivalent of how URL generation works in the `Route` class.</span></span>

<span data-ttu-id="d89dd-217">Este exemplo usa uma rota de estilo do ASP.NET MVC básica:</span><span class="sxs-lookup"><span data-stu-id="d89dd-217">This example uses a basic ASP.NET MVC style route:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id?}");
```

<span data-ttu-id="d89dd-218">Com os valores de rota `{ controller = Products, action = List }`, essa rota irá gerar a URL `/Products/List`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-218">With the route values `{ controller = Products, action = List }`, this route will generate the URL `/Products/List`.</span></span> <span data-ttu-id="d89dd-219">Os valores de rota são substituídos para os parâmetros de rota correspondente formar o caminho da URL.</span><span class="sxs-lookup"><span data-stu-id="d89dd-219">The route values are substituted for the corresponding route parameters to form the URL path.</span></span> <span data-ttu-id="d89dd-220">Como `id` é um parâmetro de rota, não há problema se ele não tem um valor.</span><span class="sxs-lookup"><span data-stu-id="d89dd-220">Since `id` is an optional route parameter, it's no problem that it doesn't have a value.</span></span>

<span data-ttu-id="d89dd-221">Com os valores de rota `{ controller = Home, action = Index }`, essa rota irá gerar a URL `/`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-221">With the route values `{ controller = Home, action = Index }`, this route will generate the URL `/`.</span></span> <span data-ttu-id="d89dd-222">Os valores de rota que foram fornecidos correspondem os valores padrão para que os segmentos correspondentes a esses valores podem ser omitidos com segurança.</span><span class="sxs-lookup"><span data-stu-id="d89dd-222">The route values that were provided match the default values so the segments corresponding to those values can be safely omitted.</span></span> <span data-ttu-id="d89dd-223">Observe que as duas URLs geradas seriam ida e volta com essa definição de rota e produzem os mesmos valores de rota que foram usados para gerar a URL.</span><span class="sxs-lookup"><span data-stu-id="d89dd-223">Note that both URLs generated would round-trip with this route definition and produce the same route values that were used to generate the URL.</span></span>

<span data-ttu-id="d89dd-224">Dica: Um aplicativo usando o ASP.NET MVC deve usar `UrlHelper` para gerar URLs, em vez de chamar diretamente o roteamento.</span><span class="sxs-lookup"><span data-stu-id="d89dd-224">Tip: An app using ASP.NET MVC should use `UrlHelper` to generate URLs instead of calling into routing directly.</span></span>

<span data-ttu-id="d89dd-225">Para obter mais detalhes sobre o processo de geração de URL, consulte [referência de geração de url](#url-generation-reference).</span><span class="sxs-lookup"><span data-stu-id="d89dd-225">For more details about the URL generation process, see [url-generation-reference](#url-generation-reference).</span></span>

## <a name="using-routing-middleware"></a><span data-ttu-id="d89dd-226">Usando o roteamento de Middleware</span><span class="sxs-lookup"><span data-stu-id="d89dd-226">Using Routing Middleware</span></span>

<span data-ttu-id="d89dd-227">Adicione o pacote do NuGet "Microsoft.AspNetCore.Routing".</span><span class="sxs-lookup"><span data-stu-id="d89dd-227">Add the NuGet package "Microsoft.AspNetCore.Routing".</span></span>

<span data-ttu-id="d89dd-228">Adicionar roteamento para o contêiner de serviço no *Startup.cs*:</span><span class="sxs-lookup"><span data-stu-id="d89dd-228">Add routing to the service container in *Startup.cs*:</span></span>

<span data-ttu-id="d89dd-229">[!code-csharp[Main](../fundamentals/routing/sample/RoutingSample/Startup.cs?highlight=3&start=11&end=14)]</span><span class="sxs-lookup"><span data-stu-id="d89dd-229">[!code-csharp[Main](../fundamentals/routing/sample/RoutingSample/Startup.cs?highlight=3&start=11&end=14)]</span></span>

<span data-ttu-id="d89dd-230">Rotas devem ser configuradas no `Configure` método o `Startup` classe.</span><span class="sxs-lookup"><span data-stu-id="d89dd-230">Routes must be configured in the `Configure` method in the `Startup` class.</span></span> <span data-ttu-id="d89dd-231">O exemplo a seguir usa essas APIs:</span><span class="sxs-lookup"><span data-stu-id="d89dd-231">The sample below uses these APIs:</span></span>

* `RouteBuilder`
* `Build`
* <span data-ttu-id="d89dd-232">`MapGet`Faz a correspondência apenas das solicitações HTTP GET</span><span class="sxs-lookup"><span data-stu-id="d89dd-232">`MapGet`  Matches only HTTP GET requests</span></span>
* `UseRouter`

<!-- literal_block {"xml:space": "preserve", "source": "fundamentals/routing/sample/RoutingSample/Startup.cs", "ids": [], "linenos": false, "highlight_args": {"linenostart": 1}} -->

```csharp
public void Configure(IApplicationBuilder app, ILoggerFactory loggerFactory)
{
    var trackPackageRouteHandler = new RouteHandler(context =>
    {
        var routeValues = context.GetRouteData().Values;
        return context.Response.WriteAsync(
            $"Hello! Route values: {string.Join(", ", routeValues)}");
    });

    var routeBuilder = new RouteBuilder(app, trackPackageRouteHandler);

    routeBuilder.MapRoute(
        "Track Package Route",
        "package/{operation:regex(^(track|create|detonate)$)}/{id:int}");

    routeBuilder.MapGet("hello/{name}", context =>
    {
        var name = context.GetRouteValue("name");
        // This is the route handler when HTTP GET "hello/<anything>"  matches
        // To match HTTP GET "hello/<anything>/<anything>,
        // use routeBuilder.MapGet("hello/{*name}"
        return context.Response.WriteAsync($"Hi, {name}!");
    });

    var routes = routeBuilder.Build();
    app.UseRouter(routes);
}
```

<span data-ttu-id="d89dd-233">A tabela a seguir mostra as respostas com os URIs determinado.</span><span class="sxs-lookup"><span data-stu-id="d89dd-233">The table below shows the responses with the given URIs.</span></span>

| <span data-ttu-id="d89dd-234">URI</span><span class="sxs-lookup"><span data-stu-id="d89dd-234">URI</span></span> | <span data-ttu-id="d89dd-235">Resposta</span><span class="sxs-lookup"><span data-stu-id="d89dd-235">Response</span></span>  |
| ------- | -------- |
| <span data-ttu-id="d89dd-236">/Package/Create/3</span><span class="sxs-lookup"><span data-stu-id="d89dd-236">/package/create/3</span></span>  | <span data-ttu-id="d89dd-237">Olá!</span><span class="sxs-lookup"><span data-stu-id="d89dd-237">Hello!</span></span> <span data-ttu-id="d89dd-238">Valores de rota: [operação, criar], [id-3]</span><span class="sxs-lookup"><span data-stu-id="d89dd-238">Route values: [operation, create], [id, 3]</span></span> |
| <span data-ttu-id="d89dd-239">pacote/faixa /-3</span><span class="sxs-lookup"><span data-stu-id="d89dd-239">/package/track/-3</span></span>  | <span data-ttu-id="d89dd-240">Olá!</span><span class="sxs-lookup"><span data-stu-id="d89dd-240">Hello!</span></span> <span data-ttu-id="d89dd-241">Valores de rota: [operação, acompanhar], [id -3]</span><span class="sxs-lookup"><span data-stu-id="d89dd-241">Route values: [operation, track], [id, -3]</span></span> |
| <span data-ttu-id="d89dd-242">/ pacote/acompanhar/3 /</span><span class="sxs-lookup"><span data-stu-id="d89dd-242">/package/track/-3/</span></span> | <span data-ttu-id="d89dd-243">Olá!</span><span class="sxs-lookup"><span data-stu-id="d89dd-243">Hello!</span></span> <span data-ttu-id="d89dd-244">Valores de rota: [operação, acompanhar], [id -3]</span><span class="sxs-lookup"><span data-stu-id="d89dd-244">Route values: [operation, track], [id, -3]</span></span>  |
| <span data-ttu-id="d89dd-245">/Package./faixa /</span><span class="sxs-lookup"><span data-stu-id="d89dd-245">/package/track/</span></span> | <span data-ttu-id="d89dd-246">\<Passar, nenhuma correspondência ></span><span class="sxs-lookup"><span data-stu-id="d89dd-246">\<Fall through, no match></span></span> |
| <span data-ttu-id="d89dd-247">OBTER /hello/Joe</span><span class="sxs-lookup"><span data-stu-id="d89dd-247">GET /hello/Joe</span></span> | <span data-ttu-id="d89dd-248">Olá, Joe!</span><span class="sxs-lookup"><span data-stu-id="d89dd-248">Hi, Joe!</span></span> |
| <span data-ttu-id="d89dd-249">Lançar /hello/Joe</span><span class="sxs-lookup"><span data-stu-id="d89dd-249">POST /hello/Joe</span></span> | <span data-ttu-id="d89dd-250">\<Passar, corresponde a HTTP GET apenas ></span><span class="sxs-lookup"><span data-stu-id="d89dd-250">\<Fall through, matches HTTP GET only></span></span> |
| <span data-ttu-id="d89dd-251">OBTER /hello/Joe/Smith</span><span class="sxs-lookup"><span data-stu-id="d89dd-251">GET /hello/Joe/Smith</span></span> | <span data-ttu-id="d89dd-252">\<Passar, nenhuma correspondência ></span><span class="sxs-lookup"><span data-stu-id="d89dd-252">\<Fall through, no match></span></span> |

<span data-ttu-id="d89dd-253">Se você estiver configurando uma única rota, chame `app.UseRouter` passando um `IRouter` instância.</span><span class="sxs-lookup"><span data-stu-id="d89dd-253">If you are configuring a single route, call `app.UseRouter` passing in an `IRouter` instance.</span></span> <span data-ttu-id="d89dd-254">Você não precisará chamar `RouteBuilder`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-254">You won't need to call `RouteBuilder`.</span></span>

<span data-ttu-id="d89dd-255">O framework fornece um conjunto de métodos de extensão para criar rotas, como:</span><span class="sxs-lookup"><span data-stu-id="d89dd-255">The framework provides a set of extension methods for creating routes such as:</span></span>

* `MapRoute`
* `MapGet`
* `MapPost`
* `MapPut`
* `MapDelete`
* `MapVerb`

<span data-ttu-id="d89dd-256">Alguns desses métodos, como `MapGet` exigem um `RequestDelegate` a ser fornecido.</span><span class="sxs-lookup"><span data-stu-id="d89dd-256">Some of these methods such as `MapGet` require a `RequestDelegate` to be provided.</span></span> <span data-ttu-id="d89dd-257">O `RequestDelegate` será usado como o *manipulador de rota* quando a rota corresponde.</span><span class="sxs-lookup"><span data-stu-id="d89dd-257">The `RequestDelegate` will be used as the *route handler* when the route matches.</span></span> <span data-ttu-id="d89dd-258">Outros métodos nesta família permitem configurar um pipeline de middleware que será usado como o manipulador de rota.</span><span class="sxs-lookup"><span data-stu-id="d89dd-258">Other methods in this family allow configuring a middleware pipeline which will be used as the route handler.</span></span> <span data-ttu-id="d89dd-259">Se o *mapa* método não aceita como um manipulador, `MapRoute`, em seguida, ele usará o `DefaultHandler`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-259">If the *Map* method doesn't accept a handler, such as `MapRoute`, then it will use the `DefaultHandler`.</span></span>

<span data-ttu-id="d89dd-260">O `Map[Verb]` métodos usam restrições para limitar a rota para o verbo HTTP no nome do método.</span><span class="sxs-lookup"><span data-stu-id="d89dd-260">The `Map[Verb]` methods use constraints to limit the route to the HTTP Verb in the method name.</span></span> <span data-ttu-id="d89dd-261">Por exemplo, consulte [MapGet](https://github.com/aspnet/Routing/blob/1.0.0/src/Microsoft.AspNetCore.Routing/RequestDelegateRouteBuilderExtensions.cs#L85-L88) e [MapVerb](https://github.com/aspnet/Routing/blob/1.0.0/src/Microsoft.AspNetCore.Routing/RequestDelegateRouteBuilderExtensions.cs#L156-L180).</span><span class="sxs-lookup"><span data-stu-id="d89dd-261">For example, see [MapGet](https://github.com/aspnet/Routing/blob/1.0.0/src/Microsoft.AspNetCore.Routing/RequestDelegateRouteBuilderExtensions.cs#L85-L88) and [MapVerb](https://github.com/aspnet/Routing/blob/1.0.0/src/Microsoft.AspNetCore.Routing/RequestDelegateRouteBuilderExtensions.cs#L156-L180).</span></span>

## <a name="route-template-reference"></a><span data-ttu-id="d89dd-262">Referência de modelo de rota</span><span class="sxs-lookup"><span data-stu-id="d89dd-262">Route Template Reference</span></span>

<span data-ttu-id="d89dd-263">Tokens entre chaves (`{ }`) definir *parâmetros de rota* que será associado se a rota for atendida.</span><span class="sxs-lookup"><span data-stu-id="d89dd-263">Tokens within curly braces (`{ }`) define *route parameters* which will be bound if the route is matched.</span></span> <span data-ttu-id="d89dd-264">Você pode definir mais de um parâmetro de rota em um segmento de rota, mas eles devem ser separados por um valor literal.</span><span class="sxs-lookup"><span data-stu-id="d89dd-264">You can define more than one route parameter in a route segment, but they must be separated by a literal value.</span></span> <span data-ttu-id="d89dd-265">Por exemplo `{controller=Home}{action=Index}` não seria uma rota válida, porque não há nenhum valor literal entre `{controller}` e `{action}`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-265">For example `{controller=Home}{action=Index}` would not be a valid route, since there is no literal value between `{controller}` and `{action}`.</span></span> <span data-ttu-id="d89dd-266">Esses parâmetros de rota devem ter um nome, e podem ter atributos adicionais especificado.</span><span class="sxs-lookup"><span data-stu-id="d89dd-266">These route parameters must have a name, and may have additional attributes specified.</span></span>

<span data-ttu-id="d89dd-267">Texto literal diferente de parâmetros de rota (por exemplo, `{id}`) e o separador de caminho `/` devem coincidir com o texto na URL.</span><span class="sxs-lookup"><span data-stu-id="d89dd-267">Literal text other than route parameters (for example, `{id}`) and the path separator `/` must match the text in the URL.</span></span> <span data-ttu-id="d89dd-268">Correspondência de texto é com base na representação decodificada do caminho URLs e diferencia maiusculas de minúsculas.</span><span class="sxs-lookup"><span data-stu-id="d89dd-268">Text matching is case-insensitive and based on the decoded representation of the URLs path.</span></span> <span data-ttu-id="d89dd-269">Para corresponder ao delimitador de parâmetro de rota literal `{` ou `}`, escape-o pelo caractere de repetição (`{{` ou `}}`).</span><span class="sxs-lookup"><span data-stu-id="d89dd-269">To match the literal route parameter delimiter `{` or  `}`, escape it by repeating the character (`{{` or `}}`).</span></span>

<span data-ttu-id="d89dd-270">Padrões de URL que tentam capturar um nome de arquivo com uma extensão de arquivo opcional têm considerações adicionais.</span><span class="sxs-lookup"><span data-stu-id="d89dd-270">URL patterns that attempt to capture a filename with an optional file extension have additional considerations.</span></span> <span data-ttu-id="d89dd-271">Por exemplo, usando o modelo `files/{filename}.{ext?}` - quando ambos `filename` e `ext` existir, os dois valores serão preenchidos.</span><span class="sxs-lookup"><span data-stu-id="d89dd-271">For example, using the template `files/{filename}.{ext?}` - When both `filename` and `ext` exist, both values will be populated.</span></span> <span data-ttu-id="d89dd-272">Se apenas `filename` existe na URL, as correspondências de rota, pois o ponto à direita `.` é opcional.</span><span class="sxs-lookup"><span data-stu-id="d89dd-272">If only `filename` exists in the URL, the route matches because the trailing period `.` is  optional.</span></span> <span data-ttu-id="d89dd-273">As seguintes URLs corresponderia essa rota:</span><span class="sxs-lookup"><span data-stu-id="d89dd-273">The following URLs would match this route:</span></span>

* `/files/myFile.txt`
* `/files/myFile.`
* `/files/myFile`

<span data-ttu-id="d89dd-274">Você pode usar o `*` caractere como um prefixo para um parâmetro de rota para ligar para o restante do URI - isso é chamado de um *pega-tudo* parâmetro.</span><span class="sxs-lookup"><span data-stu-id="d89dd-274">You can use the `*` character as a prefix to a route parameter to bind to the rest of the URI - this is called a *catch-all* parameter.</span></span> <span data-ttu-id="d89dd-275">Por exemplo, `blog/{*slug}` corresponderia qualquer URI que foi iniciado com `/blog` e tinha qualquer valor (que seriam atribuído ao `slug` rotear valor).</span><span class="sxs-lookup"><span data-stu-id="d89dd-275">For example, `blog/{*slug}` would match any URI that started with `/blog` and had any value following it (which would be assigned to the `slug` route value).</span></span> <span data-ttu-id="d89dd-276">Captura todos os parâmetros também podem corresponder a cadeia de caracteres vazia.</span><span class="sxs-lookup"><span data-stu-id="d89dd-276">Catch-all parameters can also match the empty string.</span></span>

<span data-ttu-id="d89dd-277">Parâmetros de rota podem ter *valores padrão*, designado, especificando o padrão após o nome do parâmetro, separado por um `=`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-277">Route parameters may have *default values*, designated by specifying the default after the parameter name, separated by an `=`.</span></span> <span data-ttu-id="d89dd-278">Por exemplo, `{controller=Home}` definiria `Home` como o valor padrão para `controller`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-278">For example, `{controller=Home}` would define `Home` as the default value for `controller`.</span></span> <span data-ttu-id="d89dd-279">O valor padrão é usado se nenhum valor estiver presente na URL para o parâmetro.</span><span class="sxs-lookup"><span data-stu-id="d89dd-279">The default value is used if no value is present in the URL for the parameter.</span></span> <span data-ttu-id="d89dd-280">Além dos valores padrão, os parâmetros de rota podem ser opcionais (especificado por meio do acréscimo um `?` ao final do nome do parâmetro, como em `id?`).</span><span class="sxs-lookup"><span data-stu-id="d89dd-280">In addition to default values, route parameters may be optional (specified by appending a `?` to the end of the parameter name, as in `id?`).</span></span> <span data-ttu-id="d89dd-281">A diferença entre opcional e "possui padrão" é que um parâmetro de rota com um valor padrão sempre produz um valor; um parâmetro opcional tem um valor somente quando é fornecida.</span><span class="sxs-lookup"><span data-stu-id="d89dd-281">The difference between optional and "has default" is that a route parameter with a default value always produces a value; an optional parameter has a value only when one is provided.</span></span>

<span data-ttu-id="d89dd-282">Parâmetros de rota também podem ter restrições, que devem corresponder ao valor de rota associado da URL.</span><span class="sxs-lookup"><span data-stu-id="d89dd-282">Route parameters may also have constraints, which must match the route value bound from the URL.</span></span> <span data-ttu-id="d89dd-283">Adição de dois-pontos `:` e depois que o nome do parâmetro de rota especifica o nome da restrição um *restrição embutida* em um parâmetro de rota.</span><span class="sxs-lookup"><span data-stu-id="d89dd-283">Adding a colon `:` and constraint name after the route parameter name specifies an *inline constraint* on a route parameter.</span></span> <span data-ttu-id="d89dd-284">Se a restrição requer argumentos aqueles fornecidos entre parênteses `( )` após o nome da restrição.</span><span class="sxs-lookup"><span data-stu-id="d89dd-284">If the constraint requires arguments those are provided enclosed in parentheses `( )` after the constraint name.</span></span> <span data-ttu-id="d89dd-285">Várias restrições embutido podem ser especificadas adicionando outra vírgula `:` e o nome da restrição.</span><span class="sxs-lookup"><span data-stu-id="d89dd-285">Multiple inline constraints can be specified by appending another colon `:` and constraint name.</span></span> <span data-ttu-id="d89dd-286">O nome da restrição é passado para o `IInlineConstraintResolver` service para criar uma instância de `IRouteConstraint` para usar no processamento de URL.</span><span class="sxs-lookup"><span data-stu-id="d89dd-286">The constraint name is passed to the `IInlineConstraintResolver` service to create an instance of `IRouteConstraint` to use in URL processing.</span></span> <span data-ttu-id="d89dd-287">Por exemplo, o modelo de rota `blog/{article:minlength(10)}` Especifica o `minlength` restrição com o argumento `10`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-287">For example, the route template `blog/{article:minlength(10)}` specifies the `minlength` constraint with the argument `10`.</span></span> <span data-ttu-id="d89dd-288">Para restrições da rota mais descrição e uma lista das restrições fornecidos pelo framework, consulte [referência de restrição de rota](#route-constraint-reference).</span><span class="sxs-lookup"><span data-stu-id="d89dd-288">For more description route constraints, and a listing of the constraints provided by the framework, see [route-constraint-reference](#route-constraint-reference).</span></span>

<span data-ttu-id="d89dd-289">A tabela a seguir demonstra alguns modelos de rota e seu comportamento.</span><span class="sxs-lookup"><span data-stu-id="d89dd-289">The following table demonstrates some route templates and their behavior.</span></span>

| <span data-ttu-id="d89dd-290">Modelo de rota</span><span class="sxs-lookup"><span data-stu-id="d89dd-290">Route Template</span></span> | <span data-ttu-id="d89dd-291">Exemplo de correspondência de URL</span><span class="sxs-lookup"><span data-stu-id="d89dd-291">Example Matching URL</span></span> | <span data-ttu-id="d89dd-292">Observações</span><span class="sxs-lookup"><span data-stu-id="d89dd-292">Notes</span></span> |
| -------- | -------- | ------- |
| <span data-ttu-id="d89dd-293">hello</span><span class="sxs-lookup"><span data-stu-id="d89dd-293">hello</span></span>  | <span data-ttu-id="d89dd-294">/Hello</span><span class="sxs-lookup"><span data-stu-id="d89dd-294">/hello</span></span>  | <span data-ttu-id="d89dd-295">Somente corresponda ao caminho único`/hello`</span><span class="sxs-lookup"><span data-stu-id="d89dd-295">Only matches the single path `/hello`</span></span> |
| <span data-ttu-id="d89dd-296">{Página = Home}</span><span class="sxs-lookup"><span data-stu-id="d89dd-296">{Page=Home}</span></span> | / | <span data-ttu-id="d89dd-297">Faz a correspondência e define `Page` para`Home`</span><span class="sxs-lookup"><span data-stu-id="d89dd-297">Matches and sets `Page` to `Home`</span></span> |
| <span data-ttu-id="d89dd-298">{Página = Home}</span><span class="sxs-lookup"><span data-stu-id="d89dd-298">{Page=Home}</span></span>  | <span data-ttu-id="d89dd-299">/ Contato</span><span class="sxs-lookup"><span data-stu-id="d89dd-299">/Contact</span></span>  | <span data-ttu-id="d89dd-300">Faz a correspondência e define `Page` para`Contact`</span><span class="sxs-lookup"><span data-stu-id="d89dd-300">Matches and sets `Page` to `Contact`</span></span> |
| <span data-ttu-id="d89dd-301">{controller} / {action} / {id}?</span><span class="sxs-lookup"><span data-stu-id="d89dd-301">{controller}/{action}/{id?}</span></span> | <span data-ttu-id="d89dd-302">/ / Lista de produtos</span><span class="sxs-lookup"><span data-stu-id="d89dd-302">/Products/List</span></span> | <span data-ttu-id="d89dd-303">Mapeia para `Products` controlador e `List` ação</span><span class="sxs-lookup"><span data-stu-id="d89dd-303">Maps to `Products` controller and `List`  action</span></span> |
| <span data-ttu-id="d89dd-304">{controller} / {action} / {id}?</span><span class="sxs-lookup"><span data-stu-id="d89dd-304">{controller}/{action}/{id?}</span></span> | <span data-ttu-id="d89dd-305">Produtos/123/detalhes</span><span class="sxs-lookup"><span data-stu-id="d89dd-305">/Products/Details/123</span></span>  |  <span data-ttu-id="d89dd-306">Mapeia para `Products` controlador e `Details` ação.</span><span class="sxs-lookup"><span data-stu-id="d89dd-306">Maps to `Products` controller and  `Details` action.</span></span>  <span data-ttu-id="d89dd-307">`id`definido como 123</span><span class="sxs-lookup"><span data-stu-id="d89dd-307">`id` set to 123</span></span> |
| <span data-ttu-id="d89dd-308">{controlador = Home} / {ação = índice} / {id}?</span><span class="sxs-lookup"><span data-stu-id="d89dd-308">{controller=Home}/{action=Index}/{id?}</span></span> | /  |  <span data-ttu-id="d89dd-309">Mapeia para `Home` controlador e `Index` método; `id` será ignorado.</span><span class="sxs-lookup"><span data-stu-id="d89dd-309">Maps to `Home` controller and `Index`  method; `id` is ignored.</span></span> |

<span data-ttu-id="d89dd-310">Usando um modelo geralmente é a abordagem mais simples para roteamento.</span><span class="sxs-lookup"><span data-stu-id="d89dd-310">Using a template is generally the simplest approach to routing.</span></span> <span data-ttu-id="d89dd-311">Restrições e padrões também podem ser especificados fora o modelo de rota.</span><span class="sxs-lookup"><span data-stu-id="d89dd-311">Constraints and defaults can also be specified outside the route template.</span></span>

<span data-ttu-id="d89dd-312">Dica: Habilitar [log](logging.md) para ver como o integradas em implementações de roteamentos, como `Route`, comparar solicitações.</span><span class="sxs-lookup"><span data-stu-id="d89dd-312">Tip: Enable [Logging](logging.md) to see how the built in routing implementations, such as `Route`, match requests.</span></span>

## <a name="route-constraint-reference"></a><span data-ttu-id="d89dd-313">Referência de restrição de rota</span><span class="sxs-lookup"><span data-stu-id="d89dd-313">Route Constraint Reference</span></span>

<span data-ttu-id="d89dd-314">Restrições da rota executar quando um `Route` tem correspondeu a sintaxe da URL de entrada e convertidos o caminho da URL em valores de rota.</span><span class="sxs-lookup"><span data-stu-id="d89dd-314">Route constraints execute when a `Route` has matched the syntax of the incoming URL and tokenized the URL path into route values.</span></span> <span data-ttu-id="d89dd-315">Restrições da rota geralmente inspecionar o valor de rota associado por meio do modelo de rota e fazer com que um simples Sim/não decisão sobre se o valor for aceitável.</span><span class="sxs-lookup"><span data-stu-id="d89dd-315">Route constraints generally inspect the route value associated via the route template and make a simple yes/no decision about whether or not the value is acceptable.</span></span> <span data-ttu-id="d89dd-316">Algumas restrições da rota usam dados fora do valor de rota para considerar se a solicitação pode ser roteada.</span><span class="sxs-lookup"><span data-stu-id="d89dd-316">Some route constraints use data outside the route value to consider whether the request can be routed.</span></span> <span data-ttu-id="d89dd-317">Por exemplo, o `HttpMethodRouteConstraint` pode aceitar ou rejeitar uma solicitação de acordo com o verbo HTTP.</span><span class="sxs-lookup"><span data-stu-id="d89dd-317">For example, the `HttpMethodRouteConstraint` can accept or reject a request based on its HTTP verb.</span></span>

>[!WARNING]
> <span data-ttu-id="d89dd-318">Evite usar restrições de **validação de entrada**porque fazendo isso, uma entrada inválida resultará em 404 (não encontrado) em vez de um 400 com uma mensagem de erro apropriado.</span><span class="sxs-lookup"><span data-stu-id="d89dd-318">Avoid using constraints for **input validation**, because doing so means that invalid input will result in a 404 (Not Found) instead of a 400 with an appropriate error message.</span></span> <span data-ttu-id="d89dd-319">Restrições da rota devem ser usadas para **ambiguidade** entre as rotas semelhantes, não para validar as entradas para uma rota específica.</span><span class="sxs-lookup"><span data-stu-id="d89dd-319">Route constraints should be used to **disambiguate** between similar routes, not to validate the inputs for a particular route.</span></span>

<span data-ttu-id="d89dd-320">A tabela a seguir demonstra alguns restrições da rota e o comportamento esperado.</span><span class="sxs-lookup"><span data-stu-id="d89dd-320">The following table demonstrates some route constraints and their expected behavior.</span></span>

| <span data-ttu-id="d89dd-321">restrição</span><span class="sxs-lookup"><span data-stu-id="d89dd-321">constraint</span></span> | <span data-ttu-id="d89dd-322">Exemplo</span><span class="sxs-lookup"><span data-stu-id="d89dd-322">Example</span></span> | <span data-ttu-id="d89dd-323">Correspondências de exemplo</span><span class="sxs-lookup"><span data-stu-id="d89dd-323">Example Matches</span></span> | <span data-ttu-id="d89dd-324">Observações</span><span class="sxs-lookup"><span data-stu-id="d89dd-324">Notes</span></span> |
| --------   | ------- | ------------- | ----- |
| `int` | `{id:int}` | <span data-ttu-id="d89dd-325">`123456789`, `-123456789`</span><span class="sxs-lookup"><span data-stu-id="d89dd-325">`123456789`, `-123456789`</span></span>  | <span data-ttu-id="d89dd-326">Corresponde a qualquer inteiro</span><span class="sxs-lookup"><span data-stu-id="d89dd-326">Matches any integer</span></span> |
| `bool`  | `{active:bool}` | <span data-ttu-id="d89dd-327">`true`, `FALSE`</span><span class="sxs-lookup"><span data-stu-id="d89dd-327">`true`, `FALSE`</span></span> | <span data-ttu-id="d89dd-328">Correspondências `true` ou `false` (diferencia maiusculas de minúsculas)</span><span class="sxs-lookup"><span data-stu-id="d89dd-328">Matches `true` or `false` (case-insensitive)</span></span> |
| `datetime` | `{dob:datetime}` | <span data-ttu-id="d89dd-329">`2016-12-31`, `2016-12-31 7:32pm`</span><span class="sxs-lookup"><span data-stu-id="d89dd-329">`2016-12-31`, `2016-12-31 7:32pm`</span></span>  | <span data-ttu-id="d89dd-330">Corresponde a uma opção válida `DateTime` valor (a cultura invariável - consulte aviso)</span><span class="sxs-lookup"><span data-stu-id="d89dd-330">Matches a valid `DateTime` value (in the invariant culture - see warning)</span></span> |
| `decimal` | `{price:decimal}` | <span data-ttu-id="d89dd-331">`49.99`, `-1,000.01`</span><span class="sxs-lookup"><span data-stu-id="d89dd-331">`49.99`, `-1,000.01`</span></span> | <span data-ttu-id="d89dd-332">Corresponde a uma opção válida `decimal` valor (a cultura invariável - consulte aviso)</span><span class="sxs-lookup"><span data-stu-id="d89dd-332">Matches a valid `decimal` value (in the invariant culture - see warning)</span></span> |
| `double`  | `{weight:double}` | <span data-ttu-id="d89dd-333">`1.234`, `-1,001.01e8`</span><span class="sxs-lookup"><span data-stu-id="d89dd-333">`1.234`, `-1,001.01e8`</span></span> | <span data-ttu-id="d89dd-334">Corresponde a uma opção válida `double` valor (a cultura invariável - consulte aviso)</span><span class="sxs-lookup"><span data-stu-id="d89dd-334">Matches a valid `double` value (in the invariant culture - see warning)</span></span> |
| `float`  | `{weight:float}` | <span data-ttu-id="d89dd-335">`1.234`, `-1,001.01e8`</span><span class="sxs-lookup"><span data-stu-id="d89dd-335">`1.234`, `-1,001.01e8`</span></span> | <span data-ttu-id="d89dd-336">Corresponde a uma opção válida `float` valor (a cultura invariável - consulte aviso)</span><span class="sxs-lookup"><span data-stu-id="d89dd-336">Matches a valid `float` value (in the invariant culture - see warning)</span></span> |
| `guid`  | `{id:guid}` | <span data-ttu-id="d89dd-337">`CD2C1638-1638-72D5-1638-DEADBEEF1638`, `{CD2C1638-1638-72D5-1638-DEADBEEF1638}`</span><span class="sxs-lookup"><span data-stu-id="d89dd-337">`CD2C1638-1638-72D5-1638-DEADBEEF1638`, `{CD2C1638-1638-72D5-1638-DEADBEEF1638}`</span></span> | <span data-ttu-id="d89dd-338">Corresponde a uma opção válida `Guid` valor</span><span class="sxs-lookup"><span data-stu-id="d89dd-338">Matches a valid `Guid` value</span></span> |
| `long` | `{ticks:long}` | <span data-ttu-id="d89dd-339">`123456789`, `-123456789`</span><span class="sxs-lookup"><span data-stu-id="d89dd-339">`123456789`, `-123456789`</span></span> | <span data-ttu-id="d89dd-340">Corresponde a uma opção válida `long` valor</span><span class="sxs-lookup"><span data-stu-id="d89dd-340">Matches a valid `long` value</span></span> |
| `minlength(value)` | `{username:minlength(4)}` | `Rick` | <span data-ttu-id="d89dd-341">Cadeia de caracteres deve ter pelo menos 4 caracteres</span><span class="sxs-lookup"><span data-stu-id="d89dd-341">String must be at least 4 characters</span></span> |
| `maxlength(value)` | `{filename:maxlength(8)}` | `Richard` | <span data-ttu-id="d89dd-342">Cadeia de caracteres deve ter não mais de 8 caracteres</span><span class="sxs-lookup"><span data-stu-id="d89dd-342">String must be no more than 8 characters</span></span> |
| `length(length)` | `{filename:length(12)}` | `somefile.txt` | <span data-ttu-id="d89dd-343">Cadeia de caracteres deve ter exatamente 12 caracteres</span><span class="sxs-lookup"><span data-stu-id="d89dd-343">String must be exactly 12 characters long</span></span> |
| `length(min,max)` | `{filename:length(8,16)}` | `somefile.txt` | <span data-ttu-id="d89dd-344">Cadeia de caracteres deve ter pelo menos 8 e não mais de 16 caracteres</span><span class="sxs-lookup"><span data-stu-id="d89dd-344">String must be at least 8 and no more than 16 characters long</span></span> |
| `min(value)` | `{age:min(18)}` | `19` | <span data-ttu-id="d89dd-345">Valor inteiro deve ser pelo menos 18</span><span class="sxs-lookup"><span data-stu-id="d89dd-345">Integer value must be at least 18</span></span> |
| `max(value)` | `{age:max(120)}` |  `91` | <span data-ttu-id="d89dd-346">Valor inteiro deve ser não mais de 120</span><span class="sxs-lookup"><span data-stu-id="d89dd-346">Integer value must be no more than 120</span></span> |
| `range(min,max)` | `{age:range(18,120)}` | `91` | <span data-ttu-id="d89dd-347">Valor inteiro deve ser pelo menos 18, mas não mais de 120</span><span class="sxs-lookup"><span data-stu-id="d89dd-347">Integer value must be at least 18 but no more than 120</span></span> |
| `alpha` | `{name:alpha}` | `Rick` | <span data-ttu-id="d89dd-348">Cadeia de caracteres deve consistir em um ou mais caracteres alfabéticos (`a`-`z`, diferencia maiusculas de minúsculas)</span><span class="sxs-lookup"><span data-stu-id="d89dd-348">String must consist of one or more alphabetical characters (`a`-`z`, case-insensitive)</span></span> |
| `regex(expression)` | `{ssn:regex(^\\d{{3}}-\\d{{2}}-\\d{{4}}$)}` | `123-45-6789` | <span data-ttu-id="d89dd-349">Cadeia de caracteres deve corresponder à expressão regular (consulte dicas sobre como definir uma expressão regular)</span><span class="sxs-lookup"><span data-stu-id="d89dd-349">String must match the regular expression (see tips about defining a regular expression)</span></span> |
| `required`  | `{name:required}` | `Rick` |  <span data-ttu-id="d89dd-350">Usado para impor que um valor de parâmetro não está presente durante a geração de URL</span><span class="sxs-lookup"><span data-stu-id="d89dd-350">Used to enforce that a non-parameter value is present during URL generation</span></span> |

>[!WARNING]
> <span data-ttu-id="d89dd-351">Restrições da rota que verifique a URL podem ser convertidas em um tipo CLR (como `int` ou `DateTime`) sempre usam a cultura invariável - assumem a URL é não localizáveis.</span><span class="sxs-lookup"><span data-stu-id="d89dd-351">Route constraints that verify the URL can be converted to a CLR type (such as `int` or `DateTime`) always use the invariant culture - they assume the URL is non-localizable.</span></span> <span data-ttu-id="d89dd-352">As restrições da rota fornecida pelo framework não modifique os valores armazenados nos valores de rota.</span><span class="sxs-lookup"><span data-stu-id="d89dd-352">The framework-provided route constraints do not modify the values stored in route values.</span></span> <span data-ttu-id="d89dd-353">Todos os valores de rota analisados a partir de URL serão armazenados como cadeias de caracteres.</span><span class="sxs-lookup"><span data-stu-id="d89dd-353">All route values parsed from the URL will be stored as strings.</span></span> <span data-ttu-id="d89dd-354">Por exemplo, o [restrição de rota Float](https://github.com/aspnet/Routing/blob/1.0.0/src/Microsoft.AspNetCore.Routing/Constraints/FloatRouteConstraint.cs#L44-L60) tentará converter o valor de rota em um float, mas o valor convertido é usado somente para verificar se ele pode ser convertido em float.</span><span class="sxs-lookup"><span data-stu-id="d89dd-354">For example, the [Float route constraint](https://github.com/aspnet/Routing/blob/1.0.0/src/Microsoft.AspNetCore.Routing/Constraints/FloatRouteConstraint.cs#L44-L60) will attempt to convert the route value to a float, but the converted value is used only to verify it can be converted to a float.</span></span>

## <a name="regular-expressions"></a><span data-ttu-id="d89dd-355">Expressões regulares</span><span class="sxs-lookup"><span data-stu-id="d89dd-355">Regular expressions</span></span> 

<span data-ttu-id="d89dd-356">Adiciona a estrutura do ASP.NET Core `RegexOptions.IgnoreCase | RegexOptions.Compiled | RegexOptions.CultureInvariant` para o construtor de expressão regular.</span><span class="sxs-lookup"><span data-stu-id="d89dd-356">The ASP.NET Core framework adds `RegexOptions.IgnoreCase | RegexOptions.Compiled | RegexOptions.CultureInvariant` to the regular expression constructor.</span></span> <span data-ttu-id="d89dd-357">Consulte [RegexOptions Enumeration](https://docs.microsoft.com/dotnet/api/system.text.regularexpressions.regexoptions) para obter uma descrição desses membros.</span><span class="sxs-lookup"><span data-stu-id="d89dd-357">See [RegexOptions Enumeration](https://docs.microsoft.com/dotnet/api/system.text.regularexpressions.regexoptions) for a description of these members.</span></span>

<span data-ttu-id="d89dd-358">Expressões regulares usam delimitadores e símbolos semelhantes aos usados pelo roteamento e a linguagem c#.</span><span class="sxs-lookup"><span data-stu-id="d89dd-358">Regular expressions use delimiters and tokens similar to those used by Routing and the C# language.</span></span> <span data-ttu-id="d89dd-359">Tokens de expressão regular devem ser de escape.</span><span class="sxs-lookup"><span data-stu-id="d89dd-359">Regular expression tokens must be escaped.</span></span> <span data-ttu-id="d89dd-360">Por exemplo, para usar a expressão regular `^\d{3}-\d{2}-\d{4}$` roteamento, ele precisa ter o `\` caracteres digitados no como `\\` no arquivo de origem c# para escapar o `\` caractere de escape da cadeia de caracteres (a menos que usando [textuais literais de cadeia de caracteres](https://docs.microsoft.com/dotnet/csharp/language-reference/keywords/string).</span><span class="sxs-lookup"><span data-stu-id="d89dd-360">For example, to use the regular expression `^\d{3}-\d{2}-\d{4}$` in Routing, it needs to have the `\` characters typed in as `\\` in the C# source file to escape the `\` string escape character (unless using [verbatim string literals](https://docs.microsoft.com/dotnet/csharp/language-reference/keywords/string).</span></span> <span data-ttu-id="d89dd-361">O `{` , `}` , ' [' e ']' caracteres precisam ser substituídas por aspas duplas para escapar os caracteres de delimitador de parâmetro de roteamento.</span><span class="sxs-lookup"><span data-stu-id="d89dd-361">The `{` , `}` , '[' and ']' characters need to be escaped by doubling them to escape the Routing parameter delimiter characters.</span></span>  <span data-ttu-id="d89dd-362">A tabela a seguir mostra uma expressão regular e a versão de escape.</span><span class="sxs-lookup"><span data-stu-id="d89dd-362">The table below shows a regular expression and the escaped version.</span></span>

| <span data-ttu-id="d89dd-363">Expressão</span><span class="sxs-lookup"><span data-stu-id="d89dd-363">Expression</span></span>               | <span data-ttu-id="d89dd-364">Observação</span><span class="sxs-lookup"><span data-stu-id="d89dd-364">Note</span></span> |
| ----------------- | ------------ | 
| `^\d{3}-\d{2}-\d{4}$` | <span data-ttu-id="d89dd-365">Expressão regular</span><span class="sxs-lookup"><span data-stu-id="d89dd-365">Regular expression</span></span> |
| `^\\d{{3}}-\\d{{2}}-\\d{{4}}$` | <span data-ttu-id="d89dd-366">Escape</span><span class="sxs-lookup"><span data-stu-id="d89dd-366">Escaped</span></span>  |
| `^[a-z]{2}$` | <span data-ttu-id="d89dd-367">Expressão regular</span><span class="sxs-lookup"><span data-stu-id="d89dd-367">Regular expression</span></span> |
| `^[[a-z]]{{2}}$` | <span data-ttu-id="d89dd-368">Escape</span><span class="sxs-lookup"><span data-stu-id="d89dd-368">Escaped</span></span>  |

<span data-ttu-id="d89dd-369">Expressões regulares usadas em roteamento geralmente começa com o `^` caractere (correspondência de início da cadeia de caracteres) e terminam com o `$` caractere (correspondência terminando a posição da cadeia de caracteres).</span><span class="sxs-lookup"><span data-stu-id="d89dd-369">Regular expressions used in routing will often start with the `^` character (match starting position of the string) and end with the `$` character (match ending position of the string).</span></span> <span data-ttu-id="d89dd-370">O `^` e `$` caracteres Certifique-se de que o valor do parâmetro de rota toda a correspondência da expressão regular.</span><span class="sxs-lookup"><span data-stu-id="d89dd-370">The `^` and `$` characters ensure that the regular expression match the entire route parameter value.</span></span> <span data-ttu-id="d89dd-371">Sem o `^` e `$` caracteres de expressão regular corresponderá a qualquer subcadeia de caracteres na cadeia de caracteres, que é geralmente não é o desejado.</span><span class="sxs-lookup"><span data-stu-id="d89dd-371">Without the `^` and `$` characters the regular expression will match any sub-string within the string, which is often not what you want.</span></span> <span data-ttu-id="d89dd-372">A tabela a seguir mostra alguns exemplos e explica por que eles correspondam ou não corresponde.</span><span class="sxs-lookup"><span data-stu-id="d89dd-372">The table below shows some examples and explains why they match or fail to match.</span></span>

| <span data-ttu-id="d89dd-373">Expressão</span><span class="sxs-lookup"><span data-stu-id="d89dd-373">Expression</span></span>               | <span data-ttu-id="d89dd-374">Cadeia de caracteres</span><span class="sxs-lookup"><span data-stu-id="d89dd-374">String</span></span> | <span data-ttu-id="d89dd-375">Corresponder a</span><span class="sxs-lookup"><span data-stu-id="d89dd-375">Match</span></span> | <span data-ttu-id="d89dd-376">Comentário</span><span class="sxs-lookup"><span data-stu-id="d89dd-376">Comment</span></span> |
| ----------------- | ------------ |  ------------ |  ------------ | 
| `[a-z]{2}` | <span data-ttu-id="d89dd-377">hello</span><span class="sxs-lookup"><span data-stu-id="d89dd-377">hello</span></span> | <span data-ttu-id="d89dd-378">sim</span><span class="sxs-lookup"><span data-stu-id="d89dd-378">yes</span></span> | <span data-ttu-id="d89dd-379">correspondência de subcadeia de caracteres</span><span class="sxs-lookup"><span data-stu-id="d89dd-379">substring matches</span></span> |
| `[a-z]{2}` | <span data-ttu-id="d89dd-380">123abc456</span><span class="sxs-lookup"><span data-stu-id="d89dd-380">123abc456</span></span> | <span data-ttu-id="d89dd-381">sim</span><span class="sxs-lookup"><span data-stu-id="d89dd-381">yes</span></span> | <span data-ttu-id="d89dd-382">correspondência de subcadeia de caracteres</span><span class="sxs-lookup"><span data-stu-id="d89dd-382">substring matches</span></span> |
| `[a-z]{2}` | <span data-ttu-id="d89dd-383">mz</span><span class="sxs-lookup"><span data-stu-id="d89dd-383">mz</span></span> | <span data-ttu-id="d89dd-384">sim</span><span class="sxs-lookup"><span data-stu-id="d89dd-384">yes</span></span> | <span data-ttu-id="d89dd-385">coincide com a expressão</span><span class="sxs-lookup"><span data-stu-id="d89dd-385">matches expression</span></span> |
| `[a-z]{2}` | <span data-ttu-id="d89dd-386">MZ</span><span class="sxs-lookup"><span data-stu-id="d89dd-386">MZ</span></span> | <span data-ttu-id="d89dd-387">sim</span><span class="sxs-lookup"><span data-stu-id="d89dd-387">yes</span></span> | <span data-ttu-id="d89dd-388">não entre maiusculas e minúsculas</span><span class="sxs-lookup"><span data-stu-id="d89dd-388">not case sensitive</span></span> |
| `^[a-z]{2}$` |  <span data-ttu-id="d89dd-389">hello</span><span class="sxs-lookup"><span data-stu-id="d89dd-389">hello</span></span> | <span data-ttu-id="d89dd-390">no</span><span class="sxs-lookup"><span data-stu-id="d89dd-390">no</span></span> | <span data-ttu-id="d89dd-391">consulte `^` e `$` acima</span><span class="sxs-lookup"><span data-stu-id="d89dd-391">see `^` and `$` above</span></span> |
| `^[a-z]{2}$` |  <span data-ttu-id="d89dd-392">123abc456</span><span class="sxs-lookup"><span data-stu-id="d89dd-392">123abc456</span></span> | <span data-ttu-id="d89dd-393">no</span><span class="sxs-lookup"><span data-stu-id="d89dd-393">no</span></span> | <span data-ttu-id="d89dd-394">consulte `^` e `$` acima</span><span class="sxs-lookup"><span data-stu-id="d89dd-394">see `^` and `$` above</span></span> |

<span data-ttu-id="d89dd-395">Consulte [expressões regulares do .NET Framework](https://docs.microsoft.com/dotnet/standard/base-types/regular-expression-language-quick-reference) para obter mais informações sobre a sintaxe de expressão regular.</span><span class="sxs-lookup"><span data-stu-id="d89dd-395">Refer to [.NET Framework Regular Expressions](https://docs.microsoft.com/dotnet/standard/base-types/regular-expression-language-quick-reference) for more information on regular expression syntax.</span></span>

<span data-ttu-id="d89dd-396">Para restringir um parâmetro para um conjunto conhecido de valores possíveis, use uma expressão regular.</span><span class="sxs-lookup"><span data-stu-id="d89dd-396">To constrain a parameter to a known set of possible values, use a regular expression.</span></span> <span data-ttu-id="d89dd-397">Por exemplo `{action:regex(^(list|get|create)$)}` corresponde apenas a `action` rotear o valor para `list`, `get`, ou `create`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-397">For example `{action:regex(^(list|get|create)$)}` only matches the `action` route value to `list`, `get`, or `create`.</span></span> <span data-ttu-id="d89dd-398">Se passado para o dicionário de restrições, a cadeia de caracteres "^ (lista | get | criar) $" seria equivalente.</span><span class="sxs-lookup"><span data-stu-id="d89dd-398">If passed into the constraints dictionary, the string "^(list|get|create)$" would be equivalent.</span></span> <span data-ttu-id="d89dd-399">Restrições que são passadas no dicionário de restrições (não embutido dentro de um modelo) que não correspondem a uma das restrições conhecidas também são tratadas como expressões regulares.</span><span class="sxs-lookup"><span data-stu-id="d89dd-399">Constraints that are passed in the constraints dictionary (not inline within a template) that don't match one of the known constraints are also treated as regular expressions.</span></span>

## <a name="url-generation-reference"></a><span data-ttu-id="d89dd-400">Referência de geração de URL</span><span class="sxs-lookup"><span data-stu-id="d89dd-400">URL Generation Reference</span></span>

<span data-ttu-id="d89dd-401">O exemplo a seguir mostra como gerar um link para uma rota com um dicionário de valores de rota e uma `RouteCollection`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-401">The example below shows how to generate a link to a route given a dictionary of route values and a `RouteCollection`.</span></span>

<span data-ttu-id="d89dd-402">[!code-csharp[Main](../fundamentals/routing/sample/RoutingSample/Startup.cs?range=45-59)]</span><span class="sxs-lookup"><span data-stu-id="d89dd-402">[!code-csharp[Main](../fundamentals/routing/sample/RoutingSample/Startup.cs?range=45-59)]</span></span>

<span data-ttu-id="d89dd-403">O `VirtualPath` gerado no final do exemplo acima é `/package/create/123`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-403">The `VirtualPath` generated at the end of the sample above is `/package/create/123`.</span></span>

<span data-ttu-id="d89dd-404">O segundo parâmetro para o `VirtualPathContext` construtor é uma coleção de *valores ambiente*.</span><span class="sxs-lookup"><span data-stu-id="d89dd-404">The second parameter to the `VirtualPathContext` constructor is a collection of *ambient values*.</span></span> <span data-ttu-id="d89dd-405">Valores de ambiente proporcionar conveniência, limitando o número de valores que deve especificar um desenvolvedor em um determinado contexto de solicitação.</span><span class="sxs-lookup"><span data-stu-id="d89dd-405">Ambient values provide convenience by limiting the number of values a developer must specify within a certain request context.</span></span> <span data-ttu-id="d89dd-406">Os valores de rota atual da solicitação atual são considerados valores de ambiente para a geração de link.</span><span class="sxs-lookup"><span data-stu-id="d89dd-406">The current route values of the current request are considered ambient values for link generation.</span></span> <span data-ttu-id="d89dd-407">Por exemplo, em um aplicativo ASP.NET MVC, se você estiver no `About` ação do `HomeController`, você não precisa especificar o valor de rota do controlador para vincular ao `Index` ação (o valor de ambiente de `Home` será usado).</span><span class="sxs-lookup"><span data-stu-id="d89dd-407">For example, in an ASP.NET MVC app if you are in the `About` action of the `HomeController`, you don't need to specify the controller route value to link to the `Index` action (the ambient value of `Home` will be used).</span></span>

<span data-ttu-id="d89dd-408">Valores de ambiente não corresponderem a um parâmetro são ignorados e ambiente valores também são ignorados quando um valor explicitamente fornecido substituições, vai da esquerda para a direita na URL.</span><span class="sxs-lookup"><span data-stu-id="d89dd-408">Ambient values that don't match a parameter are ignored, and ambient values are also ignored when an explicitly-provided value overrides it, going from left to right in the URL.</span></span>

<span data-ttu-id="d89dd-409">Valores que são explicitamente fornecidas, mas que não corresponde a qualquer coisa são adicionados à cadeia de caracteres de consulta.</span><span class="sxs-lookup"><span data-stu-id="d89dd-409">Values that are explicitly provided but which don't match anything are added to the query string.</span></span> <span data-ttu-id="d89dd-410">A tabela a seguir mostra o resultado ao usar o modelo de rota `{controller}/{action}/{id?}`.</span><span class="sxs-lookup"><span data-stu-id="d89dd-410">The following table shows the result when using the route template `{controller}/{action}/{id?}`.</span></span>

| <span data-ttu-id="d89dd-411">Valores de ambiente</span><span class="sxs-lookup"><span data-stu-id="d89dd-411">Ambient Values</span></span> | <span data-ttu-id="d89dd-412">Valores explícitos</span><span class="sxs-lookup"><span data-stu-id="d89dd-412">Explicit Values</span></span> | <span data-ttu-id="d89dd-413">Resultado</span><span class="sxs-lookup"><span data-stu-id="d89dd-413">Result</span></span> |
| -------------   | -------------- | ------ |
| <span data-ttu-id="d89dd-414">controlador = "Início"</span><span class="sxs-lookup"><span data-stu-id="d89dd-414">controller="Home"</span></span> | <span data-ttu-id="d89dd-415">ação = "Sobre"</span><span class="sxs-lookup"><span data-stu-id="d89dd-415">action="About"</span></span> | `/Home/About` |
| <span data-ttu-id="d89dd-416">controlador = "Início"</span><span class="sxs-lookup"><span data-stu-id="d89dd-416">controller="Home"</span></span> | <span data-ttu-id="d89dd-417">controlador = "Order", ação = "Sobre"</span><span class="sxs-lookup"><span data-stu-id="d89dd-417">controller="Order",action="About"</span></span> | `/Order/About` |
| <span data-ttu-id="d89dd-418">controlador = "Home", color = "Red"</span><span class="sxs-lookup"><span data-stu-id="d89dd-418">controller="Home",color="Red"</span></span> | <span data-ttu-id="d89dd-419">ação = "Sobre"</span><span class="sxs-lookup"><span data-stu-id="d89dd-419">action="About"</span></span> | `/Home/About` |
| <span data-ttu-id="d89dd-420">controlador = "Início"</span><span class="sxs-lookup"><span data-stu-id="d89dd-420">controller="Home"</span></span> | <span data-ttu-id="d89dd-421">ação = "Sobre" cor = "Red"</span><span class="sxs-lookup"><span data-stu-id="d89dd-421">action="About",color="Red"</span></span> | `/Home/About?color=Red`

<span data-ttu-id="d89dd-422">Se uma rota tem um valor padrão que não corresponde a um parâmetro e esse valor é explicitamente fornecido, ele deve corresponder o valor padrão.</span><span class="sxs-lookup"><span data-stu-id="d89dd-422">If a route has a default value that doesn't correspond to a parameter and that value is explicitly provided, it must match the default value.</span></span> <span data-ttu-id="d89dd-423">Por exemplo:</span><span class="sxs-lookup"><span data-stu-id="d89dd-423">For example:</span></span>

```csharp
routes.MapRoute("blog_route", "blog/{*slug}",
  defaults: new { controller = "Blog", action = "ReadPost" });
```

<span data-ttu-id="d89dd-424">Geração de link só poderia gerar um link para essa rota quando os valores correspondentes para o controlador e ação são fornecidos.</span><span class="sxs-lookup"><span data-stu-id="d89dd-424">Link generation would only generate a link for this route when the matching values for controller and action are provided.</span></span>
